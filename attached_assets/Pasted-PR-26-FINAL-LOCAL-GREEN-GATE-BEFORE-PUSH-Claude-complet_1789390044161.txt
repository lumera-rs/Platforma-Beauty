PR #26 — FINAL LOCAL GREEN GATE BEFORE PUSH

Claude completed the independent read-only review.

Do ONLY the minimum work required to make the current PR #26 stabilization patch a real full-green candidate.

DO NOT PUSH.
DO NOT MERGE.
DO NOT START P.2c.

Claude independently confirmed:

- historical-base migration fix is correct
- immutable migration history remains fail-closed
- migration gate remains database-free
- admin fixture changes are safe
- booking fixture change is safe
- fingerprint golden correction is correct
- tests/assertions were not weakened
- booking logic unchanged
- DB schema unchanged
- OpenAPI unchanged

OPEN ITEMS ONLY:

P1-1:
admin-summary latency benchmark measures first-request cold bootstrap rather than warm endpoint latency.

Claude independently reproduced 6/6 failures:

2092.0–2913.8 ms
median ~2391 ms

But actual DB work inside GET /admin/summary was only ~9 ms.

The first request includes:
- application bootstrap
- module realization/JIT
- one-time seed/backfill work

Therefore this is a benchmark methodology defect, NOT evidence that the admin-summary query itself takes >2 seconds.

TASK 1 — FIX ADMIN-SUMMARY BENCHMARK METHODOLOGY

Inspect:
artifacts/api-server/src/lib/admin-summary.test.ts

Implement the smallest correct fix.

Before starting the timed request:

1. Issue ONE discarded warm-up request to:
   GET /admin/summary

2. Require that warm-up request to succeed.
   Do not silently ignore a failed warm-up.

3. Only AFTER successful warm-up:
   startedAt = performance.now()
   execute the measured request
   keep the existing 2000 ms budget.

IMPORTANT:

DO NOT:
- increase 2000 ms
- weaken latency assertion
- change admin-summary query
- add indexes
- change application code
- change production bootstrap
- change business logic
- change fixture size
- reduce APPOINTMENTS_PER_CATEGORY
- skip the measured request
- fake/mock timing
- add retries to make the test pass

The test should measure warm endpoint performance, not process startup.

Check whether the contention measurement later in the same test has the same cold-start methodology issue.

Only change it if the same reasoning genuinely applies.

TASK 2 — VERIFY ADMIN SUMMARY

Against disposable PostgreSQL 16:

Run the real admin-summary test multiple times.

Report:
- warm-up behavior
- measured samples
- min
- median
- max
- whether every sample is <2000 ms
- query-count assertion
- HTTP correctness assertions

TASK 3 — RUN FULL DATABASE CI WRAPPER

After admin-summary is green, run the ACTUAL wrapper:

pnpm run validate:ci:database

Do not stop at test:admin-summary.

We specifically need signal from the four suites previously masked behind it:

- test:loyalty-status
- test:api-preflight
- test:api-regressions
- test:api-regressions-lifecycle

If ANY fails:

Investigate the FIRST real root cause.

Do not weaken the test.

Do not automatically fix application behavior.

If it is a genuine application/product defect, STOP and report before making a broad behavior change.

TASK 4 — RUN FULL BROWSER CI WRAPPER

Run the ACTUAL:

pnpm run validate:ci:browser

It must run the complete wrapper, including:

- validate:release:4-isolated
- validate:release:5-final

Do not substitute isolated suite results.

Confirm:
- all suites collected
- no duplicate test-title failure
- admin-access suite passes in wrapper context
- booking-settings suite passes in wrapper context
- server lifecycle succeeds
- disposable DB lifecycle succeeds
- no cascade errors
- final wrapper exit 0

TASK 5 — RECHECK CORE P.2b GATES

Run:

pnpm run test:migration-contract
pnpm run validate:ci:migration-contract
pnpm run test:release-chain
pnpm run test:generated-api
pnpm run test:github-workflows
git diff --check

Also run any existing typecheck required by the changed test file.

TASK 6 — CLEAN SCOPE REVIEW

Inspect the two committed prompt dumps:

attached_assets/Pasted-*.txt

Claude classified them P3 scope pollution and found trailing whitespace in them.

Determine whether they are needed by runtime/tests/CI.

If they are NOT required:
recommend removing them from the PR.

Do not remove unrelated legitimate project assets.

Also confirm whether:
.agents/memory/*
has any runtime/CI effect.

TASK 7 — FINAL REPORT

Return:

A. Exact files changed by this final fix

B. Admin-summary methodology change

C. Warm latency samples

D. Full validate:ci:database result

E. Results of previously masked API suites

F. Full validate:ci:browser result

G. P.2b gate results

H. Any remaining P0/P1/P2

I. Exact branch / HEAD / working-tree state

J. Explicit answers:

ADMIN-SUMMARY METHODOLOGY FIXED: YES / NO
2000MS BUDGET PRESERVED: YES / NO
ADMIN-SUMMARY QUERY CHANGED: YES / NO
APPLICATION BEHAVIOR CHANGED: YES / NO
DATABASE SCHEMA CHANGED: YES / NO
BOOKING LOGIC CHANGED: YES / NO
FULL DATABASE WRAPPER PASS: YES / NO
ALL PREVIOUSLY MASKED API SUITES PASS: YES / NO
FULL BROWSER WRAPPER PASS: YES / NO
MIGRATION CONTRACT PASS: YES / NO
RELEASE CHAIN PASS: YES / NO
GENERATED API PASS: YES / NO
ANY OPEN P0: YES / NO
ANY OPEN P1: YES / NO
ANY OPEN P2: YES / NO
P.2c STARTED: YES / NO
NEXT PUSH IS A REASONABLE FULL-GREEN CANDIDATE: YES / NO
SAFE FOR FINAL CLAUDE READ-ONLY REVIEW: YES / NO

STOP.

DO NOT PUSH.