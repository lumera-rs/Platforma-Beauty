PR #26 — FULL CI STABILIZATION / FINAL PRE-GREEN PASS

GOAL

Stop fixing GitHub Actions failures one-by-one.

Treat the current PR #26 / branch seo-max-p2b-verification as a release candidate and perform one comprehensive CI stabilization pass BEFORE the next push.

The next push should be a serious candidate for an entirely green PR, not just a fix for the latest red job.

IMPORTANT

- Do NOT push.
- Do NOT merge.
- Do NOT start P.2c.
- Do NOT weaken tests.
- Do NOT weaken security guards.
- Do NOT increase retries/timeouts merely to hide failures.
- Do NOT bypass GitHub-specific checks.
- Do NOT change booking behavior unless a real independently-proven application bug requires it.
- Do NOT change production DB semantics unless explicitly required and separately justified.
- Keep scope focused on making the existing intended CI contract reliable and reproducible.

CURRENT CONTEXT

PR #26 has already surfaced several different real CI/environment issues in sequence:

1. retail-cart isolated-CI preparation mismatch
2. destructive harness guard coverage gaps
3. lifecycle fail-fast / browser shutdown diagnostics
4. stale generated API artifacts
5. current migration-contract failure when the trusted base commit predates:
   lib/db/migrations

Current real GitHub failure:

CI migration contract validation failed:
git cat-file failed (128):
fatal: path 'lib/db/migrations' exists on disk, but not in
'd4da670f196c39fd14bbbf318992d04c459fdfb9'

The trusted/base commit appears to predate migration-directory introduction.

Generated API synchronization has already been independently reviewed and approved:
- only 3 generated files changed
- OpenAPI source unchanged
- semantics preserved
- generated artifacts canonical
- test:generated-api PASS
- migration-contract 53/53 PASS locally before this GitHub historical-base edge case
- release-chain 23/23 PASS
- booking contract 4/4 PASS
- Replit browser journey 19/19 PASS

TASK

Perform a FULL CI STABILIZATION AUDIT and implementation pass across PR #26.

Do not stop after fixing the current migration-contract failure.

PHASE 1 — REPRODUCE THE REAL GITHUB CONDITIONS

Inspect:

- .github/workflows/*
- every job used by PR #26
- checkout depth/history configuration
- event types:
  - pull_request
  - push if applicable
  - merge_group
  - scheduled/ruleset audit where relevant
- environment variables
- DB service setup
- build order
- cache assumptions
- generated artifacts
- branch/base SHA discovery
- shallow clone handling
- trusted base resolution
- scripts invoked directly and indirectly

Identify exactly which CI behavior differs from normal local execution.

Build a matrix of:

JOB
COMMAND
DEPENDENCIES
DATABASE NEEDED?
GIT HISTORY NEEDED?
NETWORK NEEDED?
GENERATED ARTIFACTS NEEDED?
KNOWN CURRENT RISK
LOCAL REPRODUCIBILITY

PHASE 2 — FIX CURRENT MIGRATION-CONTRACT HISTORICAL-BASE BUG

Current failure:

valid historical base commit exists,
but lib/db/migrations does not yet exist in that commit.

Required semantics:

1. Prove supplied base SHA is a valid trusted commit first.

2. If a VALID trusted historical commit has no
   lib/db/migrations path:
   historical migration set = EMPTY.

3. Current migrations are then additions, not mutations/deletions.

4. Do NOT broadly ignore Git exit 128.

5. Invalid SHA must fail closed.

6. Missing/unavailable ancestry must fail closed.

7. Shallow-history inability to prove trust must fail closed.

8. Existing immutable-history protection must remain intact.

Prefer a Git operation whose successful empty result naturally means
“no files at this path”, e.g. appropriate ls-tree logic, rather than
parsing generic fatal cat-file text.

Add regression tests for:

A. valid old commit before migration directory → PASS as empty history
B. valid base with migrations → normal comparison
C. historical migration modified → FAIL
D. historical migration deleted → FAIL
E. invalid/nonexistent base SHA → FAIL CLOSED
F. unavailable ancestry/history → FAIL CLOSED
G. current malformed migration contract → FAIL
H. shallow clone/deepening case if relevant
I. merge_group-style base resolution if relevant

PHASE 3 — AUDIT ALL P.2b CI GATES FOR THE SAME CLASS OF ASSUMPTIONS

Review all P.2b migration-contract logic for hidden assumptions that could fail only on GitHub:

- repository path existed historically
- full git history exists
- merge-base exists
- branch ref exists locally
- event payload contains expected fields
- PR base ref is fetched
- merge_group ref layout
- detached HEAD
- remote name assumptions
- branch-name assumptions
- tag/ref ambiguity
- force-push/rebase history
- first commit introducing a protected directory
- empty migration directory
- migration directory removed/reintroduced
- file rename/path transition
- Windows/newline assumptions where relevant
- shell quoting
- cwd assumptions
- pnpm workspace paths
- symlink behavior
- generated output leftovers
- stale temp directories

For each discovered issue:
- classify P0/P1/P2/P3
- prove it with a focused test
- fix only real defects
- do not speculative-refactor working code

PHASE 4 — RUN THE ENTIRE CI SURFACE LOCALLY AS FAR AS POSSIBLE

Run, in the SAME logical order as GitHub Actions, all safely reproducible checks.

At minimum inspect/run:

1. Migration contract database-free job
2. Release-chain database-free job
3. Build/static/type checks
4. Database checks against disposable PostgreSQL
5. Browser user journeys against disposable PostgreSQL
6. Generated API verification
7. schema-drift/fingerprint suites
8. eligibility CLI tests
9. lifecycle harness tests
10. destructive runtime guard tests
11. retail-cart isolated CI preparation
12. any workflow lint / release validation
13. git diff --check

Do not merely run individual underlying tests if GitHub invokes a wrapper.
Run the actual CI wrapper commands whenever practical.

PHASE 5 — SIMULATE GITHUB EVENT/REPOSITORY STATES

Where safe, create temporary isolated git test repositories/fixtures to simulate:

1. PR whose base predates migration directory
2. PR whose base already contains migrations
3. shallow clone with insufficient ancestry
4. deepened clone
5. detached HEAD
6. merge commit
7. merge_group style ancestry
8. invalid base SHA
9. deleted historical migration
10. modified historical migration
11. added migration only
12. migration gaps
13. malformed migration headers
14. clean current state

Do not mutate the real repository history for these tests.
Use temporary fixtures.

PHASE 6 — CHECK FOR CASCADE FAILURES

For every currently known historical CI failure determine:

- root cause
- whether fixed
- whether test exists preventing recurrence
- whether failure could cascade into misleading secondary FAIL/ERROR counts

Specifically re-check:

- generated API preflight
- browser lifecycle termination
- API server teardown
- isolated PostgreSQL teardown
- retail cart reconciliation
- destructive-runtime guards
- migration-history base resolution

The goal is that one early failure produces one clear primary failure, not a wall of misleading secondary errors.

PHASE 7 — DO NOT MASK APPLICATION BUGS

If full CI reveals a REAL application bug:

STOP and report it separately before making a broad product behavior change.

Especially do not casually change:

- booking concurrency
- appointment semantics
- availability
- tenant isolation
- auth
- resource locking
- service durations
- cancellation/reschedule behavior
- notification/outbox semantics
- payment logic
- database constraints

CI stabilization must not become an excuse for altering business rules.

PHASE 8 — FINAL VALIDATION

After all justified fixes, run as much of the final CI surface as possible.

Minimum expected final evidence:

- validate:ci:migration-contract → PASS
- test:migration-contract → PASS
- test:release-chain → PASS
- test:generated-api → PASS
- booking-settings contract → PASS
- schema-drift/fingerprint → PASS
- eligibility tests → PASS
- destructive runtime guards → PASS
- lifecycle suite → PASS
- database checks → PASS on disposable PostgreSQL
- browser user journeys → PASS on disposable PostgreSQL
- typecheck/build → PASS
- workflow lint if present → PASS
- git diff --check → PASS

If any check cannot be reproduced locally:
state exactly WHY and what GitHub must still prove.

PHASE 9 — SCOPE REVIEW

Before final report, prove exactly what changed.

Report:

- git status
- branch
- HEAD
- changed files
- lines added/removed
- whether any production runtime file changed
- whether any DB schema changed
- whether any migration was created
- whether OpenAPI changed
- whether generated API changed
- whether booking logic changed
- whether CI semantics were weakened
- whether P.2c was started

PHASE 10 — FINAL REPORT

Return:

A. Executive summary

B. Root causes found

C. Exact files changed

D. Current migration-contract historical-base fix

E. Other latent CI bugs discovered proactively

F. Tests added

G. Full local CI-equivalent results

H. What still can ONLY be proven by real GitHub Actions

I. Findings:
- P0
- P1
- P2
- P3

J. Explicit answers:

- CURRENT GITHUB FAILURE FIXED LOCALLY: YES / NO
- OTHER LIKELY CI FAILURES PROACTIVELY CHECKED: YES / NO
- IMMUTABLE MIGRATION HISTORY STILL FAIL-CLOSED: YES / NO
- DATABASE-FREE MIGRATION GATE STILL DATABASE-FREE: YES / NO
- TESTS/ASSERTIONS WEAKENED: YES / NO
- BOOKING LOGIC CHANGED: YES / NO
- DATABASE SCHEMA CHANGED: YES / NO
- OPENAPI SOURCE CHANGED: YES / NO
- P.2c STARTED: YES / NO
- NEXT PUSH IS A REASONABLE FULL-GREEN CANDIDATE: YES / NO
- SAFE FOR INDEPENDENT CLAUDE REVIEW: YES / NO

IMPORTANT FINAL RULE

Do NOT push.

STOP after the report.

The next step after your report is an independent READ-ONLY Claude Code review of the ENTIRE stabilization patch, not another immediate push.