Fix the real GitHub Actions failure in:

validate:ci:migration-contract

Current failure:

CI migration contract validation failed:
git cat-file failed (128):
fatal: path 'lib/db/migrations' exists on disk, but not in
'd4da670f196c39fd14bbbf318992d04c459fdfb9'

Context:

This is PR #26 / P.2b migration-contract CI.

The trusted/base commit used by CI predates the introduction of
lib/db/migrations.

Therefore the current migration directory exists in the PR checkout,
but legitimately does not exist in the historical base commit.

This must NOT weaken immutable migration-history protection.

TASK

Investigate the exact code path in:

scripts/src/migration-contract/

and determine why a missing migration directory in a valid historical
base commit is treated as fatal.

Implement the minimal fail-closed fix.

Required semantics:

1. First prove that the supplied base SHA is a valid commit using normal
   existing ancestry/trust validation.

2. If the valid trusted base commit simply has no
   `lib/db/migrations` path, treat its migration history as EMPTY.

3. This means migrations introduced after that commit are additions,
   not mutations/deletions of historical migrations.

4. Do NOT broadly ignore:
   - git exit code 128
   - invalid commit objects
   - missing/unreachable base commits
   - ancestry failures
   - shallow-history failures
   - malformed repository state
   - other git errors

5. Prefer a Git query whose normal successful result can represent
   "zero files under this path", for example an appropriate `git ls-tree`
   strategy, rather than parsing a generic fatal error string from
   `git cat-file`, if that fits the existing architecture.

6. Preserve all existing P.2b protections:
   - immutable migration history
   - trusted PR base handling
   - sequence/gap rules
   - migration contract validation
   - database-free capability
   - no DATABASE_URL access
   - no shell/network/database weakening
   - no test assertion weakening
   - no retries/timeouts/tolerance changes

7. Add focused regression tests covering at minimum:

   A. valid base commit predates `lib/db/migrations`
      → historical migration set = empty
      → validation succeeds when current state is otherwise valid

   B. valid base commit contains migrations
      → existing immutable-history comparisons still work

   C. historical migration modified
      → still FAILS

   D. historical migration deleted
      → still FAILS

   E. invalid/nonexistent base SHA
      → FAILS CLOSED

   F. missing/unavailable required ancestry/history
      → FAILS CLOSED

   G. current migration contract violation
      → still FAILS

8. Confirm the fix specifically covers the real base commit:

   d4da670f196c39fd14bbbf318992d04c459fdfb9

   and explain whether that commit genuinely predates the migration
   directory.

9. Run:

   pnpm run test:migration-contract
   pnpm run validate:ci:migration-contract

   plus any focused migration-contract tests required.

10. Also run:

   pnpm run test:release-chain
   git diff --check

11. Confirm the validation remains completely DATABASE-FREE.

DO NOT:
- touch booking logic
- touch OpenAPI/generated API files
- touch database schema
- create migrations
- start P.2c
- change migration IDs
- weaken immutable-history checks
- bypass the check when the directory is absent
- push anything

Return:

A. Exact root cause
B. Exact files changed
C. Exact behavioral change
D. Why the change remains fail-closed
E. New regression tests
F. Test results
G. Any P0/P1/P2/P3 findings
H. Final verdict:
   SAFE TO INDEPENDENTLY REVIEW: YES / NO

STOP.
Do not push.