openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: LUMERA beauty, wellness, booking, B2B, loyalty, and education marketplace API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: salons
  - name: appointments
  - name: customer
  - name: salon-management
  - name: shop
  - name: loyalty
  - name: education
  - name: admin
  - name: growth
  - name: beauty-jobs

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register a customer account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterInput"
      responses:
        "201":
          description: Registered user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
  /auth/business-register:
    post:
      operationId: registerBusiness
      tags: [auth]
      summary: Register a salon or education-center owner
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BusinessRegistrationInput"
      responses:
        "201":
          description: Registered business owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
  /auth/jobseeker-register:
    post:
      operationId: registerJobseeker
      tags: [auth]
      summary: Register a JOBSEEKER account
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/JobseekerRegistrationInput" }
      responses:
        "201":
          description: Registered jobseeker
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthSession" }
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Log in
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
  /auth/customer-password-setup/validate:
    post:
      operationId: validateCustomerPasswordSetup
      tags: [auth]
      summary: Validate a short-lived customer password setup token
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerPasswordSetupTokenInput" }
      responses:
        "200":
          description: Token is currently valid
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerPasswordSetupValidation" }
        "400": { description: Setup link is invalid or no longer usable }
        "429": { description: Too many setup attempts }
  /auth/customer-password-setup/complete:
    post:
      operationId: completeCustomerPasswordSetup
      tags: [auth]
      summary: Consume a one-time setup token and set the customer password
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerPasswordSetupCompletionInput" }
      responses:
        "200":
          description: Password was set and the one-time token consumed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerPasswordSetupCompletion" }
        "400": { description: Setup link is invalid or no longer usable }
        "429": { description: Too many setup attempts }
  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Log out
      responses:
        "204":
          description: Logged out
  /auth/me:
    get:
      operationId: getCurrentUser
      tags: [auth]
      summary: Get current user
      responses:
        "200":
          description: Current user or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentUserResponse"
  /auth/email-preferences:
    patch:
      operationId: updateEmailPreferences
      tags: [auth]
      summary: Update marketing email preference
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailPreferencesInput"
      responses:
        "200":
          description: Updated email preference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailPreferences"
        "400": { description: Invalid preference }
        "401": { description: Authentication required }
  /auth/sign-in-methods:
    get:
      operationId: getAuthSignInMethods
      tags: [auth]
      summary: List connected sign-in methods for the current user
      responses:
        "200":
          description: Connected sign-in methods
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignInMethods"
        "401": { description: Authentication required }
  /auth/sign-in-methods/{provider}:
    delete:
      operationId: disconnectAuthSignInMethod
      tags: [auth]
      summary: Disconnect a social sign-in method from the current user
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, facebook]
      responses:
        "200":
          description: Updated connected sign-in methods
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignInMethods"
        "400": { description: Disconnecting would leave the account without a sign-in method }
        "401": { description: Authentication required }
        "404": { description: Provider is not connected }
  /admin/email-marketing/campaigns:
    get:
      operationId: adminListEmailCampaigns
      tags: [admin]
      summary: List LUMERA email marketing campaigns
      responses:
        "200":
          description: Campaign history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminListEmailCampaignsResponse"
    post:
      operationId: adminCreateEmailCampaign
      tags: [admin]
      summary: Create and send or schedule an email campaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminCreateEmailCampaignInput"
      responses:
        "201":
          description: Created campaign
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailMarketingCampaign"
  /admin/sms-deliveries:
    get:
      operationId: adminListSmsDeliveries
      tags: [admin]
      summary: List transactional SMS delivery records
      responses:
        "200":
          description: SMS delivery log
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SmsDelivery" }
  /admin/integrations:
    get:
      operationId: adminGetIntegrations
      tags: [admin]
      summary: Get administrator integration settings and delivery health
      responses:
        "200":
          description: Integration settings, OAuth redirect URIs, and reminder status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminGetIntegrationsResponse"
  /admin/integrations/webhook-freshness:
    get:
      operationId: adminGetWebhookFreshness
      tags: [admin]
      summary: Get the latest webhook confirmation metadata and delivery health
      responses:
        "200":
          description: Time-derived webhook confirmation metadata and delivery health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminGetWebhookFreshnessResponse"
  /admin/integrations/web-push-delivery-metrics:
    get:
      operationId: adminGetWebPushDeliveryMetrics
      tags: [admin]
      summary: Get privacy-safe aggregate Web Push delivery metrics
      parameters:
        - name: periodDays
          in: query
          required: true
          schema:
            type: integer
            enum: [1, 7, 30, 90]
      responses:
        "200":
          description: Aggregate delivery and subscription health for the selected period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminWebPushDeliveryMetrics"
  /admin/integrations/{integration}:
    put:
      operationId: adminSaveIntegration
      tags: [admin]
      summary: Save administrator integration settings
      parameters:
        - name: integration
          in: path
          required: true
          schema:
            type: string
            enum: [sms, brevo, google_oauth, facebook_oauth, cloudflare, web_push]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminSaveIntegrationInput"
      responses:
        "200":
          description: Saved integration card with its next concurrency version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminIntegrationCard"
        "400": { description: Invalid integration fields or missing loaded version }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Unknown integration }
        "409":
          description: The integration was changed after the settings page loaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntegrationSettingsVersionConflict"
  /salons:
    get:
      operationId: listSalons
      tags: [salons]
      summary: Search salons
      parameters:
        - $ref: "#/components/parameters/CityQuery"
        - $ref: "#/components/parameters/CategoryQuery"
        - $ref: "#/components/parameters/TreatmentQuery"
        - $ref: "#/components/parameters/SortQuery"
        - name: priceMax
          in: query
          schema: { type: number }
        - name: municipality
          in: query
          schema: { type: string }
        - name: minRating
          in: query
          schema: { type: number, minimum: 0, maximum: 5 }
        - name: minReviewCount
          in: query
          schema: { type: number, minimum: 0 }
        - name: availability
          in: query
          schema: { type: string, enum: [today, tomorrow, this-week] }
        - name: homeService
          in: query
          schema: { type: boolean }
        - name: gender
          in: query
          schema: { type: string, enum: [all, women, men, everyone] }
        - name: discountsOnly
          in: query
          schema: { type: boolean }
        - name: acceptsCards
          in: query
          schema: { type: boolean }
        - name: openSunday
          in: query
          schema: { type: boolean }
        - name: instantBooking
          in: query
          schema: { type: boolean }
        - name: topSalon
          in: query
          schema: { type: boolean }
        - name: featured
          in: query
          schema: { type: boolean }
        - name: brand
          in: query
          schema: { type: string }
        - name: latitude
          in: query
          schema: { type: number }
        - name: longitude
          in: query
          schema: { type: number }
        - name: page
          in: query
          description: 1-based page number. All filters and the chosen sort are applied in SQL BEFORE this page is selected.
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of salons per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: One page of salon results
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonCard" }
  /discovery/home:
    get:
      operationId: getMarketplaceHomeDiscovery
      tags: [salons]
      summary: Get curated public discovery content for the marketplace homepage
      parameters:
        - $ref: "#/components/parameters/CityQuery"
      responses:
        "200":
          description: City-aware discovery shelves and popular services
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MarketplaceHomeDiscovery" }
  /cities:
    get:
      operationId: listCities
      tags: [salons]
      summary: List cities that have active salons with salon counts
      responses:
        "200":
          description: Cities derived from active salons, ordered deterministically
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CitySummary" }
  /platform/trust-stats:
    get:
      operationId: getPlatformTrustStats
      tags: [salons]
      summary: Get public marketplace trust statistics
      responses:
        "200":
          description: Aggregated marketplace trust statistics
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformTrustStats" }
  /salons/{slug}:
    get:
      operationId: getSalon
      tags: [salons]
      summary: Get salon profile
      parameters:
        - $ref: "#/components/parameters/SalonSlug"
      responses:
        "200":
          description: Salon profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalonProfile"
  /salons/{salonId}/availability:
    get:
      operationId: getSalonAvailability
      tags: [salons]
      summary: Get available booking slots
      parameters:
        - $ref: "#/components/parameters/SalonId"
        - name: serviceId
          in: query
          required: true
          schema: { type: string }
        - name: employeeId
          in: query
          schema: { type: string }
        - name: date
          in: query
          required: true
          schema: { type: string }
        - name: granularityMinutes
          in: query
          description: Optional requested cadence. The salon booking policy remains authoritative.
          schema: { type: integer, minimum: 5, maximum: 180 }
      responses:
        "200":
          description: Available slots
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TimeSlot" }
  /salons/{salonId}/first-available:
    get:
      operationId: getSalonFirstAvailable
      tags: [salons]
      summary: Get the first available slot for every active salon service
      parameters:
        - $ref: "#/components/parameters/SalonId"
      responses:
        "200":
          description: First available booking slots for active services
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonFirstAvailable" }
        "404": { description: Salon not found }
  /salons/{salonId}/grouped-availability:
    post:
      operationId: getGroupedBookingAvailability
      tags: [appointments]
      summary: Find contiguous or bounded-gap availability for an ordered treatment group
      parameters:
        - $ref: "#/components/parameters/SalonId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GroupedAvailabilityInput" }
      responses:
        "200":
          description: Grouped booking candidates with an ordered slot for every treatment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GroupedAvailabilityResponse" }
        "400": { description: Invalid date range or treatment selection }
        "404": { description: "Salon, service, or employee not found" }
  /booking-groups:
    post:
      operationId: createBookingGroup
      tags: [appointments]
      summary: Atomically book an ordered group of treatments
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BookingGroupInput" }
      responses:
        "201":
          description: Created booking group
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroup" }
        "409":
          description: A selected slot is unavailable, or the canonical active-group layout would be invalid
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupConflict" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/booking-groups:
    post:
      operationId: createSalonBookingGroup
      tags: [salon-management]
      summary: Atomically create a manual grouped booking for a CRM customer or guest
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualBookingGroupInput" }
      responses:
        "201":
          description: Created real booking group and its linked appointments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroup" }
        "403": { description: Salon owner access required }
        "409":
          description: A slot, resource, or canonical group layout conflicts
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupConflict" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /employee/booking-groups:
    post:
      operationId: createEmployeeBookingGroup
      tags: [employee-portal]
      summary: Atomically create a manual grouped booking assigned to the signed-in employee
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualBookingGroupInput" }
      responses:
        "201":
          description: Created real booking group and its linked appointments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroup" }
        "403": { description: Active employee access or customer relationship required }
        "409":
          description: A slot, resource, or canonical group layout conflicts
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupConflict" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /booking-commands/{idempotencyKey}:
    get:
      operationId: getBookingCommandReceipt
      tags: [appointments]
      summary: Recover the durable outcome of an authenticated booking command
      parameters:
        - name: idempotencyKey
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 200 }
        - name: salonId
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Original successful booking response
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingCommandReceipt" }
        "401": { description: Authentication required }
        "404": { description: No receipt exists in this actor and salon scope }
  /booking-groups/{bookingGroupId}/reschedule:
    patch:
      operationId: rescheduleBookingGroup
      tags: [appointments]
      summary: Atomically reschedule selected active members, or the complete active booking group
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BookingGroupRescheduleInput" }
      responses:
        "200":
          description: Updated group; gapWarning is retained for compatibility and is null for invariant-preserving mutations
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupOperationResult" }
        "403": { description: The actor may not manage this group }
        "404": { description: Booking group not found }
        "409":
          description: A selected slot is unavailable, or the canonical active remainder would be invalid
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupConflict" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /booking-groups/{bookingGroupId}/cancel:
    post:
      operationId: cancelBookingGroup
      tags: [appointments]
      summary: Atomically cancel selected active members, or all active members of a booking group
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BookingGroupCancelInput" }
      responses:
        "200":
          description: Updated booking group
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupOperationResult" }
        "403": { description: The actor may not manage this group }
        "404": { description: Booking group not found }
        "409":
          description: Cancelling the selected members would leave an invalid active remainder
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroupConflict" }
  /appointments:
    get:
      operationId: listMyAppointments
      tags: [appointments]
      summary: List appointments for the signed-in user
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [pending, confirmed, completed, cancelled, no-show] }
        - name: scope
          in: query
          schema: { type: string, enum: [upcoming, past, all], default: all }
        - name: page
          in: query
          description: 1-based page number for stable pagination (date asc, startTime asc, id asc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of appointments per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Appointment list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Appointment" }
    post:
      operationId: createAppointment
      tags: [appointments]
      summary: Create a booking
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AppointmentInput"
      responses:
        "201":
          description: Created booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Appointment"
        "409":
          description: >-
            Booking conflict. Package redemption failures carry a stable
            discriminator `code: PACKAGE_ERROR` plus the specific `reason`;
            plain availability conflicts omit `code`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppointmentConflictError"
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /employee/locations:
    get:
      operationId: listEmployeeAssignedLocations
      tags: [salon-management]
      summary: List the signed-in employee's active location assignments
      responses:
        "200":
          description: Active assignments and selected location
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeAssignedLocations" }
        "403": { description: Active employee access required }
  /employee/active-location:
    patch:
      operationId: selectEmployeeActiveLocation
      tags: [salon-management]
      summary: Select an actively assigned employee location
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeActiveLocationInput" }
      responses:
        "200":
          description: Selected active location
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeActiveLocation" }
        "403": { description: Employee is not actively assigned to location }
  /appointments/{appointmentId}:
    patch:
      operationId: updateAppointment
      tags: [appointments]
      summary: Reschedule or update appointment
      parameters:
        - $ref: "#/components/parameters/AppointmentId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AppointmentUpdate"
      responses:
        "200":
          description: Updated appointment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Appointment"
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /appointments/{appointmentId}/salon-contact:
    get:
      operationId: getAppointmentSalonContact
      tags: [appointments]
      summary: Get salon contact details for the signed-in customer's qualifying booking
      parameters:
        - $ref: "#/components/parameters/AppointmentId"
      responses:
        "200":
          description: Private salon contact and exact location for this booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppointmentSalonContact"
        "403": { description: The appointment is cancelled or does not belong to the signed-in customer }
        "404": { description: Appointment not found }
  /appointments/{appointmentId}/cancel:
    post:
      operationId: cancelAppointment
      tags: [appointments]
      summary: Cancel appointment
      parameters:
        - $ref: "#/components/parameters/AppointmentId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelAppointmentInput"
      responses:
        "200":
          description: Cancelled appointment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Appointment"
  /appointments/{appointmentId}/lifecycle:
    post:
      operationId: transitionAppointmentLifecycle
      tags: [appointments]
      summary: Record an audited appointment lifecycle transition
      parameters:
        - $ref: "#/components/parameters/AppointmentId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AppointmentLifecycleInput" }
      responses:
        "200":
          description: Appointment with actual timing and lifecycle audit state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Appointment" }
        "409":
          description: Transition is invalid for the current lifecycle state, or the useful late-treatment window has elapsed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentLifecycleConflict" }
  /customer/notifications:
    get:
      operationId: listCustomerNotifications
      tags: [customer]
      summary: List the signed-in customer's durable notification inbox
      parameters:
        - name: unreadOnly
          in: query
          schema: { type: boolean, default: false }
        - name: cursor
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 30 }
      responses:
        "200":
          description: Stable reverse-chronological notification page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerNotificationPage" }
  /customer/notifications/{notificationId}/read:
    post:
      operationId: markCustomerNotificationRead
      tags: [customer]
      summary: Idempotently mark a customer notification read
      parameters:
        - name: notificationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Updated notification
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerNotification" }
        "404": { description: Notification not found }
  /customer/notifications/read-all:
    post:
      operationId: markAllCustomerNotificationsRead
      tags: [customer]
      summary: Mark every current customer notification read
      responses:
        "204": { description: All notifications marked read }
  /push/config:
    get:
      operationId: getWebPushConfig
      tags: [customer]
      summary: Get the public Web Push configuration
      responses:
        "200":
          description: Public VAPID key availability; the private key is never returned
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WebPushConfig" }
  /push/deliveries/acknowledge:
    post:
      operationId: acknowledgeSystemPushDelivery
      tags: [customer]
      summary: Confirm that a service worker displayed a Web Push notification
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SystemPushDeliveryAcknowledgement" }
      responses:
        "204": { description: Delivery acknowledgement accepted }
        "400": { description: Invalid acknowledgement }
  /customer/push-subscriptions:
    get:
      operationId: listPushSubscriptions
      tags: [customer]
      summary: List Web Push devices owned by the current user
      responses:
        "200":
          description: Current user's device subscriptions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PushSubscription" }
        "401": { description: Authentication required }
    post:
      operationId: upsertPushSubscription
      tags: [customer]
      summary: Register or refresh the current browser's Web Push subscription
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PushSubscriptionInput" }
      responses:
        "200":
          description: Registered device subscription
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PushSubscription" }
        "400": { description: Invalid subscription }
        "401": { description: Authentication required }
        "503": { description: Web Push is not configured }
  /customer/push-subscriptions/{subscriptionId}:
    delete:
      operationId: deletePushSubscription
      tags: [customer]
      summary: Disable and remove a Web Push device owned by the current user
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Device subscription removed }
        "401": { description: Authentication required }
        "404": { description: Subscription not found or not owned by the current user }
  /customer/dashboard:
    get:
      operationId: getCustomerDashboard
      tags: [customer]
      summary: Customer dashboard summary
      responses:
        "200":
          description: Customer dashboard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerDashboard"
  /jobseeker/profile:
    get:
      operationId: getJobseekerProfile
      tags: [beauty-jobs]
      summary: Get the signed-in jobseeker profile
      responses:
        "200": { description: Profile, content: { application/json: { schema: { $ref: "#/components/schemas/JobseekerProfile" } } } }
        "403": { description: JOBSEEKER access required }
    put:
      operationId: updateJobseekerProfile
      tags: [beauty-jobs]
      summary: Replace the signed-in jobseeker professional profile
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/JobseekerProfileInput" }
      responses:
        "200": { description: Updated profile, content: { application/json: { schema: { $ref: "#/components/schemas/JobseekerProfile" } } } }
        "400": { description: Invalid profile or media references }
        "403": { description: JOBSEEKER access required }
  /jobseeker/dashboard:
    get:
      operationId: getJobseekerDashboard
      tags: [beauty-jobs]
      summary: Get private JOBSEEKER dashboard counts
      responses:
        "200": { description: Dashboard counts, content: { application/json: { schema: { $ref: "#/components/schemas/JobseekerDashboard" } } } }
        "403": { description: JOBSEEKER access required }
  /jobseeker/salon-interests:
    get:
      operationId: listJobseekerSalonInterests
      tags: [beauty-jobs]
      summary: List the signed-in jobseeker's salon interests
      responses:
        "200": { description: Salon ids, content: { application/json: { schema: { type: array, items: { type: string } } } } }
    put:
      operationId: replaceJobseekerSalonInterests
      tags: [beauty-jobs]
      summary: Replace the signed-in jobseeker's salon interests
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/JobseekerSalonInterestsInput" }
      responses:
        "200": { description: Salon ids, content: { application/json: { schema: { type: array, items: { type: string } } } } }
  /customer/favorites:
    get:
      operationId: listFavorites
      tags: [customer]
      summary: List favorite salons
      responses:
        "200":
          description: Favorite salons
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonCard" }
    post:
      operationId: toggleFavorite
      tags: [customer]
      summary: Add or remove a favorite salon
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FavoriteInput"
      responses:
        "200":
          description: Favorite state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FavoriteResult"
  /customer/reviews/{salonId}:
    get:
      operationId: getCustomerSalonReview
      tags: [customer]
      summary: Get the current customer's review and completed services for a salon
      parameters:
        - $ref: "#/components/parameters/SalonId"
      responses:
        "200":
          description: Customer review context
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerSalonReviewContext" }
        "403": { description: Customer access required }
        "404": { description: Salon not found }
    put:
      operationId: upsertCustomerSalonReview
      tags: [customer]
      summary: Create or update the current customer's review for a completed salon visit
      parameters:
        - $ref: "#/components/parameters/SalonId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerReviewInput" }
      responses:
        "200":
          description: Saved customer review
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerReview" }
        "400": { description: Invalid review }
        "403": { description: Customer access required }
        "404": { description: Salon not found }
    delete:
      operationId: deleteCustomerSalonReview
      tags: [customer]
      summary: Permanently delete the current customer's review for a salon
      parameters:
        - $ref: "#/components/parameters/SalonId"
      responses:
        "204": { description: Review deleted }
        "403": { description: Customer access required }
        "404": { description: Salon or customer review not found }
  /salon/dashboard:
    get:
      operationId: getSalonDashboard
      tags: [salon-management]
      summary: Active salon or owner-wide dashboard summary
      parameters:
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [location, all]
            default: location
      responses:
        "200":
          description: Salon dashboard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalonDashboard"
  /salon/profile:
    get:
      operationId: getManagedSalonProfile
      tags: [salon-management]
      summary: Get editable public profile settings for the active owned salon
      responses:
        "200":
          description: Editable salon profile settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonProfileMedia" }
        "403": { description: Salon-owner access required }
    patch:
      operationId: updateManagedSalonProfile
      tags: [salon-management]
      summary: Update public profile settings for the active owned salon
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonProfileMediaUpdate" }
      responses:
        "200":
          description: Updated salon profile settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonProfileMedia" }
        "400": { description: Invalid profile media }
        "403": { description: Salon-owner access required }
  /salon/booking-settings:
    get:
      operationId: getSalonBookingSettings
      tags: [salon-management]
      summary: Get active location booking policy, date overrides, and resource downtime
      responses:
        "200":
          description: Dynamic calendar settings for the active location
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonBookingSettings" }
        "403": { description: Salon management access required }
    put:
      operationId: replaceSalonBookingSettings
      tags: [salon-management]
      summary: Replace active location booking policy and calendar exceptions
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonBookingSettingsInput" }
      responses:
        "200":
          description: Replaced dynamic calendar settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonBookingSettings" }
        "400": { description: "Invalid granularity, reminder, hours, or downtime values" }
        "403": { description: Salon management access required }
  /media/uploads:
    post:
      operationId: requestMediaUpload
      tags: [media]
      summary: Create an owner-scoped App Storage image upload ticket
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MediaUploadInput" }
      responses:
        "200":
          description: Short-lived direct upload ticket
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MediaUploadTicket" }
        "400": { description: Invalid or unsupported image metadata }
        "403": { description: The current user cannot upload for this scope }
        "413": { description: Image exceeds the upload limit }
  /media/uploads/{uploadId}/finalize:
    post:
      operationId: finalizeMediaUpload
      tags: [media]
      summary: Validate, transform and promote a staged image
      parameters:
        - name: uploadId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Existing finalized asset (idempotent retry)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MediaAsset" }
        "201":
          description: Validated and finalized asset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MediaAsset" }
        "400": { description: The staged object is not a valid supported image }
        "403": { description: Upload ticket does not belong to the current user }
        "404": { description: Upload ticket not found }
        "410": { description: Upload ticket expired }
  /media/{assetId}:
    get:
      operationId: getMediaAsset
      tags: [media]
      summary: Serve an optimized image variant using Accept negotiation
      parameters:
        - name: assetId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        - name: size
          in: query
          schema: { type: string, enum: [thumbnail, medium, large, original], default: large }
        - name: format
          in: query
          schema: { type: string, enum: [avif, webp, fallback, original] }
        - name: v
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Optimized image bytes
          content:
            image/avif:
              schema: { type: string, format: binary }
            image/webp:
              schema: { type: string, format: binary }
            image/jpeg:
              schema: { type: string, format: binary }
            image/png:
              schema: { type: string, format: binary }
        "304": { description: Not modified }
        "403": { description: Private media access denied }
        "404": { description: Image or requested variant not found }
  /salon/appointments:
    get:
      operationId: listSalonAppointments
      tags: [salon-management]
      summary: List salon appointments
      parameters:
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
        - name: status
          in: query
          schema: { type: string }
        - name: employeeId
          in: query
          schema: { type: string }
        - name: serviceId
          in: query
          schema: { type: string }
        - name: page
          in: query
          description: 1-based page number for stable pagination (date asc, startTime asc, id asc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of appointments per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
      responses:
        "200":
          description: Salon appointments
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Appointment" }
    post:
      operationId: createSalonAppointment
      tags: [salon-management]
      summary: Create a salon appointment for a CRM customer or walk-in guest
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonAppointmentCreate" }
      responses:
        "201":
          description: Created appointment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Appointment" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/appointment-series/preview:
    post:
      operationId: previewSalonAppointmentSeries
      tags: [salon-management]
      summary: Check availability for a salon appointment series
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AppointmentSeriesPreviewInput" }
      responses:
        "200":
          description: Availability preview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesPreview" }
  /salon/package-appointments/preview:
    post:
      operationId: previewSalonPackageAppointments
      tags: [salon-management]
      summary: Preview booking every remaining session in a purchased package
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonPackageAppointmentsInput" }
      responses:
        "200":
          description: Package booking availability and entitlement preview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonPackageAppointmentsPreview" }
  /salon/package-appointments:
    post:
      operationId: createSalonPackageAppointments
      tags: [salon-management]
      summary: Atomically book every remaining session in a purchased package
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonPackageAppointmentsInput" }
      responses:
        "201":
          description: Created per-service series and package appointments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonPackageAppointmentsResult" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/time-blocks:
    get:
      operationId: listSalonTimeBlocks
      tags: [salon-management]
      summary: List owner-created employee time blocks for a date
      parameters:
        - name: date
          in: query
          required: true
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        - name: employeeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Time blocks
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/EmployeeTimeBlock" } }
    post:
      operationId: createSalonTimeBlock
      tags: [salon-management]
      summary: Create a same-day intraday employee block
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeTimeBlockInput" }
      responses:
        "201":
          description: Created time block
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeTimeBlock" }
        "409": { description: The block overlaps an active appointment }
  /salon/time-blocks/{timeBlockId}:
    delete:
      operationId: deleteSalonTimeBlock
      tags: [salon-management]
      summary: Delete an owner-created intraday employee block
      parameters:
        - name: timeBlockId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Deleted }
        "404": { description: Time block not found }
  /salon/availability/search:
    get:
      operationId: searchSalonAvailability
      tags: [salon-management]
      summary: Search server-confirmed owner booking availability over seven calendar days
      parameters:
        - name: serviceId
          in: query
          required: true
          schema: { type: string }
        - name: startDate
          in: query
          required: true
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        - name: employeeId
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: granularityMinutes
          in: query
          description: Optional requested cadence. The salon booking policy remains authoritative.
          schema: { type: integer, minimum: 5, maximum: 180 }
      responses:
        "200":
          description: Chronological server-confirmed slots in the bounded seven-day window; calendar clients treat dates without slots as unavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AvailabilitySearchResponse" }
        "404": { description: Service or employee not found in the active salon }
  /salon/calendar-day:
    get:
      operationId: getSalonCalendarDay
      tags: [salon-management]
      summary: Get active employee schedules and all-day leave for an owner calendar day
      parameters:
        - name: date
          in: query
          required: true
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
      responses:
        "200":
          description: Active employees with their explicit schedule windows and approved all-day leave
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/SalonCalendarDayEmployee" } }
  /salon/appointment-series:
    post:
      operationId: createSalonAppointmentSeries
      tags: [salon-management]
      summary: Create an atomic salon appointment series
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonAppointmentSeriesInput" }
      responses:
        "201":
          description: Created appointment series
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesResult" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/appointment-series/{seriesId}:
    delete:
      operationId: cancelSalonAppointmentSeries
      tags: [salon-management]
      summary: Cancel incomplete appointments in a salon series from today onward
      parameters:
        - name: seriesId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Cancelled series appointments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesCancellation" }
  /salon/appointment-series/{seriesId}/move/preview:
    post:
      operationId: previewSalonAppointmentSeriesMove
      tags: [salon-management]
      summary: Preview conflicts before moving all future unfinished appointments in a series
      parameters:
        - name: seriesId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AppointmentSeriesMoveInput" }
      responses:
        "200":
          description: Series move availability preview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesMovePreview" }
  /salon/appointment-series/{seriesId}/move:
    post:
      operationId: moveSalonAppointmentSeries
      tags: [salon-management]
      summary: Atomically move all future unfinished appointments in a series
      parameters:
        - name: seriesId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AppointmentSeriesMoveInput" }
      responses:
        "200":
          description: Moved appointments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesMoveResult" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/customers:
    get:
      operationId: listSalonCustomers
      tags: [salon-management]
      summary: List salon CRM customers
      parameters:
        - name: page
          in: query
          description: 1-based page number for stable pagination (lastName asc, firstName asc, id asc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of CRM customers per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Salon CRM customers
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonCustomer" }
  /employee/availability/search:
    get:
      operationId: searchEmployeeAvailability
      tags: [salon-management]
      summary: Search the signed-in employee's server-confirmed availability at their active location
      parameters:
        - name: serviceId
          in: query
          required: true
          schema: { type: string }
        - name: startDate
          in: query
          required: true
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        - name: granularityMinutes
          in: query
          description: Optional requested cadence. The active salon booking policy remains authoritative.
          schema: { type: integer, minimum: 5, maximum: 180 }
      responses:
        "200":
          description: Chronological server-confirmed slots in the bounded seven-day window for the signed-in employee; calendar clients treat dates without slots as unavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AvailabilitySearchResponse" }
        "403": { description: Service is not assigned to the signed-in employee at their active salon }
        "404": { description: Active service not found at the employee's active salon }
  /employee/appointment-series/preview:
    post:
      operationId: previewEmployeeAppointmentSeries
      tags: [employee-portal]
      summary: Check availability for an employee appointment series
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AppointmentSeriesPreviewInput" }
      responses:
        "200":
          description: Availability preview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesPreview" }
  /employee/appointment-series:
    post:
      operationId: createEmployeeAppointmentSeries
      tags: [employee-portal]
      summary: Create an atomic employee appointment series
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeAppointmentSeriesInput" }
      responses:
        "201":
          description: Created appointment series
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AppointmentSeriesResult" }
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /employee/appointments:
    post:
      operationId: createEmployeeAppointments
      tags: [employee-portal]
      summary: Atomically create one or more appointments assigned to the signed-in employee
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeAppointmentsInput" }
      responses:
        "201":
          description: Created appointments; an exact Idempotency-Key replay returns this original response
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeAppointmentsResult" }
        "400": { description: Invalid request or missing Idempotency-Key }
        "403": { description: "Active employee access, assigned service, or established customer relationship required" }
        "409":
          description: Slot/resource conflict, or Idempotency-Key reused with a different payload
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/AppointmentConflictError"
                  - $ref: "#/components/schemas/IdempotencyMismatchError"
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/customers/{customerId}:
    patch:
      operationId: updateSalonCustomer
      tags: [salon-management]
      summary: Update a salon CRM customer
      parameters:
        - name: customerId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonCustomerUpdate" }
      responses:
        "200":
          description: Updated CRM customer
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonCustomer" }
  /salon/appointments/{appointmentId}:
    patch:
      operationId: updateSalonAppointment
      tags: [salon-management]
      summary: Update appointment status or assignment
      parameters:
        - $ref: "#/components/parameters/AppointmentId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalonAppointmentUpdate"
      responses:
        "200":
          description: Updated appointment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Appointment"
        "429": { $ref: "#/components/responses/BookingCapacityReached" }
  /salon/resources:
    get:
      operationId: listSalonResources
      tags: [salon-management]
      summary: List salon resources
      responses:
        "200":
          description: Resources
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonResource" }
        "403": { description: Salon-owner access required }
    post:
      operationId: createSalonResource
      tags: [salon-management]
      summary: Create a salon resource
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonResourceInput" }
      responses:
        "201":
          description: Created resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonResource" }
        "400": { description: Invalid input }
        "403": { description: Salon-owner access required }
        "409": { description: A resource with this name already exists in the salon }
  /salon/resources/{resourceId}:
    patch:
      operationId: updateSalonResource
      tags: [salon-management]
      summary: Update a salon resource
      parameters:
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonResourceUpdate" }
      responses:
        "200":
          description: Updated resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonResource" }
        "400": { description: Invalid input }
        "403": { description: Salon-owner access required }
        "404": { description: Resource not found }
        "409": { description: A resource with this name already exists in the salon }
    delete:
      operationId: deleteSalonResource
      tags: [salon-management]
      summary: Delete a salon resource (only if no active service requirements reference it)
      parameters:
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Resource deleted }
        "403": { description: Salon-owner access required }
        "404": { description: Resource not found }
        "409":
          description: Resource is referenced by active service requirements
          content:
            application/json:
              schema:
                type: object
                properties:
                  error: { type: string }
                required: [error]
  /salon/services:
    get:
      operationId: listSalonServices
      tags: [salon-management]
      summary: List salon services
      responses:
        "200":
          description: Services
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonManagedService" }
    post:
      operationId: createSalonService
      tags: [salon-management]
      summary: Create service
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceInput"
      responses:
        "201":
          description: Created service
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
  /service-templates:
    get:
      operationId: listServiceTemplates
      tags: [salon-management]
      summary: List active service templates for a salon owner
      parameters:
        - name: search
          in: query
          schema: { type: string, maxLength: 120 }
        - name: mainCategory
          in: query
          schema: { type: string, maxLength: 120 }
        - name: subcategory
          in: query
          schema: { type: string, maxLength: 120 }
      responses:
        "200":
          description: Active service templates
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ServiceTemplate" }
  /salon/services/from-templates:
    post:
      operationId: createSalonServicesBatch
      tags: [salon-management]
      summary: Create services from curated templates with salon pricing
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ServiceTemplateBatchInput" }
      responses:
        "201":
          description: Created services and duplicate skips
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceTemplateBatchResult" }
  /salon/services/{serviceId}:
    patch:
      operationId: updateSalonService
      tags: [salon-management]
      summary: Update a salon service
      parameters:
        - name: serviceId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ServiceInput" }
      responses:
        "200":
          description: Updated service
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Service" }
    delete:
      operationId: deleteSalonService
      tags: [salon-management]
      summary: Permanently remove a salon service without appointments
      parameters:
        - name: serviceId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Service removed
        "404":
          description: Service not found
        "409":
          description: Service is linked to existing appointments
          content:
            application/json:
              schema:
                type: object
                properties:
                  error: { type: string }
                required: [error]
  /salon/employees:
    get:
      operationId: listSalonEmployees
      tags: [salon-management]
      summary: List salon employees
      responses:
        "200":
          description: Employees
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Employee" }
  /salon/locations:
    post:
      operationId: createSalonLocation
      tags: [salon-management]
      summary: Create an additional independently operated location for the current owner
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSalonLocationInput" }
      responses:
        "201":
          description: Location created, optionally with copied service and package definitions
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CreateSalonLocationResult" }
        "400": { description: Invalid location profile or unsafe package copy request }
        "403": { description: Salon-owner access required or source location is not owned }
        "409": { description: Idempotency key was previously used with a different request }
  /salon/employees/{employeeId}/locations:
    get:
      operationId: listEmployeeLocationAssignments
      tags: [salon-management]
      summary: List owner-managed location assignments for an employee
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Assignments, including location schedule metadata
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EmployeeLocationAssignment" }
        "403": { description: Employee or a requested location is outside the current owner }
        "404": { description: Employee not found }
  /salon/employees/{employeeId}/locations/{salonId}:
    put:
      operationId: upsertEmployeeLocationAssignment
      tags: [salon-management]
      summary: Create or update an employee location assignment
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
        - name: salonId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeLocationAssignmentUpdate" }
      responses:
        "200":
          description: Updated assignment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeLocationAssignment" }
        "403": { description: Employee or location is outside the current owner }
        "409": { description: Deactivation would strand future appointments }
  /salon/employees/{employeeId}/locations/{salonId}/schedule:
    put:
      operationId: replaceEmployeeLocationSchedule
      tags: [salon-management]
      summary: Replace weekly schedule windows for one active employee location assignment
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
        - name: salonId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeLocationScheduleUpdate" }
      responses:
        "200":
          description: Updated location schedule
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EmployeeLocationScheduleWindow" }
        "403": { description: Employee or location is outside the current owner }
        "409": { description: Schedule would strand future appointments }
  /salon/employees/{employeeId}/deactivation-preview:
    get:
      operationId: getSalonEmployeeDeactivationPreview
      tags: [salon-management]
      summary: Get the impact of deactivating a salon employee
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Employee deactivation impact
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeDeactivationPreview" }
  /salon/employees/{employeeId}/deactivate:
    post:
      operationId: deactivateSalonEmployee
      tags: [salon-management]
      summary: Soft-deactivate an employee and their login account
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Employee deactivated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeDeactivationResult" }
  /salon/leave-requests:
    get:
      operationId: listSalonLeaveRequests
      tags: [salon-management]
      summary: List employee leave requests for the active salon (owner)
      responses:
        "200":
          description: Leave requests for all employees in the salon
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonLeaveRequest" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
  /salon/leave-requests/{requestId}:
    patch:
      operationId: reviewSalonLeaveRequest
      tags: [salon-management]
      summary: Approve or reject an employee leave request (owner)
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LeaveRequestReviewBody" }
      responses:
        "200":
          description: Leave request reviewed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LeaveRequestReviewResult" }
        "400": { description: "Invalid status value" }
        "401": { description: "Authentication required" }
        "403": { description: "Salon owner role required, or request belongs to another salon" }
        "404": { description: "Leave request not found" }
        "409": { description: "Leave request already reviewed" }
  /employee/leave-requests:
    post:
      operationId: createEmployeeLeaveRequest
      tags: [salon-management]
      summary: Submit a leave request (employee)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeLeaveRequestCreate" }
      responses:
        "201":
          description: Leave request created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeLeaveRequest" }
        "400": { description: Invalid period or reason }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
  /shop/categories:
    get:
      operationId: listProductCategories
      tags: [shop]
      summary: List B2B product category tree
      responses:
        "200":
          description: Category tree
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProductCategory" }
  /shop/brands:
    get:
      operationId: listProductBrands
      tags: [shop]
      summary: List active B2B product brands
      responses:
        "200":
          description: Active product brands
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProductBrand" }
  /shop/public/products:
    get:
      operationId: listPublicProducts
      tags: [public-shop]
      summary: List customer-facing products that an administrator has approved for public display
      parameters:
        - name: category
          in: query
          schema: { type: string }
        - name: subcategory
          in: query
          schema: { type: string }
        - name: brand
          in: query
          schema: { type: string }
        - name: search
          in: query
          schema: { type: string }
        - name: onSale
          in: query
          schema: { type: boolean }
        - name: isNew
          in: query
          schema: { type: boolean }
        - name: isBestseller
          in: query
          schema: { type: boolean }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: Public products without B2B data
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicProductList" }
  /shop/public/products/{productId}:
    get:
      operationId: getPublicProduct
      tags: [public-shop]
      summary: Get one customer-facing product without B2B data
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Public product
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicProductDetail" }
        "404": { description: Product is not public or is unavailable }
  /shop/products:
    get:
      operationId: listProducts
      tags: [shop]
      summary: List professional products
      parameters:
        - name: category
          in: query
          schema: { type: string }
        - name: subcategory
          in: query
          schema: { type: string }
        - name: brand
          in: query
          schema: { type: string }
        - name: search
          in: query
          schema: { type: string }
        - name: onSale
          in: query
          schema: { type: boolean }
        - name: isNew
          in: query
          schema: { type: boolean }
        - name: isBestseller
          in: query
          schema: { type: boolean }
        - name: page
          in: query
          description: 1-based page number for stable pagination (name asc, id asc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of products per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: Paginated products
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductList" }
  /shop/bundles:
    get:
      operationId: listShopBundles
      tags: [shop]
      summary: List active B2B fixed-price bundles
      responses:
        "200":
          description: Available professional bundles. Component cards intentionally contain no unit prices.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Bundle" } }
        "401": { description: Authentication required }
        "403": { description: Salon-owner access required }
  /shop/bundles/{bundleId}:
    get:
      operationId: getShopBundle
      tags: [shop]
      summary: Get an active B2B fixed-price bundle
      parameters:
        - name: bundleId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200": { description: Bundle detail, content: { application/json: { schema: { $ref: "#/components/schemas/Bundle" } } } }
        "404": { description: Bundle is unavailable }
  /shop/public/bundles:
    get:
      operationId: listPublicBundles
      tags: [public-shop]
      summary: List active B2C fixed-price bundles
      responses:
        "200":
          description: Public bundles; neither B2B prices nor component unit prices are exposed.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/PublicBundle" } }
  /shop/public/bundles/{bundleId}:
    get:
      operationId: getPublicBundle
      tags: [public-shop]
      summary: Get an active B2C fixed-price bundle
      parameters:
        - name: bundleId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200": { description: Public bundle detail, content: { application/json: { schema: { $ref: "#/components/schemas/PublicBundle" } } } }
        "404": { description: Bundle is unavailable }
  /shop/products/{productId}:
    get:
      operationId: getShopProduct
      tags: [shop]
      summary: Get a professional product with reviews and related products
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Product detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductDetail" }
  /shop/products/{productId}/waitlist:
    get:
      operationId: getB2bProductWaitlistStatus
      tags: [shop]
      summary: Get the signed-in salon's product availability subscription
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200": { description: Subscription status, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWaitlistStatus" } } } }
        "401": { description: Authentication required }
        "403": { description: Salon-owner access required }
    post:
      operationId: subscribeB2bProductWaitlist
      tags: [shop]
      summary: Subscribe the signed-in salon to an out-of-stock B2B product
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses:
        "201": { description: Subscription created, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWaitlistSubscription" } } } }
        "409": { description: Product is in stock }
    delete:
      operationId: unsubscribeB2bProductWaitlist
      tags: [shop]
      summary: Unsubscribe the signed-in salon (idempotent)
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses: { "204": { description: Unsubscribed } }
  /shop/public/products/{productId}/waitlist:
    get:
      operationId: getB2cProductWaitlistStatus
      tags: [shop]
      summary: Get the signed-in customer's product availability subscription
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: Subscription status, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWaitlistStatus" } } } } }
    post:
      operationId: subscribeB2cProductWaitlist
      tags: [shop]
      summary: Subscribe the signed-in customer to an out-of-stock B2C product
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses: { "201": { description: Subscription created, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWaitlistSubscription" } } } }, "409": { description: Product is in stock } }
    delete:
      operationId: unsubscribeB2cProductWaitlist
      tags: [shop]
      summary: Unsubscribe the signed-in customer (idempotent)
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: Unsubscribed } }
  /shop/products/{productId}/reviews:
    get:
      operationId: listProductReviews
      tags: [shop]
      summary: List visible reviews for a product
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Product reviews
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProductReview" }
    post:
      operationId: upsertProductReview
      tags: [shop]
      summary: Create or update the current salon review after purchase
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProductReviewInput" }
      responses:
        "200":
          description: Saved review
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductReview" }
  /shop/summary:
    get:
      operationId: getShopSummary
      tags: [shop]
      summary: Salon shop and loyalty progress
      responses:
        "200":
          description: Shop summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShopSummary"
  /shop/order-import/template:
    get:
      operationId: downloadB2bOrderImportTemplate
      tags: [shop]
      summary: Download the UTF-8 BOM B2B order-import CSV template
      responses:
        "200":
          description: RFC4180 CSV with SKU and Quantity columns and an example row
          content:
            text/csv:
              schema: { type: string, format: binary }
        "401": { description: Authentication required }
        "403": { description: Salon-owner access required }
  /shop/order-import/preview:
    post:
      operationId: previewB2bOrderImport
      tags: [shop]
      summary: Parse and validate a B2B order-import CSV without changing the cart
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/B2bOrderImportPreviewInput" }
      responses:
        "200": { description: "Row diagnostics, including valid rows", content: { application/json: { schema: { $ref: "#/components/schemas/B2bOrderImportResult" } } } }
        "400": { description: "Malformed CSV, required columns missing, or import limits exceeded" }
        "401": { description: Authentication required }
        "403": { description: Salon-owner access required }
  /shop/order-import/apply:
    post:
      operationId: applyB2bOrderImport
      tags: [shop]
      summary: Idempotently merge valid B2B CSV rows into the authenticated salon cart
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/B2bOrderImportApplyInput" }
      responses:
        "200": { description: Existing idempotent import result, content: { application/json: { schema: { $ref: "#/components/schemas/B2bOrderImportResult" } } } }
        "201": { description: Import result after cart merge, content: { application/json: { schema: { $ref: "#/components/schemas/B2bOrderImportResult" } } } }
        "400": { description: "Malformed CSV, required columns missing, or import limits exceeded" }
        "409": { description: Invalid rows require confirmed=true before valid rows can be applied }
        "401": { description: Authentication required }
        "403": { description: Salon-owner access required }
  /shop/cart:
    get:
      operationId: getShopCart
      tags: [shop]
      summary: Get the persistent cart belonging to the current salon
      responses:
        "200":
          description: Persistent cart
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCart" }
  /shop/cart/items:
    post:
      operationId: addShopCartItem
      tags: [shop]
      summary: Add a product or variant to the persistent cart
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShopCartItemInput" }
      responses:
        "200":
          description: Updated cart
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCart" }
  /shop/cart/items/{cartItemId}:
    patch:
      operationId: updateShopCartItem
      tags: [shop]
      summary: Change a persistent cart item quantity
      parameters:
        - name: cartItemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShopCartItemQuantityInput" }
      responses:
        "200":
          description: Updated cart
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCart" }
    delete:
      operationId: removeShopCartItem
      tags: [shop]
      summary: Remove an item from the persistent cart
      parameters:
        - name: cartItemId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Updated cart
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCart" }
  /shop/cart/items/{cartItemId}/save-for-later:
    post:
      operationId: saveShopCartItemForLater
      tags: [shop]
      summary: Atomically move a cart line to saved items
      parameters: [{ name: cartItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated cart, content: { application/json: { schema: { $ref: "#/components/schemas/ShopCart" } } } }
  /shop/cart/saved-items/{savedItemId}:
    delete:
      operationId: removeSavedShopCartItem
      tags: [shop]
      summary: Remove a saved cart line
      parameters: [{ name: savedItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated cart, content: { application/json: { schema: { $ref: "#/components/schemas/ShopCart" } } } }
  /shop/cart/saved-items/{savedItemId}/restore:
    post:
      operationId: restoreSavedShopCartItem
      tags: [shop]
      summary: Restore a saved line using current eligibility, price, MOQ and stock
      parameters: [{ name: savedItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated cart, content: { application/json: { schema: { $ref: "#/components/schemas/ShopCart" } } } }
        "409": { description: Current catalog rules prevent restore }
  /shop/orders/repeat-last:
    post:
      operationId: repeatLastShopOrder
      tags: [shop]
      summary: Add eligible lines from the latest salon order at current prices
      parameters: [{ name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 8, maxLength: 200 } }]
      responses:
        "200": { description: Reorder result, content: { application/json: { schema: { $ref: "#/components/schemas/ReorderResult" } } } }
  /shop/checkout-profile:
    get:
      operationId: getShopCheckoutProfile
      tags: [shop]
      summary: Get authorized delivery salons and active-salon billing defaults
      responses:
        "200":
          description: Checkout profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCheckoutProfile" }
  /shop/approval-requests:
    get:
      operationId: listShopApprovalRequests
      tags: [shop]
      summary: List approval requests for the active location or every location owned by the owner
      parameters:
        - name: scope
          in: query
          schema: { type: string, enum: [location, all], default: location }
      responses:
        "200": { description: Requests, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ApprovalRequest" } } } } }
    post:
      operationId: createShopApprovalRequest
      tags: [shop]
      summary: Submit the active employee salon cart for approval without finalizing it
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/ApprovalRequestInput" } } }
      responses:
        "201": { description: Submitted request, content: { application/json: { schema: { $ref: "#/components/schemas/ApprovalRequest" } } } }
        "409": { description: Employee may order directly }
  /shop/approval-requests/mine:
    get:
      operationId: listMyShopApprovalRequests
      tags: [shop]
      summary: List the authenticated employee's approval requests
      responses:
        "200": { description: Requests, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ApprovalRequest" } } } } }
  /shop/approval-requests/{requestId}:
    get:
      operationId: getShopApprovalRequest
      tags: [shop]
      summary: Get an approval request in the active location or any owner-owned location
      parameters:
        - { name: requestId, in: path, required: true, schema: { type: string } }
        - name: scope
          in: query
          schema: { type: string, enum: [location, all], default: location }
      responses:
        "200": { description: Request detail, content: { application/json: { schema: { $ref: "#/components/schemas/ApprovalRequest" } } } }
        "404": { description: Request not found }
  /shop/approval-requests/{requestId}/reject:
    post:
      operationId: rejectShopApprovalRequest
      tags: [shop]
      summary: Reject a pending active-salon request
      parameters: [{ name: requestId, in: path, required: true, schema: { type: string } }]
      requestBody:
        content: { application/json: { schema: { $ref: "#/components/schemas/ApprovalRejectInput" } } }
      responses:
        "200": { description: Rejected request, content: { application/json: { schema: { $ref: "#/components/schemas/ApprovalRequest" } } } }
        "409": { description: Request is not pending }
  /shop/approval-requests/{requestId}/approve:
    post:
      operationId: approveShopApprovalRequest
      tags: [shop]
      summary: Atomically approve and finalize a pending employee order request
      parameters: [{ name: requestId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Previously finalized order, content: { application/json: { schema: { $ref: "#/components/schemas/Order" } } } }
        "201": { description: Newly finalized order, content: { application/json: { schema: { $ref: "#/components/schemas/Order" } } } }
        "409": { description: Request or checkout quote is no longer current }
  /shop/checkout-preview:
    get:
      operationId: getShopCheckoutPreview
      tags: [shop]
      summary: Calculate the server-authoritative totals for the persistent cart
      parameters:
        - name: desiredReferralCreditRsd
          in: query
          schema: { type: integer, minimum: 0 }
        - name: couponCode
          in: query
          schema: { type: string, maxLength: 40 }
      responses:
        "200":
          description: Checkout preview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShopCheckoutPreview" }
  /shop/checkout:
    post:
      operationId: checkoutShopCart
      tags: [shop]
      summary: Create an order from the persistent cart and clear it atomically
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShopCheckoutInput" }
      responses:
        "201":
          description: Created order
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Order" }
  /shop/notifications:
    get:
      operationId: listSalonNotifications
      tags: [shop]
      summary: List notifications for the current salon owner
      parameters:
        - name: page
          in: query
          description: 1-based page number for stable pagination (createdAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of notifications per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Salon notifications
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonNotification" }
  /shop/notifications/{notificationId}/read:
    patch:
      operationId: markSalonNotificationRead
      tags: [shop]
      summary: Mark one salon notification as read
      parameters:
        - name: notificationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Updated salon notification
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonNotification" }
  /shop/orders:
    get:
      operationId: listOrders
      tags: [shop]
      summary: List salon orders
      parameters:
        - name: page
          in: query
          description: 1-based page number for stable pagination (createdAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of orders per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Order" }
    post:
      operationId: createOrder
      tags: [shop]
      deprecated: true
      summary: Deprecated — use checkoutShopCart to create orders from the saved cart
      responses:
        "410":
          description: Direct ordering is retired; checkout must use the saved cart
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
  /shop/orders/{orderId}:
    get:
      operationId: getOrder
      tags: [shop]
      summary: Get one order belonging to the current salon
      parameters:
        - name: orderId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Order detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Order" }
  /shop/orders/{orderId}/invoice.pdf:
    get:
      operationId: downloadShopOrderInvoice
      tags: [shop]
      summary: Download the immutable B2B invoice PDF for an owned order
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      responses:
        "200":
          description: Invoice PDF
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "404": { description: Order or invoice not found }
  /shop/shipping-quote:
    get:
      operationId: getShippingQuote
      tags: [shop]
      summary: Calculate weight-based shipping for a cart
      parameters:
        - name: weightGrams
          in: query
          required: true
          schema: { type: number, minimum: 0, multipleOf: 1 }
        - name: subtotal
          in: query
          required: true
          schema: { type: number, minimum: 0, multipleOf: 1 }
      responses:
        "200":
          description: Shipping quote
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShippingQuote"
  /admin/orders:
    get:
      operationId: adminListOrders
      tags: [admin]
      summary: List all B2B orders for administration
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [pending, confirmed, paid, processing, shipped, delivered, cancelled] }
        - name: salon
          in: query
          schema: { type: string }
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
        - name: search
          in: query
          schema: { type: string }
        - name: paymentStatus
          in: query
          schema: { type: string, enum: [unpaid, pending, paid, refunded, failed] }
        - name: deliveryMethod
          in: query
          schema: { type: string, enum: [courier, personal_belgrade] }
        - name: page
          in: query
          description: 1-based page number for stable pagination (createdAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of rows per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Admin order list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminOrder" }
  /admin/orders/{orderId}:
    get:
      operationId: adminGetOrder
      tags: [admin]
      summary: Get complete B2B order for administration
      parameters:
        - name: orderId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Admin order detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminOrder" }
    patch:
      operationId: adminUpdateOrderStatus
      tags: [admin]
      summary: Update B2B order fulfillment, payment and internal operational details
      parameters:
        - name: orderId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminOrderUpdate" }
      responses:
        "200":
          description: Updated order
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminOrder" }
  /admin/orders/{orderId}/invoice.pdf:
    get:
      operationId: adminDownloadOrderInvoice
      tags: [admin]
      summary: Download the immutable B2B invoice PDF for administration
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      responses:
        "200":
          description: Invoice PDF
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "404": { description: Order or invoice not found }
  /admin/orders/bulk:
    patch:
      operationId: adminBulkUpdateOrders
      tags: [admin]
      summary: Apply one operational update to selected B2B orders
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminOrderBulkUpdate" }
      responses:
        "200":
          description: Updated orders
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminOrder" }
  /loyalty/status:
    get:
      operationId: getLoyaltyStatus
      tags: [loyalty]
      summary: Get current partnership tier
      responses:
        "200":
          description: Loyalty status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyStatus"
  /loyalty/tiers:
    get:
      operationId: listLoyaltyTiers
      tags: [loyalty]
      summary: List all active partnership tiers
      responses:
        "200":
          description: Loyalty tiers
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LoyaltyTier" }
  /education/center/status:
    get:
      operationId: getEducationCenterStatus
      tags: [education]
      summary: Get verification and subscription status for the current education center
      responses:
        "200":
          description: Center operational status
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCenterStatus" }
  /education/purchases:
    get:
      operationId: listEducationPurchases
      tags: [education]
      summary: List the current buyer's education purchases
      responses:
        "200":
          description: Buyer purchases
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationEnrollment" }
  /education/purchases/{enrollmentId}/messages:
    get:
      operationId: listEducationPurchaseMessages
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EnrollmentId" }]
      responses:
        "200":
          description: Purchase thread messages
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMessageThread" }
    post:
      operationId: createEducationPurchaseMessage
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EnrollmentId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: { body: { type: string, minLength: 1, maxLength: 4000 } }
              required: [body]
      responses:
        "201":
          description: Message created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMessage" }
  /education/purchases/{enrollmentId}/disputes:
    post:
      operationId: createEducationDispute
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EnrollmentId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason: { type: string, minLength: 1, maxLength: 160 }
                details: { type: string, minLength: 1, maxLength: 4000 }
              required: [reason, details]
      responses:
        "201":
          description: Dispute opened and escrow frozen
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationDispute" }
  /education/disputes:
    get:
      operationId: listEducationDisputes
      tags: [education]
      summary: List disputes visible to the current buyer, center or administrator
      responses:
        "200":
          description: Disputes
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationDispute" }
  /admin/education/settings:
    get:
      operationId: getAdminEducationSettings
      tags: [admin, education]
      responses:
        "200":
          description: Education marketplace settlement settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMarketplaceSettings" }
    patch:
      operationId: updateAdminEducationSettings
      tags: [admin, education]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationMarketplaceSettingsInput" }
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMarketplaceSettings" }
  /admin/education/centers:
    get:
      operationId: listAdminEducationCenters
      tags: [admin, education]
      responses:
        "200":
          description: Education centers and their operational status
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationAdminCenter" }
  /admin/education/centers/{centerId}:
    get:
      operationId: getAdminEducationCenter
      tags: [admin, education]
      parameters: [{ $ref: "#/components/parameters/EducationCenterId" }]
      responses:
        "200":
          description: Education center details with resolved billing settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationAdminCenterDetail" }
        "404": { description: Education center not found }
    patch:
      operationId: updateAdminEducationCenter
      tags: [admin, education]
      parameters: [{ $ref: "#/components/parameters/EducationCenterId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationAdminCenterUpdate" }
      responses:
        "200":
          description: Center details and billing overrides updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationAdminCenterDetail" }
  /admin/education/finance:
    get:
      operationId: getAdminEducationFinance
      tags: [admin, education]
      responses:
        "200":
          description: Escrow, payout and settlement overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationFinanceOverview" }
  /admin/education/enrollments/{enrollmentId}/settle:
    post:
      operationId: settleAdminEducationEnrollment
      tags: [admin, education]
      summary: Confirm a manually settled education purchase and create its escrow
      parameters: [{ $ref: "#/components/parameters/EnrollmentId" }]
      responses:
        "200":
          description: Confirmed enrollment with active course access
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationEnrollment" }
        "409": { description: Enrollment is not pending or course can no longer be settled }
  /admin/education/featured-charges/{chargeId}/settle:
    post:
      operationId: settleAdminEducationFeaturedCharge
      tags: [admin, education]
      summary: Mark a pending featured-course charge as manually settled
      parameters:
        - name: chargeId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationFeaturedChargeSettlementInput" }
      responses:
        "200":
          description: Settled featured-course charge
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationFeaturedChargeSettlement" }
        "404": { description: Featured-course charge not found }
        "409": { description: Featured-course charge is no longer pending }
  /admin/education/payouts:
    post:
      operationId: createAdminEducationPayout
      tags: [admin, education]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationPayoutInput" }
      responses:
        "201":
          description: Manual payout recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPayout" }
  /admin/education/disputes/{disputeId}:
    patch:
      operationId: resolveAdminEducationDispute
      tags: [admin, education]
      parameters: [{ $ref: "#/components/parameters/EducationDisputeId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationDisputeResolutionInput" }
      responses:
        "200":
          description: Dispute resolved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationDispute" }
  /admin/education/taxonomy/proposals:
    get:
      operationId: listAdminEducationTaxonomyProposals
      tags: [admin, education]
      summary: List education course-type proposals
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [pending, approved, rejected] }
      responses:
        "200":
          description: Taxonomy proposals
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCourseType" }
  /admin/education/taxonomy/proposals/{proposalId}/review:
    post:
      operationId: reviewAdminEducationTaxonomyProposal
      tags: [admin, education]
      summary: Approve or reject an education course-type proposal
      parameters:
        - name: proposalId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                decision: { type: string, enum: [approved, rejected] }
                note: { type: ["string", "null"], maxLength: 1000 }
              required: [decision]
      responses:
        "200":
          description: Reviewed proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCourseType" }
        "409": { description: Proposal was already reviewed or duplicates approved taxonomy }
  /admin/education/taxonomy/{kind}/{taxonomyId}:
    patch:
      operationId: updateAdminEducationTaxonomy
      tags: [admin, education]
      summary: Update a taxonomy item's display fields
      parameters:
        - name: kind
          in: path
          required: true
          schema: { type: string, enum: [sections, categories, subcategories, course-types] }
        - name: taxonomyId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationTaxonomyPatch" }
      responses:
        "200":
          description: Updated taxonomy item
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationTaxonomyItem" }
  /admin/education/placement-settings:
    get:
      operationId: getAdminEducationPlacementSettings
      tags: [admin, education]
      responses:
        "200":
          description: Placement products
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationPlacementSetting" }
    patch:
      operationId: updateAdminEducationPlacementSettings
      tags: [admin, education]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items: { $ref: "#/components/schemas/EducationPlacementSettingInput" }
      responses:
        "200":
          description: Updated placement products
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationPlacementSetting" }
  /admin/featured-placements:
    get:
      operationId: listAdminFeaturedPlacements
      tags: [admin]
      summary: List the shared paid-placement confirmation queue
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [pending_payment, active, expired, cancelled, rejected] }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Paginated shared placement queue
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeaturedPlacementPage" }
  /admin/featured-placements/{placementId}/confirm:
    post:
      operationId: confirmAdminFeaturedPlacement
      tags: [admin]
      summary: Idempotently confirm a pending placement bank transfer
      parameters:
        - name: placementId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Confirmed placement; repeated confirmation returns the same activation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeaturedPlacementConfirmation" }
        "404": { description: Placement not found }
        "409": { description: Placement cannot be confirmed }
  /education/courses:
    get:
      operationId: listCourses
      tags: [education]
      summary: Browse business education courses
      parameters:
        - name: format
          in: query
          schema: { type: string, enum: [online, in-person, hybrid] }
        - name: city
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
        - name: q
          in: query
          schema: { type: string, maxLength: 160 }
        - name: sectionId
          in: query
          schema: { type: string }
        - name: categoryId
          in: query
          schema: { type: string }
        - name: subcategoryId
          in: query
          schema: { type: string }
        - name: courseTypeId
          in: query
          schema: { type: string }
        - name: language
          in: query
          schema: { type: string }
        - name: accredited
          in: query
          schema: { type: boolean }
        - name: center
          in: query
          schema: { type: string }
        - name: certification
          in: query
          schema: { type: boolean }
        - name: minPrice
          in: query
          schema: { type: number, minimum: 0 }
        - name: maxPrice
          in: query
          schema: { type: number, minimum: 0 }
        - name: startDate
          in: query
          schema:
            type: string
            description: Include courses that start on or after this date.
        - name: minRating
          in: query
          schema: { type: number, minimum: 0, maximum: 5 }
        - name: mine
          in: query
          schema: { type: boolean }
        - name: page
          in: query
          description: 1-based page index for stable createdAt desc, id desc ordering.
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of courses per page.
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: Courses
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Course" }
        "403":
          description: Business access required
    post:
      operationId: createEducationCourse
      tags: [education]
      summary: Create a course owned by the current business
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationCourseInput" }
      responses:
        "201":
          description: Course created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Business access required }
  /education/courses/{courseId}:
    get:
      operationId: getEducationCourse
      tags: [education]
      summary: Get a business course and its curriculum
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Course detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Business access required }
        "404": { description: Course not found }
    patch:
      operationId: updateEducationCourse
      tags: [education]
      summary: Update an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationCourseUpdate" }
      responses:
        "200":
          description: Updated course
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Business access required }
    delete:
      operationId: archiveEducationCourse
      tags: [education]
      summary: Archive an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "204": { description: Course archived }
        "403": { description: Business access required }
  /education/courses/{courseId}/publish:
    post:
      operationId: publishEducationCourse
      tags: [education]
      summary: Publish an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Published course
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Business access required }
  /education/courses/{courseId}/gallery/upload-url:
    post:
      operationId: requestEducationCourseGalleryUpload
      tags: [education]
      summary: Request a direct upload URL for an owned course gallery image
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGalleryUploadInput" }
      responses:
        "200":
          description: Direct upload information
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationGalleryUpload" }
        "413": { description: Image exceeds the 8 MB limit }
        "403": { description: Education-center ownership required }
  /education/courses/{courseId}/gallery:
    post:
      operationId: addEducationCourseGalleryMedia
      tags: [education]
      summary: Attach an uploaded image to an owned course gallery
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGalleryMediaInput" }
      responses:
        "200":
          description: Gallery image was already attached
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMedia" }
        "201":
          description: Gallery image attached
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationMedia" }
        "403": { description: Education-center ownership required }
    put:
      operationId: reorderEducationCourseGallery
      tags: [education]
      summary: Reorder and update captions in an owned course gallery
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGalleryReorderInput" }
      responses:
        "200":
          description: Updated gallery
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationMedia" }
        "403": { description: Education-center ownership required }
  /education/courses/{courseId}/gallery/{mediaId}:
    delete:
      operationId: deleteEducationCourseGalleryMedia
      tags: [education]
      summary: Remove an image from an owned course gallery
      parameters:
        - $ref: "#/components/parameters/CourseId"
        - name: mediaId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "204": { description: Gallery image removed }
        "403": { description: Education-center ownership required }
  /education/media/{mediaId}:
    get:
      operationId: getEducationGalleryMedia
      tags: [education]
      summary: Serve a public course gallery image or an owner's unpublished image
      parameters:
        - name: mediaId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Image bytes
          content:
            image/jpeg: { schema: { type: string, format: binary } }
            image/png: { schema: { type: string, format: binary } }
            image/webp: { schema: { type: string, format: binary } }
            image/gif: { schema: { type: string, format: binary } }
        "403": { description: Owner access required for unpublished course media }
        "404": { description: Gallery image not found }
  /education/courses/{courseId}/modules:
    get:
      operationId: listEducationModules
      tags: [education]
      summary: List course modules and lessons
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Course modules
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationModule" }
        "403": { description: Business access required }
    post:
      operationId: createEducationModule
      tags: [education]
      summary: Create a module in an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationModuleInput" }
      responses:
        "201":
          description: Module created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationModule" }
        "403": { description: Business access required }
  /education/modules/{moduleId}:
    patch:
      operationId: updateEducationModule
      tags: [education]
      summary: Update a module
      parameters:
        - $ref: "#/components/parameters/ModuleId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationModuleInput" }
      responses:
        "200":
          description: Updated module
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationModule" }
        "403": { description: Business access required }
    delete:
      operationId: deleteEducationModule
      tags: [education]
      summary: Delete a module
      parameters:
        - $ref: "#/components/parameters/ModuleId"
      responses:
        "204": { description: Module deleted }
        "403": { description: Business access required }
  /education/modules/{moduleId}/lessons:
    post:
      operationId: createEducationLesson
      tags: [education]
      summary: Create a lesson in an owned module
      parameters:
        - $ref: "#/components/parameters/ModuleId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationLessonInput" }
      responses:
        "201":
          description: Lesson created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationLesson" }
        "403": { description: Business access required }
  /education/lessons/{lessonId}:
    patch:
      operationId: updateEducationLesson
      tags: [education]
      summary: Update a lesson
      parameters:
        - $ref: "#/components/parameters/LessonId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationLessonInput" }
      responses:
        "200":
          description: Updated lesson
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationLesson" }
        "403": { description: Business access required }
    delete:
      operationId: deleteEducationLesson
      tags: [education]
      summary: Delete a lesson
      parameters:
        - $ref: "#/components/parameters/LessonId"
      responses:
        "204": { description: Lesson deleted }
        "403": { description: Business access required }
  /education/courses/{courseId}/availability:
    get:
      operationId: getEducationCourseAvailability
      tags: [education]
      summary: Get canonical Europe/Belgrade individual-course availability and occupancy
      parameters:
        - $ref: "#/components/parameters/CourseId"
        - { name: date, in: query, schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' } }
        - { name: educatorStaffId, in: query, schema: { type: string } }
      responses:
        "200":
          description: Availability projection
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalAvailability" } } }
        "400": { description: Invalid calendar request }
        "404": { description: Course or educator not found }
  /education/operations/bookings:
    get:
      operationId: listMyEducationOperationalBookings
      tags: [education]
      summary: List operational education booking groups visible to the purchaser or a named participant
      responses:
        "200":
          description: Authorized operational booking groups
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationOperationalBookingDetail" } } } }
        "401": { description: Authentication required }
    post:
      operationId: createEducationOperationalBooking
      tags: [education]
      summary: Atomically create an online or center-created named-seat booking
      parameters: [{ $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingInput" } } }
      responses:
        "201":
          description: Durable booking command receipt
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBooking" } } }
        "401": { description: Authentication required }
        "403": { description: Center role is insufficient }
        "404": { description: "Course, session, or center not found" }
        "409": { description: "Idempotency mismatch, capacity, or availability conflict" }
  /education/operations/bookings/{bookingGroupId}:
    get:
      operationId: getMyEducationOperationalBooking
      tags: [education]
      summary: Get an operational booking group visible to the purchaser or a named participant
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Authorized operational booking group
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingDetail" } } }
        "401": { description: Authentication required }
        "404": { description: Booking group not found or not visible }
  /education/operations/bookings/{bookingGroupId}/calendar.ics:
    get:
      operationId: downloadEducationOperationalBookingCalendar
      tags: [education]
      summary: Download an authorized operational booking calendar
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: RFC 5545 calendar
          content: { text/calendar: { schema: { type: string } } }
        "401": { description: Authentication required }
        "404": { description: Booking group not found or not visible }
  /education/operations/bookings/{bookingGroupId}/installments/{installmentNumber}/ips-qr:
    get:
      operationId: getEducationOperationalInstallmentIpsQr
      tags: [education]
      summary: Get deterministic NBS IPS QR payment data for a due booking installment
      parameters:
        - { name: bookingGroupId, in: path, required: true, schema: { type: string } }
        - { name: installmentNumber, in: path, required: true, schema: { type: integer, minimum: 1 } }
      responses:
        "200":
          description: Payment data only; retrieval never confirms payment
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationIpsQrPayment" } } }
        "400": { description: IPS payment account is not configured or invalid }
        "401": { description: Authentication required }
        "404": { description: Booking or due installment not found or not visible }
  /education/operations/bookings/{bookingGroupId}/payment-plan:
    get:
      operationId: getEducationOperationalPaymentPlan
      tags: [education]
      summary: Get purchaser-visible installment status for an operational booking
      parameters:
        - { name: bookingGroupId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Payment plan, content: { application/json: { schema: { $ref: "#/components/schemas/EducationPaymentPlan" } } } }
        "401": { description: Authentication required }
        "404": { description: Booking not found or not visible }
  /admin/education/installments/{installmentId}/settle:
    post:
      operationId: settleAdminEducationInstallment
      tags: [admin, education]
      summary: Manually settle one pending operational installment
      parameters:
        - { name: installmentId, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Stable settlement result, content: { application/json: { schema: { $ref: "#/components/schemas/EducationInstallmentSettlement" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Installment not found }
        "409": { description: Settled installment or idempotency mismatch }
  /admin/education/installments:
    get:
      operationId: listAdminEducationInstallments
      tags: [admin, education]
      summary: List due operational installments
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [pending, settled, refunded, cancelled] } }
        - { name: reference, in: query, schema: { type: string } }
      responses:
        "200": { description: Installments, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationAdminInstallment" } } } } }
  /education/operations/bookings/{bookingGroupId}/cancel:
    post:
      operationId: cancelEducationOperationalBooking
      tags: [education]
      summary: Cancel a whole operational booking group or selected named seats
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingCancelInput" } } }
      responses:
        "200": { description: Cancellation result, content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingOperation" } } } }
        "401": { description: Authentication required }
        "404": { description: Booking group not found or not visible }
        "409": { description: Booking snapshot or refund is no longer eligible }
  /education/operations/bookings/{bookingGroupId}/reschedule:
    patch:
      operationId: rescheduleEducationOperationalBooking
      tags: [education]
      summary: Atomically move whole or selected operational booking seats to a compatible session
      parameters:
        - name: bookingGroupId
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingRescheduleInput" } } }
      responses:
        "200": { description: Reschedule result, content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalBookingOperation" } } } }
        "401": { description: Authentication required }
        "404": { description: Booking group not found or not visible }
        "409": { description: "Idempotency mismatch, target incompatibility, or capacity conflict" }
  /education/operations/centers/{centerId}/sessions/{sessionId}/attendance/{participantId}:
    get:
      operationId: getEducationOperationalAttendance
      tags: [education]
      summary: Get attendance for a named participant in a center session
      parameters:
        - { name: centerId, in: path, required: true, schema: { type: string } }
        - { name: sessionId, in: path, required: true, schema: { type: string } }
        - { name: participantId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Attendance record, content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalAttendance" } } } }
        "403": { description: Attendance access required }
        "404": { description: Participant or session not found in center }
    put:
      operationId: upsertEducationOperationalAttendance
      tags: [education]
      summary: Record attendance for a named participant in a center session
      parameters:
        - { name: centerId, in: path, required: true, schema: { type: string } }
        - { name: sessionId, in: path, required: true, schema: { type: string } }
        - { name: participantId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalAttendanceInput" } } }
      responses:
        "200": { description: Attendance recorded, content: { application/json: { schema: { $ref: "#/components/schemas/EducationOperationalAttendance" } } } }
        "403": { description: Attendance access required }
        "404": { description: Participant or session not found in center }
  /education/operations/centers/{centerId}/sessions/{sessionId}/educator:
    patch:
      operationId: substituteEducationSessionEducator
      tags: [education]
      summary: Substitute the educator assigned to an operational session
      parameters:
        - { name: centerId, in: path, required: true, schema: { type: string } }
        - { name: sessionId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationSessionEducatorSubstitutionInput" } } }
      responses:
        "200": { description: Updated assignment, content: { application/json: { schema: { $ref: "#/components/schemas/EducationSessionEducatorAssignment" } } } }
        "403": { description: Center management access required }
        "404": { description: Session or educator not found in center }
        "409": { description: Replacement educator is unavailable }
  /education/operations/centers/{centerId}/sessions/{sessionId}/cancel:
    post:
      operationId: cancelEducationOperationalSession
      tags: [education]
      summary: Cancel an operational session and process center-caused refunds
      parameters:
        - { name: centerId, in: path, required: true, schema: { type: string } }
        - { name: sessionId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: false
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationSessionCancellationInput" } } }
      responses:
        "200": { description: Session cancellation result, content: { application/json: { schema: { $ref: "#/components/schemas/EducationSessionCancellation" } } } }
        "403": { description: Center management or assigned educator access required }
        "404": { description: Session not found in center }
        "409": { description: Payout prevents refund or session is already cancelled }
  /education/operations/courses/{courseId}/recurrence/preview:
    post:
      operationId: previewEducationCourseRecurrence
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/CourseId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationCourseRecurrenceInput" } } }
      responses:
        "200": { description: Generated schedule candidates, content: { application/json: { schema: { $ref: "#/components/schemas/EducationCourseRecurrencePreview" } } } }
        "400": { description: Invalid Belgrade calendar range }
        "403": { description: Center calendar access required }
        "404": { description: Course or educator not found }
  /education/operations/courses/{courseId}/recurrence/commit:
    post:
      operationId: commitEducationCourseRecurrence
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/CourseId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationCourseRecurrenceInput" } } }
      responses:
        "201": { description: Created sessions, content: { application/json: { schema: { $ref: "#/components/schemas/EducationCourseRecurrenceCommit" } } } }
        "400": { description: Invalid Belgrade calendar range }
        "403": { description: Center calendar access required }
        "404": { description: Course or educator not found }
        "409": { description: Idempotency fingerprint or educator schedule conflict }
  /education/operations/centers/{centerId}/permissions:
    get:
      operationId: getEducationCenterOperationalPermissions
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EducationCenterId" }]
      responses:
        "200":
          description: Current scoped operational permissions
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationCenterOperationalPermissions" } } }
        "401": { description: Authentication required }
        "403": { description: Center access required }
  /education/operations/centers/{centerId}/calendar:
    get:
      operationId: getEducationCenterOperationsCalendar
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: startDate, in: query, required: true, schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' } }
        - { name: endDate, in: query, required: true, schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' } }
        - { name: educatorStaffId, in: query, schema: { type: string } }
      responses:
        "200": { description: Center operational sessions, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationOperationsCalendarSession" } } } } }
        "400": { description: Invalid or unbounded date range }
        "403": { description: Center access required }
  /education/operations/centers/{centerId}/staff:
    get:
      operationId: listEducationCenterOperationalStaff
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EducationCenterId" }]
      responses:
        "200":
          description: Center staff
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationCenterStaffMember" } } } }
        "403": { description: Center access required }
    post:
      operationId: createEducationCenterOperationalStaff
      tags: [education]
      parameters: [{ $ref: "#/components/parameters/EducationCenterId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationCenterStaffInput" } } }
      responses:
        "201":
          description: Staff membership created
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationCenterStaffMember" } } }
        "403": { description: Owner or administrator access required }
        "409": { description: Existing membership or active educator center conflict }
  /education/operations/centers/{centerId}/staff/{staffId}:
    patch:
      operationId: updateEducationCenterOperationalStaff
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationCenterStaffUpdate" } } }
      responses:
        "200":
          description: Staff membership updated
          content: { application/json: { schema: { $ref: "#/components/schemas/EducationCenterStaffMember" } } }
        "403": { description: Owner or administrator access required }
        "404": { description: Staff member not in center }
        "409": { description: Active educator center conflict }
  /education/operations/centers/{centerId}/educators/{staffId}/weekly-availability:
    get:
      operationId: listEducationEducatorWeeklyAvailability
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Weekly availability, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationEducatorWeeklyAvailability" } } } } }
        "403": { description: Center access required }
        "404": { description: Educator not in center }
    post:
      operationId: createEducationEducatorWeeklyAvailability
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorWeeklyAvailabilityInput" } } }
      responses:
        "201": { description: Availability block created, content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorWeeklyAvailability" } } } }
        "403": { description: Center access required }
        "404": { description: Educator not in center }
        "409": { description: Duplicate availability block }
  /education/operations/centers/{centerId}/educators/{staffId}/weekly-availability/{availabilityId}:
    patch:
      operationId: updateEducationEducatorWeeklyAvailability
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
        - { name: availabilityId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorWeeklyAvailabilityInput" } } }
      responses:
        "200": { description: Availability block updated, content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorWeeklyAvailability" } } } }
        "403": { description: "Only owner, administrator, or the educator may edit" }
        "404": { description: Availability block not in educator scope }
    delete:
      operationId: deleteEducationEducatorWeeklyAvailability
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
        - { name: availabilityId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Availability block deleted }
        "403": { description: "Only owner, administrator, or the educator may edit" }
        "404": { description: Availability block not in educator scope }
  /education/operations/centers/{centerId}/educators/{staffId}/absences:
    get:
      operationId: listEducationEducatorAbsences
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Absence blocks, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/EducationEducatorAbsence" } } } } }
        "403": { description: Center access required }
        "404": { description: Educator not in center }
    post:
      operationId: createEducationEducatorAbsence
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsenceInput" } } }
      responses:
        "201": { description: Absence created, content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsence" } } } }
        "400": { description: Invalid absence range }
        "401": { description: Authentication required }
        "403": { description: Center access required }
        "404": { description: Educator not in center }
        "409": { description: Active assigned sessions must be replaced or cancelled first }
  /education/operations/centers/{centerId}/educators/{staffId}/absences/preview:
    post:
      operationId: previewEducationEducatorAbsence
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsenceInput" } } }
      responses:
        "200": { description: Existing session conflicts, content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsencePreview" } } } }
        "400": { description: Invalid absence range }
        "401": { description: Authentication required }
        "403": { description: Center access required }
        "404": { description: Educator not in center }
  /education/operations/centers/{centerId}/educators/{staffId}/absences/{absenceId}:
    patch:
      operationId: updateEducationEducatorAbsence
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
        - { name: absenceId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsenceInput" } } }
      responses:
        "200": { description: Absence updated, content: { application/json: { schema: { $ref: "#/components/schemas/EducationEducatorAbsence" } } } }
        "400": { description: Invalid absence range }
        "401": { description: Authentication required }
        "403": { description: "Only owner, administrator, or the educator may edit" }
        "404": { description: Absence not in educator scope }
        "409": { description: Active assigned sessions must be replaced or cancelled first }
    delete:
      operationId: deleteEducationEducatorAbsence
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - { name: staffId, in: path, required: true, schema: { type: string } }
        - { name: absenceId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Absence deleted }
        "403": { description: "Only owner, administrator, or the educator may edit" }
        "404": { description: Absence not in educator scope }
  /education/courses/{courseId}/sessions:
    get:
      operationId: listEducationSessions
      tags: [education]
      summary: List course sessions
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Course sessions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationSession" }
        "403": { description: Business access required }
    post:
      operationId: createEducationSession
      tags: [education]
      summary: Schedule a live course session
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationSessionInput" }
      responses:
        "201":
          description: Session created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationSession" }
        "403": { description: Business access required }
  /education/courses/{courseId}/enrollments:
    post:
      operationId: enrollInEducationCourse
      tags: [education]
      summary: Request a course purchase; access and escrow begin only after manual settlement
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationEnrollmentInput" }
      responses:
        "201":
          description: Pending enrollment request created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationEnrollment" }
        "403": { description: Business access required }
        "409": { description: Course is full or already enrolled }
  /education/course-types/proposals:
    post:
      operationId: proposeEducationCourseType
      tags: [education]
      summary: Propose a course type for administrator review
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                subcategoryId: { type: string }
                name: { type: string, minLength: 2, maxLength: 160 }
              required: [subcategoryId, name]
      responses:
        "201":
          description: Pending proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCourseType" }
        "409": { description: An exact normalized type already exists }
  /featured-placements/mine:
    get:
      operationId: listMyFeaturedPlacements
      tags: [salon-management, education]
      summary: List paid placements owned by the signed-in business owner
      responses:
        "200":
          description: Owner-visible placement history including payment instructions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/FeaturedPlacement" }
        "403": { description: Business owner access required }
  /featured-placements:
    post:
      operationId: createFeaturedPlacement
      tags: [salon-management, education]
      summary: Create a shared pending placement charge and server-generated IPS payload
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeaturedPlacementInput" }
      responses:
        "201":
          description: Pending fixed-price, fixed-duration placement charge
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeaturedPlacement" }
        "409": { description: "Product, payment account, ownership, or availability is invalid" }
  /education/sessions/{sessionId}:
    patch:
      operationId: updateEducationSession
      tags: [education]
      summary: Update a live course session
      parameters:
        - $ref: "#/components/parameters/SessionId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationSessionInput" }
      responses:
        "200":
          description: Updated session
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationSession" }
        "403": { description: Business access required }
    delete:
      operationId: deleteEducationSession
      tags: [education]
      summary: Delete a live course session
      parameters:
        - $ref: "#/components/parameters/SessionId"
      responses:
        "204": { description: Session deleted }
        "403": { description: Business access required }
  /education/enrollments:
    get:
      operationId: listEnrollments
      tags: [education]
      summary: List business course enrollments
      parameters:
        - name: page
          in: query
          description: 1-based page number for stable pagination (purchasedAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of rows per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Enrollments
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationEnrollment" }
        "403": { description: Business access required }
  /education/waitlist/{waitlistId}/accept:
    post:
      operationId: acceptEducationWaitlistOffer
      tags: [education]
      summary: Accept an active waitlist offer, reserving the held seat and creating a pending enrollment
      description: >-
        A promoted "offered" waitlist entry holds one reserved seat on the
        session for a 24-hour window. Accepting the offer transfers that held
        seat to a pending enrollment; access begins only after manual
        settlement. Fails if the offer has expired or is no longer active.
      parameters:
        - $ref: "#/components/parameters/WaitlistId"
      responses:
        "201":
          description: Pending enrollment created from the accepted offer
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationEnrollment" }
        "403": { description: Waitlist access required }
        "404": { description: Waitlist offer not found }
        "409": { description: "Offer expired, already used, or session filled" }
  /education/notifications:
    get:
      operationId: listEducationNotifications
      tags: [education]
      summary: List the current learner's education notifications and waitlist offers
      responses:
        "200":
          description: Education notifications for the signed-in learner
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationNotificationList" }
        "403": { description: Learner access required }
  /education/notifications/{notificationId}/read:
    patch:
      operationId: markEducationNotificationRead
      tags: [education]
      summary: Mark an education notification as read
      parameters:
        - name: notificationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Notification marked as read
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MarkEducationNotificationReadResponse" }
        "404": { description: Notification not found }
  /education/enrollments/{enrollmentId}/lms:
    get:
      operationId: getEducationLms
      tags: [education]
      summary: Open the enrolled learner LMS
      parameters:
        - $ref: "#/components/parameters/EnrollmentId"
      responses:
        "200":
          description: LMS course content and progress
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationLms" }
        "403": { description: Enrollment access required }
  /education/enrollments/{enrollmentId}/certificate:
    get:
      operationId: downloadEducationCertificate
      tags: [education]
      summary: Download the PDF completion certificate for a paid, completed enrollment
      parameters:
        - $ref: "#/components/parameters/EnrollmentId"
      responses:
        "200":
          description: Certificate PDF document
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "403": { description: Enrollment access required }
        "404": { description: "Enrollment or course not found" }
        "409": { description: "Certificate is unavailable until the course is completed, paid, and offers certification" }
  /education/enrollments/{enrollmentId}/session.ics:
    get:
      operationId: downloadEducationSessionCalendar
      tags: [education]
      summary: Download the RFC 5545 calendar (.ics) file for a paid live-session enrollment
      parameters:
        - $ref: "#/components/parameters/EnrollmentId"
      responses:
        "200":
          description: iCalendar (RFC 5545) event file
          content:
            text/calendar:
              schema:
                type: string
        "403": { description: Enrollment access required }
        "404": { description: "Enrollment, session, or course not found" }
        "409": { description: "Calendar is unavailable until payment is confirmed and a session is assigned" }
  /education/enrollments/{enrollmentId}/lessons/{lessonId}/complete:
    post:
      operationId: completeEducationLesson
      tags: [education]
      summary: Mark an enrolled lesson as complete
      parameters:
        - $ref: "#/components/parameters/EnrollmentId"
        - $ref: "#/components/parameters/LessonId"
      responses:
        "200":
          description: Updated enrollment progress
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationEnrollment" }
        "403": { description: Enrollment access required }
  /education/instructors:
    get:
      operationId: listEducationInstructors
      tags: [education]
      summary: List instructor profiles for the current education center
      responses:
        "200":
          description: Center instructor profiles
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationInstructorProfile" }
        "403": { description: Education center owner access required }
    post:
      operationId: createEducationInstructor
      tags: [education]
      summary: Create an instructor profile for the current education center
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationInstructorInput" }
      responses:
        "201":
          description: Created instructor profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationInstructorProfile" }
        "403": { description: Education center owner access required }
  /education/instructors/{instructorId}:
    patch:
      operationId: updateEducationInstructor
      tags: [education]
      summary: Update an instructor profile owned by the current education center
      parameters:
        - name: instructorId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationInstructorInput" }
      responses:
        "200":
          description: Updated instructor profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationInstructorProfile" }
        "403": { description: Education center owner access required }
        "404": { description: Instructor not found }
    delete:
      operationId: deleteEducationInstructor
      tags: [education]
      summary: Delete an instructor profile owned by the current education center
      parameters:
        - name: instructorId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Instructor deleted }
        "403": { description: Education center owner access required }
        "404": { description: Instructor not found }
  /education/instructors/{instructorId}/public:
    get:
      operationId: getPublicInstructorProfile
      tags: [education]
      summary: Get public instructor profile with bio, qualifications, rating and courses
      parameters:
        - name: instructorId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Public instructor profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationInstructorPublicProfile" }
        "404": { description: Instructor not found }
  /education/courses/{courseId}/featured:
    get:
      operationId: getEducationCourseFeaturedStatus
      tags: [education]
      summary: Get featured status and current platform price for a course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Featured status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCourseFeaturedStatus" }
        "403": { description: Course owner access required }
        "404": { description: Course not found }
    patch:
      operationId: updateEducationCourseFeatured
      tags: [education]
      summary: Activate or deactivate featured placement for an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationCourseFeaturedInput" }
      responses:
        "200":
          description: Updated featured status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCourseFeaturedStatus" }
        "403": { description: Course owner access required }
        "404": { description: Course not found }
  /education/courses/{courseId}/instructor:
    patch:
      operationId: linkEducationCourseInstructor
      tags: [education]
      summary: Link or unlink an instructor profile to an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                instructorId: { type: ["string", "null"] }
      responses:
        "200":
          description: Updated course
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Course owner access required }
        "404": { description: Course or instructor not found }
  /education/courses/{courseId}/days:
    put:
      operationId: replaceEducationCourseDays
      tags: [education]
      summary: Replace the public daily program for an owned course
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationCourseDaysInput" }
      responses:
        "200":
          description: Updated course
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        "403": { description: Course owner access required }
        "404": { description: Course not found }
  /education/public/courses:
    get:
      operationId: listPublicEducationCourses
      tags: [education]
      summary: List courses available for public purchase
      parameters:
        - name: format
          in: query
          schema: { type: string, enum: [online, in-person, hybrid] }
        - name: city
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
        - name: q
          in: query
          schema: { type: string, maxLength: 160 }
        - name: sectionId
          in: query
          schema: { type: string }
        - name: categoryId
          in: query
          schema: { type: string }
        - name: subcategoryId
          in: query
          schema: { type: string }
        - name: courseTypeId
          in: query
          schema: { type: string }
        - name: language
          in: query
          schema: { type: string }
        - name: accredited
          in: query
          schema: { type: boolean }
        - name: certification
          in: query
          schema: { type: boolean }
        - name: minRating
          in: query
          description: Minimum aggregate rating from published course reviews; unrated courses have rating 0.
          schema: { type: number, minimum: 0, maximum: 5 }
        - name: level
          in: query
          schema: { type: string, enum: [beginner, intermediate, advanced, all-levels] }
        - name: minPrice
          in: query
          schema: { type: number, minimum: 0 }
        - name: maxPrice
          in: query
          schema: { type: number, minimum: 0 }
        - name: startDate
          in: query
          description: Include courses that start on or after this date.
          schema: { type: string, format: date }
        - name: maxDurationDays
          in: query
          description: Include courses whose published daily program has at most this many days.
          schema: { type: number, multipleOf: 1, minimum: 1 }
        - name: minDurationMinutes
          in: query
          description: Include only courses with an explicit parsable duration at or above this bound.
          schema: { type: integer, minimum: 1 }
        - name: maxDurationMinutes
          in: query
          description: Include only courses with an explicit parsable duration at or below this bound.
          schema: { type: integer, minimum: 1 }
        - name: page
          in: query
          description: 1-based page index for stable createdAt desc, id desc ordering.
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of courses per page.
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: Public courses with redacted logistics
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCourseDetail" }
  /education/public/courses/{courseId}:
    get:
      operationId: getPublicEducationCourse
      tags: [education]
      summary: Get a public course detail with redacted logistics
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "200":
          description: Public course detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCourseDetail" }
        "404": { description: Course not available }
  /education/public/courses/{courseId}/related:
    get:
      operationId: listRelatedEducationCourses
      tags: [education]
      summary: List eligible related courses, preferring the same subcategory and then shared tags
      parameters:
        - $ref: "#/components/parameters/CourseId"
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 20, default: 6 }
      responses:
        "200":
          description: Public related courses excluding the current course
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCourseDetail" }
        "404": { description: Course not available }
  /education/public/courses/{courseId}/inquiries:
    post:
      operationId: createPublicEducationCourseInquiry
      tags: [education]
      summary: Send an authenticated inquiry to a course center
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                message: { type: ["string", "null"], maxLength: 2000 }
      responses:
        "201":
          description: Inquiry recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationInquiry" }
        "409": { description: A recent duplicate inquiry already exists }
  /education/public/taxonomy:
    get:
      operationId: getPublicEducationTaxonomy
      tags: [education]
      responses:
        "200":
          description: Ordered active taxonomy with public course counts
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationTaxonomySection" }
  /education/public/search-suggestions:
    get:
      operationId: listPublicEducationSearchSuggestions
      tags: [education]
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 30, default: 10 }
      responses:
        "200":
          description: Mixed taxonomy and course suggestions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationSearchSuggestion" }
  /education/public/rankings:
    get:
      operationId: getPublicEducationRankings
      tags: [education]
      responses:
        "200":
          description: Organic education marketplace rankings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationRankings" }
  /education/public/placements:
    get:
      operationId: listPublicEducationPlacements
      tags: [education]
      parameters:
        - name: kind
          in: query
          schema: { type: string, enum: [featured_center, special_offer] }
        - name: scope
          in: query
          schema: { type: string, enum: [home, category, subcategory] }
        - name: scopeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Active in-window placements in deterministic fair rotation
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationPublicPlacement" }
  /education/public/categories:
    get:
      operationId: listPublicEducationCategories
      tags: [education]
      summary: List public education categories with live course counts
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCategory" }
  /education/public/popular:
    get:
      operationId: listPopularEducationCourses
      tags: [education]
      summary: List popular education courses from verified centers
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 12 }
      responses:
        "200":
          description: Popular courses
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCourseDetail" }
  /education/public/centers/{centerId}:
    get:
      operationId: getPublicEducationCenter
      tags: [education]
      summary: Get a public education center profile and its live courses
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
      responses:
        "200":
          description: Public center profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCenterPublic" }
        "404": { description: Center not available }
  /education/public/centers/{centerId}/reviews:
    get:
      operationId: listPublicEducationCenterReviews
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/EducationCenterId"
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Published canonical center reviews
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCenterReviewPage" }
  /education/center-reviews:
    post:
      operationId: createEducationCenterReview
      tags: [education]
      summary: Review the center for a completed enrollment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationCenterReviewInput" }
      responses:
        "201":
          description: Review submitted for moderation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationCenterReview" }
        "403": { description: Completed enrollment ownership required }
        "409": { description: Enrollment already reviewed }
  /admin/education/center-reviews:
    get:
      operationId: adminListEducationCenterReviews
      tags: [admin, education]
      summary: List center reviews for moderation without learner PII
      parameters:
        - name: centerId
          in: query
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [pending, published, rejected, all], default: pending }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Stable oldest-first center review moderation queue
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminEducationCenterReviewPage" }
        "401": { description: Authentication required }
        "403": { description: Admin role required }
  /admin/education/center-reviews/{reviewId}:
    patch:
      operationId: adminModerateEducationCenterReview
      tags: [admin, education]
      parameters:
        - name: reviewId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                status: { type: string, enum: [published, rejected] }
                adminNote: { type: ["string", "null"], maxLength: 2000 }
              required: [status]
      responses:
        "200":
          description: Moderated center review
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminEducationCenterReview" }
        "404": { description: Review not found }
  /education/wishlist:
    get:
      operationId: listEducationWishlist
      tags: [education]
      summary: List the signed-in user's fresh, publicly eligible saved courses
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 24 }
      responses:
        "200":
          description: Private paginated education wishlist
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationWishlistPage" }
    post:
      operationId: addEducationWishlistItem
      tags: [education]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationWishlistInput" }
      responses:
        "201":
          description: Course saved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationWishlistItem" }
        "200":
          description: Course was already saved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationWishlistItem" }
        "404": { description: Public course not available }
  /education/wishlist/{courseId}:
    delete:
      operationId: removeEducationWishlistItem
      tags: [education]
      parameters:
        - $ref: "#/components/parameters/CourseId"
      responses:
        "204": { description: Removed or already absent }
  /education/gift-vouchers:
    get:
      operationId: listEducationGiftVouchers
      tags: [education]
      summary: List vouchers purchased by or assigned to the signed-in user
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Private voucher page; redemption codes are never returned
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationGiftVoucherPage" }
    post:
      operationId: purchaseEducationGiftVoucher
      tags: [education]
      summary: Create a manual-payment gift voucher purchase
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGiftVoucherPurchaseInput" }
      responses:
        "201":
          description: Pending manual-payment voucher; raw code is disclosed once
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationGiftVoucherPurchase" }
        "409": { description: Idempotency key was reused with incompatible input }
  /education/gift-vouchers/redeem:
    post:
      operationId: redeemEducationGiftVoucher
      tags: [education]
      summary: Atomically redeem one active assigned voucher into one enrollment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGiftVoucherRedeemInput" }
      responses:
        "201":
          description: Voucher redeemed and enrollment created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationEnrollment" }
        "409": { description: "Voucher is invalid, unpaid, unassigned, or already redeemed" }
  /admin/education/gift-vouchers/{voucherId}/settle:
    post:
      operationId: adminSettleEducationGiftVoucher
      tags: [admin, education]
      parameters:
        - name: voucherId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Manual payment confirmed, or the existing active voucher returned for an idempotent settlement retry
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationGiftVoucher" }
  /admin/education/gift-vouchers:
    get:
      operationId: adminListEducationGiftVouchers
      tags: [admin, education]
      summary: List all customer education gift vouchers for settlement and refund administration
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [all, pending_payment, active, redeemed, refunded, cancelled], default: all }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Admin voucher page; code hashes and raw redemption codes are never exposed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminEducationGiftVoucherPage" }
  /admin/education/gift-vouchers/{voucherId}/refund:
    post:
      operationId: adminRefundEducationGiftVoucher
      tags: [admin, education]
      parameters:
        - name: voucherId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EducationGiftVoucherRefundInput" }
      responses:
        "200":
          description: Voucher refunded with audit linkage
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EducationGiftVoucher" }
  /education/public/featured:
    get:
      operationId: listFeaturedEducationCourses
      tags: [education]
      summary: List currently featured education courses for the public catalog shelf
      responses:
        "200":
          description: Featured courses ordered by activation time
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EducationCourseDetail" }
  /admin/summary:
    get:
      operationId: getAdminSummary
      tags: [admin]
      summary: Admin platform overview with aggregates and trends
      responses:
        "200":
          description: Admin summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSummary"
  /admin/salons:
    get:
      operationId: adminListSalons
      tags: [admin]
      summary: Searchable/filterable salon list with subscription and loyalty details
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: city
          in: query
          schema: { type: string }
        - name: active
          in: query
          schema: { type: boolean }
        - name: featured
          in: query
          schema: { type: boolean }
        - name: subscriptionStatus
          in: query
          schema: { type: string }
        - name: page
          in: query
          description: 1-based page number for stable pagination (createdAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of rows per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Admin salon list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminSalon" }
  /admin/salons/{salonId}:
    get:
      operationId: adminGetSalon
      tags: [admin]
      summary: Admin salon profile with B2B order aggregates
      parameters:
        - $ref: "#/components/parameters/SalonId"
      responses:
        "200":
          description: Detailed salon profile and all B2B orders
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminSalonDetail" }
        "404": { description: Salon not found }
    patch:
      operationId: adminUpdateSalon
      tags: [admin]
      summary: Update salon active or featured status
      parameters:
        - $ref: "#/components/parameters/SalonId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminSalonUpdate"
      responses:
        "200":
          description: Updated salon
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSalon"
  /admin/users:
    get:
      operationId: adminListUsers
      tags: [admin]
      summary: Searchable/filterable user list with role and active status
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: role
          in: query
          schema:
            type: string
            enum: [SUPER_ADMIN, ADMIN, SALON_OWNER, SALON_EMPLOYEE, EDUKATIVNI_CENTAR, INSTRUCTOR, CUSTOMER, STUDENT]
        - name: active
          in: query
          schema: { type: boolean }
        - name: page
          in: query
          description: 1-based page number for stable pagination (createdAt desc, id desc).
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          description: Number of rows per page (1..100).
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Admin user list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminUser" }
  /admin/customers/setup:
    post:
      operationId: adminCreateCustomerSetup
      tags: [admin]
      summary: Create a CUSTOMER account and return its one-time password setup URL
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminCreateCustomerSetupInput" }
      responses:
        "201":
          description: Customer and one-time setup URL created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCreateCustomerSetupResponse" }
        "400": { description: Invalid customer details }
        "401": { description: Authentication required }
        "403": { description: SUPER_ADMIN access required }
        "409": { description: Email is already registered }
        "429": { description: Too many account creation attempts }
  /admin/customers/{userId}/setup:
    post:
      operationId: adminReissueCustomerSetup
      tags: [admin]
      summary: Replace an uncompleted CUSTOMER account's password setup URL
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
      responses:
        "201":
          description: Replacement one-time setup URL created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCreateCustomerSetupResponse" }
        "401": { description: Authentication required }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Eligible uncompleted CUSTOMER account not found }
        "429": { description: Too many account setup requests }
  /admin/accounts/setup:
    post:
      operationId: adminCreateAccountSetup
      tags: [admin]
      summary: Transactionally create a non-SUPER_ADMIN account with its required business relation and a one-time password setup URL
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminCreateAccountSetupInput" }
      responses:
        "201":
          description: Account, matching business relation, and one-time setup URL created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCreateCustomerSetupResponse" }
        "400": { description: Invalid common account details }
        "401": { description: Authentication required }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Selected active salon or education center does not exist }
        "409": { description: "Email, slug, user link, or legal-entity ownership conflicts with existing data" }
        "422": { description: "Missing, mismatched, blank, or invalid role-specific data" }
        "429": { description: Too many account creation attempts }
  /admin/accounts/{userId}/setup:
    post:
      operationId: adminReissueAccountSetup
      tags: [admin]
      summary: Replace an active unconfigured non-SUPER_ADMIN account's setup URL
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
      responses:
        "201":
          description: Replacement one-time setup URL created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCreateCustomerSetupResponse" }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Eligible unconfigured account not found }
  /admin/users/{userId}:
    patch:
      operationId: adminUpdateUser
      tags: [admin]
      summary: Update user role or active status (protects last active SUPER_ADMIN)
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUserUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUser"
        "422": { description: Requested business-role transition is forbidden because the required business relation is absent or incompatible }
  /admin/users/{userId}/business-conversion:
    post:
      operationId: adminConvertUserToBusinessAccount
      tags: [admin]
      summary: Atomically convert a standalone account to a business account and create its required companion record
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminBusinessAccountConversionInput" }
      responses:
        "200":
          description: User role and matching business companion were committed atomically
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminUser" }
        "400": { description: Invalid user id or common conversion details }
        "401": { description: Authentication required }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Target user or selected active salon or education center does not exist }
        "409": { description: "Companion record, slug, user link, or legal-entity ownership conflicts with existing data" }
        "422": { description: "Target is not a standalone account, already has business relations, or role-specific data is missing or mismatched" }
  /admin/users/{userId}/business-role-transition:
    get:
      operationId: adminGetBusinessRoleTransition
      tags: [admin]
      summary: Inspect all business relations and safe decisions before changing a business role
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Current account and every relation that must be decided explicitly
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminBusinessRoleTransitionState" }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Target user not found }
    post:
      operationId: adminTransitionBusinessRole
      tags: [admin]
      summary: Atomically exit or change a business role while preserving all historical relation rows
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminBusinessRoleTransitionInput" }
      responses:
        "200":
          description: Role, active salon, and all relation decisions were committed atomically
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminBusinessRoleTransitionResult" }
        "400": { description: Invalid request }
        "403": { description: SUPER_ADMIN access required }
        "404": { description: Target user or transfer target not found }
        "409": { description: "Concurrent state changed, a transfer target is ineligible, or the transition would orphan a relation" }
        "422": { description: "Missing, duplicate, foreign, or unsafe relation decision" }
  /admin/loyalty-tiers:
    get:
      operationId: adminListLoyaltyTiers
      tags: [admin]
      summary: List all loyalty tiers
      responses:
        "200":
          description: Loyalty tiers
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LoyaltyTier" }
    post:
      operationId: adminCreateLoyaltyTier
      tags: [admin]
      summary: Create a loyalty tier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoyaltyTierInput"
      responses:
        "201":
          description: Created loyalty tier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyTier"
  /admin/loyalty-tiers/{tierId}:
    patch:
      operationId: adminUpdateLoyaltyTier
      tags: [admin]
      summary: Update or deactivate a loyalty tier
      parameters:
        - name: tierId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoyaltyTierUpdate"
      responses:
        "200":
          description: Updated loyalty tier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyTier"
    delete:
      operationId: adminDeleteLoyaltyTier
      tags: [admin]
      summary: Delete or deactivate a loyalty tier
      parameters:
        - name: tierId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Deleted/deactivated loyalty tier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyTier"
  /admin/subscription-plans:
    get:
      operationId: adminListSubscriptionPlans
      tags: [admin]
      summary: List all subscription plans
      responses:
        "200":
          description: Subscription plans
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SubscriptionPlan" }
    post:
      operationId: adminCreateSubscriptionPlan
      tags: [admin]
      summary: Create a subscription plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubscriptionPlanInput"
      responses:
        "201":
          description: Created subscription plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionPlan"
  /admin/subscription-plans/{planId}:
    patch:
      operationId: adminUpdateSubscriptionPlan
      tags: [admin]
      summary: Update or deactivate a subscription plan
      parameters:
        - name: planId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubscriptionPlanUpdate"
      responses:
        "200":
          description: Updated subscription plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionPlan"
    delete:
      operationId: adminDeleteSubscriptionPlan
      tags: [admin]
      summary: Delete or deactivate a subscription plan
      parameters:
        - name: planId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Deleted/deactivated subscription plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionPlan"
  /admin/reviews:
    get:
      operationId: adminListReviews
      tags: [admin]
      summary: List reviews with salon and customer context
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: salonId
          in: query
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        - name: visible
          in: query
          schema: { type: boolean }
        - name: minRating
          in: query
          schema: { type: number, minimum: 1, maximum: 5 }
        - name: maxRating
          in: query
          schema: { type: number, minimum: 1, maximum: 5 }
      responses:
        "200":
          description: Admin review list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminReview" }
  /admin/reviews/{reviewId}:
    patch:
      operationId: adminUpdateReview
      tags: [admin]
      summary: Moderate review visibility
      parameters:
        - name: reviewId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminReviewUpdate"
      responses:
        "200":
          description: Updated review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReview"
    delete:
      operationId: adminDeleteReview
      tags: [admin]
      summary: Permanently delete a review
      parameters:
        - name: reviewId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "204":
          description: Review deleted
  /admin/product-waitlist:
    get:
      operationId: adminListProductWaitlist
      tags: [admin]
      summary: List product availability subscriptions without contact details
      parameters:
        - { name: audience, in: query, schema: { type: string, enum: [B2B, B2C] } }
        - { name: status, in: query, schema: { type: string, enum: [ACTIVE, NOTIFIED, UNSUBSCRIBED] } }
        - { name: productId, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated privacy-safe waitlist rows and count
          content: { application/json: { schema: { $ref: "#/components/schemas/AdminProductWaitlistPage" } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /admin/products:
    get:
      operationId: adminListProducts
      tags: [admin]
      summary: List B2B products with filters, sorting and pagination
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
        - name: subcategory
          in: query
          schema: { type: string }
        - name: brand
          in: query
          schema: { type: string }
        - name: supplierId
          in: query
          schema: { type: string }
        - name: productIds
          in: query
          description: Comma-separated product IDs used to hydrate already selected relationships
          schema: { type: string }
        - name: market
          in: query
          schema: { type: string, enum: [B2B, B2C] }
        - name: lowStock
          in: query
          schema: { type: boolean }
        - name: status
          in: query
          schema: { type: string, enum: [in-stock, out-of-stock, new, on-sale, inactive] }
        - name: sortBy
          in: query
          schema: { type: string, enum: [name, price, stock, createdAt], default: createdAt }
        - name: sortDir
          in: query
          schema: { type: string, enum: [asc, desc], default: desc }
        - name: page
          in: query
          schema: { type: number, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: number, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Paginated products
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProductList"
    post:
      operationId: adminCreateProduct
      tags: [admin]
      summary: Create a B2B product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductInput"
      responses:
        "201":
          description: Created product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProduct"
  /admin/aftercare/treatments:
    get:
      operationId: adminListAftercareTreatments
      tags: [admin]
      summary: List active normalized aftercare treatment taxonomy
      parameters:
        - { name: search, in: query, schema: { type: string, maxLength: 120 } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Active treatment taxonomy terms
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/AftercareTreatmentTaxonomy" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /admin/aftercare/settings:
    get:
      operationId: adminGetAftercareSettings
      tags: [admin]
      summary: Get current versioned aftercare settings
      responses:
        "200": { description: Current aftercare settings, content: { application/json: { schema: { $ref: "#/components/schemas/AftercareSettings" } } } }
    put:
      operationId: adminUpdateAftercareSettings
      tags: [admin]
      summary: Version and replace aftercare settings
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/AftercareSettingsUpdate" } } }
      responses:
        "200": { description: Current aftercare settings, content: { application/json: { schema: { $ref: "#/components/schemas/AftercareSettings" } } } }
        "409": { description: Stale settings version }
  /admin/aftercare/statistics:
    get:
      operationId: adminGetAftercareStatistics
      tags: [admin]
      summary: Aggregate B2C aftercare delivery and conversion statistics
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' } }
        - { name: to, in: query, required: true, schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' } }
        - { name: treatmentId, in: query, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
        - { name: productId, in: query, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
        - { name: bundleId, in: query, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
        - { name: kind, in: query, schema: { type: string, enum: [PRODUCT, PREMADE_BUNDLE, PERSONALIZED_BUNDLE] } }
      responses:
        "200": { description: B2C aftercare statistics, content: { application/json: { schema: { $ref: "#/components/schemas/AftercareStatistics" } } } }
        "400": { description: Invalid date range or filter }
        "403": { description: Administrator access required }
  /customer/aftercare/recommendations:
    get:
      operationId: customerListAftercareRecommendations
      tags: [customer]
      summary: List the signed-in customer's aftercare recommendations
      parameters:
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 30 } }
      responses:
        "200": { description: Recommendations, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/CustomerAftercareRecommendation" } } } } }
  /customer/aftercare/recommendations/{recommendationId}:
    get:
      operationId: customerGetAftercareRecommendation
      tags: [customer]
      summary: Get one signed-in customer's aftercare recommendation
      parameters:
        - { name: recommendationId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Recommendation, content: { application/json: { schema: { $ref: "#/components/schemas/CustomerAftercareRecommendation" } } } }
        "404": { description: Recommendation not found }
  /customer/aftercare/recommendations/{recommendationId}/read:
    post:
      operationId: customerReadAftercareRecommendation
      tags: [customer]
      summary: Mark one signed-in customer's recommendation read
      parameters:
        - { name: recommendationId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Recommendation, content: { application/json: { schema: { $ref: "#/components/schemas/CustomerAftercareRecommendation" } } } }
        "404": { description: Recommendation not found }
  /admin/bundles:
    get:
      operationId: adminListBundles
      tags: [admin]
      summary: List all fixed-price product bundles
      responses:
        "200":
          description: Bundles including administrative market prices and derived stock
          content:
            application/json:
                schema: { type: array, items: { $ref: "#/components/schemas/AdminBundle" } }
    post:
      operationId: adminCreateBundle
      tags: [admin]
      summary: Create a fixed-price bundle with at least two same-supplier components
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminBundleInput" }
      responses:
        "201": { description: Created bundle, content: { application/json: { schema: { $ref: "#/components/schemas/AdminBundle" } } } }
        "400": { description: Invalid prices or components }
        "409": { description: "Supplier, channel, or component isolation conflict" }
  /admin/bundles/{bundleId}:
    patch:
      operationId: adminUpdateBundle
      tags: [admin]
      summary: Replace bundle metadata, market prices, and optionally components
      parameters:
        - name: bundleId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminBundleInput" }
      responses:
        "200": { description: Updated bundle, content: { application/json: { schema: { $ref: "#/components/schemas/AdminBundle" } } } }
        "404": { description: Bundle not found }
        "409": { description: Supplier or component isolation conflict }
  /admin/bundles/{bundleId}/deactivate:
    post:
      operationId: adminDeactivateBundle
      tags: [admin]
      summary: Deactivate a bundle without deleting historical references
      parameters:
        - name: bundleId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200": { description: Deactivated bundle, content: { application/json: { schema: { $ref: "#/components/schemas/AdminBundle" } } } }
        "404": { description: Bundle not found }
  /admin/products/bulk:
    post:
      operationId: adminBulkUpdateProducts
      tags: [admin]
      summary: Apply a bulk action to multiple products
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductBulkUpdate"
      responses:
        "200":
          description: Bulk update result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBulkResult"
  /admin/products/{productId}:
    get:
      operationId: adminGetProduct
      tags: [admin]
      summary: Get an administrative product
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200": { description: Product, content: { application/json: { schema: { $ref: "#/components/schemas/AdminProduct" } } } }
        "404": { description: Product not found }
    patch:
      operationId: adminUpdateProduct
      tags: [admin]
      summary: Update a B2B product
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductUpdate"
      responses:
        "200":
          description: Updated product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProduct"
    delete:
      operationId: adminDeleteProduct
      tags: [admin]
      summary: Delete a product (or deactivate it when referenced by orders)
      parameters:
        - name: productId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Deleted or deactivated product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProduct"
  /admin/product-categories:
    get:
      operationId: adminListProductCategories
      tags: [admin]
      summary: List all B2B product categories (flat)
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminProductCategory" }
    post:
      operationId: adminCreateProductCategory
      tags: [admin]
      summary: Create a product category or subcategory
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductCategoryInput"
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProductCategory"
  /admin/product-categories/{categoryId}:
    patch:
      operationId: adminUpdateProductCategory
      tags: [admin]
      summary: Update a product category
      parameters:
        - name: categoryId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductCategoryUpdate"
      responses:
        "200":
          description: Updated category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProductCategory"
    delete:
      operationId: adminDeleteProductCategory
      tags: [admin]
      summary: Delete a product category
      parameters:
        - name: categoryId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "204":
          description: Category deleted
        "409":
          description: Category is still in use
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /admin/suppliers:
    get:
      operationId: adminListSuppliers
      tags: [admin]
      summary: List platform-managed suppliers
      responses:
        "200": { description: Suppliers, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Supplier" } } } } }
    post:
      operationId: adminCreateSupplier
      tags: [admin]
      summary: Create a platform-managed supplier
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SupplierInput" } } }
      responses:
        "201": { description: Created supplier, content: { application/json: { schema: { $ref: "#/components/schemas/Supplier" } } } }
  /admin/suppliers/{supplierId}:
    get:
      operationId: adminGetSupplier
      tags: [admin]
      summary: Get a supplier
      parameters: [{ name: supplierId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Supplier, content: { application/json: { schema: { $ref: "#/components/schemas/Supplier" } } } }
        "404": { description: Supplier not found }
    patch:
      operationId: adminUpdateSupplier
      tags: [admin]
      summary: Update supplier details or status without changing catalog history
      parameters: [{ name: supplierId, in: path, required: true, schema: { type: string } }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SupplierUpdate" } } }
      responses:
        "200": { description: Updated supplier, content: { application/json: { schema: { $ref: "#/components/schemas/Supplier" } } } }
  /suppliers:
    get:
      operationId: listPublicSuppliers
      tags: [shop]
      summary: List active public suppliers
      responses:
        "200": { description: Active suppliers, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Supplier" } } } } }
  /suppliers/{supplierSlug}:
    get:
      operationId: getPublicSupplier
      tags: [shop]
      summary: Get an active supplier and its category tree
      parameters: [{ name: supplierSlug, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Supplier, content: { application/json: { schema: { $ref: "#/components/schemas/Supplier" } } } }
        "404": { description: Supplier not found }
  /suppliers/{supplierSlug}/categories:
    get:
      operationId: listSupplierCategories
      tags: [shop]
      summary: List the full active category tree for one supplier as flat path-aware nodes
      parameters: [{ name: supplierSlug, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Supplier categories, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/SupplierCategoryNode" } } } } }
        "404": { description: Supplier not found }
  /suppliers/{supplierSlug}/products:
    get:
      operationId: listSupplierProducts
      tags: [shop]
      summary: List active B2B products from one active supplier
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: categoryId, in: query, schema: { type: string } }
        - { name: search, in: query, schema: { type: string } }
        - { name: brand, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses:
        "200": { description: Supplier products, content: { application/json: { schema: { $ref: "#/components/schemas/ProductList" } } } }
        "401": { description: Authentication required }
        "404": { description: Supplier not found }
  /suppliers/{supplierSlug}/public-products:
    get:
      operationId: listSupplierPublicProducts
      tags: [shop]
      summary: List active retail products from one active supplier
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: categoryId, in: query, schema: { type: string } }
        - { name: search, in: query, schema: { type: string } }
        - { name: brand, in: query, description: Comma-separated brand values, schema: { type: string } }
        - { name: productType, in: query, description: Comma-separated product type slugs, schema: { type: string } }
        - { name: needTag, in: query, description: Comma-separated need/problem tag keys, schema: { type: string } }
        - { name: minPrice, in: query, schema: { type: integer, minimum: 0 } }
        - { name: maxPrice, in: query, schema: { type: integer, minimum: 0 } }
        - { name: sort, in: query, schema: { $ref: "#/components/schemas/B2cProductSort" } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses:
        "200": { description: Supplier-scoped products and disjunctive facets, content: { application/json: { schema: { $ref: "#/components/schemas/B2cProductSearchResponse" } } } }
        "404": { description: Supplier not found }
  /suppliers/{supplierSlug}/products/{productId}:
    get:
      operationId: getSupplierProduct
      tags: [shop]
      summary: Get an active B2B product from one active supplier
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Supplier product, content: { application/json: { schema: { $ref: "#/components/schemas/ProductDetail" } } } }
        "404": { description: Product or supplier not found }
  /suppliers/{supplierSlug}/public-products/{productId}:
    get:
      operationId: getSupplierPublicProduct
      tags: [shop]
      summary: Get an active retail product from one active supplier
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Supplier product, content: { application/json: { schema: { $ref: "#/components/schemas/PublicProductDetail" } } } }
        "404": { description: Product or supplier not found }
  /suppliers/{supplierSlug}/recently-viewed:
    get:
      operationId: listRecentlyViewedProducts
      tags: [shop]
      summary: List the current browser or signed-in retail account's recently viewed public product cards
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Supplier-scoped public product cards; no browsing identity is created by this request, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/PublicProduct" } } } } }
        "404": { description: Supplier not found }
  /suppliers/{supplierSlug}/recently-viewed/{productId}:
    post:
      operationId: recordRecentlyViewedProduct
      tags: [shop]
      summary: Record a successfully viewed public retail product for the current browser or signed-in CUSTOMER/JOBSEEKER account
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Recorded (idempotent) }
        "404": { description: Product or supplier not found }
  /suppliers/{supplierSlug}/banners:
    get:
      operationId: listSupplierB2cBanners
      tags: [shop]
      summary: List active in-window promotional banners for one active B2C supplier
      parameters: [{ name: supplierSlug, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Public banners, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/PublicB2cBanner" } } } } }
        "404": { description: Supplier not found }
  /b2c/display-config:
    get:
      operationId: getB2cDisplayConfig
      tags: [shop]
      summary: Get display-safe global B2C configuration
      responses:
        "200": { description: Display configuration, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDisplayConfig" } } } }
  /admin/b2c/product-types:
    get:
      operationId: adminListB2cProductTypes
      tags: [admin]
      responses:
        "200": { description: Product types, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/B2cDictionaryValue" } } } } }
    post:
      operationId: adminCreateB2cProductType
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryCreate" } } } }
      responses:
        "201": { description: Created product type, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryValue" } } } }
  /admin/b2c/product-types/{id}:
    patch:
      operationId: adminUpdateB2cProductType
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryUpdate" } } } }
      responses:
        "200": { description: Updated product type, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryValue" } } } }
        "409": { description: Version conflict or referenced value }
    delete:
      operationId: adminDeleteB2cProductType
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: expectedVersion, in: query, required: true, schema: { type: integer, minimum: 1 } }
      responses: { "204": { description: Deleted }, "409": { description: Version conflict or referenced value } }
  /admin/b2c/product-types/reorder:
    put:
      operationId: adminReorderB2cProductTypes
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cReorderInput" } } } }
      responses: { "200": { description: Reordered values } }
  /admin/b2c/need-tags:
    get:
      operationId: adminListB2cNeedTags
      tags: [admin]
      responses:
        "200": { description: Need tags, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/B2cDictionaryValue" } } } } }
    post:
      operationId: adminCreateB2cNeedTag
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cNeedTagCreate" } } } }
      responses:
        "201": { description: Created need tag, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryValue" } } } }
  /admin/b2c/need-tags/{id}:
    patch:
      operationId: adminUpdateB2cNeedTag
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cDictionaryUpdate" } } } }
      responses: { "200": { description: Updated need tag }, "409": { description: Version conflict } }
    delete:
      operationId: adminDeleteB2cNeedTag
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: expectedVersion, in: query, required: true, schema: { type: integer, minimum: 1 } }
      responses: { "204": { description: Deleted }, "409": { description: Version conflict or referenced value } }
  /admin/b2c/need-tags/reorder:
    put:
      operationId: adminReorderB2cNeedTags
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cReorderInput" } } } }
      responses: { "200": { description: Reordered values } }
  /admin/b2c/banners:
    get:
      operationId: adminListB2cBanners
      tags: [admin]
      parameters: [{ name: supplierId, in: query, schema: { type: string } }]
      responses: { "200": { description: Banners } }
    post:
      operationId: adminCreateB2cBanner
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminB2cBannerInput" } } } }
      responses: { "201": { description: Created banner } }
  /admin/b2c/banners/{id}:
    patch:
      operationId: adminUpdateB2cBanner
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminB2cBannerUpdate" } } } }
      responses: { "200": { description: Updated banner }, "409": { description: Version conflict } }
    delete:
      operationId: adminDeleteB2cBanner
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: expectedVersion, in: query, required: true, schema: { type: integer, minimum: 1 } }
      responses: { "204": { description: Deleted }, "409": { description: Version conflict } }
  /admin/b2c/banners/reorder:
    put:
      operationId: adminReorderB2cBanners
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/B2cReorderInput" } } } }
      responses: { "200": { description: Reordered banners } }
  /admin/b2c/display-settings:
    get:
      operationId: adminGetB2cDisplaySettings
      tags: [admin]
      responses: { "200": { description: Display settings, content: { application/json: { schema: { $ref: "#/components/schemas/AdminB2cDisplaySettings" } } } } }
    put:
      operationId: adminUpdateB2cDisplaySettings
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminB2cDisplaySettingsInput" } } } }
      responses:
        "200": { description: Updated settings, content: { application/json: { schema: { $ref: "#/components/schemas/AdminB2cDisplaySettings" } } } }
        "409": { description: Version conflict }
  /admin/service-categories:
    get:
      operationId: adminListServiceCategories
      tags: [admin]
      summary: List marketplace service categories and their category-card fallback images
      responses:
        "200":
          description: Service categories
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminServiceCategory" }
  /admin/service-categories/image-upload-url:
    post:
      operationId: adminRequestServiceCategoryImageUpload
      tags: [admin]
      deprecated: true
      summary: Deprecated unvalidated service-category upload flow
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ServiceCategoryImageUploadInput" }
      responses:
        "410":
          description: Use the validated media upload and finalize endpoints
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
  /admin/service-categories/{categoryId}:
    patch:
      operationId: adminUpdateServiceCategory
      tags: [admin]
      summary: Update a marketplace service category fallback image
      parameters:
        - name: categoryId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminServiceCategoryImageUpdate" }
      responses:
        "200":
          description: Updated service category
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminServiceCategory" }
  /admin/service-templates:
    get:
      operationId: adminListServiceTemplates
      tags: [admin]
      summary: List service templates for administration
      parameters:
        - name: search
          in: query
          schema: { type: string, maxLength: 120 }
        - name: mainCategory
          in: query
          schema: { type: string, maxLength: 120 }
        - name: subcategory
          in: query
          schema: { type: string, maxLength: 120 }
      responses:
        "200":
          description: Service templates
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ServiceTemplate" }
    post:
      operationId: adminCreateServiceTemplate
      tags: [admin]
      summary: Create a service template
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminServiceTemplateInput" }
      responses:
        "201":
          description: Created service template
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceTemplate" }
  /admin/service-templates/{templateId}:
    patch:
      operationId: adminUpdateServiceTemplate
      tags: [admin]
      summary: Update a service template
      parameters:
        - name: templateId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminServiceTemplateUpdate" }
      responses:
        "200":
          description: Updated service template
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceTemplate" }
    delete:
      operationId: adminDeleteServiceTemplate
      tags: [admin]
      summary: Delete a service template
      parameters:
        - name: templateId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Deleted service template
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceTemplate" }
  /admin/brands:
    get:
      operationId: adminListBrands
      tags: [admin]
      summary: List product brands
      responses:
        "200":
          description: Brands
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminBrand" }
    post:
      operationId: adminCreateBrand
      tags: [admin]
      summary: Create a brand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminBrandInput"
      responses:
        "201":
          description: Created brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBrand"
  /admin/brands/{brandId}:
    patch:
      operationId: adminUpdateBrand
      tags: [admin]
      summary: Update a brand
      parameters:
        - name: brandId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminBrandUpdate"
      responses:
        "200":
          description: Updated brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBrand"
    delete:
      operationId: adminDeleteBrand
      tags: [admin]
      summary: Delete a brand (or deactivate it when in use)
      parameters:
        - name: brandId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
      responses:
        "200":
          description: Deleted or deactivated brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBrand"
  /admin/coupons:
    get:
      operationId: adminListCoupons
      tags: [admin]
      summary: List all commerce coupons
      responses:
        "200": { description: Coupons, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Coupon" } } } } }
    post:
      operationId: adminCreateCoupon
      tags: [admin]
      summary: Create a coupon
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CouponInput" } } }
      responses:
        "201": { description: Created coupon, content: { application/json: { schema: { $ref: "#/components/schemas/Coupon" } } } }
        "409": { description: Coupon code already exists }
  /admin/coupons/{couponId}:
    put:
      operationId: adminUpdateCoupon
      tags: [admin]
      summary: Replace coupon rules
      parameters: [{ name: couponId, in: path, required: true, schema: { type: string } }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CouponInput" } } }
      responses:
        "200": { description: Updated coupon, content: { application/json: { schema: { $ref: "#/components/schemas/Coupon" } } } }
        "404": { description: Coupon not found }
    delete:
      operationId: adminDeactivateCoupon
      tags: [admin]
      summary: Deactivate a coupon without deleting its audit history
      parameters: [{ name: couponId, in: path, required: true, schema: { type: string } }]
      responses:
        "204": { description: Coupon deactivated }
        "404": { description: Coupon not found }
  /admin/shop-settings:
    get:
      operationId: adminGetShopSettings
      tags: [admin]
      summary: Get versioned commerce presentation and loyalty settings
      responses:
        "200": { description: Shop settings, content: { application/json: { schema: { $ref: "#/components/schemas/AdminShopSettings" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
    put:
      operationId: adminUpdateShopSettings
      tags: [admin]
      summary: Replace versioned commerce presentation and loyalty settings
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/AdminShopSettingsInput" } } }
      responses:
        "200": { description: Updated shop settings, content: { application/json: { schema: { $ref: "#/components/schemas/AdminShopSettings" } } } }
        "400": { description: Invalid settings }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "409": { description: Settings version conflict }
  /admin/shipping:
    get:
      operationId: adminGetShippingConfig
      tags: [admin]
      summary: Get weight-based shipping configuration
      responses:
        "200":
          description: Shipping configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShippingConfig"
    put:
      operationId: adminUpdateShippingConfig
      tags: [admin]
      summary: Replace weight-based shipping configuration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShippingConfigInput"
      responses:
        "200":
          description: Updated shipping configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShippingConfig"
  /admin/courier-services:
    get:
      operationId: adminListCourierServices
      tags: [admin]
      summary: List editable courier services
      responses:
        "200":
          description: Courier services
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CourierService" }
    post:
      operationId: adminCreateCourierService
      tags: [admin]
      summary: Create a courier service
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CourierServiceInput" }
      responses:
        "201":
          description: Created courier service
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourierService" }
  /admin/courier-services/{courierServiceId}:
    patch:
      operationId: adminUpdateCourierService
      tags: [admin]
      summary: Update a courier service
      parameters:
        - name: courierServiceId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CourierServiceUpdate" }
      responses:
        "200":
          description: Updated courier service
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourierService" }
    delete:
      operationId: adminDeleteCourierService
      tags: [admin]
      summary: Delete a courier service
      parameters:
        - name: courierServiceId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Courier service deleted

  # ---------------------------------------------------------------------------
  # Business Growth Phase 2
  # ---------------------------------------------------------------------------

  /growth/retention:
    get:
      operationId: ownerListRetention
      tags: [growth]
      summary: List retention status for all salon customers (owner)
      responses:
        "200":
          description: Retention list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CustomerRetentionItem" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/retention/{salonCustomerId}:
    get:
      operationId: ownerGetRetentionDetail
      tags: [growth]
      summary: Get retention detail for a single salon customer (owner)
      parameters:
        - name: salonCustomerId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Retention detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerRetentionDetail" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "404": { description: Customer not found }

  /growth/automations:
    get:
      operationId: ownerListAutomations
      tags: [growth]
      summary: List automation rules for the active salon (owner)
      responses:
        "200":
          description: Automation rules
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AutomationRule" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
    post:
      operationId: ownerCreateAutomation
      tags: [growth]
      summary: Create a new automation rule (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateAutomationRuleBody" }
      responses:
        "201":
          description: Created automation rule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/automations/{automationId}:
    get:
      operationId: ownerGetAutomation
      tags: [growth]
      summary: Get an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Automation rule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "404": { description: Not found }
    patch:
      operationId: ownerUpdateAutomation
      tags: [growth]
      summary: Update an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateAutomationRuleBody" }
      responses:
        "200":
          description: Updated automation rule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "404": { description: Not found }
    delete:
      operationId: ownerDeleteAutomation
      tags: [growth]
      summary: Delete an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Deleted }
        "404": { description: Not found }

  /growth/automations/{automationId}/activate:
    post:
      operationId: ownerActivateAutomation
      tags: [growth]
      summary: Activate an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Activated automation rule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "404": { description: Not found }

  /growth/automations/{automationId}/pause:
    post:
      operationId: ownerPauseAutomation
      tags: [growth]
      summary: Pause an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Paused automation rule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "404": { description: Not found }

  /growth/automations/{automationId}/stats:
    get:
      operationId: ownerGetAutomationStats
      tags: [growth]
      summary: Get run statistics for an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
        - name: period
          in: query
          description: Time window for run/delivery aggregation (defaults to all time)
          schema: { type: string, enum: [7d, 30d, 90d, all] }
        - name: from
          in: query
          description: Custom window start date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: to
          in: query
          description: Custom window end date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: compare
          in: query
          description: Set to "previous" to also return counts for the preceding window of the same length. Requires a bounded period (7d, 30d, 90d) or a complete custom from/to range.
          schema: { type: string, enum: [previous] }
      responses:
        "200":
          description: Automation stats
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationStats" }
        "400": { description: Invalid period or date range }
        "404": { description: Not found }

  /growth/automations/{automationId}/attributed-appointments:
    get:
      operationId: ownerListAutomationAttributedAppointments
      tags: [growth]
      summary: List the concrete appointments attributed to an automation rule (owner, paginated)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
        - name: period
          in: query
          description: Time window for run attribution, matching the stats endpoints (defaults to all time)
          schema: { type: string, enum: [7d, 30d, 90d, all] }
        - name: from
          in: query
          description: Custom window start date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: to
          in: query
          description: Custom window end date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: clientType
          in: query
          description: Restrict the list to only new or only returning clients, using the same derivation as the per-row isReturning field. Rows without a linked salon customer match neither segment. Applied to both the rows and the total.
          schema: { type: string, enum: [new, returning] }
        - name: limit
          in: query
          description: Page size (defaults to 25, maximum 100)
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: offset
          in: query
          description: Number of rows to skip (defaults to 0)
          schema: { type: integer, minimum: 0 }
      responses:
        "200":
          description: One page of attributed appointments (date, service, price in RSD), newest first, plus the total count
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationAttributedAppointmentsPage" }
        "400": { description: "Invalid period, limit, or offset" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "404": { description: Not found }

  /growth/automation-stats:
    get:
      operationId: ownerListAutomationStats
      tags: [growth]
      summary: List performance stats for every automation rule of the active salon (owner)
      parameters:
        - name: period
          in: query
          description: Time window for run/delivery aggregation (defaults to all time)
          schema: { type: string, enum: [7d, 30d, 90d, all] }
        - name: from
          in: query
          description: Custom window start date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: to
          in: query
          description: Custom window end date (inclusive, YYYY-MM-DD); cannot be combined with period
          schema: { type: string, format: date }
        - name: compare
          in: query
          description: Set to "previous" to also return counts for the preceding window of the same length. Requires a bounded period (7d, 30d, 90d) or a complete custom from/to range.
          schema: { type: string, enum: [previous] }
      responses:
        "200":
          description: Per-rule campaign performance overview
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AutomationStatsOverviewItem" }
        "400": { description: "Invalid period, date range, or compare value" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/automations/{automationId}/test-run:
    post:
      operationId: ownerTestRunAutomation
      tags: [growth]
      summary: Perform a safe manual test-run of an automation rule (owner)
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Test run result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationTestRunResult" }
        "404": { description: Not found }

  /growth/automations/from-ai-proposal:
    post:
      operationId: ownerCreateAutomationFromAiProposal
      tags: [growth]
      summary: Explicitly confirm creation of a paused automation from an AI proposal (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateAutomationFromAiProposalBody" }
      responses:
        "201":
          description: Created paused automation from AI proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutomationRule" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/packages:
    get:
      operationId: ownerListPackages
      tags: [growth]
      summary: List treatment packages for the active salon (owner)
      responses:
        "200":
          description: Treatment packages
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TreatmentPackage" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
    post:
      operationId: ownerCreatePackage
      tags: [growth]
      summary: Create a treatment package (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTreatmentPackageBody" }
      responses:
        "201":
          description: Created treatment package
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TreatmentPackage" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/packages/quick-sale:
    post:
      operationId: ownerQuickCreatePackagePurchase
      tags: [growth]
      summary: Atomically create a treatment package definition and sell it to an existing salon customer (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuickPackagePurchaseInput" }
      responses:
        "201":
          description: Created package definition and customer purchase
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuickPackagePurchaseResult" }
        "400": { description: Invalid package or service quotas }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "404": { description: Salon customer not found }

  /growth/packages/{packageId}:
    get:
      operationId: ownerGetPackage
      tags: [growth]
      summary: Get a treatment package (owner)
      parameters:
        - name: packageId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Treatment package
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TreatmentPackage" }
        "404": { description: Not found }
    patch:
      operationId: ownerUpdatePackage
      tags: [growth]
      summary: Update a treatment package (owner)
      parameters:
        - name: packageId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTreatmentPackageBody" }
      responses:
        "200":
          description: Updated treatment package
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TreatmentPackage" }
        "404": { description: Not found }
    delete:
      operationId: ownerDeletePackage
      tags: [growth]
      summary: Delete a treatment package (owner)
      parameters:
        - name: packageId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Deleted }
        "404": { description: Not found }

  /growth/packages/public:
    get:
      operationId: customerListPublicPackages
      tags: [growth]
      summary: List available packages for a salon (public/customer)
      parameters:
        - name: salonId
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Available packages
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TreatmentPackagePublic" }

  /growth/packages/{packageId}/purchases:
    post:
      operationId: customerPurchasePackage
      tags: [growth]
      summary: Create a pending package purchase (customer)
      parameters:
        - name: packageId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PurchasePackageBody" }
      responses:
        "201":
          description: Created pending purchase
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PackagePurchase" }
        "401": { description: Authentication required }
        "403": { description: Customer access required }
        "404": { description: Package not found }

  /growth/packages/{packageId}/purchases/{purchaseId}/confirm-payment:
    post:
      operationId: ownerConfirmPackagePayment
      tags: [growth]
      summary: Confirm payment for a package purchase (owner)
      parameters:
        - name: packageId
          in: path
          required: true
          schema: { type: string }
        - name: purchaseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Purchase activated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PackagePurchase" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "404": { description: Not found }

  /growth/my-purchases:
    get:
      operationId: customerListMyPurchases
      tags: [growth]
      summary: List own package purchases (customer)
      responses:
        "200":
          description: Customer's package purchases
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PackagePurchase" }
        "401": { description: Authentication required }

  /growth/my-purchases/{purchaseId}/redeem:
    post:
      operationId: customerRedeemPackageSession
      tags: [growth]
      summary: Redeem one session from a package against an appointment (customer)
      parameters:
        - name: purchaseId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RedeemSessionBody" }
      responses:
        "200":
          description: Session redeemed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RedeemSessionResult" }
        "400": { description: Validation error }
        "401": { description: Authentication required }
        "403": { description: Access denied (purchase not owned by caller) }
        "404": { description: Purchase not found }
        "409":
          description: >-
            Redemption not allowed. Carries the stable package-error
            discriminator `code: PACKAGE_ERROR` plus the specific `reason`
            (e.g. appointment_not_eligible, already_redeemed, no_sessions_left,
            expired, not_active, service_not_covered).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppointmentConflictError"

  /growth/salon-customer-packages:
    get:
      operationId: ownerListCustomerPackages
      tags: [growth]
      summary: List all customer package purchases for the active salon (owner)
      parameters:
        - name: salonCustomerId
          in: query
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [pending_payment, active, completed, expired, cancelled] }
      responses:
        "200":
          description: Customer package purchases
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PackagePurchase" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/employees/performance:
    get:
      operationId: ownerListEmployeePerformance
      tags: [growth]
      summary: List employee performance metrics for the active location or all owner locations
      parameters:
        - name: scope
          in: query
          schema: { type: string, enum: [location, all], default: location }
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Employee performance list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeePerformanceResponse"
        "401": { description: Authentication required }
        "403": { description: Salon owner required }

  /growth/employees/{employeeId}/commission:
    patch:
      operationId: ownerUpdateEmployeeCommission
      tags: [growth]
      summary: Update commission settings for an employee (owner)
      parameters:
        - name: employeeId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateCommissionBody" }
      responses:
        "200":
          description: Updated commission settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeCommissionSettings" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "404": { description: Employee not found }

  /growth/my-performance:
    get:
      operationId: employeeGetMyPerformance
      tags: [growth]
      summary: Get performance metrics for the logged-in employee
      parameters:
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Employee self-performance metrics
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeePerformanceMetrics" }
        "401": { description: Authentication required }
        "403": { description: Employee access required }
        "404": { description: Employee profile not found }

  /growth/ai/ask:
    post:
      operationId: ownerAskGrowthAi
      tags: [growth]
      summary: Ask the AI growth advisor a question grounded in salon data (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GrowthAiQuestionBody" }
      responses:
        "200":
          description: AI answer with auditable snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrowthAiAnswer" }
        "401": { description: Authentication required }
        "403": { description: Salon owner required }
        "502": { description: AI provider unavailable }

  /growth/redemptions/{redemptionId}/reverse:
    post:
      operationId: ownerReverseRedemption
      tags: [growth]
      summary: Reverse a package session redemption (owner), restoring session count and appointment price
      parameters:
        - name: redemptionId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Reversal result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReversalResult" }
        "403": { description: Salon owner required }
        "404": { description: Redemption not found }
        "409": { description: Already reversed }

  /growth/admin/summary:
    get:
      operationId: adminGetGrowthSummary
      tags: [growth]
      summary: Platform-wide growth summary for admin (read-only, no mutation)
      responses:
        "200":
          description: Platform growth summary
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrowthAdminSummary" }
        "403": { description: Admin access required }

  /growth/admin/retention-settings:
    get:
      operationId: adminGetRetentionSettings
      tags: [growth]
      summary: Active platform retention classification thresholds (admin)
      responses:
        "200":
          description: Active retention settings (version 0 = platform defaults)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RetentionSettings" }
        "401": { description: Authentication required }
        "403": { description: Admin access required }
    put:
      operationId: adminUpdateRetentionSettings
      tags: [growth]
      summary: Update platform retention thresholds (admin); records an audited new version. Requires the expected active version (optimistic concurrency) — a stale version is rejected with 409.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RetentionSettingsUpdate" }
      responses:
        "200":
          description: Newly active retention settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RetentionSettings" }
        "400": { description: Validation error (out-of-range or inconsistent thresholds) }
        "401": { description: Authentication required }
        "403": { description: Admin access required }
        "409":
          description: Another admin saved a newer version since the client last loaded the settings; refetch and re-confirm
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RetentionSettingsVersionConflict" }

  /growth/admin/retention-settings/preview:
    post:
      operationId: adminPreviewRetentionSettings
      tags: [growth]
      summary: Dry-run candidate retention thresholds against current data (admin); never persists a settings version
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RetentionThresholds" }
      responses:
        "200":
          description: Platform-wide per-status counts under current vs. candidate thresholds; above the exact-preview cap the counts are extrapolated from a uniform random sample and flagged with isEstimate/sampleSize
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RetentionSettingsPreview" }
        "400": { description: Validation error (out-of-range or inconsistent thresholds) }
        "401": { description: Authentication required }
        "403": { description: Admin access required }
        "503": { description: "Preview temporarily unavailable — the computation exceeded its time budget, or a single customer's history exceeds the per-batch row budget (friendly message in `error`)" }

  /growth/admin/retention-settings/history:
    get:
      operationId: adminGetRetentionSettingsHistory
      tags: [growth]
      summary: Audit history of retention threshold changes (admin), newest first
      responses:
        "200":
          description: Change history with previous values per entry
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RetentionSettingsHistoryEntry" }
        "401": { description: Authentication required }
        "403": { description: Admin access required }

  # ── Phase 3: before/after treatment photos ────────────────────────────────
  /employee/appointments/{appointmentId}/treatment-photos:
    get:
      operationId: listEmployeeAppointmentTreatmentPhotos
      tags: [salon-management]
      summary: List before/after photos for an own appointment (employee)
      parameters:
        - { name: appointmentId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Photos for the appointment
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TreatmentPhoto" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "404": { description: Appointment not found }
    post:
      operationId: createEmployeeTreatmentPhoto
      tags: [salon-management]
      summary: Attach a before/after photo to a completed own appointment (employee)
      parameters:
        - { name: appointmentId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TreatmentPhotoCreate" }
      responses:
        "201":
          description: Photo saved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TreatmentPhoto" }
        "400": { description: "Missing consent, invalid photo or appointment not completed" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "404": { description: Appointment not found }
        "409": { description: Photo already attached elsewhere }
  /appointments/{appointmentId}/treatment-photos:
    get:
      operationId: listCustomerAppointmentTreatmentPhotos
      tags: [appointments]
      summary: List before/after photos of an own appointment (customer)
      parameters:
        - { name: appointmentId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Photos for the customer's own appointment
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TreatmentPhoto" }
        "401": { description: Authentication required }
        "404": { description: Appointment not found }
  /salon/customers/{customerId}/treatment-photos:
    get:
      operationId: listSalonCustomerTreatmentPhotos
      tags: [salon-management]
      summary: List a CRM client's before/after photos chronologically (owner)
      parameters:
        - { name: customerId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Photos grouped chronologically by treatment
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TreatmentPhoto" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Client not found }

  # ── Phase 3: service consumption mapping + salon inventory ────────────────
  /salon/services/{serviceId}/consumptions:
    get:
      operationId: getServiceConsumptions
      tags: [salon-management]
      summary: List product consumption mappings for a service (owner)
      parameters:
        - { name: serviceId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Consumption mappings
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ServiceConsumption" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Service not found }
    put:
      operationId: putServiceConsumptions
      tags: [salon-management]
      summary: Replace product consumption mappings for a service (owner)
      parameters:
        - { name: serviceId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ServiceConsumptionsPut" }
      responses:
        "200":
          description: Updated consumption mappings
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ServiceConsumption" }
        "400": { description: Invalid mapping payload }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Service or product not found }
  /salon/inventory:
    get:
      operationId: listSalonInventory
      tags: [salon-management]
      summary: List salon-owned product inventory with low-stock flags (owner)
      responses:
        "200":
          description: Inventory items
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonInventoryItem" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
  /salon/inventory/{productId}:
    patch:
      operationId: updateSalonInventoryItem
      tags: [salon-management]
      summary: Adjust quantity, threshold or unit settings of an inventory item (owner)
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonInventoryItemUpdate" }
      responses:
        "200":
          description: Updated inventory item
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonInventoryItem" }
        "400": { description: Invalid values }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Inventory item not found }

  # ── Phase 3: employee time clock ───────────────────────────────────────────
  /employee/clock:
    get:
      operationId: getEmployeeClock
      tags: [salon-management]
      summary: Current clock status and recent entries (employee)
      responses:
        "200":
          description: Clock status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeClockStatus" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
  /employee/clock-in:
    post:
      operationId: employeeClockIn
      tags: [salon-management]
      summary: Start a shift (employee)
      responses:
        "201":
          description: Shift started
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeClockEntry" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "409": { description: A shift is already open }
  /employee/clock-out:
    post:
      operationId: employeeClockOut
      tags: [salon-management]
      summary: End the open shift (employee)
      responses:
        "200":
          description: Shift closed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeClockEntry" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "409": { description: No open shift }
  /salon/clock-entries:
    get:
      operationId: listSalonClockEntries
      tags: [salon-management]
      summary: Work-hour records per employee for a period, with appointment counts (owner)
      parameters:
        - { name: from, in: query, required: true, schema: { type: string } }
        - { name: to, in: query, required: true, schema: { type: string } }
        - { name: employeeId, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Per-employee work-hour summaries
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonClockEmployeeSummary" }
        "400": { description: Invalid period }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
  /salon/clock-entries/{entryId}:
    patch:
      operationId: updateSalonClockEntry
      tags: [salon-management]
      summary: Manually correct or close a clock entry (owner)
      parameters:
        - { name: entryId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SalonClockEntryUpdate" }
      responses:
        "200":
          description: Corrected entry
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeClockEntry" }
        "400": { description: Invalid correction }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Entry not found }

  # ── Phase 3: shift swapping ────────────────────────────────────────────────
  /employee/shift-swaps:
    get:
      operationId: listEmployeeShiftSwaps
      tags: [salon-management]
      summary: Own outgoing/incoming shift-swap requests and colleagues (employee)
      responses:
        "200":
          description: Shift swap overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeShiftSwapOverview" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
    post:
      operationId: createEmployeeShiftSwap
      tags: [salon-management]
      summary: Propose swapping a working day with a colleague (employee)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShiftSwapCreate" }
      responses:
        "201":
          description: Swap request created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShiftSwapRequest" }
        "400": { description: Invalid colleague or date }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "409": { description: An open request for that day already exists }
  /employee/shift-swaps/{requestId}/respond:
    post:
      operationId: respondEmployeeShiftSwap
      tags: [salon-management]
      summary: Accept or decline a colleague's swap proposal (employee)
      parameters:
        - { name: requestId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShiftSwapRespondBody" }
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShiftSwapRequest" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "404": { description: Request not found }
        "409": { description: Request no longer awaiting your response }
  /employee/shift-swaps/{requestId}/cancel:
    post:
      operationId: cancelEmployeeShiftSwap
      tags: [salon-management]
      summary: Cancel an own pending swap request (employee)
      parameters:
        - { name: requestId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Cancelled request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShiftSwapRequest" }
        "401": { description: Authentication required }
        "403": { description: Salon employee role required }
        "404": { description: Request not found }
        "409": { description: Request can no longer be cancelled }
  /salon/shift-swaps:
    get:
      operationId: listSalonShiftSwaps
      tags: [salon-management]
      summary: Shift-swap requests awaiting approval and history, with both employees' appointments (owner)
      responses:
        "200":
          description: Swap requests
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SalonShiftSwapRequest" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
  /salon/shift-swaps/{requestId}/review:
    post:
      operationId: reviewSalonShiftSwap
      tags: [salon-management]
      summary: Approve or decline an accepted swap; approval reassigns that day's appointments (owner)
      parameters:
        - { name: requestId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShiftSwapReviewBody" }
      responses:
        "200":
          description: Reviewed request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SalonShiftSwapRequest" }
        "401": { description: Authentication required }
        "403": { description: Salon owner role required }
        "404": { description: Request not found }
        "409": { description: Request not awaiting owner review }

  # ── Phase 3: public booking widget (CORS-open, rate limited) ──────────────
  /widget/salons/{slug}:
    get:
      operationId: getWidgetSalon
      tags: [widget]
      summary: Public widget bootstrap — salon identity, services and employees
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Widget bootstrap data
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WidgetSalon" }
        "404": { description: Salon not found or not bookable }
        "429": { description: Too many requests }
  /widget/salons/{slug}/availability:
    get:
      operationId: getWidgetAvailability
      tags: [widget]
      summary: Public widget availability slots for a service and date
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
        - { name: serviceId, in: query, required: true, schema: { type: string } }
        - { name: date, in: query, required: true, schema: { type: string } }
        - { name: employeeId, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Available slots
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WidgetSlot" }
        "400": { description: Invalid parameters }
        "404": { description: Salon or service not found }
        "429": { description: Too many requests }
  /widget/salons/{slug}/appointments:
    post:
      operationId: createWidgetAppointment
      tags: [widget]
      summary: Public widget booking — creates a pending appointment for the salon
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WidgetAppointmentCreate" }
      responses:
        "201":
          description: Appointment request created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WidgetAppointmentCreated" }
        "400": { description: Invalid booking data }
        "404": { description: Salon or service not found }
        "409": { description: Slot no longer available }
        "429":
          description: Widget rate limit or booking admission capacity reached. Capacity responses use code BOOKING_CAPACITY and include Retry-After.
          headers:
            Retry-After:
              schema: { type: integer, minimum: 1 }
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/BookingCapacityError"
                  - type: object
                    properties:
                      error: { type: string }
                    required: [error]
  /widget/salons/{slug}/booking-groups:
    post:
      operationId: createWidgetBookingGroup
      tags: [widget]
      summary: Public rate-limited grouped booking for an anonymous salon customer
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WidgetBookingGroupCreate" }
      responses:
        "201":
          description: Grouped appointment request created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BookingGroup" }
        "400": { description: Invalid booking data }
        "404": { description: Salon or service not found }
        "409": { description: One or more slots are no longer available }
        "429":
          description: Widget rate limit or booking admission capacity reached. Capacity responses use code BOOKING_CAPACITY and include Retry-After.
          headers:
            Retry-After:
              schema: { type: integer, minimum: 1 }
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/BookingCapacityError"
                  - type: object
                    properties:
                      error: { type: string }
                    required: [error]

  /retail/wishlist:
    get:
      operationId: listProductWishlist
      tags: [shop]
      summary: List the signed-in customer's saved B2C products, including unavailable items
      responses:
        "200": { description: Wishlist items, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ProductWishlistItem" } } } } }
        "401": { description: Customer authentication required }
        "403": { description: Customer access required }
    post:
      operationId: addProductWishlistItem
      tags: [shop]
      summary: Idempotently save a currently B2C-visible product
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWishlistInput" } } } }
      responses:
        "200": { description: Already saved wishlist item, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWishlistItem" } } } }
        "201": { description: Saved wishlist item, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWishlistItem" } } } }
        "404": { description: Product is not B2C-visible }
        "401": { description: Customer authentication required }
  /retail/wishlist/toggle:
    post:
      operationId: toggleProductWishlistItem
      tags: [shop]
      summary: Atomically add or remove a customer wishlist item
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWishlistInput" } } } }
      responses:
        "200": { description: Wishlist state, content: { application/json: { schema: { $ref: "#/components/schemas/ProductWishlistToggleResult" } } } }
        "404": { description: Product is not B2C-visible when adding }
        "401": { description: Customer authentication required }
  /retail/wishlist/{productId}:
    delete:
      operationId: removeProductWishlistItem
      tags: [shop]
      summary: Remove a saved product variant from the signed-in customer's wishlist
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
        - { name: variantValue, in: query, schema: { type: string } }
      responses:
        "204": { description: Removed; missing entries are idempotently ignored }
        "401": { description: Customer authentication required }
  /retail/cart:
    get:
      operationId: getRetailCart
      tags: [shop]
      summary: Get the current guest or authenticated CUSTOMER/JOBSEEKER retail cart
      responses:
        "200": { description: Retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
  /retail/cart-summary:
    get:
      operationId: getRetailCartSummary
      tags: [shop]
      summary: Get the current retail cart item count without creating a cart
      responses:
        "200": { description: Retail cart item count, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCartSummary" } } } }
  /retail/cart/contact:
    put:
      operationId: captureRetailCartContact
      tags: [shop]
      summary: Capture a guest email for an abandoned-cart reminder without checkout
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCartContactInput" } } } }
      responses:
        "204": { description: Contact captured }
        "400": { description: Invalid email address }
  /retail/cart/items:
    post:
      operationId: addRetailCartItem
      tags: [shop]
      summary: Add a retail product to the separate guest or CUSTOMER/JOBSEEKER cart
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCartItemInput" } } } }
      responses:
        "201": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
  /retail/cart/items/{cartItemId}:
    patch:
      operationId: updateRetailCartItem
      tags: [shop]
      summary: Change a retail cart item quantity
      parameters: [{ name: cartItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
        "409": { description: Requested quantity is unavailable }
    delete:
      operationId: removeRetailCartItem
      tags: [shop]
      summary: Remove an item from the current retail cart
      parameters: [{ name: cartItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
  /retail/cart/items/{cartItemId}/save-for-later:
    post:
      operationId: saveRetailCartItemForLater
      tags: [shop]
      summary: Atomically move a retail cart line to saved items
      parameters: [{ name: cartItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
  /retail/cart/saved-items/{savedItemId}:
    delete:
      operationId: removeSavedRetailCartItem
      tags: [shop]
      summary: Remove a saved retail cart line
      parameters: [{ name: savedItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
  /retail/cart/saved-items/{savedItemId}/restore:
    post:
      operationId: restoreSavedRetailCartItem
      tags: [shop]
      summary: Restore a saved retail line with current eligibility and price
      parameters: [{ name: savedItemId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail cart, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCart" } } } }
        "409": { description: Current catalog rules prevent restore }
  /retail/orders/repeat-last:
    post:
      operationId: repeatLastRetailOrder
      tags: [shop]
      summary: Add eligible lines from the authenticated CUSTOMER or JOBSEEKER account's latest retail order
      parameters: [{ name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 8, maxLength: 200 } }]
      responses:
        "200": { description: Reorder result, content: { application/json: { schema: { $ref: "#/components/schemas/ReorderResult" } } } }
        "401": { description: CUSTOMER or JOBSEEKER authentication required }
  /retail/checkout-preview:
    get:
      operationId: previewRetailCheckout
      tags: [shop]
      summary: Calculate delivery and final total for the selected retail destination
      parameters:
        - { name: deliveryMethod, in: query, schema: { type: string, enum: [courier, personal_belgrade] } }
        - { name: city, in: query, schema: { type: string } }
        - { name: desiredReferralCreditRsd, in: query, schema: { type: integer, minimum: 0 } }
        - { name: couponCode, in: query, schema: { type: string, maxLength: 40 } }
        - { name: aftercareRecommendationId, in: query, description: "Authenticated customer's opaque aftercare recommendation id.", schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
      responses:
        "200":
          description: Checkout pricing preview
          content: { application/json: { schema: { $ref: "#/components/schemas/RetailCheckoutPreview" } } }
        "400": { description: Delivery method is not available for the destination }
        "409":
          description: "A cart item price or availability changed"
          content: { application/json: { schema: { $ref: "#/components/schemas/ApiError" } } }
  /retail/checkout:
    post:
      operationId: checkoutRetailCart
      tags: [shop]
      summary: Create an idempotent retail order from the current guest or authenticated CUSTOMER/JOBSEEKER cart
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailCheckoutInput" } } } }
      responses:
        "201": { description: Retail order, content: { application/json: { schema: { $ref: "#/components/schemas/RetailOrder" } } } }
        "409":
          description: "The cart, stock, or displayed checkout quote changed"
          content: { application/json: { schema: { $ref: "#/components/schemas/ApiError" } } }
  /retail/orders/track:
    get:
      operationId: trackRetailOrder
      tags: [shop]
      summary: Track a retail order using its opaque guest access token
      parameters: [{ name: token, in: query, required: true, schema: { type: string, minLength: 32 } }]
      responses:
        "200": { description: Minimized public tracking state, content: { application/json: { schema: { $ref: "#/components/schemas/PublicRetailOrderTracking" } } } }
        "404": { description: "Generic not-found response for invalid, expired, or revoked tokens" }
  /retail/orders/track/lookup:
    post:
      operationId: lookupRetailOrderTracking
      tags: [shop]
      summary: Look up retail tracking by exact order number and normalized email
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailTrackingLookupInput" } } } }
      responses:
        "200": { description: Minimized public tracking state; a fresh opaque link is emailed, content: { application/json: { schema: { $ref: "#/components/schemas/PublicRetailOrderTracking" } } } }
        "404": { description: Generic failure for every credential mismatch }
        "429": { description: Generic rate-limit response }
  /customer/retail-orders:
    get:
      operationId: listCustomerRetailOrders
      tags: [customer]
      summary: List only the authenticated CUSTOMER or JOBSEEKER account's retail orders
      responses:
        "200": { description: Retail orders, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/RetailOrder" } } } } }
  /customer/retail-orders/{orderId}:
    get:
      operationId: getCustomerRetailOrder
      tags: [customer]
      summary: Get one retail order owned by the authenticated CUSTOMER or JOBSEEKER account
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Customer-owned retail order, content: { application/json: { schema: { $ref: "#/components/schemas/RetailOrder" } } } }
        "404": { description: Order not found or not owned by the authenticated retail account }
  /admin/retail-orders:
    get:
      operationId: adminListRetailOrders
      tags: [admin]
      summary: List retail orders for administration
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, confirmed, paid, processing, shipped, delivered, cancelled]
        - name: search
          in: query
          required: false
          description: Search order details or the immutable catalog reference saved on an order item.
          schema:
            type: string
            minLength: 1
            maxLength: 200
      responses:
        "200": { description: Retail orders, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/RetailOrder" } } } } }
  /admin/retail-orders/{orderId}:
    get:
      operationId: adminGetRetailOrder
      tags: [admin]
      summary: Get the retail order snapshot and customer delivery details
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Retail order and fulfillment audit, content: { application/json: { schema: { $ref: "#/components/schemas/AdminRetailOrder" } } } }
  /admin/retail-orders/{orderId}/status:
    patch:
      operationId: adminUpdateRetailOrderStatus
      tags: [admin]
      summary: Update retail fulfillment status
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminFulfillmentUpdate" } } } }
      responses:
        "200": { description: Updated retail order, content: { application/json: { schema: { $ref: "#/components/schemas/AdminRetailOrder" } } } }
  /admin/commerce/profitability:
    get:
      operationId: adminGetCommerceProfitability
      tags: [admin]
      summary: Aggregate immutable B2C/B2B order-line profitability snapshots
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date } }
        - { name: to, in: query, required: true, schema: { type: string, format: date } }
        - { name: market, in: query, schema: { type: string, enum: [B2C, B2B, BOTH], default: BOTH } }
        - { name: supplierId, in: query, schema: { type: string } }
        - { name: categoryId, in: query, schema: { type: string } }
        - { name: brand, in: query, schema: { type: string, minLength: 1, maxLength: 120 } }
        - { name: productId, in: query, schema: { type: string } }
        - { name: granularity, in: query, schema: { type: string, enum: [DAY, WEEK, MONTH], default: DAY } }
      responses:
        "200": { description: "Profitability KPIs, time series, and bounded product groups", content: { application/json: { schema: { $ref: "#/components/schemas/AdminProfitabilityReport" } } } }
        "400": { description: Invalid or unbounded date/filter range }
        "403": { description: Administrator access required }
  /admin/retail-orders/{orderId}/payment-status:
    patch:
      operationId: adminUpdateRetailPaymentStatus
      tags: [admin]
      summary: Update retail payment state separately from fulfillment status
      parameters: [{ name: orderId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Updated retail order, content: { application/json: { schema: { $ref: "#/components/schemas/RetailOrder" } } } }
  /retail/products/{productId}/reviews:
    get:
      operationId: listRetailProductReviews
      tags: [shop]
      summary: List public aggregated retail reviews for a product
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Public retail reviews, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/PublicRetailProductReview" } } } } }
        "404": { description: Product is not publicly available in the B2C catalogue }
  /suppliers/{supplierSlug}/public-products/{productId}/reviews:
    get:
      operationId: listSupplierPublicProductReviews
      tags: [shop]
      summary: List privacy-safe published verified-purchase reviews and the moderated product summary
      parameters:
        - { name: supplierSlug, in: path, required: true, schema: { type: string } }
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Published verified-purchase reviews only, content: { application/json: { schema: { $ref: "#/components/schemas/PublicRetailProductReviewList" } } } }
  /customer/retail-products/{productId}/review-context:
    get:
      operationId: getCustomerRetailProductReviewContext
      tags: [customer]
      summary: Get current-account verified-purchase eligibility and own review
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Eligibility and private own-review context, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewContext" } } } }
  /customer/retail-products/{productId}/reviews:
    post:
      operationId: createCustomerRetailProductReview
      tags: [customer]
      summary: Create one verified-purchase review for an authenticated active CUSTOMER or JOBSEEKER
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewInput" } } } }
      responses:
        "201": { description: Retail review created, content: { application/json: { schema: { $ref: "#/components/schemas/OwnRetailProductReview" } } } }
        "403": { description: No qualifying account-owned delivered retail order item }
        "409": { description: An active review already exists for user and product }
  /customer/retail-products/{productId}/reviews/{reviewId}:
    patch:
      operationId: updateCustomerRetailProductReview
      tags: [customer]
      summary: Update own verified-purchase review without bypassing moderation
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
        - { name: reviewId, in: path, required: true, schema: { type: string } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewInput" } } } }
      responses:
        "200": { description: Updated review, content: { application/json: { schema: { $ref: "#/components/schemas/OwnRetailProductReview" } } } }
    delete:
      operationId: deleteCustomerRetailProductReview
      tags: [customer]
      summary: Remove own B2C review
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
        - { name: reviewId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Review removed }
  /retail-product-reviews/{reviewId}/reports:
    post:
      operationId: reportRetailProductReview
      tags: [customer]
      summary: Create or update one authenticated reporter's moderation report
      parameters: [{ name: reviewId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewReportInput" } } } }
      responses:
        "201": { description: Report recorded, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewReportResult" } } } }
        "404": { description: Review is missing or removed }
  /admin/retail-product-reviews:
    get:
      operationId: adminListRetailProductReviews
      tags: [admin]
      summary: List B2C review moderation queue tabs with filters and pagination
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [REPORTED, AUTO_FLAGGED, PUBLISHED, REMOVED, ALL], default: ALL } }
        - { name: supplierId, in: query, schema: { type: string } }
        - { name: productId, in: query, schema: { type: string } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: page, in: query, schema: { type: integer, minimum: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100 } }
      responses:
        "200": { description: Moderation queue, content: { application/json: { schema: { $ref: "#/components/schemas/AdminRetailProductReviewList" } } } }
  /admin/retail-product-reviews/{reviewId}:
    get:
      operationId: adminGetRetailProductReview
      tags: [admin]
      summary: Get review moderation detail, verified badge, reports and audit history
      parameters: [{ name: reviewId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Moderation detail, content: { application/json: { schema: { $ref: "#/components/schemas/AdminRetailProductReviewDetail" } } } }
  /admin/retail-product-reviews/{reviewId}/moderation:
    post:
      operationId: adminModerateRetailProductReview
      tags: [admin]
      summary: Keep, dismiss reports, remove, or restore a review atomically
      parameters: [{ name: reviewId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewModerationInput" } } } }
      responses:
        "200": { description: Moderation action applied, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductReviewModerationResult" } } } }

  /beauty-jobs/categories:
    get:
      operationId: listBeautyJobCategories
      tags: [beauty-jobs]
      responses:
        "200": { description: Categories, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobCategoriesResponse" } } } }
  /beauty-jobs:
    get:
      operationId: listBeautyJobs
      tags: [beauty-jobs]
      parameters:
        - { name: category, in: query, schema: { type: string } }
        - { name: type, in: query, schema: { type: string, enum: [job, equipment_rental, space_rental, freelance] } }
        - { name: intent, in: query, schema: { type: string, enum: [offering, seeking] } }
        - { name: listingMode, in: query, description: Derived marketplace intent; legacy seeking remains a broad backward-compatible alias., schema: { type: string, enum: [offering, rental, seeking, seeking_work, seeking_rental] } }
        - { name: city, in: query, schema: { type: string } }
        - { name: region, in: query, schema: { type: string } }
        - { name: minPrice, in: query, schema: { type: integer, minimum: 0 } }
        - { name: maxPrice, in: query, schema: { type: integer, minimum: 0 } }
        - { name: availability, in: query, schema: { type: string } }
        - { name: query, in: query, schema: { type: string } }
        - { name: latitude, in: query, schema: { type: number } }
        - { name: longitude, in: query, schema: { type: number } }
        - { name: sort, in: query, schema: { type: string, enum: [newest, oldest, price_asc, price_desc, nearest], default: newest } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses:
        "200": { description: Listing page, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListResponse" } } } }
    post:
      operationId: createBeautyJob
      tags: [beauty-jobs]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobCreateInput" } } } }
      responses:
        "201": { description: Created listing, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } }
  /beauty-jobs/mine:
    get:
      operationId: listMyBeautyJobs
      tags: [beauty-jobs]
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [pending, active, rejected, expiring, expired, filled] } }
        - { name: type, in: query, description: Grouped owner listing type., schema: { type: string, enum: [job, rental, freelance] } }
        - { name: listingMode, in: query, schema: { type: string, enum: [offering, rental, seeking, seeking_work, seeking_rental] } }
        - { name: category, in: query, schema: { type: string } }
        - { name: posted, in: query, schema: { type: string, enum: [today, week, month, custom, all], default: all } }
        - { name: from, in: query, description: Strict YYYY-MM-DD; required with custom., schema: { type: string, format: date } }
        - { name: to, in: query, description: Strict YYYY-MM-DD; required with custom., schema: { type: string, format: date } }
        - { name: query, in: query, description: Title-only search., schema: { type: string, maxLength: 200 } }
        - { name: sort, in: query, schema: { type: string, enum: [oldest, newest, activity], default: newest } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses:
        "200": { description: Owned listings, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListResponse" } } } }
  /beauty-jobs/{listingId}/applicants:
    get:
      operationId: listBeautyJobApplicants
      tags: [beauty-jobs]
      parameters:
        - { name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
      responses:
        "200": { description: Employment applicants for an owned job listing, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobApplicantsResponse" } } } }
        "403": { description: Active salon membership required }
        "409": { description: Listing is not an owned employment listing }
  /beauty-jobs/{listingId}/applicants/decision:
    post:
      operationId: decideBeautyJobApplicants
      tags: [beauty-jobs]
      parameters:
        - { name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobApplicantDecisionInput" } } }
      responses:
        "200": { description: Updated employment applications, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobApplicantsResponse" } } } }
        "409": { description: "Stale, foreign, or non-job application state; no contacts were changed" }
  /beauty-jobs/saved:
    get:
      operationId: listSavedBeautyJobs
      tags: [beauty-jobs]
      responses:
        "200": { description: Saved listings, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListResponse" } } } }
  /beauty-jobs/notifications:
    get:
      operationId: listBeautyJobNotifications
      tags: [beauty-jobs]
      responses:
        "200": { description: Notifications, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobNotificationsResponse" } } } }
  /beauty-jobs/notifications/{notificationId}/read:
    post:
      operationId: markBeautyJobNotificationRead
      tags: [beauty-jobs]
      parameters: [{ name: notificationId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses:
        "200": { description: Notification, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobNotification" } } } }
  /beauty-jobs/rental-requests/mine:
    get:
      operationId: listMyBeautyJobRentalRequests
      tags: [beauty-jobs]
      responses:
        "200": { description: Requests sent by the authenticated user, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequestsResponse" } } } }
  /beauty-jobs/rental-requests/inbox:
    get:
      operationId: listBeautyJobRentalRequestInbox
      tags: [beauty-jobs]
      responses:
        "200": { description: Rental requests received by the authenticated listing author, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequestsResponse" } } } }
  /beauty-jobs/rental-requests/{requestId}:
    patch:
      operationId: respondToBeautyJobRentalRequest
      tags: [beauty-jobs]
      parameters: [{ name: requestId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequestUpdateInput" } } } }
      responses:
        "200": { description: Updated request, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequest" } } } }
  /beauty-jobs/{listingId}:
    get:
      operationId: getBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses:
        "200": { description: Listing, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } }
    patch:
      operationId: updateBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobUpdateInput" } } } }
      responses:
        "200": { description: Updated listing, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } }
  /beauty-jobs/{listingId}/renew:
    post:
      operationId: renewBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses: { "200": { description: Renewed, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } } }
  /beauty-jobs/{listingId}/close:
    post:
      operationId: closeBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses: { "200": { description: Closed, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } } }
  /beauty-jobs/{listingId}/save:
    post:
      operationId: toggleSavedBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses: { "200": { description: Saved state, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobSaveResult" } } } } }
  /beauty-jobs/{listingId}/contact:
    post:
      operationId: contactBeautyJobAuthor
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobContactCreateInput" } } } }
      responses: { "201": { description: Contact, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobContact" } } } } }
  /beauty-jobs/{listingId}/rental-requests:
    post:
      operationId: createBeautyJobRentalRequest
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequestCreateInput" } } } }
      responses:
        "201": { description: Created request, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRentalRequest" } } } }
  /beauty-jobs/{listingId}/report:
    post:
      operationId: reportBeautyJob
      tags: [beauty-jobs]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobReportCreateInput" } } } }
      responses: { "201": { description: Reported, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobReport" } } } } }
  /beauty-jobs/inbox:
    get:
      operationId: listBeautyJobInbox
      tags: [beauty-jobs]
      responses: { "200": { description: Contacts, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobContactsResponse" } } } } }
  /beauty-jobs/contacts/{contactId}:
    patch:
      operationId: replyToBeautyJobContact
      tags: [beauty-jobs]
      parameters: [{ name: contactId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobContactUpdateInput" } } } }
      responses: { "200": { description: Contact, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobContact" } } } } }
  /admin/beauty-jobs/settings:
    get:
      operationId: getBeautyJobSettings
      tags: [admin]
      responses: { "200": { description: Settings, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobSettings" } } } } }
    patch:
      operationId: updateBeautyJobSettings
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobSettingsUpdateInput" } } } }
      responses: { "200": { description: Settings, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobSettings" } } } } }
  /admin/beauty-jobs/expiry-sweep:
    post:
      operationId: sweepExpiredBeautyJobs
      tags: [admin]
      responses: { "200": { description: Sweep result, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobSweepResult" } } } } }
  /admin/beauty-jobs/email-deliveries:
    get:
      operationId: getBeautyJobDeliveryIssues
      tags: [admin]
      summary: List delayed or terminal Beauty Poslovi email deliveries without recipient data
      responses:
        "200":
          description: Privacy-safe Beauty Poslovi delivery issues
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BeautyJobDeliveryIssues" }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /admin/beauty-jobs/email-deliveries/{deliveryId}/retry:
    post:
      operationId: retryBeautyJobDelivery
      tags: [admin]
      summary: Retry one terminal Beauty Poslovi email with a retryable failure
      parameters:
        - name: deliveryId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
      responses:
        "200":
          description: Retry attempted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BeautyJobDeliveryRetryResult" }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Delivery not found }
        "409": { description: Delivery is not retryable }
  /admin/beauty-jobs/queue:
    get:
      operationId: getBeautyJobModerationQueue
      tags: [admin]
      summary: Search and paginate authorized Beauty Poslovi moderation listings
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [pending, active, rejected, expiring, expired, filled], default: pending } }
        - { name: type, in: query, schema: { type: string, enum: [job, equipment_rental, space_rental, freelance] } }
        - { name: listingMode, in: query, description: Derived marketplace intent; legacy seeking remains a broad backward-compatible alias., schema: { type: string, enum: [offering, rental, seeking, seeking_work, seeking_rental] } }
        - { name: category, in: query, schema: { type: string } }
        - { name: postedBy, in: query, schema: { type: string, enum: [salon, user] } }
        - { name: period, in: query, schema: { type: string, enum: [today, week, month, custom, all], default: all } }
        - { name: from, in: query, schema: { type: string, format: date } }
        - { name: to, in: query, schema: { type: string, format: date } }
        - { name: search, in: query, schema: { type: string } }
        - { name: reportedOnly, in: query, schema: { type: boolean } }
        - { name: sort, in: query, schema: { type: string, enum: [oldest, newest, activity], default: oldest } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses: { "200": { description: Queue, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobModerationQueue" } } } } }
  /admin/beauty-jobs/bulk-moderation:
    post:
      operationId: bulkModerateBeautyJobs
      tags: [admin]
      summary: Atomically moderate up to 100 Beauty Poslovi listings
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobBulkModerationInput" } } } }
      responses:
        "200": { description: Per-listing moderation results, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobBulkModerationResult" } } } }
        "400": { description: Invalid bulk request }
        "409": { description: One or more listings cannot be moderated from their current lifecycle state }
  /admin/beauty-jobs/rejected:
    get:
      operationId: listRejectedBeautyJobs
      tags: [admin]
      summary: List rejected Beauty Poslovi listings for moderation review
      parameters:
        - { name: period, in: query, schema: { type: string, enum: [week, month, last_30_days, custom, all], default: month } }
        - { name: from, in: query, schema: { type: string, format: date } }
        - { name: to, in: query, schema: { type: string, format: date } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 24 } }
      responses:
        "200": { description: Rejected listing page, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobRejectedListResponse" } } } }
  /admin/beauty-jobs/{listingId}/preview:
    get:
      operationId: getBeautyJobAdminPreview
      tags: [admin]
      summary: Preview a Beauty Poslovi listing regardless of public moderation status
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      responses:
        "200": { description: Private administrator preview with append-only moderation history, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobAdminPreview" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Listing not found }
  /admin/beauty-jobs/{listingId}/moderation:
    post:
      operationId: moderateBeautyJob
      tags: [admin]
      parameters: [{ name: listingId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobModerationInput" } } } }
      responses: { "200": { description: Moderated listing, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobListing" } } } } }
  /admin/beauty-jobs/reports/{reportId}/resolve:
    post:
      operationId: resolveBeautyJobReport
      tags: [admin]
      parameters: [{ name: reportId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobReportResolveInput" } } } }
      responses: { "200": { description: Resolved report, content: { application/json: { schema: { $ref: "#/components/schemas/BeautyJobReport" } } } } }
  /referrals/validate/{code}:
    get:
      operationId: validateReferralCode
      tags: [auth]
      summary: Validate a public referral code without disclosing referrer identity
      parameters:
        - name: code
          in: path
          required: true
          schema: { type: string, minLength: 3, maxLength: 64 }
      responses:
        "200": { description: Valid referral code, content: { application/json: { schema: { $ref: "#/components/schemas/ReferralCodeValidation" } } } }
        "404": { description: Unknown or inactive referral code }
  /referrals/dashboard:
    get:
      operationId: getReferralDashboard
      tags: [customer]
      summary: Get the signed-in user's tenant-safe referral dashboard
      responses:
        "200": { description: "Referral summary, channels, history and ledger", content: { application/json: { schema: { $ref: "#/components/schemas/ReferralDashboard" } } } }
        "401": { description: Authentication required }
  /admin/referrals/approvals:
    get:
      operationId: adminListReferralApprovals
      tags: [admin]
      summary: List pending referred businesses awaiting approval
      responses:
        "200": { description: Referral approval queue, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ReferralApproval" } } } } }
  /admin/referrals/approvals/{attributionId}:
    patch:
      operationId: adminDecideReferralApproval
      tags: [admin]
      summary: Approve, reject, or resubmit a referred salon or education center
      parameters:
        - name: attributionId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReferralApprovalDecision" }
      responses:
        "200": { description: Recorded approval decision, content: { application/json: { schema: { $ref: "#/components/schemas/ReferralApprovalDecisionResult" } } } }
        "404": { description: Referral business approval not found }
  /admin/referrals/reviews:
    get:
      operationId: adminListReferralReviews
      tags: [admin]
      summary: List suspicious referral records
      responses:
        "200": { description: Referral fraud queue, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ReferralReview" } } } } }
  /admin/referrals/reviews/{reviewId}:
    patch:
      operationId: adminReviewReferral
      tags: [admin]
      summary: Resolve a suspicious referral review
      parameters:
        - name: reviewId
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReferralReviewUpdate" }
      responses:
        "200": { description: Resolved review, content: { application/json: { schema: { $ref: "#/components/schemas/ReferralReview" } } } }
        "404": { description: Referral review not found }
        "409": { description: Review was already resolved or the requested decision conflicts with terminal referral lifecycle }
  /customer/retail-subscriptions:
    get:
      operationId: listRetailProductSubscriptions
      tags: [customer]
      summary: List the signed-in customer's physical-product replenishment subscriptions
      responses:
        "200": { description: Subscriptions, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/RetailProductSubscription" } } } } }
        "401": { description: Authentication required }
    post:
      operationId: createRetailProductSubscription
      tags: [customer]
      summary: Create a recurring physical-product order
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscriptionInput" } } } }
      responses:
        "201": { description: Created subscription, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscription" } } } }
        "400": { description: "Invalid payment, delivery, or contact snapshot" }
        "409": { description: Product is unavailable or quantity violates MOQ/stock }
  /customer/retail-subscriptions/{subscriptionId}:
    get:
      operationId: getRetailProductSubscription
      tags: [customer]
      summary: Get one owned physical-product replenishment subscription
      parameters: [{ name: subscriptionId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Subscription, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscription" } } } }
        "404": { description: Not found }
  /customer/retail-subscriptions/{subscriptionId}/pause:
    post:
      operationId: pauseRetailProductSubscription
      tags: [customer]
      summary: Pause an owned subscription idempotently
      parameters: [{ name: subscriptionId, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: Subscription, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscription" } } } } }
  /customer/retail-subscriptions/{subscriptionId}/resume:
    post:
      operationId: resumeRetailProductSubscription
      tags: [customer]
      summary: Resume an owned paused subscription idempotently
      parameters: [{ name: subscriptionId, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: Subscription, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscription" } } } } }
  /customer/retail-subscriptions/{subscriptionId}/cancel:
    post:
      operationId: cancelRetailProductSubscription
      tags: [customer]
      summary: Cancel an owned subscription idempotently
      parameters: [{ name: subscriptionId, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: Subscription, content: { application/json: { schema: { $ref: "#/components/schemas/RetailProductSubscription" } } } } }
  /catalog/feed:
    get:
      operationId: getCatalogFeed
      tags: [shop]
      summary: Provider-neutral public catalog feed using canonical product URLs
      responses:
        "200":
          description: Eligible B2C catalog items (price-on-request products excluded)
          content:
            application/json:
              schema:
                type: object
                required: [generatedAt, items]
                properties:
                  generatedAt: { type: string, format: date-time }
                  items:
                    type: array
                    items:
                      type: object
                      required: [id, title, supplier, url, price, currency, availability, images]
                      properties:
                        id: { type: string }
                        title: { type: string }
                        supplier: { type: string }
                        url: { type: string }
                        price: { type: [integer, "null"] }
                        currency: { type: string, enum: [RSD] }
                        availability: { type: string, enum: [in_stock] }
                        images: { type: array, items: { type: string } }
        "503": { description: Canonical public origin is not configured safely }
  /public/suppliers/{supplierId}/products/{productId}/price-inquiries:
    post:
      operationId: createPriceInquiry
      tags: [shop]
      summary: Create an inquiry in an exact supplier and price-on-request product scope
      parameters:
        - { name: supplierId, in: path, required: true, schema: { type: string } }
        - { name: productId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, email, phone, message]
              properties:
                name: { type: string, minLength: 2, maxLength: 120 }
                email: { type: string, maxLength: 254 }
                phone: { type: string, minLength: 6, maxLength: 40 }
                message: { type: string, minLength: 10, maxLength: 2000 }
      responses:
        "201":
          description: Inquiry accepted
          content:
            application/json:
              schema:
                type: object
                required: [id, status, createdAt]
                properties:
                  id: { type: string }
                  status: { type: string, enum: [NEW] }
                  createdAt: { type: string, format: date-time }
        "400": { description: Invalid contact data }
        "404": { description: Supplier/product scope not found }
        "409": { description: Product is currently directly purchasable }
  /shop/quotes:
    post:
      operationId: createShopQuote
      tags: [shop]
      summary: Snapshot the authenticated salon cart without creating an order
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                customerCompanyName: { type: string, maxLength: 200 }
                validityDays: { type: integer, minimum: 1, maximum: 90 }
      responses:
        "201": { description: Immutable quote snapshot, content: { application/json: { schema: { $ref: "#/components/schemas/B2bQuote" } } } }
        "401": { description: Authentication required }
        "403": { description: Salon owner access required }
  /public/products/{productId}/bulk-matrix:
    get:
      operationId: getPublicBulkMatrix
      tags: [shop]
      summary: Get public variant availability, promoted swatches and optional tier-price preview
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Bulk matrix
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BulkMatrixResponse" }
        "404": { description: Product or matrix unavailable }
  /shop/cart/bulk-matrix:
    post:
      operationId: addShopBulkMatrix
      tags: [shop]
      summary: Validate and add all variant rows atomically
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rows]
              properties:
                rows:
                  type: array
                  minItems: 1
                  maxItems: 200
                  items:
                    type: object
                    required: [productId, variantValue, quantity]
                    properties:
                      productId: { type: string }
                      variantValue: { type: string, minLength: 1, maxLength: 200 }
                      quantity: { type: integer, minimum: 1, maximum: 10000 }
      responses:
        "200":
          description: Every row added
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BulkMatrixAddResponse" }
        "409": { description: No rows added because one row was invalid or unavailable }
  /shop/quotes/{publicId}:
    get:
      operationId: getShopQuote
      tags: [shop]
      summary: Get an owned quote from immutable snapshots
      parameters:
        - { name: publicId, in: path, required: true, schema: { type: string, minLength: 20, maxLength: 80 } }
      responses:
        "200": { description: Quote snapshot, content: { application/json: { schema: { $ref: "#/components/schemas/B2bQuote" } } } }
        "404": { description: Quote absent or not owned by active salon }
  /shop/quotes/{publicId}/restore-cart:
    post:
      operationId: restoreShopQuoteCart
      tags: [shop]
      summary: Atomically replace the owned salon cart with quote snapshot lines
      parameters:
        - { name: publicId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Cart replaced }
        "404": { description: Quote absent or not owned by active salon }
  /shop/quotes/{publicId}/pdf:
    get:
      operationId: getShopQuotePdf
      tags: [shop]
      summary: Render an owned quote PDF only from immutable snapshots
      parameters:
        - { name: publicId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Quote PDF
          content:
            application/pdf:
              schema: { type: string, format: binary }
  /admin/catalog/meta/status:
    get:
      operationId: adminGetMetaCatalogStatus
      tags: [admin]
      summary: Report provider-neutral catalog infrastructure connection status
      responses:
        "200":
          description: Meta remains truthfully disconnected
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MetaCatalogStatus" }
  /admin/catalog/meta/validate:
    post:
      operationId: adminValidateMetaCatalog
      tags: [admin]
      summary: Validate feed configuration without connecting or storing provider credentials
      responses:
        "200":
          description: Validation run with NOT_CONNECTED status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MetaCatalogValidationResponse" }
  /orders/{orderId}/rmas:
    post:
      operationId: createOrderRma
      tags: [shop]
      summary: Create an RMA for an eligible delivered item and atomically claim private photos
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderItemId, quantity, reason, description]
              properties:
                orderItemId: { type: string }
                quantity: { type: integer, minimum: 1 }
                reason: { type: string, minLength: 2, maxLength: 120 }
                description: { type: string, minLength: 10, maxLength: 3000 }
                photoUrls: { type: array, maxItems: 6, items: { type: string } }
      responses:
        "201": { description: RMA received }
        "403": { description: Unsupported role or not owner }
        "409": { description: Item ineligible or media claim conflict }
  /retail/orders/{orderId}/rmas:
    post:
      operationId: createRetailOrderRma
      tags: [shop]
      summary: Create a customer/jobseeker RMA for an owned delivered retail-order item
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderItemId, quantity, reason, description]
              properties:
                orderItemId: { type: string }
                quantity: { type: integer, minimum: 1 }
                reason: { type: string, minLength: 2, maxLength: 120 }
                description: { type: string, minLength: 10, maxLength: 3000 }
                photoUrls: { type: array, maxItems: 6, items: { type: string } }
      responses:
        "201": { description: RMA received }
        "403": { description: Customer/jobseeker access required }
        "404": { description: Owned retail item not found }
  /admin/rmas:
    get:
      operationId: adminListRmas
      tags: [admin]
      summary: List RMAs
      responses:
        "200": { description: RMA list }
  /admin/rmas/{id}:
    get:
      operationId: adminGetRma
      tags: [admin]
      summary: Get RMA, private attachment count and status audit
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: RMA detail }
        "404": { description: RMA not found }
  /admin/rmas/{id}/status:
    patch:
      operationId: adminUpdateRmaStatus
      tags: [admin]
      summary: Change RMA status with durable audit and email outbox event
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [RECEIVED, IN_REVIEW, APPROVED, REJECTED] }
      responses:
        "200": { description: Current RMA and whether a real transition occurred }
  /admin/review-rewards:
    get:
      operationId: adminGetReviewRewardSettings
      tags: [admin]
      summary: Get bounded review reward settings and issuance stats
      responses:
        "200":
          description: Settings and aggregate stats
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReviewRewardSettingsResponse" }
    patch:
      operationId: adminUpdateReviewRewardSettings
      tags: [admin]
      summary: Optimistically update bounded review invitation and reward settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled, invitationDelayDays, percent, validityDays, version]
              properties:
                enabled: { type: boolean }
                invitationDelayDays: { type: integer, minimum: 7, maximum: 10 }
                percent: { type: integer, minimum: 1, maximum: 100 }
                validityDays: { type: integer, minimum: 1, maximum: 365 }
                version: { type: integer, minimum: 1 }
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReviewRewardSettings" }
  /commerce/header-bar:
    get:
      operationId: getCommerceHeaderBar
      tags: [shop]
      summary: Get rotating global commerce header messages
      responses:
        "200": { description: Header bar configuration, content: { application/json: { schema: { $ref: "#/components/schemas/HeaderBarConfig" } } } }
  /admin/commerce-experience:
    get:
      operationId: adminGetCommerceExperience
      tags: [admin]
      responses:
        "200": { description: Commerce experience settings, content: { application/json: { schema: { $ref: "#/components/schemas/CommerceExperienceSettings" } } } }
    put:
      operationId: adminUpdateCommerceExperience
      tags: [admin]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CommerceExperienceSettingsInput" } } }
      responses:
        "200": { description: Updated settings, content: { application/json: { schema: { $ref: "#/components/schemas/CommerceExperienceSettings" } } } }
        "409": { description: Version conflict }
  /commerce/search-suggestions:
    get:
      operationId: getCommerceSearchSuggestions
      tags: [shop]
      parameters:
        - { name: audience, in: query, required: true, schema: { type: string, enum: [B2B, B2C] } }
        - { name: q, in: query, schema: { type: string, maxLength: 100 } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 5, default: 5 } }
      responses:
        "200": { description: Eligible variant-aware product suggestions, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/SearchProductSuggestion" } } } } }
  /commerce/bestsellers:
    get:
      operationId: listCommerceBestsellers
      tags: [shop]
      parameters:
        - { name: audience, in: query, required: true, schema: { type: string, enum: [B2B, B2C] } }
        - { name: periodDays, in: query, schema: { type: integer, enum: [30, 60] } }
        - { name: categoryId, in: query, schema: { type: string } }
        - { name: supplierSlug, in: query, schema: { type: string, minLength: 1, maxLength: 120 } }
      responses:
        "200": { description: Top ten eligible products by completed quantity, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/BestsellerRanking" } } } } }
  /admin/commerce/bestsellers:
    get:
      operationId: adminListCommerceBestsellers
      tags: [admin]
      parameters:
        - { name: audience, in: query, required: true, schema: { type: string, enum: [B2B, B2C] } }
        - { name: periodDays, in: query, schema: { type: integer, enum: [30, 60] } }
        - { name: categoryId, in: query, schema: { type: string } }
        - { name: supplierSlug, in: query, schema: { type: string, minLength: 1, maxLength: 120 } }
      responses:
        "200": { description: Administrative top ten ranking, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/BestsellerRanking" } } } } }
  /products/{productId}/documents:
    get:
      operationId: listProductDocuments
      tags: [shop]
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
        - { name: audience, in: query, required: true, schema: { type: string, enum: [B2B, B2C] } }
      responses:
        "200": { description: Public eligible product documents, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ProductDocument" } } } } }
        "404": { description: Product not eligible }
  /admin/products/{productId}/documents:
    get:
      operationId: adminListProductDocuments
      tags: [admin]
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Administrative product documents, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ProductDocument" } } } } }
    post:
      operationId: adminAttachProductDocument
      tags: [admin]
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProductDocumentInput" }
      responses:
        "200": { description: Document attached, content: { application/json: { schema: { $ref: "#/components/schemas/ProductDocument" } } } }
    put:
      operationId: adminReorderProductDocuments
      tags: [admin]
      parameters:
        - { name: productId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProductDocumentOrderInput" }
      responses:
        "204": { description: Reordered }
  /admin/products/documents/{documentId}:
    delete:
      operationId: adminDeleteProductDocument
      tags: [admin]
      parameters:
        - { name: documentId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Deleted }
  /admin/products/{productId}/upsells:
    put:
      operationId: adminReplaceProductUpsells
      tags: [admin]
      summary: Replace the ordered upsell products for a product
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpsellProductInput" } } } }
      responses:
        "200": { description: Saved upsell links, content: { application/json: { schema: { $ref: "#/components/schemas/UpsellProductInput" } } } }
        "400": { description: Invalid or unavailable product links }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /public/products/{productId}/upsells:
    get:
      operationId: getPublicProductUpsells
      tags: [shop]
      summary: List public upsell products
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Public upsell products, content: { application/json: { schema: { $ref: "#/components/schemas/UpsellProductList" } } } }
        "400": { description: Invalid product }
  /admin/loyalty-pricing-tiers:
    get:
      operationId: adminListLoyaltyPricingTiers
      tags: [admin]
      responses:
        "200": { description: Loyalty pricing tiers, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/PricingLoyaltyTier" } } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
    post:
      operationId: adminCreateLoyaltyPricingTier
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PricingLoyaltyTierInput" } } } }
      responses:
        "201": { description: Created loyalty tier, content: { application/json: { schema: { $ref: "#/components/schemas/PricingLoyaltyTier" } } } }
        "400": { description: Invalid or duplicate tier }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /admin/loyalty-pricing-tiers/{id}:
    patch:
      operationId: adminUpdateLoyaltyPricingTier
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PricingLoyaltyTierUpdate" } } } }
      responses:
        "200": { description: Updated loyalty tier, content: { application/json: { schema: { $ref: "#/components/schemas/PricingLoyaltyTier" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Tier not found }
        "409": { description: Version or threshold conflict }
    delete:
      operationId: adminDeleteLoyaltyPricingTier
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: version, in: query, required: true, schema: { type: integer, minimum: 1 } }]
      responses:
        "204": { description: Deleted }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "409": { description: Tier not found or changed }
  /customer/loyalty-pricing:
    get:
      operationId: getCustomerLoyaltyPricing
      tags: [customer, loyalty]
      responses:
        "200": { description: Customer loyalty progress, content: { application/json: { schema: { $ref: "#/components/schemas/LoyaltyProgress" } } } }
        "401": { description: Authentication required }
        "403": { description: Customer or salon-owner access required }
  /admin/bulk-sale-campaigns:
    get:
      operationId: adminListBulkSaleCampaigns
      tags: [admin]
      responses:
        "200": { description: Bulk sale campaigns, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/BulkSaleCampaign" } } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
    post:
      operationId: adminCreateBulkSaleCampaign
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BulkSaleCampaignInput" } } } }
      responses:
        "201": { description: Created campaign, content: { application/json: { schema: { $ref: "#/components/schemas/BulkSaleCampaign" } } } }
        "400": { description: Invalid campaign or targets }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
  /admin/bulk-sale-campaigns/{id}:
    patch:
      operationId: adminUpdateBulkSaleCampaign
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BulkSaleCampaignUpdate" } } } }
      responses:
        "200": { description: Updated campaign, content: { application/json: { schema: { $ref: "#/components/schemas/BulkSaleCampaign" } } } }
        "401": { description: Authentication required }
        "403": { description: Administrator access required }
        "404": { description: Campaign not found }
        "409": { description: Version conflict or invalid campaign }
    delete:
      operationId: adminDeleteBulkSaleCampaign
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: version, in: query, required: true, schema: { type: integer, minimum: 1 } }]
      responses: { "204": { description: Deleted }, "401": { description: Authentication required }, "403": { description: Administrator access required }, "409": { description: Campaign not found or changed } }
  /admin/cart-threshold-rewards:
    get:
      operationId: adminListCartThresholdRewards
      tags: [admin]
      responses: { "200": { description: Cart threshold rewards, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/CartThresholdReward" } } } } }, "401": { description: Authentication required }, "403": { description: Administrator access required } }
    post:
      operationId: adminCreateCartThresholdReward
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CartThresholdRewardInput" } } } }
      responses: { "201": { description: Created reward, content: { application/json: { schema: { $ref: "#/components/schemas/CartThresholdReward" } } } }, "400": { description: Invalid or duplicate reward }, "401": { description: Authentication required }, "403": { description: Administrator access required } }
  /admin/cart-threshold-rewards/{id}:
    patch:
      operationId: adminUpdateCartThresholdReward
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CartThresholdRewardUpdate" } } } }
      responses: { "200": { description: Updated reward, content: { application/json: { schema: { $ref: "#/components/schemas/CartThresholdReward" } } } }, "401": { description: Authentication required }, "403": { description: Administrator access required }, "404": { description: Reward not found }, "409": { description: Version conflict or invalid reward } }
    delete:
      operationId: adminDeleteCartThresholdReward
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: version, in: query, required: true, schema: { type: integer, minimum: 1 } }]
      responses: { "204": { description: Deleted }, "401": { description: Authentication required }, "403": { description: Administrator access required }, "409": { description: Reward not found or changed } }
  /customer/cart-threshold-rewards:
    get:
      operationId: getCustomerCartThresholdRewards
      tags: [customer, loyalty]
      parameters: [{ name: subtotalRsd, in: query, required: true, schema: { type: integer, minimum: 0 } }]
      responses: { "200": { description: Reached and next cart rewards, content: { application/json: { schema: { $ref: "#/components/schemas/CartThresholdRewardProgress" } } } }, "400": { description: Invalid subtotal }, "401": { description: Authentication required }, "403": { description: Customer or salon-owner access required } }
  /admin/automatic-xy-promotions:
    get:
      operationId: adminListAutomaticXyPromotions
      tags: [admin]
      responses: { "200": { description: Automatic X plus Y promotions, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/AutomaticXyPromotion" } } } } }, "401": { description: Authentication required }, "403": { description: Administrator access required } }
    post:
      operationId: adminCreateAutomaticXyPromotion
      tags: [admin]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AutomaticXyPromotionInput" } } } }
      responses: { "201": { description: Created promotion, content: { application/json: { schema: { $ref: "#/components/schemas/AutomaticXyPromotion" } } } }, "400": { description: Invalid promotion or targets }, "401": { description: Authentication required }, "403": { description: Administrator access required } }
  /admin/automatic-xy-promotions/{id}:
    patch:
      operationId: adminUpdateAutomaticXyPromotion
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AutomaticXyPromotionUpdate" } } } }
      responses: { "200": { description: Updated promotion, content: { application/json: { schema: { $ref: "#/components/schemas/AutomaticXyPromotion" } } } }, "401": { description: Authentication required }, "403": { description: Administrator access required }, "404": { description: Promotion not found }, "409": { description: Version conflict or invalid promotion } }
    delete:
      operationId: adminDeleteAutomaticXyPromotion
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: version, in: query, required: true, schema: { type: integer, minimum: 1 } }]
      responses: { "204": { description: Deleted }, "401": { description: Authentication required }, "403": { description: Administrator access required }, "409": { description: Promotion not found or changed } }
  /public/products/{productId}/automatic-xy-promotions:
    get:
      operationId: getPublicProductAutomaticXyPromotions
      tags: [shop]
      parameters: [{ name: productId, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: Active product promotion descriptors, content: { application/json: { schema: { $ref: "#/components/schemas/AutomaticXyPromotionList" } } } }, "400": { description: Invalid product }, "404": { description: Product not found } }
components:
  responses:
    BookingCapacityReached:
      description: Booking admission capacity is full; retry after the indicated delay.
      headers:
        Retry-After:
          description: Suggested delay in seconds before retrying the booking.
          schema: { type: integer, minimum: 1 }
      content:
        application/json:
          schema: { $ref: "#/components/schemas/BookingCapacityError" }
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: Client-generated command identifier; reuse it only to retry the identical booking payload.
      schema: { type: string, minLength: 1, maxLength: 200 }
    CityQuery:
      name: city
      in: query
      schema: { type: string }
    CategoryQuery:
      name: category
      in: query
      schema: { type: string }
    TreatmentQuery:
      name: treatment
      in: query
      schema: { type: string }
    SortQuery:
      name: sort
      in: query
      schema: { type: string, enum: [recommended, top-rated, cheapest, largest-discount, nearest, first-available, most-popular, most-booked-recently, newest], default: recommended }
    SalonSlug:
      name: slug
      in: path
      required: true
      schema: { type: string }
    SalonId:
      name: salonId
      in: path
      required: true
      schema: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
    AppointmentId:
      name: appointmentId
      in: path
      required: true
      schema: { type: string }
    CourseId:
      name: courseId
      in: path
      required: true
      schema: { type: string }
    ModuleId:
      name: moduleId
      in: path
      required: true
      schema: { type: string }
    LessonId:
      name: lessonId
      in: path
      required: true
      schema: { type: string }
    EnrollmentId:
      name: enrollmentId
      in: path
      required: true
      schema: { type: string }
    EducationCenterId:
      name: centerId
      in: path
      required: true
      schema: { type: string }
    EducationDisputeId:
      name: disputeId
      in: path
      required: true
      schema: { type: string }
    SessionId:
      name: sessionId
      in: path
      required: true
      schema: { type: string }
    WaitlistId:
      name: waitlistId
      in: path
      required: true
      schema: { type: string }
  schemas:
    CatalogSyncRun:
      type: object
      required: [id, provider, status, itemCount, validationErrors, createdAt]
      properties:
        id: { type: string }
        provider: { type: string }
        status: { type: string }
        itemCount: { type: integer, minimum: 0 }
        validationErrors: { type: array, items: { type: string } }
        requestedByUserId: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
    MetaCatalogStatus:
      type: object
      required: [connectionStatus, canSync, latestRun]
      properties:
        connectionStatus: { type: string, enum: [NOT_CONNECTED] }
        canSync: { type: boolean }
        latestRun:
          anyOf:
            - { $ref: "#/components/schemas/CatalogSyncRun" }
            - { type: "null" }
    MetaCatalogValidationResponse:
      type: object
      required: [connectionStatus, canSync, run]
      properties:
        connectionStatus: { type: string, enum: [NOT_CONNECTED] }
        canSync: { type: boolean }
        run: { $ref: "#/components/schemas/CatalogSyncRun" }
    ReviewRewardSettings:
      type: object
      required: [enabled, invitationDelayDays, percent, validityDays, version]
      properties:
        enabled: { type: boolean }
        invitationDelayDays: { type: integer, minimum: 7, maximum: 10 }
        percent: { type: integer, minimum: 1, maximum: 100 }
        validityDays: { type: integer, minimum: 1, maximum: 365 }
        version: { type: integer, minimum: 1 }
    ReviewRewardSettingsResponse:
      type: object
      required: [settings, stats]
      properties:
        settings: { $ref: "#/components/schemas/ReviewRewardSettings" }
        stats:
          type: object
          required: [issued]
          properties:
            issued: { type: integer, minimum: 0 }
    BulkMatrixTierPrice:
      type: object
      required: [minQuantity, maxQuantity, unitPrice]
      properties:
        minQuantity: { type: integer, minimum: 1 }
        maxQuantity: { type: [integer, "null"], minimum: 1 }
        unitPrice: { type: integer, minimum: 0 }
    BulkMatrixSwatch:
      oneOf:
        - type: object
          required: [kind, hex]
          properties:
            kind: { type: string, enum: [COLOR] }
            hex: { type: string, pattern: "^#[0-9A-F]{6}$" }
        - type: object
          required: [kind, imageUrl]
          properties:
            kind: { type: string, enum: [IMAGE] }
            imageUrl: { type: string }
        - type: object
          required: [kind, text]
          properties:
            kind: { type: string, enum: [TEXT] }
            text: { type: string }
    BulkMatrixRow:
      type: object
      required: [value, label, sku, available, stock, swatch, mainImageUrl, altText, sortOrder]
      properties:
        value: { type: string }
        label: { type: string }
        sku: { type: [string, "null"] }
        available: { type: boolean }
        stock:
          type: integer
          minimum: 0
          description: Effective available quantity; shared-stock variants expose current product stock.
        swatch:
          anyOf:
            - { $ref: "#/components/schemas/BulkMatrixSwatch" }
            - { type: "null" }
        mainImageUrl: { type: [string, "null"] }
        altText: { type: [string, "null"] }
        sortOrder: { type: integer }
        unitPrice:
          type: integer
          minimum: 0
          description: Omitted when priceOnRequest is true.
        tierPricePreview:
          type: array
          items: { $ref: "#/components/schemas/BulkMatrixTierPrice" }
          description: Omitted when priceOnRequest is true.
    BulkMatrixResponse:
      type: object
      required: [productId, priceOnRequest, cartEligible, rows]
      properties:
        productId: { type: string }
        priceOnRequest: { type: boolean }
        cartEligible: { type: boolean }
        rows:
          type: array
          items: { $ref: "#/components/schemas/BulkMatrixRow" }
    BulkMatrixAddResponse:
      type: object
      required: [cartId, addedRows]
      properties:
        cartId: { type: string }
        addedRows: { type: integer, minimum: 0 }
    B2bQuoteRecipientSnapshot:
      type: object
      required: [companyName]
      properties:
        companyName: { type: string }
        registeredCompanyName: { type: string }
        taxId: { type: string }
        registrationNumber: { type: string }
        address: { type: string }
        city: { type: string }
        postalCode: { type: string }
        email: { type: string }
        phone: { type: string }
    B2bQuoteSellerSnapshot:
      type: object
      required: [companyName]
      properties:
        companyName: { type: string }
        taxId: { type: string }
        registrationNumber: { type: string }
        address: { type: string }
        city: { type: string }
        postalCode: { type: string }
        bankAccount: { type: string }
        email: { type: string }
        phone: { type: string }
        recipient: { $ref: "#/components/schemas/B2bQuoteRecipientSnapshot" }
    B2bQuoteItemSnapshot:
      type: object
      required: [productId, bundleId, productName, productImageUrl, variantValue, variantLabel, productSku, unitPrice, quantity, lineTotal]
      properties:
        productId: { type: [string, "null"] }
        bundleId: { type: [string, "null"] }
        productName: { type: string }
        productImageUrl: { type: string }
        variantValue: { type: [string, "null"] }
        variantLabel: { type: [string, "null"] }
        productSku: { type: [string, "null"] }
        unitPrice: { type: integer }
        quantity: { type: integer, minimum: 1 }
        lineTotal: { type: integer }
    B2bQuote:
      type: object
      required: [id, publicId, salonId, sellerSnapshot, itemSnapshots, subtotalWithoutVat, vatAmount, totalWithVat, currency, validUntil, createdAt]
      properties:
        id: { type: string }
        publicId: { type: string }
        salonId: { type: string }
        sourceCartId: { type: [string, "null"] }
        customerCompanyName: { type: [string, "null"] }
        sellerSnapshot: { $ref: "#/components/schemas/B2bQuoteSellerSnapshot" }
        itemSnapshots:
          type: array
          items: { $ref: "#/components/schemas/B2bQuoteItemSnapshot" }
        subtotalWithoutVat: { type: integer, minimum: 0 }
        vatAmount: { type: integer, minimum: 0 }
        totalWithVat: { type: integer, minimum: 0 }
        currency: { type: string, enum: [RSD] }
        validUntil: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
    RetailProductSubscription:
      type: object
      required: [id, productId, quantity, frequency, status, discountPercent, paymentMethod, deliveryMethod, nextDueAt, createdAt]
      properties:
        id: { type: string }
        productId: { type: string }
        quantity: { type: integer, minimum: 1 }
        frequency: { type: string, enum: [WEEKLY, BIWEEKLY, MONTHLY, EVERY_TWO_MONTHS] }
        status: { type: string, enum: [ACTIVE, PAUSED, CANCELLED] }
        discountPercent: { type: integer, minimum: 0, maximum: 100 }
        paymentMethod: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
        deliveryMethod: { type: string, enum: [courier, personal_belgrade] }
        nextDueAt: { type: string, format: date-time }
        blockedUntil: { type: [string, "null"], format: date-time }
        pausedAt: { type: [string, "null"], format: date-time }
        cancelledAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    RetailProductSubscriptionInput:
      type: object
      required: [productId, quantity, frequency, paymentMethod, deliveryMethod, contact, delivery]
      properties:
        productId: { type: string }
        quantity: { type: integer, minimum: 1 }
        frequency: { type: string, enum: [WEEKLY, BIWEEKLY, MONTHLY, EVERY_TWO_MONTHS] }
        paymentMethod: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
        deliveryMethod: { type: string, enum: [courier, personal_belgrade] }
        firstDueAt: { type: string, format: date-time }
        contact:
          type: object
          required: [firstName, lastName, email, phone]
          properties: { firstName: { type: string }, lastName: { type: string }, email: { type: string }, phone: { type: string } }
        delivery:
          type: object
          required: [street, city, postalCode]
          properties: { street: { type: string }, city: { type: string }, postalCode: { type: string }, note: { type: string }, shippingCost: { type: integer, minimum: 0 } }
    ProductWaitlistSubscription:
      type: object
      required: [id, status, subscribed]
      properties:
        id: { type: string }
        status: { type: string, enum: [ACTIVE, NOTIFIED, UNSUBSCRIBED] }
        subscribed: { type: boolean }
    ProductWaitlistStatus:
      type: object
      required: [subscribed, status, notifiedAt]
      properties:
        subscribed: { type: boolean }
        status: { type: [string, "null"], enum: [ACTIVE, NOTIFIED, UNSUBSCRIBED, null] }
        notifiedAt: { type: [string, "null"], format: date-time }
    AdminProductWaitlistPage:
      type: object
      required: [items, total, page, pageSize, totalPages]
      properties:
        items:
          type: array
          items:
            type: object
            required: [id, audience, status, notifiedAt, createdAt, product, salon, customer]
            properties:
              id: { type: string }
              audience: { type: string, enum: [B2B, B2C] }
              status: { type: string, enum: [ACTIVE, NOTIFIED, UNSUBSCRIBED] }
              notifiedAt: { type: [string, "null"], format: date-time }
              createdAt: { type: string, format: date-time }
              product:
                type: object
                required: [id, name, sku, catalogReference]
                properties:
                  id: { type: string }
                  name: { type: string }
                  sku: { type: string }
                  catalogReference: { type: string }
              salon:
                type: [object, "null"]
                properties:
                  id: { type: string }
                  name: { type: string }
              customer:
                type: [object, "null"]
                properties:
                  id: { type: string }
                  firstName: { type: string }
                  lastName: { type: string }
        total: { type: integer }
        page: { type: integer }
        pageSize: { type: integer }
        totalPages: { type: integer }
    ReferralCodeValidation:
      type: object
      properties:
        valid: { type: boolean }
        code: { type: string }
        channel: { type: string, enum: [A, B1, B2, C, D] }
        link: { type: string }
      required: [valid, code, channel, link]
    ReferralChannelSummary:
      type: object
      properties:
        channel: { type: string, enum: [A, B1, B2, C, D] }
        sourceBusinessId: { type: ["string", "null"] }
        sourceBusinessKind: { type: string, enum: [account, salon, education_center] }
        sourceBusinessName: { type: string }
        code: { type: string }
        link: { type: string, format: uri }
        qualified: { type: integer, minimum: 0 }
        pending: { type: integer, minimum: 0 }
        terms: { type: string }
        cap:
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/ReferralChannelCap"
      required: [channel, sourceBusinessId, sourceBusinessKind, sourceBusinessName, code, link, qualified, pending, terms, cap]
    ReferralChannelCap:
      type: object
      properties:
        limit: { type: integer, minimum: 1 }
        used: { type: integer, minimum: 0 }
        period: { type: string, enum: [calendar_month, calendar_week] }
      required: [limit, used, period]
    ReferralLedgerEntry:
      type: object
      properties:
        id: { type: string }
        type: { type: string, enum: [held, available, redeemed, expired, reversed, negative_offset, restored] }
        amountRsd: { type: integer }
        effectiveAt: { type: string, format: date-time }
        expiresAt: { type: ["string", "null"], format: date-time }
        reason: { type: string }
      required: [id, type, amountRsd, effectiveAt, expiresAt, reason]
    ReferralDashboard:
      type: object
      properties:
        availableRsd: { type: integer }
        expiringSoonRsd: { type: integer }
        channels: { type: array, items: { $ref: "#/components/schemas/ReferralChannelSummary" } }
        ledger: { type: array, items: { $ref: "#/components/schemas/ReferralLedgerEntry" } }
      required: [availableRsd, expiringSoonRsd, channels, ledger]
    ReferralApproval:
      type: object
      properties:
        attributionId: { type: string }
        channel: { type: string, enum: [A, B1] }
        businessKind: { type: string, enum: [salon, education_center] }
        status: { type: string }
        createdAt: { type: string, format: date-time }
      required: [attributionId, channel, businessKind, status, createdAt]
    ReferralApprovalDecision:
      type: object
      required: [action]
      properties:
        action: { type: string, enum: [approve, reject, resubmit] }
        reason: { type: string, maxLength: 1000 }
    ReferralApprovalDecisionResult:
      type: object
      required: [businessKind, action, status]
      properties:
        businessKind: { type: string, enum: [salon, education_center] }
        action: { type: string, enum: [approve, reject, resubmit] }
        status: { type: string, enum: [tracking, pending_verification] }
    ReferralReview:
      type: object
      properties:
        id: { type: string }
        status: { type: string, enum: [open, approved, rejected, dismissed] }
        reasonCode: { type: string }
        detail: { type: ["string", "null"] }
        score: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, status, reasonCode, detail, score, createdAt]
    ReferralReviewUpdate:
      type: object
      properties:
        status: { type: string, enum: [approved, rejected, dismissed] }
        detail: { type: string, maxLength: 2000 }
      required: [status]
    HealthStatus:
      type: object
      properties:
        status: { type: string }
        databasePool:
          type: object
          properties:
            total: { type: number, minimum: 0, multipleOf: 1 }
            idle: { type: number, minimum: 0, multipleOf: 1 }
            waiting: { type: number, minimum: 0, multipleOf: 1 }
            max: { type: number, minimum: 1, multipleOf: 1 }
          required: [total, idle, waiting, max]
        schedulerJobs:
          type: array
          description: Last known local status of each periodic database-backed scheduler job.
          items: { $ref: "#/components/schemas/SchedulerJobHealth" }
      required: [status, databasePool, schedulerJobs]
    SchedulerJobHealth:
      type: object
      properties:
        job: { type: string }
        state: { type: string, enum: [idle, running, retrying, failed] }
        lastStartedAt: { type: ["string", "null"], format: date-time }
        lastSucceededAt: { type: ["string", "null"], format: date-time }
        lastFailedAt: { type: ["string", "null"], format: date-time }
        lastFailureClass: { type: ["string", "null"], enum: [transient_database, permanent] }
        consecutiveFailures: { type: number, minimum: 0, multipleOf: 1 }
        deferredCycles: { type: number, minimum: 0, multipleOf: 1 }
        nextRetryAt: { type: ["string", "null"], format: date-time }
      required: [job, state, lastStartedAt, lastSucceededAt, lastFailedAt, lastFailureClass, consecutiveFailures, deferredCycles, nextRetryAt]
    User:
      type: object
      properties:
        id: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: string }
        phone: { type: ["string", "null"] }
        dateOfBirth: { type: ["string", "null"], pattern: '^\d{4}-\d{2}-\d{2}$' }
        role:
          type: string
          enum: [SUPER_ADMIN, ADMIN, SALON_OWNER, SALON_EMPLOYEE, EDUKATIVNI_CENTAR, INSTRUCTOR, CUSTOMER, JOBSEEKER, STUDENT]
        active: { type: boolean }
        mustChangePassword: { type: boolean }
        marketingEmailsEnabled: { type: boolean }
      required: [id, firstName, lastName, email, dateOfBirth, role, active, mustChangePassword, marketingEmailsEnabled]
    EmailPreferencesInput:
      type: object
      properties:
        marketingEmailsEnabled: { type: boolean }
      required: [marketingEmailsEnabled]
    EmailPreferences:
      type: object
      properties:
        marketingEmailsEnabled: { type: boolean }
      required: [marketingEmailsEnabled]
    RegisterInput:
      type: object
      properties:
        firstName: { type: string, minLength: 1 }
        lastName: { type: string, minLength: 1 }
        email: { type: string }
        password: { type: string, minLength: 8 }
        phone: { type: string }
        phoneVerificationCode: { type: string, minLength: 6, maxLength: 6 }
        referralCode: { type: string, minLength: 3, maxLength: 64 }
      required: [firstName, lastName, email, password, phone, phoneVerificationCode]
    JobseekerRegistrationInput:
      type: object
      properties:
        firstName: { type: string, minLength: 1 }
        lastName: { type: string, minLength: 1 }
        email: { type: string }
        password: { type: string, minLength: 8 }
        phone: { type: string }
        phoneVerificationCode: { type: string, minLength: 6, maxLength: 6 }
        dateOfBirth: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        referralCode: { type: string, minLength: 3, maxLength: 64 }
      required: [firstName, lastName, email, password, phone, phoneVerificationCode, dateOfBirth]
    JobseekerProfileInput:
      type: object
      properties:
        bio: { type: string, maxLength: 4000 }
        portfolioMedia: { type: array, minItems: 3, maxItems: 5, items: { type: string, pattern: '^/api/media/[0-9a-fA-F-]{36}' } }
        skillTags: { type: array, maxItems: 30, items: { type: string, minLength: 1, maxLength: 80 } }
        categoryTags: { type: array, maxItems: 20, items: { type: string, minLength: 1, maxLength: 80 } }
      required: [bio, portfolioMedia, skillTags, categoryTags]
    JobseekerProfile:
      allOf:
        - $ref: "#/components/schemas/JobseekerProfileInput"
        - type: object
          properties:
            userId: { type: string }
            dateOfBirth: { type: ["string", "null"], pattern: '^\d{4}-\d{2}-\d{2}$' }
          required: [userId, dateOfBirth]
    JobseekerSalonInterestsInput:
      type: object
      properties:
        salonIds: { type: array, maxItems: 200, items: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
      required: [salonIds]
    JobseekerDashboard:
      type: object
      properties:
        activeListings: { type: integer, minimum: 0 }
        receivedContacts: { type: integer, minimum: 0 }
        enrollments: { type: integer, minimum: 0 }
      required: [activeListings, receivedContacts, enrollments]
    BusinessRegistrationInput:
      type: object
      properties:
        firstName: { type: string, minLength: 1 }
        lastName: { type: string, minLength: 1 }
        email: { type: string }
        password: { type: string, minLength: 8 }
        phone: { type: string, minLength: 6 }
        businessType:
          type: string
          enum: [SALON, EDUCATION_CENTER]
        businessName: { type: string, minLength: 2 }
        city: { type: string, minLength: 2 }
        municipality: { type: string, minLength: 2 }
        address: { type: string, minLength: 3 }
        postalCode: { type: string, minLength: 4 }
        contactEmail: { type: string }
        contactPhone: { type: string, minLength: 6 }
        contactAddress: { type: string, minLength: 3 }
        pib: { type: string, minLength: 8, maxLength: 50 }
        websiteUrl: { type: string, format: uri }
        instagramUrl: { type: string, format: uri }
        description: { type: string, minLength: 20, maxLength: 2000 }
        referralCode: { type: string, minLength: 3, maxLength: 64 }
      required: [firstName, lastName, email, phone, businessType, businessName, city, municipality, address, postalCode, pib]
    LoginInput:
      type: object
      properties:
        email: { type: string }
        password: { type: string, minLength: 1 }
      required: [email, password]
    AuthSession:
      type: object
      properties:
        user: { $ref: "#/components/schemas/User" }
        message: { type: string }
      required: [user, message]
    CurrentUserResponse:
      type: object
      properties:
        user:
          anyOf:
            - $ref: "#/components/schemas/User"
            - type: "null"
      required: [user]
    SignInProvider:
      type: object
      properties:
        provider:
          type: string
          enum: [google, facebook]
        email: { type: string }
        connectedAt: { type: string, format: date-time }
        canDisconnect: { type: boolean }
      required: [provider, email, connectedAt, canDisconnect]
    SignInMethods:
      type: object
      properties:
        passwordAvailable: { type: boolean }
        providers:
          type: array
          items: { $ref: "#/components/schemas/SignInProvider" }
      required: [passwordAvailable, providers]
    EmailMarketingCampaign:
      type: object
      properties:
        id: { type: string }
        audience: { type: string, enum: [customers, salons, loyalty] }
        loyaltyTierId: { type: ["string", "null"] }
        title: { type: string }
        subject: { type: string }
        htmlContent: { type: string }
        scheduledAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [draft, scheduled, sent, failed] }
        recipientCount: { type: number }
        brevoCampaignId: { type: ["number", "null"] }
        errorMessage: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        sentAt: { type: ["string", "null"], format: date-time }
      required: [id, audience, loyaltyTierId, title, subject, htmlContent, scheduledAt, status, recipientCount, brevoCampaignId, errorMessage, createdAt, sentAt]
    AdminListEmailCampaignsResponse:
      type: object
      properties:
        campaigns:
          type: array
          items: { $ref: "#/components/schemas/EmailMarketingCampaign" }
      required: [campaigns]
    AdminIntegrationCard:
      type: object
      properties:
        enabled: { type: boolean }
        configuredInDatabase: { type: boolean }
        complete: { type: boolean }
        version: { type: ["string", "null"] }
        values:
          type: object
          additionalProperties: { type: ["string", "null"] }
      required: [enabled, configuredInDatabase, complete, version, values]
    AdminSaveIntegrationInput:
      type: object
      properties:
        enabled: { type: boolean }
        expectedVersion:
          type: ["string", "null"]
          description: Configuration version returned when this settings card was loaded; stale versions are rejected with 409.
        values:
          type: object
          additionalProperties: { type: string }
      required: [enabled, expectedVersion, values]
    IntegrationSettingsVersionConflict:
      type: object
      properties:
        error: { type: string }
        code: { type: string, enum: [INTEGRATION_SETTINGS_VERSION_CONFLICT] }
        expectedVersion: { type: ["string", "null"] }
        currentVersion: { type: ["string", "null"] }
      required: [error, code, expectedVersion, currentVersion]
    AdminWebhookIntegrationCard:
      allOf:
        - $ref: "#/components/schemas/AdminIntegrationCard"
        - type: object
          properties:
            webhookSecretPendingReconfirmation: { type: boolean }
            webhookVerifiedAt: { type: ["string", "null"], format: date-time }
            webhookVerificationStale: { type: boolean }
            webhookConfirmationMaxAgeDays: { type: number, minimum: 0, multipleOf: 1 }
          required: [webhookSecretPendingReconfirmation, webhookVerifiedAt, webhookVerificationStale, webhookConfirmationMaxAgeDays]
    AdminBrevoWebhookIntegrationCard:
      allOf:
        - $ref: "#/components/schemas/AdminWebhookIntegrationCard"
        - type: object
          properties:
            brevoRegistrationMissingEvents:
              type: array
              items: { type: string }
          required: [brevoRegistrationMissingEvents]
    AdminDeliveryReportStatus:
      type: object
      properties:
        lastEventAt: { type: ["string", "null"], format: date-time }
        rejectedPayloadCount: { type: number, minimum: 0 }
        lastRejectedAt: { type: ["string", "null"], format: date-time }
        malformedWebhookState: { type: string, enum: [normal, observing, alerted, recovered] }
        lastAutomationSentAt: { type: ["string", "null"], format: date-time }
        recentSendCount: { type: number, minimum: 0, multipleOf: 1 }
        warning: { type: boolean }
      required: [lastEventAt, rejectedPayloadCount, lastRejectedAt, malformedWebhookState, lastAutomationSentAt, recentSendCount, warning]
    AdminDeliveryReports:
      type: object
      properties:
        providers:
          type: object
          properties:
            brevo: { $ref: "#/components/schemas/AdminDeliveryReportStatus" }
            infobip: { $ref: "#/components/schemas/AdminDeliveryReportStatus" }
          required: [brevo, infobip]
        windowHours: { type: number, minimum: 1, multipleOf: 1 }
        graceMinutes: { type: number, minimum: 0, multipleOf: 1 }
        rejectionAlertThreshold: { type: number, minimum: 1, multipleOf: 1 }
      required: [providers, windowHours, graceMinutes, rejectionAlertThreshold]
    AdminSmsFallbackReachableAdmin:
      type: object
      properties:
        firstName: { type: string }
        lastName: { type: string }
      required: [firstName, lastName]
    AdminSmsFallback:
      type: object
      properties:
        reachableAdminCount: { type: number, minimum: 0, multipleOf: 1 }
        reachableAdmins:
          type: array
          items: { $ref: "#/components/schemas/AdminSmsFallbackReachableAdmin" }
      required: [reachableAdminCount, reachableAdmins]
    AdminSmsWebhookRegistration:
      type: object
      properties:
        state:
          type: string
          enum: [no_secret, confirmed, misconfigured, stale_secret, unconfirmed]
        secretSavedAt: { type: ["string", "null"], format: date-time }
        lastReportAt: { type: ["string", "null"], format: date-time }
      required: [state, secretSavedAt, lastReportAt]
    AdminSmsReminder:
      type: object
      properties:
        command: { type: string }
        active: { type: boolean }
        instructions:
          type: array
          items: { type: string }
      required: [command, active, instructions]
    AdminGetIntegrationsResponse:
      type: object
      properties:
        integrations:
          type: object
          properties:
            sms: { $ref: "#/components/schemas/AdminWebhookIntegrationCard" }
            brevo: { $ref: "#/components/schemas/AdminBrevoWebhookIntegrationCard" }
            google_oauth: { $ref: "#/components/schemas/AdminIntegrationCard" }
            facebook_oauth: { $ref: "#/components/schemas/AdminIntegrationCard" }
            cloudflare: { $ref: "#/components/schemas/AdminIntegrationCard" }
            web_push: { $ref: "#/components/schemas/AdminIntegrationCard" }
          required: [sms, brevo, google_oauth, facebook_oauth, cloudflare, web_push]
        deliveryReports: { $ref: "#/components/schemas/AdminDeliveryReports" }
        smsFallback: { $ref: "#/components/schemas/AdminSmsFallback" }
        smsWebhookRegistration: { $ref: "#/components/schemas/AdminSmsWebhookRegistration" }
        redirectUris:
          type: object
          properties:
            google: { type: string, format: uri }
            facebook: { type: string, format: uri }
          required: [google, facebook]
        redirectUriWarning: { type: string }
        smsReminder: { $ref: "#/components/schemas/AdminSmsReminder" }
      required: [integrations, deliveryReports, smsFallback, smsWebhookRegistration, redirectUris, smsReminder]
    AdminWebhookFreshness:
      type: object
      properties:
        webhookVerifiedAt: { type: ["string", "null"], format: date-time }
        webhookVerificationStale: { type: boolean }
        webhookConfirmationMaxAgeDays: { type: number, minimum: 0, multipleOf: 1 }
      required: [webhookVerifiedAt, webhookVerificationStale, webhookConfirmationMaxAgeDays]
    AdminGetWebhookFreshnessResponse:
      type: object
      properties:
        integrations:
          type: object
          properties:
            sms: { $ref: "#/components/schemas/AdminWebhookFreshness" }
            brevo: { $ref: "#/components/schemas/AdminWebhookFreshness" }
          required: [sms, brevo]
        deliveryReports: { $ref: "#/components/schemas/AdminDeliveryReports" }
      required: [integrations, deliveryReports]
    AdminWebPushDeliveryMetrics:
      type: object
      properties:
        periodDays: { type: number, enum: [1, 7, 30, 90] }
        periodStartedAt: { type: string, format: date-time }
        deliveries:
          type: object
          properties:
            sent: { type: number, minimum: 0, multipleOf: 1 }
            acknowledged: { type: number, minimum: 0, multipleOf: 1 }
            failed: { type: number, minimum: 0, multipleOf: 1 }
            retried: { type: number, minimum: 0, multipleOf: 1 }
            pending: { type: number, minimum: 0, multipleOf: 1 }
            expiredOrChanged: { type: number, minimum: 0, multipleOf: 1 }
            providerErrors: { type: number, minimum: 0, multipleOf: 1 }
          required: [sent, acknowledged, failed, retried, pending, expiredOrChanged, providerErrors]
        devices:
          type: object
          properties:
            active: { type: number, minimum: 0, multipleOf: 1 }
            automaticallyDeactivated: { type: number, minimum: 0, multipleOf: 1 }
          required: [active, automaticallyDeactivated]
      required: [periodDays, periodStartedAt, deliveries, devices]
    AdminCreateEmailCampaignInput:
      type: object
      properties:
        audience: { type: string, enum: [customers, salons, loyalty] }
        loyaltyTierId: { type: ["string", "null"] }
        title: { type: string, minLength: 3, maxLength: 120 }
        subject: { type: string, minLength: 3, maxLength: 160 }
        htmlContent: { type: string, minLength: 20, maxLength: 100000 }
        sendMode: { type: string, enum: [now, scheduled] }
        scheduledAt: { type: ["string", "null"], format: date-time }
      required: [audience, title, subject, htmlContent, sendMode]
    SalonCard:
      type: object
      properties:
        id: { type: string }
        slug: { type: string }
        name: { type: string }
        city: { type: string }
        municipality: { type: string }
        imageUrl: { type: string }
        rating: { type: number }
        reviewCount: { type: number }
        shortDescription: { type: string }
        popularServices:
          type: array
          items: { type: string }
        startingPrice: { type: number }
        earliestSlot: { type: ["string", "null"] }
        homeService: { type: boolean }
        featured: { type: boolean }
        topSalon: { type: boolean }
        acceptsCards: { type: boolean }
        instantBooking: { type: boolean }
        servesMen: { type: boolean }
        isVerified: { type: boolean }
        hasDiscount: { type: boolean }
        openSunday: { type: boolean }
        lastBookedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, slug, name, city, municipality, imageUrl, rating, reviewCount, shortDescription, popularServices, startingPrice, homeService, featured, topSalon, acceptsCards, instantBooking, servesMen, isVerified, hasDiscount, openSunday, createdAt]
    DiscoveryService:
      type: object
      properties:
        name: { type: string }
        categoryName: { type: string }
        bookingCount: { type: number, minimum: 0 }
        imageUrl: { type: string }
      required: [name, categoryName, bookingCount, imageUrl]
    DiscountedSalonCard:
      allOf:
        - $ref: "#/components/schemas/SalonCard"
        - type: object
          properties:
            discount:
              type: object
              properties:
                serviceName: { type: string }
                price: { type: number, minimum: 0 }
                promoPrice: { type: number, minimum: 0 }
              required: [serviceName, price, promoPrice]
          required: [discount]
    MarketplaceHomeDiscovery:
      type: object
      properties:
        popularServices:
          type: array
          items: { $ref: "#/components/schemas/DiscoveryService" }
        featuredSalons:
          type: array
          items: { $ref: "#/components/schemas/SalonCard" }
        newSalons:
          type: array
          items: { $ref: "#/components/schemas/SalonCard" }
        discountedSalons:
          type: array
          items: { $ref: "#/components/schemas/DiscountedSalonCard" }
        popularSalons:
          type: array
          items: { $ref: "#/components/schemas/SalonCard" }
        topRatedSalons:
          type: array
          items: { $ref: "#/components/schemas/SalonCard" }
      required: [popularServices, featuredSalons, newSalons, discountedSalons, popularSalons, topRatedSalons]
    CitySummary:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        salonCount: { type: integer, minimum: 0 }
      required: [name, salonCount]
    PlatformTrustStats:
      type: object
      properties:
        activeSalons: { type: number, minimum: 0 }
        bookingsThisMonth: { type: number, minimum: 0 }
        customerAccounts: { type: number, minimum: 0 }
      required: [activeSalons, bookingsThisMonth, customerAccounts]
    SalonProfile:
      allOf:
        - $ref: "#/components/schemas/SalonCard"
        - type: object
          properties:
            gallery:
              type: array
              items: { type: string }
            videoUrl: { type: ["string", "null"] }
            description: { type: string }
            topServices:
              type: array
              items: { $ref: "#/components/schemas/PopularSalonService" }
            hours:
              type: array
              items: { $ref: "#/components/schemas/OpeningHour" }
            staff:
              type: array
              items: { $ref: "#/components/schemas/Employee" }
            services:
              type: array
              items: { $ref: "#/components/schemas/Service" }
            reviews:
              type: array
              items: { $ref: "#/components/schemas/Review" }
            returnClientRate: { type: ["number", "null"], minimum: 0, maximum: 100 }
            homeServiceRadiusKm: { type: number, minimum: 1, maximum: 100 }
          required: [gallery, videoUrl, description, topServices, hours, staff, services, reviews, returnClientRate, homeServiceRadiusKm]
    PopularSalonService:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        durationMinutes: { type: number, minimum: 1 }
        price: { type: number, minimum: 0 }
        promoPrice: { type: ["number", "null"], minimum: 0 }
        bookingCount: { type: number, minimum: 1 }
      required: [id, name, description, durationMinutes, price, promoPrice, bookingCount]
    SalonProfileMedia:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        active: { type: boolean }
        isVerified: { type: boolean }
        videoUrl: { type: ["string", "null"] }
        acceptsCards: { type: boolean }
        instantBooking: { type: boolean }
        homeService: { type: boolean }
        homeServiceRadiusKm: { type: number, minimum: 1, maximum: 100 }
        servesMen: { type: boolean }
        openSunday: { type: boolean }
        imageUrl: { type: string }
        gallery:
          type: array
          items: { type: string }
      required: [id, name, slug, active, isVerified, videoUrl, acceptsCards, instantBooking, homeService, homeServiceRadiusKm, servesMen, openSunday, imageUrl, gallery]
    SalonProfileMediaUpdate:
      type: object
      properties:
        videoUrl: { type: ["string", "null"] }
        acceptsCards: { type: boolean }
        instantBooking: { type: boolean }
        homeServiceRadiusKm: { type: number, minimum: 1, maximum: 100 }
        servesMen: { type: boolean }
        imageUrl: { type: string, minLength: 1 }
        gallery:
          type: array
          maxItems: 20
          items: { type: string, minLength: 1 }
    MediaUploadInput:
      type: object
      properties:
        scope:
          type: string
          enum: [salon-profile, salon-gallery, employee-avatar, product, product-document, supplier, education-cover, education-gallery, education-center, instructor-avatar, service-category, product-category, treatment-photo, jobseeker-portfolio, rma-photo, retail-review-photo]
        resourceId:
          type: ["string", "null"]
          pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$'
        name: { type: string, minLength: 1, maxLength: 240 }
        size: { type: number, minimum: 1, maximum: 12582912, multipleOf: 1 }
        contentType: { type: string, enum: [image/jpeg, image/png, image/webp, image/avif, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document] }
      required: [scope, name, size, contentType]
    MediaUploadTicket:
      type: object
      properties:
        uploadId: { type: string }
        uploadUrl: { type: string }
        expiresAt: { type: string, format: date-time }
      required: [uploadId, uploadUrl, expiresAt]
    MediaAsset:
      type: object
      properties:
        id: { type: string }
        imageUrl: { type: string }
        width: { type: number, minimum: 1, multipleOf: 1 }
        height: { type: number, minimum: 1, multipleOf: 1 }
        contentHash: { type: string }
      required: [id, imageUrl, width, height, contentHash]
    OpeningHour:
      type: object
      properties:
        day: { type: string }
        open: { type: string }
        close: { type: string }
        closed: { type: boolean }
      required: [day, open, close, closed]
    Employee:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        role: { type: string }
        bio: { type: string }
        avatarUrl: { type: string }
        specialties:
          type: array
          items: { type: string }
        serviceIds:
          type: array
          items: { type: string }
        serviceNames:
          type: array
          items: { type: string }
        canOrderIndependently: { type: boolean, description: Owner-controlled B2B purchasing permission. }
      required: [id, name, role, bio, avatarUrl, specialties, serviceIds, serviceNames, canOrderIndependently]
    EmployeeDeactivationPreview:
      type: object
      properties:
        employeeId: { type: string }
        employeeName: { type: string }
        locationName:
          type: ["string", "null"]
          description: Exact salon location that owns this request; present for owner queue reads.
        futureAppointmentCount: { type: number, minimum: 0 }
        hasLoginAccount: { type: boolean }
      required: [employeeId, employeeName, futureAppointmentCount, hasLoginAccount]
    EmployeeDeactivationResult:
      type: object
      properties:
        employeeId: { type: string }
        deactivated: { type: boolean }
        futureAppointmentCount: { type: number, minimum: 0 }
        loginAccountDeactivated: { type: boolean }
      required: [employeeId, deactivated, futureAppointmentCount, loginAccountDeactivated]
    SalonResource:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        name: { type: string }
        type:
          type: string
          enum: [chair, booth, bed, room, equipment, other]
        capacity: { type: number, minimum: 1 }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, salonId, name, type, capacity, active, createdAt, updatedAt]
    SalonResourceInput:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        type:
          type: string
          enum: [chair, booth, bed, room, equipment, other]
        capacity: { type: number, minimum: 1, maximum: 1000, multipleOf: 1 }
        active: { type: boolean }
      required: [name, type, capacity]
    SalonResourceUpdate:
      type: object
      minProperties: 1
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        type:
          type: string
          enum: [chair, booth, bed, room, equipment, other]
        capacity: { type: number, minimum: 1, maximum: 1000, multipleOf: 1 }
        active: { type: boolean }
    ServiceResourceRequirement:
      type: object
      properties:
        resourceId: { type: string }
        quantity: { type: number, minimum: 1, multipleOf: 1 }
      required: [resourceId, quantity]
    AllocatedResource:
      type: object
      properties:
        resourceId: { type: string }
        resourceName: { type: string }
        quantity: { type: number, minimum: 1 }
      required: [resourceId, resourceName, quantity]
    Service:
      type: object
      properties:
        id: { type: string }
        category: { type: string }
        name: { type: string }
        description: { type: string }
        durationMinutes: { type: number }
        bufferMinutes: { type: number, minimum: 0, default: 0 }
        price: { type: number }
        promoPrice: { type: ["number", "null"] }
        tags: { type: array, items: { type: string } }
        packageTreatments: { type: ["number", "null"] }
        imageUrl: { type: string }
        active: { type: boolean }
        homeServiceAvailable: { type: boolean }
        homeServiceFee: { type: number, minimum: 0 }
        homeServiceMinimumOrder: { type: ["number", "null"], minimum: 0 }
        resourceRequirements:
          type: array
          items: { $ref: "#/components/schemas/ServiceResourceRequirement" }
      required: [id, category, name, description, durationMinutes, price, imageUrl, active, homeServiceAvailable, homeServiceFee, homeServiceMinimumOrder, resourceRequirements]
    SalonManagedService:
      type: object
      properties:
        id: { type: string }
        category: { type: string }
        name: { type: string }
        description: { type: string }
        durationMinutes: { type: number }
        bufferMinutes: { type: number, minimum: 0, default: 0 }
        price: { type: number }
        promoPrice: { type: ["number", "null"] }
        tags: { type: array, items: { type: string } }
        packageTreatments: { type: ["number", "null"] }
        imageUrl: { type: string }
        active: { type: boolean }
        homeServiceAvailable: { type: boolean }
        homeServiceFee: { type: number, minimum: 0 }
        homeServiceMinimumOrder: { type: ["number", "null"], minimum: 0 }
        canBePermanentlyDeleted: { type: boolean }
        resourceRequirements:
          type: array
          items: { $ref: "#/components/schemas/ServiceResourceRequirement" }
      required: [id, category, name, description, durationMinutes, price, imageUrl, active, homeServiceAvailable, homeServiceFee, homeServiceMinimumOrder, canBePermanentlyDeleted, resourceRequirements]
    ServiceTemplate:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        name: { type: string }
        mainCategory: { type: string }
        subcategory: { type: string }
        typicalDurationMinutes: { type: number, minimum: 5 }
        priceMin: { type: number, minimum: 0 }
        priceMax: { type: number, minimum: 0 }
        description: { type: ["string", "null"] }
        active: { type: boolean }
      required: [id, name, mainCategory, subcategory, typicalDurationMinutes, priceMin, priceMax, description, active]
    ServiceTemplateBatchItem:
      type: object
      properties:
        templateId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        price: { type: number, minimum: 1, maximum: 1000000 }
        durationMinutes: { type: number, minimum: 5, maximum: 720 }
      required: [templateId, price]
    ServiceTemplateBatchInput:
      type: object
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 50
          items: { $ref: "#/components/schemas/ServiceTemplateBatchItem" }
      required: [items]
    ServiceTemplateBatchResult:
      type: object
      properties:
        created:
          type: array
          items: { $ref: "#/components/schemas/Service" }
        skipped:
          type: array
          items: { type: string }
      required: [created, skipped]
    AdminServiceTemplateInput:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 160, pattern: '.*\S.*' }
        mainCategory: { type: string, minLength: 1, maxLength: 120, pattern: '.*\S.*' }
        subcategory: { type: string, minLength: 1, maxLength: 120, pattern: '.*\S.*' }
        typicalDurationMinutes: { type: number, minimum: 5, maximum: 720, multipleOf: 1 }
        priceMin: { type: number, minimum: 0, maximum: 1000000, multipleOf: 1 }
        priceMax: { type: number, minimum: 0, maximum: 1000000, multipleOf: 1 }
        description: { type: ["string", "null"], maxLength: 1000 }
        active: { type: boolean }
      required: [name, mainCategory, subcategory, typicalDurationMinutes, priceMin, priceMax, active]
    AdminServiceTemplateUpdate:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 160, pattern: '.*\S.*' }
        mainCategory: { type: string, minLength: 1, maxLength: 120, pattern: '.*\S.*' }
        subcategory: { type: string, minLength: 1, maxLength: 120, pattern: '.*\S.*' }
        typicalDurationMinutes: { type: number, minimum: 5, maximum: 720, multipleOf: 1 }
        priceMin: { type: number, minimum: 0, maximum: 1000000, multipleOf: 1 }
        priceMax: { type: number, minimum: 0, maximum: 1000000, multipleOf: 1 }
        description: { type: ["string", "null"], maxLength: 1000 }
        active: { type: boolean }
      minProperties: 1
    Review:
      type: object
      properties:
        id: { type: string }
        authorName: { type: string }
        avatarUrl: { type: ["string", "null"] }
        verifiedBooking: { type: boolean }
        rating: { type: number, minimum: 1, maximum: 5, multipleOf: 1 }
        text: { type: string }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        serviceName: { type: string }
      required: [id, authorName, avatarUrl, verifiedBooking, rating, text, date, serviceName]
    CustomerReview:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        serviceName: { type: string }
        rating: { type: number, minimum: 1, maximum: 5, multipleOf: 1 }
        text: { type: string }
        showProfilePhoto: { type: boolean }
      required: [id, salonId, serviceName, rating, text, showProfilePhoto]
    CustomerReviewInput:
      type: object
      properties:
        serviceName: { type: string, minLength: 1, maxLength: 160 }
        rating: { type: number, minimum: 1, maximum: 5, multipleOf: 1 }
        text: { type: string, minLength: 1, maxLength: 1000 }
        showProfilePhoto: { type: boolean }
      required: [serviceName, rating, text, showProfilePhoto]
    CustomerSalonReviewContext:
      type: object
      properties:
        review:
          anyOf:
            - $ref: "#/components/schemas/CustomerReview"
            - type: "null"
        eligibleServices:
          type: array
          items: { type: string }
      required: [review, eligibleServices]
    BookingReminderChannel:
      type: string
      enum: [email, sms, push]
    SalonDateHours:
      type: object
      properties:
        id: { type: string }
        date: { type: string, format: date }
        closed: { type: boolean }
        openTime:
          type: ["string", "null"]
          pattern: '^(?:[01][0-9]|2[0-3]):[0-5][0-9]$'
        closeTime:
          type: ["string", "null"]
          pattern: '^(?:[01][0-9]|2[0-3]):[0-5][0-9]$'
        reason: { type: ["string", "null"] }
      required: [date, closed, openTime, closeTime, reason]
    ResourceDowntime:
      type: object
      properties:
        id: { type: string }
        resourceId: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        reason: { type: ["string", "null"] }
      required: [resourceId, startsAt, endsAt, reason]
    SalonBookingSettingsInput:
      type: object
      properties:
        slotGranularityMinutes: { type: integer, enum: [5, 10, 15, 30], default: 15 }
        minimumLeadTimeMinutes: { type: integer, minimum: 0 }
        cancellationDeadlineMinutes:
          type: integer
          enum: [720, 1440, 2880]
        reminderOffsetsMinutes:
          type: array
          uniqueItems: true
          items:
            type: integer
            enum: [120, 720, 1440]
        reminderChannels:
          type: array
          uniqueItems: true
          items: { $ref: "#/components/schemas/BookingReminderChannel" }
        maxVisitGapMinutes: { type: integer, minimum: 0, default: 0 }
        minimumUsefulLateTreatmentMinutes: { type: integer, minimum: 0 }
        dateHours:
          type: array
          items: { $ref: "#/components/schemas/SalonDateHours" }
        resourceDowntime:
          type: array
          items: { $ref: "#/components/schemas/ResourceDowntime" }
      required: [slotGranularityMinutes, minimumLeadTimeMinutes, cancellationDeadlineMinutes, reminderOffsetsMinutes, reminderChannels, maxVisitGapMinutes, minimumUsefulLateTreatmentMinutes, dateHours, resourceDowntime]
    SalonBookingSettings:
      allOf:
        - $ref: "#/components/schemas/SalonBookingSettingsInput"
        - type: object
          properties:
            salonId: { type: string }
            updatedAt: { type: string, format: date-time }
          required: [salonId, updatedAt]
    GroupedTreatmentRequest:
      type: object
      properties:
        serviceId: { type: string }
        employeeId: { type: ["string", "null"] }
      required: [serviceId]
    GroupedAvailabilityInput:
      type: object
      properties:
        resultMode:
          type: string
          enum: [list, calendar]
          default: list
          description: List preserves the legacy cross-range maximum of five candidates. Calendar returns a day entry for every requested date and supports ranges of up to 14 days.
        treatments:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/GroupedTreatmentRequest" }
        fromDate: { type: string, format: date }
        toDate: { type: string, format: date }
        allowMultipleDays:
          type: boolean
          default: false
          description: False keeps every treatment on one calendar day; true permits later treatments on later days.
      required: [treatments, fromDate, toDate]
    GroupedTreatmentSlot:
      type: object
      properties:
        position: { type: integer, minimum: 0 }
        serviceId: { type: string }
        date: { type: string, format: date }
        employeeId: { type: ["string", "null"] }
        startTime: { type: string }
        endTime: { type: string }
        bufferMinutes: { type: integer, minimum: 0 }
      required: [position, serviceId, date, employeeId, startTime, endTime, bufferMinutes]
    GroupedAvailabilityCandidate:
      type: object
      properties:
        date: { type: string, format: date }
        startTime: { type: string }
        endTime: { type: string }
        treatments:
          type: array
          items: { $ref: "#/components/schemas/GroupedTreatmentSlot" }
      required: [date, startTime, endTime, treatments]
    GroupedAvailabilityCalendarDay:
      type: object
      properties:
        date: { type: string, format: date }
        candidates:
          type: array
          maxItems: 20
          description: Complete valid treatment-group combinations that start on this date.
          items: { $ref: "#/components/schemas/GroupedAvailabilityCandidate" }
        truncated:
          type: boolean
          description: True when more than the safe per-day maximum of 20 valid combinations exist or the bounded candidate-search budget was reached.
      required: [date, candidates, truncated]
    GroupedAvailabilityResponse:
      type: object
      properties:
        salonId: { type: string }
        generatedAt: { type: string, format: date-time }
        candidates:
          type: array
          maxItems: 20
          description: Legacy list-mode candidates. Empty when resultMode is calendar.
          items: { $ref: "#/components/schemas/GroupedAvailabilityCandidate" }
        calendarDays:
          type: array
          maxItems: 14
          description: Present in calendar mode and includes every requested date, including dates with no candidates.
          items: { $ref: "#/components/schemas/GroupedAvailabilityCalendarDay" }
      required: [salonId, generatedAt, candidates]
    BookingGroupTreatmentInput:
      type: object
      properties:
        serviceId: { type: string }
        date: { type: string, format: date }
        employeeId: { type: ["string", "null"] }
        startTime: { type: string }
      required: [serviceId, startTime]
    BookingGroupInput:
      type: object
      properties:
        salonId: { type: string }
        date: { type: string, format: date }
        treatments:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/BookingGroupTreatmentInput" }
        notes: { type: ["string", "null"], maxLength: 1000 }
      required: [salonId, date, treatments]
    ManualBookingGroupInput:
      type: object
      properties:
        salonCustomerId: { type: string }
        guest:
          type: object
          properties:
            firstName: { type: string, minLength: 1, maxLength: 100 }
            lastName: { type: string, maxLength: 100 }
            phone: { type: string, minLength: 5, maxLength: 50 }
            email: { type: string, maxLength: 320 }
          required: [firstName, phone]
        treatments:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/ManualBookingGroupTreatmentInput" }
        notes: { type: ["string", "null"], maxLength: 1000 }
      required: [treatments]
    ManualBookingGroupTreatmentInput:
      type: object
      properties:
        serviceId: { type: string }
        date: { type: string, format: date }
        employeeId: { type: ["string", "null"] }
        startTime: { type: string }
      required: [serviceId, date, startTime]
    BookingGroupRescheduleTreatmentInput:
      type: object
      properties:
        appointmentId: { type: string }
        date: { type: string, format: date }
        startTime: { type: string }
        employeeId: { type: ["string", "null"] }
      required: [appointmentId, date, startTime]
    BookingGroupRescheduleInput:
      type: object
      properties:
        treatments:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/BookingGroupRescheduleTreatmentInput" }
      required: [treatments]
    BookingGroupCancelInput:
      type: object
      properties:
        appointmentIds:
          type: array
          minItems: 1
          maxItems: 5
          uniqueItems: true
          items: { type: string }
        reason: { type: ["string", "null"], maxLength: 1000 }
    BookingGroupOperationResult:
      type: object
      properties:
        group: { $ref: "#/components/schemas/BookingGroup" }
        gapWarning: { type: ["string", "null"] }
      required: [group, gapWarning]
    BookingGroupConflict:
      type: object
      properties:
        code:
          type: string
          enum: [BOOKING_GROUP_LAYOUT_CONFLICT]
          description: Present when the canonical active booking-group layout invariant failed.
        error: { type: string }
      required: [error]
    BookingCommandReceipt:
      type: object
      required: [idempotencyKey, commandType, responseStatus, responseBody, createdAt]
      properties:
        idempotencyKey: { type: string }
        commandType: { type: string }
        responseStatus: { type: integer }
        responseBody: {}
        createdAt: { type: string, format: date-time }
    BookingGroup:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        appointments:
          type: array
          items: { $ref: "#/components/schemas/Appointment" }
        createdAt: { type: string, format: date-time }
      required: [id, salonId, appointments, createdAt]
    AppointmentLifecycleInput:
      type: object
      properties:
        action:
          type: string
          enum: [confirm, arrive, start, complete, cancel, no-show]
        occurredAt: { type: string, format: date-time }
        reason: { type: ["string", "null"], maxLength: 1000 }
      required: [action]
    EmployeeAssignedLocation:
      type: object
      properties:
        salonId: { type: string }
        name: { type: string }
        isDefault: { type: boolean }
      required: [salonId, name, isDefault]
    EmployeeAssignedLocations:
      type: object
      properties:
        activeSalonId: { type: string }
        locations:
          type: array
          items: { $ref: "#/components/schemas/EmployeeAssignedLocation" }
      required: [activeSalonId, locations]
    EmployeeActiveLocationInput:
      type: object
      properties:
        salonId: { type: string }
      required: [salonId]
    EmployeeActiveLocation:
      type: object
      properties:
        activeSalonId: { type: string }
      required: [activeSalonId]
    AppointmentLifecycleConflict:
      type: object
      properties:
        error: { type: string }
        code:
          type: string
          enum: [INVALID_TRANSITION, INSUFFICIENT_USEFUL_TIME]
        remainingMinutes: { type: integer }
        minimumUsefulMinutes: { type: integer }
        plannedEndAt: { type: string, format: date-time }
      required: [error, code]
    CustomerNotification:
      type: object
      properties:
        id: { type: string }
        eventKey: { type: string }
        category:
          type: string
          enum: [booking, reminder, cancellation, review, loyalty, commerce, education, system]
        title: { type: string }
        body: { type: string }
        deepLink: { type: ["string", "null"] }
        readAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, eventKey, category, title, body, deepLink, readAt, createdAt]
    CustomerNotificationPage:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/CustomerNotification" }
        nextCursor: { type: ["string", "null"] }
        unreadCount: { type: integer, minimum: 0 }
      required: [items, nextCursor, unreadCount]
    WebPushConfig:
      type: object
      properties:
        configured: { type: boolean }
        publicKey: { type: ["string", "null"] }
      required: [configured, publicKey]
    SystemPushDeliveryAcknowledgement:
      type: object
      properties:
        deliveryId:
          type: string
          pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
        token: { type: string, minLength: 43, maxLength: 43 }
      required: [deliveryId, token]
    PushSubscriptionKeysInput:
      type: object
      properties:
        p256dh: { type: string, minLength: 1, maxLength: 1024 }
        auth: { type: string, minLength: 1, maxLength: 512 }
      required: [p256dh, auth]
    PushSubscriptionInput:
      type: object
      properties:
        endpoint: { type: string, format: uri, maxLength: 4096 }
        keys: { $ref: "#/components/schemas/PushSubscriptionKeysInput" }
      required: [endpoint, keys]
    PushSubscription:
      type: object
      properties:
        id: { type: string }
        endpoint: { type: string, format: uri }
        enabled: { type: boolean }
        lastSeenAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, endpoint, enabled, lastSeenAt, createdAt]
    TimeSlot:
      type: object
      properties:
        start: { type: string }
        end: { type: string }
        employeeId: { type: ["string", "null"] }
        employeeName: { type: ["string", "null"] }
      required: [start, end]
    FirstAvailableServiceSlot:
      type: object
      properties:
        serviceId: { type: string }
        date: { type: ["string", "null"] }
        startTime: { type: ["string", "null"] }
        endTime: { type: ["string", "null"] }
        employeeId: { type: ["string", "null"] }
        employeeName: { type: ["string", "null"] }
      required: [serviceId, date, startTime, endTime, employeeId, employeeName]
    SalonFirstAvailable:
      type: object
      properties:
        generatedAt: { type: string, format: date-time }
        horizonDays: { type: number, minimum: 1 }
        services:
          type: array
          items: { $ref: "#/components/schemas/FirstAvailableServiceSlot" }
      required: [generatedAt, horizonDays, services]
    Appointment:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        salonSlug: { type: string }
        salonName: { type: string }
        serviceId: { type: string }
        customerName: { type: string }
        serviceName: { type: string }
        employeeId: { type: ["string", "null"] }
        employeeName: { type: string }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: string }
        endTime: { type: string }
        durationMinutes: { type: number }
        price: { type: number }
        treatmentLocation: { type: string, enum: [salon, home] }
        travelFee: { type: number, minimum: 0 }
        treatmentAddress:
          type: ["object", "null"]
          properties:
            line1: { type: string }
            city: { type: string }
            postalCode: { type: ["string", "null"] }
            details: { type: ["string", "null"] }
          required: [line1, city, postalCode, details]
        seriesId: { type: ["string", "null"] }
        status:
          type: string
          enum: [pending, confirmed, completed, cancelled, no-show]
        notes: { type: ["string", "null"] }
        rescheduledConfirmation:
          type: ["object", "null"]
          properties:
            sms:
              type: ["object", "null"]
              properties:
                status: { type: string, enum: [queued, processing, sent, failed, skipped] }
                nextRetryAt: { type: ["string", "null"], format: date-time }
              required: [status]
            email:
              type: ["object", "null"]
              properties:
                status: { type: string, enum: [queued, processing, sent, failed, skipped] }
                nextRetryAt: { type: ["string", "null"], format: date-time }
              required: [status]
          required: [sms, email]
        allocatedResources:
          type: array
          items: { $ref: "#/components/schemas/AllocatedResource" }
        bookingGroupId: { type: ["string", "null"] }
        treatments:
          type: array
          items: { $ref: "#/components/schemas/GroupedTreatmentSlot" }
        plannedDate: { type: ["string", "null"], format: date }
        plannedStartTime: { type: ["string", "null"] }
        plannedEndTime: { type: ["string", "null"] }
        arrivedAt: { type: ["string", "null"], format: date-time }
        arrivedByUserId: { type: ["string", "null"] }
        actualStartedAt: { type: ["string", "null"], format: date-time }
        startedByUserId: { type: ["string", "null"] }
        actualCompletedAt: { type: ["string", "null"], format: date-time }
        updatedByUserId: { type: ["string", "null"] }
        cancelledByUserId: { type: ["string", "null"] }
        completedByUserId: { type: ["string", "null"] }
        noShowByUserId: { type: ["string", "null"] }
        confirmedAt: { type: ["string", "null"], format: date-time }
        cancelledAt: { type: ["string", "null"], format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
        noShowAt: { type: ["string", "null"], format: date-time }
      required: [id, salonId, salonSlug, salonName, serviceId, customerName, serviceName, employeeId, employeeName, date, startTime, endTime, durationMinutes, price, treatmentLocation, travelFee, treatmentAddress, status, allocatedResources]
    AppointmentSalonContact:
      type: object
      properties:
        appointmentId: { type: string }
        name: { type: string }
        phone: { type: string }
        email: { type: string }
        address: { type: string }
        postalCode: { type: ["string", "null"] }
        city: { type: string }
        latitude: { type: ["number", "null"], format: double }
        longitude: { type: ["number", "null"], format: double }
      required: [appointmentId, name, phone, email, address, postalCode, city, latitude, longitude]
    AppointmentInput:
      type: object
      properties:
        salonId: { type: string }
        serviceId: { type: string }
        employeeId: { type: ["string", "null"] }
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^(?:[01][0-9]|2[0-3]):[0-5][0-9]$' }
        notes: { type: string }
        packagePurchaseId:
          type: ["string", "null"]
          description: >-
            Optional active package purchase to redeem for this booking. When
            supplied the appointment is created and a session is redeemed in the
            same transaction; the appointment price is set to 0. The purchase
            must belong to the authenticated customer, cover the booked service,
            be active, non-expired and have a remaining session. Any failure
            rolls back the entire booking.
        treatmentLocation: { type: string, enum: [salon, home] }
        treatmentAddress:
          type: object
          properties:
            line1: { type: string, minLength: 3, maxLength: 200 }
            city: { type: string, minLength: 2, maxLength: 100 }
            postalCode: { type: string, maxLength: 20 }
            details: { type: string, maxLength: 300 }
          required: [line1, city]
      required: [salonId, serviceId, date, startTime]
    AppointmentUpdate:
      type: object
      properties:
        date: { type: string, format: date }
        startTime: { type: string }
        employeeId: { type: ["string", "null"] }
        notes: { type: string }
    SalonAppointmentUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [pending, confirmed, completed, cancelled, no-show]
        employeeId: { type: ["string", "null"] }
        notes: { type: string }
    SalonCustomer:
      type: object
      properties:
        id: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        smsOptOut: { type: boolean }
        birthDate: { type: ["string", "null"], format: date }
        visitCount: { type: number }
        noShowCount: { type: number, minimum: 0 }
        isRegistered: { type: boolean }
        series:
          type: array
          items: { $ref: "#/components/schemas/CustomerAppointmentSeries" }
      required: [id, firstName, lastName, phone, smsOptOut, visitCount, noShowCount, isRegistered]
    AppointmentSlot:
      type: object
      properties:
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
      required: [date, startTime]
    AppointmentSeriesPreviewInput:
      type: object
      properties:
        serviceId: { type: string }
        employeeId: { type: ["string", "null"] }
        packagePurchaseId: { type: ["string", "null"] }
        salonCustomerId: { type: ["string", "null"] }
        slots:
          type: array
          minItems: 1
          maxItems: 24
          items: { $ref: "#/components/schemas/AppointmentSlot" }
      required: [serviceId, slots]
    AppointmentSeriesPreviewSlot:
      allOf:
        - $ref: "#/components/schemas/AppointmentSlot"
        - type: object
          properties:
            available: { type: boolean }
            reason: { type: ["string", "null"] }
          required: [available, reason]
    AppointmentSeriesPreview:
      type: object
      properties:
        slots:
          type: array
          items: { $ref: "#/components/schemas/AppointmentSeriesPreviewSlot" }
        allAvailable: { type: boolean }
        packageEligible: { type: ["boolean", "null"] }
        packageReason: { type: ["string", "null"] }
      required: [slots, allAvailable]
    SalonAppointmentSeriesInput:
      allOf:
        - $ref: "#/components/schemas/AppointmentSeriesPreviewInput"
        - type: object
          properties:
            notes: { type: string }
            salonCustomerId: { type: string }
            guest:
              type: object
              properties:
                firstName: { type: string, minLength: 1 }
                lastName: { type: string, minLength: 1 }
                phone: { type: string, minLength: 5 }
                email: { type: string }
              required: [firstName, lastName, phone]
            packagePurchaseId: { type: ["string", "null"] }
    EmployeeAppointmentSeriesInput:
      allOf:
        - $ref: "#/components/schemas/AppointmentSeriesPreviewInput"
        - type: object
          properties:
            salonCustomerId: { type: string }
            guest:
              type: object
              properties:
                firstName: { type: string, minLength: 1 }
                lastName: { type: string, minLength: 1 }
                phone: { type: string, minLength: 5 }
                email: { type: string }
              required: [firstName, lastName, phone]
    EmployeeAppointmentsInput:
      type: object
      properties:
        serviceId: { type: string }
        salonCustomerId: { type: string }
        guest:
          type: object
          properties:
            firstName: { type: string, minLength: 1 }
            lastName: { type: string }
            phone: { type: string, minLength: 5 }
            email: { type: string }
          required: [firstName, phone]
        slots:
          type: array
          minItems: 1
          maxItems: 12
          items: { $ref: "#/components/schemas/AppointmentSlot" }
      required: [serviceId, slots]
      description: Supply exactly one of salonCustomerId or guest.
    EmployeeAppointmentResultItem:
      type: object
      properties:
        id: { type: string }
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        status: { type: string, enum: [pending, confirmed, completed, cancelled, no-show] }
        allocatedResources:
          type: array
          items: { type: object, additionalProperties: true }
      required: [id, date, startTime, status, allocatedResources]
    EmployeeAppointmentsResult:
      type: object
      properties:
        appointments:
          type: array
          items: { $ref: "#/components/schemas/EmployeeAppointmentResultItem" }
      required: [appointments]
    IdempotencyMismatchError:
      type: object
      properties:
        code: { type: string, enum: [IDEMPOTENCY_KEY_REUSED] }
        error: { type: string }
      required: [code, error]
    AppointmentSeriesResult:
      type: object
      properties:
        id: { type: string }
        totalAppointments: { type: number }
        appointments:
          type: array
          items: { $ref: "#/components/schemas/Appointment" }
      required: [id, totalAppointments, appointments]
    AppointmentSeriesCancellation:
      type: object
      properties:
        id: { type: string }
        cancelledAppointments: { type: number }
      required: [id, cancelledAppointments]
    AppointmentSeriesMoveInput:
      type: object
      properties:
        dayOffset: { type: number, minimum: -365, maximum: 365, multipleOf: 1 }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
    AppointmentSeriesMovePreviewSlot:
      type: object
      properties:
        appointmentId: { type: string }
        currentDate: { type: string, format: date }
        currentStartTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        endTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        available: { type: boolean }
        reason: { type: ["string", "null"] }
      required: [appointmentId, currentDate, currentStartTime, date, startTime, endTime, available, reason]
    AppointmentSeriesMovePreview:
      type: object
      properties:
        slots:
          type: array
          items: { $ref: "#/components/schemas/AppointmentSeriesMovePreviewSlot" }
        allAvailable: { type: boolean }
      required: [slots, allAvailable]
    AppointmentSeriesMoveResult:
      type: object
      properties:
        id: { type: string }
        movedAppointments: { type: number }
        appointments:
          type: array
          items: { $ref: "#/components/schemas/Appointment" }
      required: [id, movedAppointments, appointments]
    CustomerAppointmentSeries:
      type: object
      properties:
        id: { type: string }
        serviceName: { type: string }
        totalAppointments: { type: number }
        completedAppointments: { type: number }
        upcomingAppointments: { type: number }
      required: [id, serviceName, totalAppointments, completedAppointments, upcomingAppointments]
    AppointmentConflictError:
      type: object
      properties:
        error: { type: string, description: "Localized human-readable message" }
        code:
          type: string
          enum: [PACKAGE_ERROR]
          description: "Stable discriminator; present only for package redemption failures"
        reason:
          type: string
          enum: [not_found, wrong_salon, wrong_customer, already_redeemed, no_sessions_left, expired, not_active, service_not_covered, appointment_not_eligible]
          description: "Specific package redemption failure reason (present only when code=PACKAGE_ERROR)"
      required: [error]
    BookingCapacityError:
      type: object
      properties:
        code:
          type: string
          enum: [BOOKING_CAPACITY]
          description: Stable discriminator for retryable booking admission overload.
        error: { type: string, description: "Localized human-readable message" }
      required: [code, error]
    EmployeeLeaveRequest:
      type: object
      description: A single employee leave request row.
      properties:
        id: { type: string }
        employeeId: { type: string }
        startDate: { type: string, format: date, description: "Leave start date (YYYY-MM-DD)" }
        endDate: { type: string, format: date, description: "Leave end date (YYYY-MM-DD)" }
        reason: { type: string }
        status: { type: string, enum: [pending, approved, rejected] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, employeeId, startDate, endDate, reason, status, createdAt]
    SalonLeaveRequest:
      allOf:
        - $ref: "#/components/schemas/EmployeeLeaveRequest"
        - type: object
          properties:
            employeeName: { type: string }
          required: [employeeName]
    EmployeeLeaveRequestCreate:
      type: object
      properties:
        startDate: { type: string, format: date, description: "Leave start date (YYYY-MM-DD)" }
        endDate: { type: string, format: date, description: "Leave end date (YYYY-MM-DD); must be >= startDate" }
        reason: { type: string, minLength: 1 }
      required: [startDate, endDate, reason]
    LeaveRequestReviewBody:
      type: object
      properties:
        status: { type: string, enum: [approved, rejected] }
      required: [status]
    LeaveRequestReviewResult:
      type: object
      properties:
        id: { type: string }
        status: { type: string, enum: [approved, rejected] }
      required: [id, status]
    SalonCustomerUpdate:
      type: object
      properties:
        smsOptOut: { type: boolean }
        birthDate: { type: ["string", "null"], format: date, description: "Customer date of birth for birthday automation (YYYY-MM-DD)" }
    SalonAppointmentCreate:
      type: object
      properties:
        serviceId: { type: string }
        employeeId: { type: ["string", "null"] }
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        notes: { type: string }
        salonCustomerId: { type: string }
        packagePurchaseId: { type: ["string", "null"] }
        guest:
          type: object
          properties:
            firstName: { type: string, minLength: 1 }
            lastName: { type: string, minLength: 1 }
            phone: { type: string, minLength: 5 }
            email: { type: string }
          required: [firstName, lastName, phone]
      required: [serviceId, date, startTime]
    SalonPackageAppointmentSlot:
      type: object
      additionalProperties: false
      properties:
        serviceId: { type: string }
        date: { type: string, format: date }
        startTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        employeeId: { type: ["string", "null"] }
      required: [serviceId, date, startTime]
    SalonPackageAppointmentsInput:
      type: object
      additionalProperties: false
      properties:
        packagePurchaseId: { type: string }
        slots:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: "#/components/schemas/SalonPackageAppointmentSlot" }
      required: [packagePurchaseId, slots]
    SalonPackageAppointmentsPreview:
      type: object
      properties:
        slots:
          type: array
          items:
            allOf:
              - $ref: "#/components/schemas/SalonPackageAppointmentSlot"
              - type: object
                properties:
                  available: { type: boolean }
                  reason: { type: ["string", "null"] }
                required: [available, reason]
        allAvailable: { type: boolean }
        packageEligible: { type: boolean }
        packageReason: { type: ["string", "null"] }
        remainingSessions: { type: number }
      required: [slots, allAvailable, packageEligible, packageReason, remainingSessions]
    SalonPackageAppointmentsResult:
      type: object
      properties:
        series:
          type: array
          items: { $ref: "#/components/schemas/AppointmentSeriesResult" }
        remainingSessions: { type: number }
      required: [series, remainingSessions]
    SmsDelivery:
      type: object
      properties:
        id: { type: string }
        salonName: { type: ["string", "null"] }
        recipientPhone: { type: string }
        messageType: { type: string, enum: [appointment_confirmation, appointment_reminder] }
        status: { type: string, enum: [queued, sent, failed, skipped] }
        errorMessage: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, recipientPhone, messageType, status, createdAt]
    CancelAppointmentInput:
      type: object
      properties:
        reason: { type: string }
    CustomerDashboard:
      type: object
      properties:
        upcoming: { type: array, items: { $ref: "#/components/schemas/Appointment" } }
        recentSalons: { type: array, items: { $ref: "#/components/schemas/SalonCard" } }
        recommendations: { type: array, items: { $ref: "#/components/schemas/SalonCard" } }
        favoriteCount: { type: number }
        visitCount: { type: number }
      required: [upcoming, recentSalons, recommendations, favoriteCount, visitCount]
    FavoriteInput:
      type: object
      properties:
        salonId: { type: string }
      required: [salonId]
    FavoriteResult:
      type: object
      properties:
        salonId: { type: string }
        favorited: { type: boolean }
      required: [salonId, favorited]
    SalonDashboard:
      type: object
      properties:
        scope: { type: string, enum: [location, all] }
        loyaltyScope: { type: string, enum: [owner] }
        salon: { $ref: "#/components/schemas/SalonCard" }
        locations:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              revenueThisMonth: { type: number }
              bookingsThisMonth: { type: number }
              newCustomers: { type: number }
            required: [id, name, revenueThisMonth, bookingsThisMonth, newCustomers]
        todayAppointments: { type: array, items: { $ref: "#/components/schemas/Appointment" } }
        revenueThisMonth: { type: number }
        bookingsThisMonth: { type: number }
        newCustomers: { type: number }
        rating: { type: number }
        revenueChange: { type: number }
        loyalty: { $ref: "#/components/schemas/LoyaltyStatus" }
      required: [scope, loyaltyScope, salon, locations, todayAppointments, revenueThisMonth, bookingsThisMonth, newCustomers, rating, revenueChange, loyalty]
    ServiceInput:
      type: object
      properties:
        category: { type: string }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        description: { type: string }
        durationMinutes: { type: number, minimum: 5 }
        bufferMinutes: { type: number, minimum: 0, default: 0 }
        price: { type: number, minimum: 0 }
        promoPrice: { type: ["number", "null"] }
        imageUrl: { type: string }
        active: { type: boolean }
        homeServiceAvailable: { type: boolean }
        homeServiceFee: { type: number, minimum: 0 }
        homeServiceMinimumOrder: { type: ["number", "null"], minimum: 0 }
        resourceRequirements:
          type: array
          maxItems: 20
          items: { $ref: "#/components/schemas/ServiceResourceRequirement" }
      required: [category, name, description, durationMinutes, price, imageUrl, active, homeServiceAvailable, homeServiceFee]
    RetailCartItemInput:
      oneOf:
        - type: object
          additionalProperties: false
          properties:
            productId: { type: string, minLength: 1 }
            variantValue: { type: string, minLength: 1, maxLength: 120 }
            quantity: { type: integer, minimum: 1, maximum: 100 }
          required: [productId, quantity]
        - type: object
          additionalProperties: false
          properties:
            bundleId: { type: string, minLength: 1 }
            quantity: { type: integer, minimum: 1, maximum: 100 }
          required: [bundleId, quantity]
    RetailCart:
      type: object
      properties:
        id: { type: string }
        itemCount: { type: integer, minimum: 0 }
        subtotal: { type: integer, minimum: 0 }
        referralCreditMerchandiseSubtotalRsd: { type: integer, minimum: 0 }
        items:
          type: array
          items:
            oneOf:
              - type: object
                properties:
                  id: { type: string }
                  productId: { type: string }
                  kind: { type: string, enum: [product] }
                  name: { type: string }
                  imageUrl: { type: string }
                  sku: { type: string, description: "Immutable customer-facing catalog reference; retained under the legacy sku response key." }
                  variantValue: { type: ["string", "null"] }
                  variantLabel: { type: ["string", "null"] }
                  quantity: { type: integer, minimum: 1 }
                  unitPrice: { type: integer, minimum: 0 }
                  lineTotal: { type: integer, minimum: 0 }
                  lowStock: { type: boolean }
                required: [id, kind, productId, name, imageUrl, sku, variantValue, variantLabel, quantity, unitPrice, lineTotal, lowStock]
              - type: object
                properties:
                  id: { type: string }
                  bundleId: { type: string }
                  kind: { type: string, enum: [bundle] }
                  name: { type: string }
                  imageUrl: { type: ["string", "null"] }
                  sku: { type: ["string", "null"] }
                  quantity: { type: integer, minimum: 1 }
                  unitPrice: { type: integer, minimum: 0 }
                  lineTotal: { type: integer, minimum: 0 }
                  lowStock: { type: boolean }
                required: [id, kind, bundleId, name, imageUrl, sku, quantity, unitPrice, lineTotal, lowStock]
        savedItems:
          type: array
          items: { $ref: "#/components/schemas/SavedCartItem" }
        crossSellProducts:
          type: array
          items: { $ref: "#/components/schemas/CartCrossSellProduct" }
        freeShippingProgress: { $ref: "#/components/schemas/FreeShippingProgress" }
        estimatedDeliveryDate: { type: string, format: date }
        showLoyaltyPoints: { type: boolean }
        currentLoyaltyPoints: { type: integer, minimum: 0 }
        projectedLoyaltyPoints: { type: integer, minimum: 0 }
      required: [id, itemCount, subtotal, referralCreditMerchandiseSubtotalRsd, items, savedItems, crossSellProducts, freeShippingProgress, estimatedDeliveryDate, showLoyaltyPoints, currentLoyaltyPoints, projectedLoyaltyPoints]
    CartCrossSellProduct:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        brand: { type: ["string", "null"] }
        price: { type: integer, minimum: 0 }
        discountPrice: { type: ["integer", "null"], minimum: 0 }
      required: [id, name, imageUrl, brand, price, discountPrice]
    FreeShippingProgress:
      type: object
      properties:
        threshold: { type: integer, minimum: 0 }
        subtotal: { type: integer, minimum: 0 }
        remaining: { type: integer, minimum: 0 }
        qualifies: { type: boolean }
        loyaltyFreeShipping: { type: boolean }
      required: [threshold, subtotal, remaining, qualifies, loyaltyFreeShipping]
    AdminShopSettings:
      type: object
      properties:
        showLoyaltyPoints: { type: boolean }
        pointsPer100Rsd: { type: integer, minimum: 0 }
        lowStockThreshold: { type: integer, minimum: 1 }
        defaultDeliveryBusinessDays: { type: integer, minimum: 1, maximum: 365 }
        retailCartReminderEnabled: { type: boolean }
        retailCartReminderDelayHours: { type: integer, minimum: 1, maximum: 720 }
        retailCartReminderBrevoTemplateId: { type: [integer, "null"], minimum: 1 }
        freeShippingThreshold: { type: integer, minimum: 0 }
        version: { type: integer, minimum: 1 }
        updatedAt: { type: string, format: date-time }
        seller: { $ref: "#/components/schemas/SellerIdentity" }
      required: [showLoyaltyPoints, pointsPer100Rsd, lowStockThreshold, defaultDeliveryBusinessDays, retailCartReminderEnabled, retailCartReminderDelayHours, retailCartReminderBrevoTemplateId, freeShippingThreshold, version, updatedAt, seller]
    AdminShopSettingsInput:
      type: object
      additionalProperties: false
      properties:
        showLoyaltyPoints: { type: boolean }
        pointsPer100Rsd: { type: integer, minimum: 0 }
        lowStockThreshold: { type: integer, minimum: 1 }
        defaultDeliveryBusinessDays: { type: integer, minimum: 1, maximum: 365 }
        retailCartReminderEnabled: { type: boolean }
        retailCartReminderDelayHours: { type: integer, minimum: 1, maximum: 720 }
        retailCartReminderBrevoTemplateId: { type: [integer, "null"], minimum: 1 }
        freeShippingThreshold: { type: integer, minimum: 0 }
        version: { type: integer, minimum: 1 }
        seller: { $ref: "#/components/schemas/SellerIdentity" }
      required: [showLoyaltyPoints, pointsPer100Rsd, lowStockThreshold, defaultDeliveryBusinessDays, freeShippingThreshold, version]
    RetailCartContactInput:
      type: object
      additionalProperties: false
      properties:
        email: { type: string, minLength: 3, maxLength: 320 }
      required: [email]
    SellerIdentity:
      type: object
      additionalProperties: false
      properties:
        companyName: { type: string, minLength: 1, maxLength: 300 }
        taxId: { type: string, minLength: 1, maxLength: 300 }
        registrationNumber: { type: string, minLength: 1, maxLength: 300 }
        address: { type: string, minLength: 1, maxLength: 300 }
        city: { type: string, minLength: 1, maxLength: 300 }
        postalCode: { type: string, minLength: 1, maxLength: 300 }
        bankAccount: { type: string, minLength: 1, maxLength: 300 }
        contactEmail: { type: string, minLength: 1, maxLength: 300 }
        contactPhone: { type: string, minLength: 1, maxLength: 300 }
      required: [companyName, taxId, registrationNumber, address, city, postalCode, bankAccount, contactEmail, contactPhone]
    CouponInput:
      type: object
      additionalProperties: false
      properties:
        code: { type: string, pattern: '^[A-Za-z0-9_-]{2,40}$' }
        active: { type: boolean }
        audience: { type: ["string", "null"], enum: [B2B, B2C, null] }
        discountType: { type: string, enum: [PERCENTAGE, FIXED_RSD] }
        discountValue: { type: integer, minimum: 1 }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        minimumSpendRsd: { type: integer, minimum: 0 }
        maximumSpendRsd: { type: ["integer", "null"], minimum: 0 }
        freeShipping: { type: boolean }
        includeProductIds: { type: array, items: { type: string } }
        excludeProductIds: { type: array, items: { type: string } }
        includeCategoryIds: { type: array, items: { type: string } }
        excludeCategoryIds: { type: array, items: { type: string } }
        includeBundleIds: { type: array, items: { type: string } }
        excludeBundleIds: { type: array, items: { type: string } }
        usageLimit: { type: ["integer", "null"], minimum: 1 }
        perCustomerUsageLimit: { type: ["integer", "null"], minimum: 1 }
      required: [code, discountType, discountValue]
    Coupon:
      allOf:
        - $ref: "#/components/schemas/CouponInput"
        - type: object
          properties:
            id: { type: string }
            usageCount: { type: integer, minimum: 0 }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
          required: [id, active, audience, startsAt, endsAt, minimumSpendRsd, maximumSpendRsd, freeShipping, includeProductIds, excludeProductIds, includeCategoryIds, excludeCategoryIds, includeBundleIds, excludeBundleIds, usageLimit, perCustomerUsageLimit, usageCount, createdAt, updatedAt]
    CouponApplication:
      type: object
      properties:
        code: { type: string }
        discountRsd: { type: integer, minimum: 0 }
        freeShipping: { type: boolean }
        allocations:
          type: object
          additionalProperties: { type: integer, minimum: 0 }
      required: [code, discountRsd, freeShipping, allocations]
    ApprovalRequestInput:
      type: object
      additionalProperties: false
      properties:
        idempotencyKey: { type: string, minLength: 1, maxLength: 200 }
        couponCode: { type: string, maxLength: 40 }
        desiredReferralCreditRsd: { type: integer, minimum: 0 }
      required: [idempotencyKey]
    ApprovalRejectInput:
      type: object
      additionalProperties: false
      properties:
        reason: { type: string, maxLength: 1000 }
    ApprovalRequest:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        status: { type: string, enum: [PENDING, APPROVED, REJECTED, EXPIRED] }
        employeeId: { type: string }
        employeeName: { type: string }
        locationName: { type: string }
        quote: { type: integer, minimum: 0 }
        quoteVersion: { type: string }
        couponCode: { type: ["string", "null"], maxLength: 40 }
        referralCreditIntentRsd: { type: integer, minimum: 0 }
        reviewerReason: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        decidedAt: { type: [string, "null"], format: date-time }
        finalizedOrderId: { type: [string, "null"] }
        lines:
          type: array
          items:
            type: object
            additionalProperties: false
            properties:
              productId: { type: ["string", "null"] }
              bundleId: { type: ["string", "null"] }
              productName: { type: string }
              sku: { type: ["string", "null"] }
              quantity: { type: integer, minimum: 1 }
              catalog:
                type: object
                additionalProperties: false
                properties:
                  price: { type: integer, minimum: 0 }
                  listPrice: { type: integer, minimum: 0 }
                  variantValue: { type: ["string", "null"] }
                required: [price, listPrice, variantValue]
            required: [productId, bundleId, productName, sku, quantity, catalog]
      required: [id, status, employeeId, employeeName, locationName, quote, quoteVersion, couponCode, referralCreditIntentRsd, reviewerReason, createdAt, decidedAt, finalizedOrderId, lines]
    SavedCartItem:
      type: object
      properties:
        id: { type: string }
        productId: { type: ["string", "null"] }
        bundleId: { type: ["string", "null"] }
        variantValue: { type: ["string", "null"] }
        quantity: { type: integer, minimum: 1 }
      required: [id, productId, bundleId, variantValue, quantity]
    ReorderResult:
      type: object
      properties:
        added: { type: array, items: { type: object, additionalProperties: true } }
        skipped: { type: array, items: { type: object, additionalProperties: true } }
        adjusted: { type: array, items: { type: object, additionalProperties: true } }
        cart: { oneOf: [{ $ref: "#/components/schemas/RetailCart" }, { $ref: "#/components/schemas/ShopCart" }] }
      required: [added, skipped, adjusted, cart]
    RetailCartSummary:
      type: object
      properties:
        itemCount: { type: integer, minimum: 0 }
      required: [itemCount]
    RetailCheckoutInput:
      type: object
      additionalProperties: false
      properties:
        idempotencyKey: { type: string, minLength: 16, maxLength: 200 }
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, minLength: 5, maxLength: 320, pattern: '^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$' }
        phone: { type: string, minLength: 6, maxLength: 40 }
        street: { type: string, minLength: 2, maxLength: 250 }
        city: { type: string, minLength: 2, maxLength: 120 }
        postalCode: { type: string, minLength: 3, maxLength: 20 }
        note: { type: string, maxLength: 1000 }
        paymentMethod: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
        deliveryMethod: { type: string, enum: [courier, personal_belgrade] }
        expectedSubtotal: { type: integer, minimum: 0, description: "Optional subtotal from the displayed checkout quote; checkout rejects a changed quote." }
        expectedShippingCost: { type: integer, minimum: 0, description: "Optional delivery amount from the displayed checkout quote; checkout rejects a changed quote." }
        expectedTotal: { type: integer, minimum: 0, description: "Optional final amount from the displayed checkout quote; checkout rejects a changed quote." }
        desiredReferralCreditRsd: { type: integer, minimum: 0, default: 0 }
        couponCode: { type: ["string", "null"], maxLength: 40 }
      required: [idempotencyKey, firstName, lastName, email, phone, street, city, postalCode, paymentMethod]
    RetailCheckoutPreview:
      type: object
      properties:
        cart: { $ref: "#/components/schemas/RetailCart" }
        shipping: { $ref: "#/components/schemas/ShippingQuote" }
        total: { type: integer, minimum: 0 }
        referralCreditAvailableRsd: { type: integer, minimum: 0 }
        referralCreditAppliedRsd: { type: integer, minimum: 0 }
        merchandiseSubtotalRsd: { type: integer, minimum: 0 }
        shippingRsd: { type: integer, minimum: 0 }
        payableTotalRsd: { type: integer, minimum: 0 }
        coupon: { oneOf: [{ $ref: "#/components/schemas/CouponApplication" }, { type: "null" }] }
        couponDiscountRsd: { type: integer, minimum: 0 }
        personalizedTreatmentBundleDiscountRsd: { type: integer, minimum: 0 }
        postTreatmentRecommendationDiscountRsd: { type: integer, minimum: 0 }
        automaticPromotionDiscountRsd: { type: integer, minimum: 0 }
        thresholdRewardDiscountRsd: { type: integer, minimum: 0 }
        thresholdQualificationSubtotalRsd: { type: integer, minimum: 0 }
        thresholdFreeShipping: { type: boolean }
        thresholdReached: { type: array, items: { $ref: "#/components/schemas/CheckoutThresholdRewardDescriptor" } }
        thresholdNext: { oneOf: [{ $ref: "#/components/schemas/CheckoutThresholdNextDescriptor" }, { type: "null" }] }
        rewardGifts: { type: array, items: { $ref: "#/components/schemas/CheckoutRewardGiftDescriptor" } }
        paymentMethods:
          type: array
          items: { type: string, enum: [BANK_TRANSFER, CASH_ON_DELIVERY] }
      required: [cart, shipping, total, paymentMethods, referralCreditAvailableRsd, referralCreditAppliedRsd, merchandiseSubtotalRsd, shippingRsd, payableTotalRsd, coupon, couponDiscountRsd, personalizedTreatmentBundleDiscountRsd, postTreatmentRecommendationDiscountRsd, automaticPromotionDiscountRsd, thresholdRewardDiscountRsd, thresholdQualificationSubtotalRsd, thresholdFreeShipping, thresholdReached, thresholdNext, rewardGifts]
    RetailOrder:
      type: object
      properties:
        id: { type: string }
        orderNumber: { type: string }
        status: { type: string }
        fulfillmentStatus: { type: string, enum: [RECEIVED, PREPARING, PACKING, SHIPPED, COMPLETED, CANCELLED] }
        trackingNumber: { type: ["string", "null"] }
        trackingUrl: { type: ["string", "null"] }
        paymentMethod: { type: string }
        paymentStatus: { type: string }
        deliveryMethod: { type: string }
        subtotal: { type: integer }
        shippingCost: { type: integer }
        total: { type: integer }
        couponCode: { type: ["string", "null"] }
        couponDiscountRsd: { type: integer, minimum: 0 }
        couponFreeShipping: { type: boolean }
        referralCreditMerchandiseSubtotalRsd: { type: integer, minimum: 0 }
        referralCreditPreCreditPayableTotalRsd: { type: integer, minimum: 0 }
        referralCreditAppliedRsd: { type: integer, minimum: 0 }
        createdAt: { type: string, format: date-time }
        items:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              productId: { type: string }
              name: { type: string }
              imageUrl: { type: string }
              sku: { type: string, description: "Immutable customer-facing catalog reference." }
              quantity: { type: integer }
              unitPrice: { type: integer }
            required: [id, productId, name, imageUrl, sku, quantity, unitPrice]
      required: [id, orderNumber, status, fulfillmentStatus, trackingNumber, trackingUrl, paymentMethod, paymentStatus, deliveryMethod, subtotal, shippingCost, total, referralCreditMerchandiseSubtotalRsd, referralCreditPreCreditPayableTotalRsd, referralCreditAppliedRsd, createdAt, items]
    RetailTrackingLookupInput:
      type: object
      additionalProperties: false
      properties:
        orderNumber: { type: string, minLength: 1, maxLength: 80 }
        email: { type: string, minLength: 5, maxLength: 320, pattern: '^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$' }
      required: [orderNumber, email]
    PublicRetailOrderTracking:
      type: object
      additionalProperties: false
      properties:
        orderNumber: { type: string }
        status: { type: string, enum: [RECEIVED, PREPARING, PACKING, SHIPPED, COMPLETED, CANCELLED] }
        statusLabel: { type: string }
        createdAt: { type: string, format: date-time }
        statusUpdatedAt: { type: string, format: date-time }
        progressStage: { type: integer, minimum: 0, maximum: 5 }
        trackingNumber: { type: ["string", "null"] }
        courierUrl: { type: ["string", "null"] }
      required: [orderNumber, status, statusLabel, createdAt, statusUpdatedAt, progressStage, trackingNumber, courierUrl]
    AdminFulfillmentUpdate:
      type: object
      additionalProperties: false
      properties:
        fulfillmentStatus: { type: string, enum: [RECEIVED, PREPARING, PACKING, SHIPPED, COMPLETED, CANCELLED] }
        status: { type: string, enum: [pending, confirmed, paid, processing, shipped, delivered, cancelled], deprecated: true }
        trackingNumber: { type: ["string", "null"], maxLength: 120 }
        trackingUrl: { type: ["string", "null"], format: uri, maxLength: 1000 }
      anyOf:
        - required: [fulfillmentStatus]
        - required: [status]
    FulfillmentHistoryEntry:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        actorName: { type: string }
        field: { type: string }
        previousValue: { type: ["string", "null"] }
        nextValue: { type: ["string", "null"] }
        note: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, actorName, field, previousValue, nextValue, note, createdAt]
    AdminRetailOrder:
      allOf:
        - { $ref: "#/components/schemas/RetailOrder" }
        - type: object
          properties:
            fulfillmentHistory: { type: array, items: { $ref: "#/components/schemas/FulfillmentHistoryEntry" } }
          required: [fulfillmentHistory]
    AdminProfitabilityKpis:
      type: object
      additionalProperties: false
      properties:
        revenueRsd: { type: integer }
        cogsRsd: { type: integer }
        profitRsd: { type: integer }
        marginPercent: { type: ["number", "null"] }
        units: { type: integer, minimum: 0 }
      required: [revenueRsd, cogsRsd, profitRsd, marginPercent, units]
    AdminProfitabilityPoint:
      type: object
      additionalProperties: false
      properties:
        period: { type: string }
        revenueRsd: { type: integer }
        cogsRsd: { type: integer }
        profitRsd: { type: integer }
        marginPercent: { type: ["number", "null"] }
      required: [period, revenueRsd, cogsRsd, profitRsd, marginPercent]
    AdminProfitabilityProductRow:
      type: object
      additionalProperties: false
      properties:
        productId: { type: ["string", "null"] }
        productName: { type: string }
        supplierId: { type: string }
        supplierName: { type: string }
        categoryId: { type: ["string", "null"] }
        categoryName: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        units: { type: integer, minimum: 0 }
        realizedRevenueRsd: { type: integer }
        cogsRsd: { type: integer }
        profitRsd: { type: integer }
        marginPercent: { type: ["number", "null"] }
        averageDiscountPercent: { type: ["number", "null"] }
      required: [productId, productName, supplierId, supplierName, categoryId, categoryName, brand, units, realizedRevenueRsd, cogsRsd, profitRsd, marginPercent, averageDiscountPercent]
    AdminProfitabilityReport:
      type: object
      additionalProperties: false
      properties:
        kpis: { $ref: "#/components/schemas/AdminProfitabilityKpis" }
        timeSeries: { type: array, maxItems: 366, items: { $ref: "#/components/schemas/AdminProfitabilityPoint" } }
        products: { type: array, maxItems: 200, items: { $ref: "#/components/schemas/AdminProfitabilityProductRow" } }
        treatment: { type: string }
      required: [kpis, timeSeries, products, treatment]
    ProductCategory:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        sortOrder: { type: number }
        icon: { type: ["string", "null"] }
        subcategories:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              slug: { type: string }
              sortOrder: { type: number }
            required: [id, name, slug, sortOrder]
      required: [id, name, slug, sortOrder, subcategories]
    Product:
      type: object
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        category: { type: string }
        categoryId: { type: ["string", "null"] }
        subcategory: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        description: { type: string }
        imageUrl: { type: string }
        price: { type: number, multipleOf: 1 }
        discountPrice: { type: ["number", "null"], multipleOf: 1 }
        saleEndsAt: { type: ["string", "null"], format: date-time }
        discountPercent: { type: ["number", "null"] }
        stock: { type: number, minimum: 0, multipleOf: 1 }
        sku: { type: string }
        unit: { type: string }
        isNew: { type: boolean }
        isBestseller: { type: boolean }
        weightGrams: { type: ["number", "null"], multipleOf: 1 }
        shortDescription: { type: ["string", "null"] }
        images:
          type: array
          items: { type: string }
        variants:
          type: ["array", "null"]
          items:
            type: object
            properties:
              label: { type: string }
              value: { type: string }
              priceAdjust: { type: number, multipleOf: 1 }
              price: { type: number, minimum: 0, multipleOf: 1 }
              sku: { type: string, minLength: 1 }
              stock: { type: number, minimum: 0, multipleOf: 1 }
            required: [label, value]
        variantType: { type: ["string", "null"] }
        averageRating: { type: ["number", "null"] }
        reviewCount: { type: number }
        deliveryBusinessDaysOverride: { type: ["integer", "null"], minimum: 1, maximum: 365 }
        characteristics:
          type: array
          items: { $ref: "#/components/schemas/ProductCharacteristic" }
      required: [id, supplierId, name, category, categoryId, description, imageUrl, price, discountPrice, saleEndsAt, stock, sku, unit, isNew, isBestseller, images, variantType, reviewCount, deliveryBusinessDaysOverride, characteristics]
    ProductList:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Product" }
        total: { type: number }
        page: { type: number }
        pageSize: { type: number }
        totalPages: { type: number }
      required: [items, total, page, pageSize, totalPages]
    BundleComponentInput:
      type: object
      additionalProperties: false
      properties:
        productId: { type: string }
        quantity: { type: integer, minimum: 1 }
        sortOrder: { type: integer, minimum: 0 }
      required: [productId, quantity]
    BundleInput:
      type: object
      properties:
        supplierId: { type: string }
        name: { type: string, minLength: 1, maxLength: 250 }
        description: { type: ["string", "null"], maxLength: 10000 }
        imageUrl: { type: ["string", "null"] }
        market: { type: string, enum: [B2B, B2C, BOTH] }
        b2bPrice: { type: ["integer", "null"], minimum: 1 }
        b2cPrice: { type: ["integer", "null"], minimum: 1 }
        components: { type: array, minItems: 2, items: { $ref: "#/components/schemas/BundleComponentInput" } }
        active: { type: boolean }
      required: [supplierId, name, market, b2bPrice, b2cPrice]
    AdminBundleInput:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string }
        name: { type: string, minLength: 1, maxLength: 250 }
        description: { type: ["string", "null"], maxLength: 10000 }
        imageUrl: { type: ["string", "null"] }
        market: { type: string, enum: [B2B, B2C, BOTH] }
        b2bPrice: { type: ["integer", "null"], minimum: 1 }
        b2cPrice: { type: ["integer", "null"], minimum: 1 }
        components: { type: array, minItems: 2, items: { $ref: "#/components/schemas/BundleComponentInput" } }
        active: { type: boolean }
        aftercareTreatmentTaxonomyId: { type: ["string", "null"] }
      required: [supplierId, name, market, b2bPrice, b2cPrice]
    BundleComponentCard:
      type: object
      description: Safe component identity and quantity. Unit prices are never exposed through bundle catalog endpoints.
      properties:
        productId: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        catalogReference: { type: string }
        quantity: { type: integer, minimum: 1 }
      required: [productId, name, imageUrl, catalogReference, quantity]
    Bundle:
      type: object
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        description: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        market: { type: string, enum: [B2B, B2C, BOTH] }
        b2bPrice: { type: ["integer", "null"] }
        b2cPrice: { type: ["integer", "null"] }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        derivedStock: { type: integer, minimum: 0 }
        components: { type: array, items: { $ref: "#/components/schemas/BundleComponentCard" } }
      required: [id, supplierId, name, description, imageUrl, market, b2bPrice, b2cPrice, active, createdAt, updatedAt, derivedStock, components]
    AdminBundle:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        description: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        market: { type: string, enum: [B2B, B2C, BOTH] }
        b2bPrice: { type: ["integer", "null"] }
        b2cPrice: { type: ["integer", "null"] }
        aftercareTreatmentTaxonomyId: { type: ["string", "null"] }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        derivedStock: { type: integer, minimum: 0 }
        components: { type: array, items: { $ref: "#/components/schemas/BundleComponentCard" } }
      required: [id, supplierId, name, description, imageUrl, market, b2bPrice, b2cPrice, aftercareTreatmentTaxonomyId, active, createdAt, updatedAt, derivedStock, components]
    AftercareTreatmentTaxonomy:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        taxonomyKey: { type: string }
        categoryName: { type: string }
        treatmentName: { type: string }
        searchTerms: { type: array, items: { type: string } }
      required: [id, taxonomyKey, categoryName, treatmentName, searchTerms]
    AftercareSettings:
      type: object
      additionalProperties: false
      properties:
        version: { type: integer, minimum: 1 }
        firstTiming: { type: string, enum: [IMMEDIATE_AFTER_COMPLETION, NEXT_DAY] }
        cooldownDays: { type: integer, minimum: 1, maximum: 3650 }
        secondReminderDelayDays: { type: integer, minimum: 1, maximum: 3650 }
        postTreatmentDiscountEnabled: { type: boolean }
        postTreatmentDiscountPercent: { type: integer, minimum: 0, maximum: 100 }
        postTreatmentDiscountValidityDays: { type: integer, minimum: 1, maximum: 3650 }
        personalizedBundleDiscountPercent: { type: integer, minimum: 1, maximum: 100 }
        combinationWindowDays: { type: integer, minimum: 1, maximum: 3650 }
      required: [version, firstTiming, cooldownDays, secondReminderDelayDays, postTreatmentDiscountEnabled, postTreatmentDiscountPercent, postTreatmentDiscountValidityDays, personalizedBundleDiscountPercent, combinationWindowDays]
    AftercareSettingsUpdate:
      type: object
      additionalProperties: false
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        firstTiming: { type: string, enum: [IMMEDIATE_AFTER_COMPLETION, NEXT_DAY] }
        cooldownDays: { type: integer, minimum: 1, maximum: 3650 }
        secondReminderDelayDays: { type: integer, minimum: 1, maximum: 3650 }
        postTreatmentDiscountEnabled: { type: boolean }
        postTreatmentDiscountPercent: { type: integer, minimum: 0, maximum: 100 }
        postTreatmentDiscountValidityDays: { type: integer, minimum: 1, maximum: 3650 }
        personalizedBundleDiscountPercent: { type: integer, minimum: 1, maximum: 100 }
        combinationWindowDays: { type: integer, minimum: 1, maximum: 3650 }
      required: [expectedVersion, firstTiming, cooldownDays, secondReminderDelayDays, postTreatmentDiscountEnabled, postTreatmentDiscountPercent, postTreatmentDiscountValidityDays, personalizedBundleDiscountPercent, combinationWindowDays]
    CustomerAftercareItemCard:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: ["string", "null"] }
        actionPath: { type: string }
      required: [id, name, imageUrl, actionPath]
    CustomerAftercareRecommendationLine:
      type: object
      additionalProperties: false
      properties:
        kind: { type: string, enum: [PRODUCT, PREMADE_BUNDLE, PERSONALIZED_BUNDLE] }
        item: { type: ["object", "null"], additionalProperties: false, properties: { id: { type: string }, name: { type: string }, imageUrl: { type: ["string", "null"] }, actionPath: { type: string } }, required: [id, name, imageUrl, actionPath] }
        coveredProducts: { type: array, items: { $ref: "#/components/schemas/CustomerAftercareItemCard" } }
        discountPercent: { type: integer, minimum: 0, maximum: 100 }
        validUntil: { type: ["string", "null"], format: date-time }
      required: [kind, item, coveredProducts, discountPercent, validUntil]
    CustomerAftercareRecommendation:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        status: { type: string, enum: [PENDING, ACTIVE, CONVERTED, EXPIRED, CANCELLED] }
        createdAt: { type: string, format: date-time }
        firstSentAt: { type: ["string", "null"], format: date-time }
        secondSentAt: { type: ["string", "null"], format: date-time }
        readAt: { type: ["string", "null"], format: date-time }
        expiresAt: { type: string, format: date-time }
        convertedAt: { type: ["string", "null"], format: date-time }
        treatments: { type: array, items: { type: string } }
        tips: { type: array, items: { type: string } }
        lines: { type: array, items: { $ref: "#/components/schemas/CustomerAftercareRecommendationLine" } }
      required: [id, status, createdAt, firstSentAt, secondSentAt, readAt, expiresAt, convertedAt, treatments, tips, lines]
    AftercareStatisticsKpis:
      type: object
      additionalProperties: false
      properties:
        recommendationsCreated: { type: integer, minimum: 0 }
        firstSent: { type: integer, minimum: 0 }
        secondSent: { type: integer, minimum: 0 }
        replenishmentSent: { type: integer, minimum: 0 }
        convertedRecommendations: { type: integer, minimum: 0 }
        conversionRevenueRsd: { type: integer, minimum: 0 }
        conversionRatePercent: { type: number, minimum: 0, maximum: 100 }
      required: [recommendationsCreated, firstSent, secondSent, replenishmentSent, convertedRecommendations, conversionRevenueRsd, conversionRatePercent]
    AftercareStatisticsTimePoint:
      type: object
      additionalProperties: false
      properties:
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        recommendationsCreated: { type: integer, minimum: 0 }
        firstSent: { type: integer, minimum: 0 }
        secondSent: { type: integer, minimum: 0 }
        replenishmentSent: { type: integer, minimum: 0 }
        convertedRecommendations: { type: integer, minimum: 0 }
        conversionRevenueRsd: { type: integer, minimum: 0 }
      required: [date, recommendationsCreated, firstSent, secondSent, replenishmentSent, convertedRecommendations, conversionRevenueRsd]
    AftercareStatisticsTreatment:
      type: object
      additionalProperties: false
      properties:
        treatmentId: { type: string }
        taxonomyKey: { type: string }
        categoryName: { type: string }
        treatmentName: { type: string }
        recommendationsCreated: { type: integer, minimum: 0 }
        sent: { type: integer, minimum: 0 }
        convertedRecommendations: { type: integer, minimum: 0 }
        conversionRevenueRsd: { type: integer, minimum: 0 }
      required: [treatmentId, taxonomyKey, categoryName, treatmentName, recommendationsCreated, sent, convertedRecommendations, conversionRevenueRsd]
    AftercareStatisticsItem:
      type: object
      additionalProperties: false
      properties:
        kind: { type: string, enum: [PRODUCT, PREMADE_BUNDLE, PERSONALIZED_BUNDLE] }
        itemId: { type: ["string", "null"] }
        itemName: { type: string }
        recommendationsCreated: { type: integer, minimum: 0 }
        sent: { type: integer, minimum: 0 }
        convertedRecommendations: { type: integer, minimum: 0 }
        conversionRevenueRsd: { type: integer, minimum: 0 }
      required: [kind, itemId, itemName, recommendationsCreated, sent, convertedRecommendations, conversionRevenueRsd]
    AftercareStatistics:
      type: object
      additionalProperties: false
      properties:
        kpis: { $ref: "#/components/schemas/AftercareStatisticsKpis" }
        timeSeries: { type: array, maxItems: 366, items: { $ref: "#/components/schemas/AftercareStatisticsTimePoint" } }
        byTreatment: { type: array, maxItems: 200, items: { $ref: "#/components/schemas/AftercareStatisticsTreatment" } }
        byItem: { type: array, maxItems: 200, items: { $ref: "#/components/schemas/AftercareStatisticsItem" } }
      required: [kpis, timeSeries, byTreatment, byItem]
    PublicBundle:
      type: object
      description: Customer-safe fixed-price bundle. Wholesale price and all component unit prices are deliberately omitted.
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        description: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        market: { type: string, enum: [B2C, BOTH] }
        b2cPrice: { type: integer, minimum: 1 }
        derivedStock: { type: integer, minimum: 0 }
        components: { type: array, items: { $ref: "#/components/schemas/BundleComponentCard" } }
      required: [id, supplierId, name, description, imageUrl, market, b2cPrice, derivedStock, components]
    PublicProductVariant:
      type: object
      properties:
        value: { type: string }
        label: { type: string }
        cartEligible: { type: boolean }
        swatch:
          anyOf:
            - { $ref: "#/components/schemas/BulkMatrixSwatch" }
            - { type: "null" }
        imageUrl: { type: ["string", "null"] }
      required: [value, label, cartEligible, swatch, imageUrl]
    PublicProduct:
      type: object
      description: Customer-facing product fields only. B2B price, SKU, stock, weight, variants and salon-only review data are deliberately omitted.
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        category: { type: string }
        categoryId: { type: ["string", "null"] }
        subcategory: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        description: { type: string }
        imageUrl: { type: string }
        images:
          type: array
          items: { type: string }
        price: { type: ["number", "null"], minimum: 1, multipleOf: 1 }
        discountPrice: { type: ["number", "null"], minimum: 1, multipleOf: 1 }
        saleEndsAt: { type: ["string", "null"], format: date-time }
        discountPercent: { type: ["number", "null"] }
        priceOnRequest: { type: boolean }
        cartEligible: { type: boolean }
        unit: { type: string }
        isNew: { type: boolean }
        isBestseller: { type: boolean }
        deliveryBusinessDaysOverride: { type: ["integer", "null"], minimum: 1, maximum: 365 }
        characteristics:
          type: array
          items: { $ref: "#/components/schemas/ProductCharacteristic" }
        subscriptionAllowed: { type: boolean }
        subscriptionDiscountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        loyaltyPricingExcluded: { type: boolean, default: false }
        reviewSummary: { $ref: "#/components/schemas/RetailProductReviewSummary" }
        variantType: { type: ["string", "null"] }
        variants:
          type: array
          items: { $ref: "#/components/schemas/PublicProductVariant" }
      required: [id, supplierId, name, category, categoryId, description, imageUrl, images, price, discountPrice, saleEndsAt, priceOnRequest, cartEligible, unit, isNew, isBestseller, deliveryBusinessDaysOverride, characteristics, subscriptionAllowed, subscriptionDiscountPercent, reviewSummary, variantType, variants]
    RelatedProductCard:
      type: object
      description: Channel-priced related-product allowlist; excludes inventory, SKU, weight, variants, wholesale administration and relationship IDs.
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        brand: { type: ["string", "null"] }
        price: { type: integer, minimum: 0 }
        discountPrice: { type: ["integer", "null"], minimum: 0 }
        saleEndsAt: { type: ["string", "null"], format: date-time }
      required: [id, name, imageUrl, brand, price, discountPrice, saleEndsAt]
    PublicRelatedProductCard:
      type: object
      description: Public retail related-product allowlist with explicit price-on-request state.
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        brand: { type: ["string", "null"] }
        price: { type: ["integer", "null"], minimum: 1 }
        discountPrice: { type: ["integer", "null"], minimum: 1 }
        saleEndsAt: { type: ["string", "null"], format: date-time }
        priceOnRequest: { type: boolean }
        cartEligible: { type: boolean }
      required: [id, name, imageUrl, brand, price, discountPrice, saleEndsAt, priceOnRequest, cartEligible]
    PublicProductList:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/PublicProduct" }
        total: { type: number }
        page: { type: number }
        pageSize: { type: number }
        totalPages: { type: number }
      required: [items, total, page, pageSize, totalPages]
    PublicProductDetail:
      allOf:
        - $ref: "#/components/schemas/PublicProduct"
        - type: object
          properties:
            ingredients: { type: ["string", "null"] }
            usageInstructions: { type: ["string", "null"] }
            productType:
              anyOf: [{ $ref: "#/components/schemas/B2cPublicTaxonomyValue" }, { type: "null" }]
            needTags:
              type: array
              items: { $ref: "#/components/schemas/B2cPublicNeedTag" }
            relatedProducts:
              type: array
              items: { $ref: "#/components/schemas/PublicRelatedProductCard" }
          required: [relatedProducts]
    B2cProductSort:
      type: string
      enum: [RECOMMENDED, PRICE_ASC, PRICE_DESC, NEWEST, BEST_RATED, MOST_POPULAR]
    B2cPublicTaxonomyValue:
      type: object
      additionalProperties: false
      properties: { slug: { type: string }, label: { type: string } }
      required: [slug, label]
    B2cPublicNeedTag:
      type: object
      additionalProperties: false
      properties: { key: { type: string }, label: { type: string } }
      required: [key, label]
    B2cFacetValue:
      type: object
      properties:
        id: { type: ["string", "null"] }
        value: { type: ["string", "null"] }
        label: { type: ["string", "null"] }
        count: { type: integer, minimum: 0 }
      required: [count]
    B2cProductSearchResponse:
      allOf:
        - $ref: "#/components/schemas/PublicProductList"
        - type: object
          properties:
            activeRange:
              type: object
              properties:
                minPrice: { type: ["integer", "null"] }
                maxPrice: { type: ["integer", "null"] }
              required: [minPrice, maxPrice]
            facets:
              type: object
              properties:
                categories: { type: array, items: { $ref: "#/components/schemas/B2cFacetValue" } }
                brands: { type: array, items: { $ref: "#/components/schemas/B2cFacetValue" } }
                productTypes: { type: array, items: { $ref: "#/components/schemas/B2cFacetValue" } }
                needTags: { type: array, items: { $ref: "#/components/schemas/B2cFacetValue" } }
              required: [categories, brands, productTypes, needTags]
          required: [activeRange, facets]
    B2cDictionaryValue:
      type: object
      properties:
        id: { type: string }
        slug: { type: string }
        key: { type: string }
        label: { type: string }
        active: { type: boolean }
        sortOrder: { type: integer }
        version: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, label, active, sortOrder, version, createdAt, updatedAt]
    B2cDictionaryCreate:
      type: object
      additionalProperties: false
      properties:
        slug: { type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$' }
        label: { type: string, minLength: 1 }
        active: { type: boolean }
        sortOrder: { type: integer, minimum: 0 }
      required: [slug, label]
    B2cNeedTagCreate:
      type: object
      additionalProperties: false
      properties:
        key: { type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$' }
        label: { type: string, minLength: 1 }
        active: { type: boolean }
        sortOrder: { type: integer, minimum: 0 }
      required: [key, label]
    B2cDictionaryUpdate:
      type: object
      additionalProperties: false
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        label: { type: string, minLength: 1 }
        active: { type: boolean }
        sortOrder: { type: integer, minimum: 0 }
      required: [expectedVersion]
    B2cReorderInput:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            properties:
              id: { type: string }
              sortOrder: { type: integer, minimum: 0 }
              expectedVersion: { type: integer, minimum: 1 }
            required: [id, sortOrder, expectedVersion]
      required: [items]
    B2cDisplayConfig:
      type: object
      properties:
        defaultSort: { $ref: "#/components/schemas/B2cProductSort" }
        enabledSortOptions: { type: array, items: { $ref: "#/components/schemas/B2cProductSort" } }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
        showOutOfStock: { type: boolean }
        recentlyViewedEnabled: { type: boolean }
        recentlyViewedMax: { type: integer, minimum: 1, maximum: 100 }
      required: [defaultSort, enabledSortOptions, pageSize, showOutOfStock, recentlyViewedEnabled, recentlyViewedMax]
    AdminB2cDisplaySettings:
      allOf:
        - $ref: "#/components/schemas/B2cDisplayConfig"
        - type: object
          properties:
            id: { type: string }
            version: { type: integer }
            updatedAt: { type: string, format: date-time }
          required: [id, version, updatedAt]
    AdminB2cDisplaySettingsInput:
      allOf:
        - $ref: "#/components/schemas/B2cDisplayConfig"
        - type: object
          properties: { expectedVersion: { type: integer, minimum: 1 } }
          required: [expectedVersion]
    PublicB2cBanner:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        desktopImageUrl: { type: string }
        mobileImageUrl: { type: ["string", "null"] }
        headline: { type: string }
        text: { type: ["string", "null"] }
        ctaLabel: { type: ["string", "null"] }
        placement: { type: string, enum: [HERO, BELOW_CATEGORIES, IN_RESULTS] }
        destination: { type: object, additionalProperties: true }
      required: [id, desktopImageUrl, mobileImageUrl, headline, text, ctaLabel, placement, destination]
    AdminB2cBannerInput:
      type: object
      additionalProperties: true
      required: [internalName, supplierId, desktopImageUrl, headline, destinationKind, placement]
    AdminB2cBannerUpdate:
      type: object
      additionalProperties: true
      properties: { expectedVersion: { type: integer, minimum: 1 } }
      required: [expectedVersion]
    ProductReview:
      type: object
      properties:
        id: { type: string }
        salonName: { type: string }
        rating: { type: number, minimum: 1, maximum: 5 }
        comment: { type: string }
        createdAt: { type: string, format: date-time }
        mine: { type: boolean }
      required: [id, salonName, rating, comment, createdAt, mine]
    ProductReviewInput:
      type: object
      properties:
        rating: { type: number, minimum: 1, maximum: 5, multipleOf: 1 }
        comment: { type: string, maxLength: 2000 }
      required: [rating]
    ProductDetail:
      allOf:
        - $ref: "#/components/schemas/Product"
        - type: object
          properties:
            reviews:
              type: array
              items: { $ref: "#/components/schemas/ProductReview" }
            relatedProducts:
              type: array
              items: { $ref: "#/components/schemas/RelatedProductCard" }
          required: [reviews, relatedProducts]
    ProductRelatedDetail:
      allOf:
        - $ref: "#/components/schemas/Product"
        - type: object
          properties:
            relatedProducts:
              type: array
              items: { $ref: "#/components/schemas/RelatedProductCard" }
          required: [relatedProducts]
    ShopSummary:
      type: object
      properties:
        monthlySpend: { type: number }
        nextTierSpend: { type: number }
        amountToNextTier: { type: number }
        currentTier: { type: string }
        subscriptionDue: { type: number }
        subscriptionDiscount: { type: number }
        benefits:
          type: array
          items: { type: string }
        cartCount: { type: number }
      required: [monthlySpend, nextTierSpend, amountToNextTier, currentTier, subscriptionDue, subscriptionDiscount, benefits, cartCount]
    ShopCartItem:
      oneOf:
        - type: object
          properties:
            id: { type: string }
            kind: { type: string, enum: [product] }
            productId: { type: string }
            productName: { type: string }
            productImageUrl: { type: string }
            variantValue: { type: ["string", "null"] }
            variantLabel: { type: ["string", "null"] }
            productSku: { type: ["string", "null"] }
            unitPrice: { type: number, minimum: 0, multipleOf: 1 }
            quantity: { type: number, minimum: 1, multipleOf: 1 }
            lineTotal: { type: number, minimum: 0, multipleOf: 1 }
            availableStock: { type: number, minimum: 0, multipleOf: 1 }
            weightGrams: { type: number, minimum: 0, multipleOf: 1 }
            lowStock: { type: boolean }
          required: [id, kind, productId, productName, productImageUrl, variantValue, variantLabel, productSku, unitPrice, quantity, lineTotal, availableStock, weightGrams, lowStock]
        - type: object
          properties:
            id: { type: string }
            kind: { type: string, enum: [bundle] }
            bundleId: { type: string }
            productName: { type: string }
            productImageUrl: { type: ["string", "null"] }
            variantValue: { type: ["string", "null"] }
            variantLabel: { type: ["string", "null"] }
            productSku: { type: ["string", "null"] }
            unitPrice: { type: number, minimum: 0, multipleOf: 1 }
            quantity: { type: number, minimum: 1, multipleOf: 1 }
            lineTotal: { type: number, minimum: 0, multipleOf: 1 }
            availableStock: { type: number, minimum: 0, multipleOf: 1 }
            weightGrams: { type: number, minimum: 0, multipleOf: 1 }
            lowStock: { type: boolean }
          required: [id, kind, bundleId, productName, productImageUrl, variantValue, variantLabel, productSku, unitPrice, quantity, lineTotal, availableStock, weightGrams, lowStock]
    ShopCart:
      type: object
      properties:
        id: { type: ["string", "null"] }
        items: { type: array, items: { $ref: "#/components/schemas/ShopCartItem" } }
        savedItems: { type: array, items: { $ref: "#/components/schemas/SavedCartItem" } }
        itemCount: { type: number, minimum: 0, multipleOf: 1 }
        subtotal: { type: number, minimum: 0, multipleOf: 1 }
        referralCreditMerchandiseSubtotalRsd: { type: integer, minimum: 0 }
        totalWeightGrams: { type: number, minimum: 0, multipleOf: 1 }
        crossSellProducts: { type: array, items: { $ref: "#/components/schemas/CartCrossSellProduct" } }
        freeShippingProgress: { $ref: "#/components/schemas/FreeShippingProgress" }
        estimatedDeliveryDate: { type: string, format: date }
        showLoyaltyPoints: { type: boolean }
        currentLoyaltyPoints: { type: integer, minimum: 0 }
        projectedLoyaltyPoints: { type: integer, minimum: 0 }
      required: [id, items, savedItems, itemCount, subtotal, referralCreditMerchandiseSubtotalRsd, totalWeightGrams, crossSellProducts, freeShippingProgress, estimatedDeliveryDate, showLoyaltyPoints, currentLoyaltyPoints, projectedLoyaltyPoints]
    B2bOrderImportPreviewInput:
      type: object
      additionalProperties: false
      properties:
        csvText: { type: string, minLength: 1, maxLength: 524288 }
      required: [csvText]
    B2bOrderImportApplyInput:
      type: object
      additionalProperties: false
      properties:
        csvText: { type: string, minLength: 1, maxLength: 524288 }
        confirmed: { type: boolean, default: false }
        idempotencyKey: { type: string, minLength: 8, maxLength: 200 }
      required: [csvText, confirmed, idempotencyKey]
    B2bOrderImportDiagnostic:
      type: object
      properties:
        rowNumber: { type: integer, minimum: 2 }
        sku: { type: ["string", "null"] }
        quantity: { type: ["integer", "null"] }
        status: { type: string, enum: [matched, unmatched, invalid] }
        code: { type: ["string", "null"], enum: [EMPTY_SKU, INVALID_QUANTITY, SKU_NOT_FOUND, SUPPLIER_SCOPE, PRODUCT_UNAVAILABLE, VARIANT_UNAVAILABLE, MOQ_NOT_MET, INSUFFICIENT_STOCK, DUPLICATE_SKU, null] }
        message: { type: ["string", "null"] }
        productId: { type: ["string", "null"] }
        variantValue: { type: ["string", "null"] }
      required: [rowNumber, sku, quantity, status, code, message, productId, variantValue]
    B2bOrderImportResult:
      type: object
      properties:
        applied: { type: boolean }
        idempotent: { type: boolean }
        validRowCount: { type: integer, minimum: 0 }
        invalidRowCount: { type: integer, minimum: 0 }
        matchedRows: { type: array, items: { $ref: "#/components/schemas/B2bOrderImportDiagnostic" } }
        unmatchedRows: { type: array, items: { $ref: "#/components/schemas/B2bOrderImportDiagnostic" } }
        invalidRows: { type: array, items: { $ref: "#/components/schemas/B2bOrderImportDiagnostic" } }
        cart: { oneOf: [{ $ref: "#/components/schemas/ShopCart" }, { type: "null" }] }
      required: [applied, idempotent, validRowCount, invalidRowCount, matchedRows, unmatchedRows, invalidRows, cart]
    ProductWishlistInput:
      type: object
      additionalProperties: false
      properties:
        productId: { type: string, minLength: 1 }
        variantValue: { type: ["string", "null"], minLength: 1 }
      required: [productId]
    ProductWishlistItem:
      type: object
      properties:
        id: { type: string }
        productId: { type: string }
        variantValue: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        available: { type: boolean }
        unavailableReason: { type: ["string", "null"], enum: [PRODUCT_INACTIVE, RETAIL_DISABLED, OUT_OF_STOCK, VARIANT_UNAVAILABLE, null] }
        product: { oneOf: [{ $ref: "#/components/schemas/PublicProduct" }, { type: "null" }] }
      required: [id, productId, variantValue, createdAt, available, unavailableReason, product]
    ProductWishlistToggleResult:
      type: object
      properties:
        saved: { type: boolean }
        item: { oneOf: [{ $ref: "#/components/schemas/ProductWishlistItem" }, { type: "null" }] }
      required: [saved, item]
    ShopCartItemInput:
      oneOf:
        - type: object
          additionalProperties: false
          properties:
            productId: { type: string, minLength: 1 }
            variantValue: { type: string, minLength: 1 }
            quantity: { type: number, minimum: 1, multipleOf: 1 }
          required: [productId]
        - type: object
          additionalProperties: false
          properties:
            bundleId: { type: string, minLength: 1 }
            quantity: { type: number, minimum: 1, multipleOf: 1 }
          required: [bundleId]
    ShopCartItemQuantityInput:
      type: object
      properties:
        quantity: { type: number, minimum: 1, multipleOf: 1 }
      required: [quantity]
    DeliveryAddressInput:
      type: object
      properties:
        recipientName: { type: string, minLength: 1 }
        street: { type: string, minLength: 1 }
        city: { type: string, minLength: 1 }
        postalCode: { type: string }
        phone: { type: string, minLength: 1 }
        email: { type: string, minLength: 3 }
      required: [recipientName, street, city, postalCode, phone, email]
    BillingDetailsInput:
      type: object
      properties:
        companyName: { type: string, minLength: 1 }
        pib: { type: string, minLength: 1 }
        registrationNumber: { type: string, minLength: 1 }
        street: { type: string, minLength: 1 }
        city: { type: string, minLength: 1 }
        postalCode: { type: string, minLength: 1 }
      required: [companyName, pib, registrationNumber, street, city, postalCode]
    ShopCheckoutProfile:
      type: object
      properties:
        profileKey: { type: string, description: Authenticated profile and active-salon scope key for checkout draft isolation. }
        activeSalonId: { type: string }
        salonName: { type: string }
        salonAddress: { $ref: "#/components/schemas/DeliveryAddressInput" }
        billingDefaults:
          type: ["object", "null"]
          allOf:
            - $ref: "#/components/schemas/BillingDetailsInput"
        deliverySalons:
          type: array
          items: { $ref: "#/components/schemas/ShopCheckoutSalon" }
        paymentMethods:
          type: array
          items: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
      required: [profileKey, activeSalonId, salonName, salonAddress, billingDefaults, deliverySalons, paymentMethods]
    ShopCheckoutSalon:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        address: { $ref: "#/components/schemas/DeliveryAddressInput" }
        addressComplete: { type: boolean }
        companyDetails: { $ref: "#/components/schemas/ShopCheckoutCompanyDetails" }
        billingDetails:
          type: ["object", "null"]
          allOf:
            - $ref: "#/components/schemas/BillingDetailsInput"
        billingComplete: { type: boolean }
      required: [id, name, address, addressComplete, companyDetails, billingDetails, billingComplete]
    ShopCheckoutCompanyDetails:
      type: object
      properties:
        companyName: { type: string }
        pib: { type: string }
        registrationNumber: { type: string }
        street: { type: string }
        city: { type: string }
        postalCode: { type: string }
      required: [companyName, pib, registrationNumber, street, city, postalCode]
    ShopCheckoutPreview:
      type: object
      properties:
        cart: { $ref: "#/components/schemas/ShopCart" }
        shipping: { $ref: "#/components/schemas/ShippingQuote" }
        total: { type: number, minimum: 0, multipleOf: 1 }
        referralCreditAvailableRsd: { type: integer, minimum: 0 }
        referralCreditAppliedRsd: { type: integer, minimum: 0 }
        merchandiseSubtotalRsd: { type: integer, minimum: 0 }
        shippingRsd: { type: integer, minimum: 0 }
        payableTotalRsd: { type: integer, minimum: 0 }
        coupon: { oneOf: [{ $ref: "#/components/schemas/CouponApplication" }, { type: "null" }] }
        couponDiscountRsd: { type: integer, minimum: 0 }
        automaticPromotionDiscountRsd: { type: integer, minimum: 0 }
        thresholdRewardDiscountRsd: { type: integer, minimum: 0 }
        thresholdQualificationSubtotalRsd: { type: integer, minimum: 0 }
        rewardGifts: { type: array, items: { $ref: "#/components/schemas/CheckoutRewardGiftDescriptor" } }
        paymentMethods:
          type: array
          items: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
      required: [cart, shipping, total, paymentMethods, referralCreditAvailableRsd, referralCreditAppliedRsd, merchandiseSubtotalRsd, shippingRsd, payableTotalRsd, coupon, couponDiscountRsd, automaticPromotionDiscountRsd, thresholdRewardDiscountRsd, thresholdQualificationSubtotalRsd, rewardGifts]
    ShopCheckoutInput:
      type: object
      properties:
        useSalonAddress: { type: boolean }
        deliverySalonId:
          type: ["string", "null"]
          description: Authorized salon whose registered delivery address is selected. The active salon remains cart and order owner.
        deliveryAddress: { $ref: "#/components/schemas/DeliveryAddressInput" }
        billingDetails:
          type: ["object", "null"]
          allOf:
            - $ref: "#/components/schemas/BillingDetailsInput"
        paymentMethod: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
        deliveryMethod: { type: string, enum: [courier, personal_belgrade] }
        note: { type: ["string", "null"], maxLength: 1000 }
        desiredReferralCreditRsd: { type: integer, minimum: 0, default: 0 }
        expectedSubtotal: { type: integer, minimum: 0 }
        expectedShippingCost: { type: integer, minimum: 0 }
        expectedTotal: { type: integer, minimum: 0 }
        termsAccepted: { type: boolean }
        couponCode: { type: ["string", "null"], maxLength: 40 }
      required: [useSalonAddress, paymentMethod, deliveryMethod, termsAccepted]
    Order:
      type: object
      properties:
        id: { type: string }
        status: { type: string, enum: [pending, confirmed, paid, processing, shipped, delivered, cancelled] }
        fulfillmentStatus: { type: string, enum: [RECEIVED, PREPARING, PACKING, SHIPPED, COMPLETED, CANCELLED] }
        paymentStatus: { type: string, enum: [unpaid, pending, paid, refunded, failed] }
        deliveryMethod: { type: string, enum: [courier, personal_belgrade] }
        courierServiceId: { type: ["string", "null"] }
        courierService: { type: ["string", "null"] }
        trackingNumber: { type: ["string", "null"] }
        trackingUrl: { type: ["string", "null"] }
        total: { type: number, multipleOf: 1 }
        subtotal: { type: number, multipleOf: 1 }
        shippingCost: { type: number, multipleOf: 1 }
        totalWeightGrams: { type: number, multipleOf: 1 }
        couponCode: { type: ["string", "null"] }
        couponDiscountRsd: { type: integer, minimum: 0 }
        couponFreeShipping: { type: boolean }
        referralCreditMerchandiseSubtotalRsd: { type: integer, minimum: 0 }
        referralCreditPreCreditPayableTotalRsd: { type: integer, minimum: 0 }
        referralCreditAppliedRsd: { type: integer, minimum: 0 }
        invoice:
          type: ["object", "null"]
          properties:
            number: { type: string }
            issuedAt: { type: string, format: date-time }
          required: [number, issuedAt]
        itemCount: { type: number }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        salon:
          type: object
          properties:
            id: { type: string }
            name: { type: string }
            phone: { type: string }
            email: { type: string }
            address: { type: string }
            city: { type: string }
            postalCode: { type: ["string", "null"] }
          required: [id, name, phone, email, address, city, postalCode]
        delivery:
          type: object
          properties:
            recipientName: { type: string }
            address: { type: string }
            city: { type: ["string", "null"] }
            postalCode: { type: ["string", "null"] }
            phone: { type: ["string", "null"] }
            note: { type: ["string", "null"] }
            usesSalonAddress: { type: boolean }
          required: [recipientName, address, usesSalonAddress]
        billing:
          type: ["object", "null"]
          properties:
            companyName: { type: string }
            pib: { type: string }
            registrationNumber: { type: string }
            address: { type: string }
            city: { type: string }
            postalCode: { type: string }
          required: [companyName, pib, registrationNumber, address, city, postalCode]
        items: { type: array, items: { $ref: "#/components/schemas/OrderItem" } }
      required: [id, status, fulfillmentStatus, paymentStatus, deliveryMethod, courierServiceId, courierService, trackingNumber, trackingUrl, total, subtotal, shippingCost, totalWeightGrams, couponCode, couponDiscountRsd, couponFreeShipping, referralCreditMerchandiseSubtotalRsd, referralCreditPreCreditPayableTotalRsd, referralCreditAppliedRsd, invoice, itemCount, createdAt, updatedAt, salon, delivery, billing, items]
    SalonNotification:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        message: { type: string }
        href: { type: ["string", "null"] }
        readAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, title, message, href, readAt, createdAt]
    AdminOrder:
      allOf:
        - $ref: "#/components/schemas/Order"
        - type: object
          properties:
            adminNote: { type: ["string", "null"] }
            history: { type: array, items: { $ref: "#/components/schemas/OrderHistoryEvent" } }
          required: [adminNote, history]
    OrderItem:
      type: object
      properties:
        productId: { type: ["string", "null"] }
        bundleId: { type: ["string", "null"] }
        productName: { type: string }
        variantValue: { type: ["string", "null"] }
        variantLabel: { type: ["string", "null"] }
        productSku: { type: ["string", "null"] }
        quantity: { type: number, multipleOf: 1 }
        price: { type: number, multipleOf: 1 }
        couponDiscountRsd: { type: integer, minimum: 0 }
      required: [productId, bundleId, productName, quantity, price]
    OrderInput:
      type: object
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            properties:
              productId: { type: string }
              variantValue: { type: string }
              quantity: { type: number, minimum: 1, multipleOf: 1 }
            required: [productId, quantity]
        useSalonAddress: { type: boolean }
        deliveryAddress:
          type: object
          properties:
            recipientName: { type: string, minLength: 1 }
            street: { type: string, minLength: 1 }
            city: { type: string, minLength: 1 }
            postalCode: { type: string, minLength: 1 }
            phone: { type: string, minLength: 1 }
            note: { type: ["string", "null"] }
          required: [recipientName, street, city, postalCode, phone]
        billingDetails:
          type: ["object", "null"]
          properties:
            companyName: { type: string, minLength: 1 }
            pib: { type: string, minLength: 1 }
            registrationNumber: { type: string, minLength: 1 }
            street: { type: string, minLength: 1 }
            city: { type: string, minLength: 1 }
            postalCode: { type: string, minLength: 1 }
          required: [companyName, pib, registrationNumber, street, city, postalCode]
        shippingName: { type: string }
        shippingAddress: { type: string }
        paymentMethod: { type: string, enum: [CARD, BANK_TRANSFER, CASH_ON_DELIVERY] }
      required: [items, paymentMethod]
    AdminOrderUpdate:
      type: object
      additionalProperties: false
      properties:
        status: { type: string, enum: [confirmed, shipped, delivered, cancelled] }
        fulfillmentStatus: { type: string, enum: [RECEIVED, PREPARING, PACKING, SHIPPED, COMPLETED, CANCELLED] }
        paymentStatus: { type: string, enum: [unpaid, pending, paid, refunded, failed] }
        courierServiceId: { type: ["string", "null"] }
        trackingNumber: { type: ["string", "null"], maxLength: 120 }
        trackingUrl: { type: ["string", "null"], format: uri, maxLength: 1000 }
        adminNote: { type: ["string", "null"], maxLength: 2000 }
    AdminOrderBulkUpdate:
      type: object
      additionalProperties: false
      properties:
        orderIds: { type: array, minItems: 1, items: { type: string } }
        status: { type: string, enum: [confirmed, shipped, delivered, cancelled] }
        paymentStatus: { type: string, enum: [unpaid, pending, paid, refunded, failed] }
      required: [orderIds]
    CourierService:
      type: object
      properties:
        id: { type: string }
        code: { type: string }
        name: { type: string }
        trackingUrlTemplate: { type: ["string", "null"] }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, code, name, trackingUrlTemplate, active, createdAt, updatedAt]
    CourierServiceInput:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        trackingUrlTemplate: { type: ["string", "null"], maxLength: 1000 }
        active: { type: boolean }
      required: [name]
    CourierServiceUpdate:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        trackingUrlTemplate: { type: ["string", "null"], maxLength: 1000 }
        active: { type: boolean }
    OrderHistoryEvent:
      type: object
      properties:
        id: { type: string }
        actorName: { type: string }
        field: { type: string }
        previousValue: { type: ["string", "null"] }
        nextValue: { type: ["string", "null"] }
        note: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, actorName, field, previousValue, nextValue, note, createdAt]
    LoyaltyStatus:
      type: object
      properties:
        currentTier: { type: string }
        monthlySpend: { type: number }
        tierThreshold: { type: number }
        amountToNextTier: { type: number }
        nextTier: { type: ["string", "null"] }
        subscriptionDue: { type: number }
        subscriptionDiscountPercent: { type: number }
        productDiscountPercent: { type: number }
        benefits: { type: array, items: { type: string } }
        freeSubscription: { type: boolean }
      required: [currentTier, monthlySpend, tierThreshold, amountToNextTier, subscriptionDue, subscriptionDiscountPercent, productDiscountPercent, benefits, freeSubscription]
    Course:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        instructor: { type: string }
        instructorProfileId: { type: ["string", "null"] }
        instructorProfile:
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/EducationInstructorSummary"
        publisher: { type: string }
        publisherType: { type: string, enum: [SALON, EDUCATION_CENTER] }
        category: { type: string }
        sectionId: { type: ["string", "null"] }
        sectionName: { type: ["string", "null"] }
        categoryId: { type: ["string", "null"] }
        categoryName: { type: ["string", "null"] }
        subcategoryId: { type: ["string", "null"] }
        subcategoryName: { type: ["string", "null"] }
        courseTypeId: { type: ["string", "null"] }
        courseTypeName: { type: ["string", "null"] }
        taxonomyPath: { type: array, items: { type: string } }
        format: { type: string, enum: [online, in-person, hybrid] }
        city: { type: ["string", "null"] }
        price: { type: number }
        duration: { type: string }
        durationMinutes: { type: ["number", "null"], multipleOf: 1, minimum: 1 }
        level: { type: string, enum: [beginner, intermediate, advanced, all-levels] }
        learningOutcomes: { type: array, items: { type: string } }
        includedItems: { type: array, items: { type: string } }
        requirements: { type: string }
        rating: { type: number }
        reviewCount: { type: integer, minimum: 0 }
        certification: { type: boolean }
        theoryHours: { type: ["number", "null"], minimum: 0 }
        practicalHours: { type: ["number", "null"], minimum: 0 }
        certificateName: { type: ["string", "null"] }
        accredited: { type: boolean }
        language: { type: ["string", "null"] }
        trailerUrl: { type: ["string", "null"] }
        tags: { type: array, items: { type: string } }
        faq:
          type: array
          items:
            type: object
            properties: { question: { type: string }, answer: { type: string } }
            required: [question, answer]
        paymentMode: { type: string, enum: [online_full, live_deposit, live_off_platform] }
        depositAmount: { type: ["number", "null"], minimum: 0 }
        studentCount: { type: number, multipleOf: 1, minimum: 0 }
        completedLearnerCount: { type: number, multipleOf: 1, minimum: 0 }
        inquiryCount30d: { type: number, multipleOf: 1, minimum: 0 }
        viewCount30d: { type: number, multipleOf: 1, minimum: 0 }
        imageUrl: { type: string }
        startDate: { type: ["string", "null"], format: date }
        published: { type: boolean }
        archived: { type: boolean }
        featured: { type: boolean }
        featuredUntil: { type: ["string", "null"], format: date-time }
        publisherVerified: { type: boolean }
        refundPolicy: { type: ["string", "null"] }
        groupDiscountMinimum: { type: ["number", "null"] }
        groupDiscountPercent: { type: ["number", "null"] }
        giftVoucherEligible: { type: boolean }
        schedulingMode: { type: string, enum: [fixed_group, individual_calendar] }
        cancellationCutoffHours: { type: integer, minimum: 0, maximum: 8760 }
        depositDisposition: { type: string, enum: [refund, forfeit, transfer] }
        minimumEnrollmentRiskDeadline: { type: ["string", "null"], format: date-time }
        earlyBirdPrice: { type: ["number", "null"], minimum: 0 }
        earlyBirdCutoff: { type: ["string", "null"], format: date-time }
        installmentCount: { type: integer, enum: [1, 2, 3] }
        centerId: { type: ["string", "null"] }
        availableSeats: { type: ["number", "null"] }
        enrollmentStatus: { type: ["string", "null"], enum: [pending, active, completed, cancelled, null] }
      required: [id, title, description, instructor, publisher, publisherType, category, taxonomyPath, format, price, duration, level, learningOutcomes, includedItems, requirements, rating, reviewCount, certification, accredited, tags, faq, paymentMode, studentCount, inquiryCount30d, viewCount30d, imageUrl, published, archived]
    EducationCourseInput:
      type: object
      properties:
        title: { type: string, minLength: 2 }
        description: { type: string }
        category: { type: string, minLength: 2 }
        categoryId: { type: ["string", "null"] }
        subcategoryId: { type: ["string", "null"] }
        courseTypeId: { type: ["string", "null"] }
        format: { type: string, enum: [online, in-person, hybrid] }
        city: { type: ["string", "null"] }
        price: { type: number, minimum: 0 }
        duration: { type: string, minLength: 1 }
        durationMinutes: { type: ["number", "null"], multipleOf: 1, minimum: 1, maximum: 5256000 }
        level: { type: string, enum: [beginner, intermediate, advanced, all-levels] }
        learningOutcomes: { type: array, maxItems: 20, items: { type: string, minLength: 1, maxLength: 240 } }
        includedItems: { type: array, maxItems: 20, items: { type: string, minLength: 1, maxLength: 240 } }
        requirements: { type: string, maxLength: 2000 }
        certification: { type: boolean }
        theoryHours: { type: ["number", "null"], minimum: 0, maximum: 100000 }
        practicalHours: { type: ["number", "null"], minimum: 0, maximum: 100000 }
        certificateName: { type: ["string", "null"], maxLength: 240 }
        accredited: { type: boolean }
        language: { type: ["string", "null"], maxLength: 80 }
        trailerUrl: { type: ["string", "null"], maxLength: 2000 }
        tags: { type: array, maxItems: 30, items: { type: string, minLength: 1, maxLength: 80 } }
        faq:
          type: array
          maxItems: 30
          items:
            type: object
            properties:
              question: { type: string, minLength: 1, maxLength: 300 }
              answer: { type: string, minLength: 1, maxLength: 2000 }
            required: [question, answer]
        paymentMode: { type: string, enum: [online_full, live_deposit, live_off_platform] }
        depositAmount: { type: ["number", "null"], minimum: 0 }
        imageUrl: { type: string, minLength: 1 }
        startDate: { type: ["string", "null"], format: date }
        refundPolicy: { type: string, minLength: 1, maxLength: 2000 }
        giftVoucherEligible: { type: boolean }
        groupDiscountMinimum: { type: ["number", "null"], minimum: 2, maximum: 999 }
        groupDiscountPercent: { type: ["number", "null"], minimum: 0, maximum: 100 }
        schedulingMode: { type: string, enum: [fixed_group, individual_calendar] }
        cancellationCutoffHours: { type: integer, minimum: 0, maximum: 8760 }
        depositDisposition: { type: string, enum: [refund, forfeit, transfer] }
        minimumEnrollmentRiskDeadline: { type: ["string", "null"], format: date-time }
        earlyBirdPrice: { type: ["number", "null"], minimum: 0 }
        earlyBirdCutoff: { type: ["string", "null"], format: date-time }
        installmentCount: { type: integer, enum: [1, 2, 3] }
      required: [title, category, format, price, duration, imageUrl]
    EducationCourseUpdate:
      type: object
      properties:
        title: { type: string, minLength: 2 }
        description: { type: string }
        category: { type: string, minLength: 2 }
        categoryId: { type: ["string", "null"] }
        subcategoryId: { type: ["string", "null"] }
        courseTypeId: { type: ["string", "null"] }
        format: { type: string, enum: [online, in-person, hybrid] }
        city: { type: ["string", "null"] }
        price: { type: number, minimum: 0 }
        duration: { type: string, minLength: 1 }
        durationMinutes: { type: ["number", "null"], multipleOf: 1, minimum: 1, maximum: 5256000 }
        level: { type: string, enum: [beginner, intermediate, advanced, all-levels] }
        learningOutcomes: { type: array, maxItems: 20, items: { type: string, minLength: 1, maxLength: 240 } }
        includedItems: { type: array, maxItems: 20, items: { type: string, minLength: 1, maxLength: 240 } }
        requirements: { type: string, maxLength: 2000 }
        certification: { type: boolean }
        theoryHours: { type: ["number", "null"], minimum: 0, maximum: 100000 }
        practicalHours: { type: ["number", "null"], minimum: 0, maximum: 100000 }
        certificateName: { type: ["string", "null"], maxLength: 240 }
        accredited: { type: boolean }
        language: { type: ["string", "null"], maxLength: 80 }
        trailerUrl: { type: ["string", "null"], maxLength: 2000 }
        tags: { type: array, maxItems: 30, items: { type: string, minLength: 1, maxLength: 80 } }
        faq:
          type: array
          maxItems: 30
          items:
            type: object
            properties:
              question: { type: string, minLength: 1, maxLength: 300 }
              answer: { type: string, minLength: 1, maxLength: 2000 }
            required: [question, answer]
        paymentMode: { type: string, enum: [online_full, live_deposit, live_off_platform] }
        depositAmount: { type: ["number", "null"], minimum: 0 }
        imageUrl: { type: string, minLength: 1 }
        startDate: { type: ["string", "null"], format: date }
        published: { type: boolean }
        refundPolicy: { type: string, minLength: 1, maxLength: 2000 }
        giftVoucherEligible: { type: boolean }
        groupDiscountMinimum: { type: ["number", "null"], minimum: 2, maximum: 999 }
        groupDiscountPercent: { type: ["number", "null"], minimum: 0, maximum: 100 }
        schedulingMode: { type: string, enum: [fixed_group, individual_calendar] }
        cancellationCutoffHours: { type: integer, minimum: 0, maximum: 8760 }
        depositDisposition: { type: string, enum: [refund, forfeit, transfer] }
        minimumEnrollmentRiskDeadline: { type: ["string", "null"], format: date-time }
        earlyBirdPrice: { type: ["number", "null"], minimum: 0 }
        earlyBirdCutoff: { type: ["string", "null"], format: date-time }
        installmentCount: { type: integer, enum: [1, 2, 3] }
    EducationSession:
      type: object
      properties:
        id: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        location: { type: ["string", "null"] }
        capacity: { type: number }
        reservedSeats: { type: number }
        availableSeats: { type: number }
        minimumEnrollments: { type: ["number", "null"] }
        cancelledAt: { type: ["string", "null"], format: date-time }
        educatorStaffId: { type: ["string", "null"] }
      required: [id, startsAt, endsAt, capacity, reservedSeats, availableSeats]
    EducationSessionInput:
      type: object
      properties:
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        location: { type: ["string", "null"] }
        capacity: { type: number, minimum: 1 }
        minimumEnrollments: { type: ["number", "null"], minimum: 0, maximum: 9999 }
        educatorStaffId: { type: ["string", "null"] }
      required: [startsAt, endsAt, capacity]
    EducationLesson:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        content:
          type: string
          description: Full lesson content. Returned only to the publisher, an administrator, or an authorized LMS enrollment.
        durationMinutes: { type: number }
        sortOrder: { type: number }
        completed: { type: boolean }
      required: [id, title, description, durationMinutes, sortOrder, completed]
    EducationLessonInput:
      type: object
      properties:
        title: { type: string, minLength: 1 }
        description: { type: string }
        content: { type: string }
        durationMinutes: { type: number, minimum: 1 }
        sortOrder: { type: number, minimum: 0 }
      required: [title]
    EducationModule:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        sortOrder: { type: number }
        lessons:
          type: array
          items: { $ref: "#/components/schemas/EducationLesson" }
      required: [id, title, description, sortOrder, lessons]
    EducationModuleInput:
      type: object
      properties:
        title: { type: string, minLength: 1 }
        description: { type: string }
        sortOrder: { type: number, minimum: 0 }
      required: [title]
    EducationCourseDetail:
      allOf:
        - $ref: "#/components/schemas/Course"
        - type: object
          properties:
            publicModules:
              type: array
              items: { $ref: "#/components/schemas/EducationPublicModule" }
            sessions:
              type: array
              items: { $ref: "#/components/schemas/EducationSession" }
            dayProgram:
              type: array
              items: { $ref: "#/components/schemas/EducationCourseDay" }
            gallery:
              type: array
              items: { $ref: "#/components/schemas/EducationMedia" }
            center:
              oneOf:
                - type: "null"
                - $ref: "#/components/schemas/EducationCenterPublic"
            reviews:
              type: array
              items: { $ref: "#/components/schemas/EducationCourseReview" }
          required: [publicModules, sessions, dayProgram, gallery, reviews]
    EducationPrivateCourseDetail:
      allOf:
        - $ref: "#/components/schemas/EducationCourseDetail"
        - type: object
          properties:
            modules:
              type: array
              items: { $ref: "#/components/schemas/EducationModule" }
          required: [modules]
    EducationPublicModule:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        sortOrder: { type: integer, minimum: 0 }
        lessonCount: { type: integer, minimum: 0 }
      required: [id, title, description, sortOrder, lessonCount]
    EducationCourseDay:
      type: object
      properties:
        id: { type: string }
        dayNumber: { type: number, multipleOf: 1, minimum: 1 }
        title: { type: string }
        description: { type: string }
        durationMinutes: { type: ["number", "null"], multipleOf: 1, minimum: 0 }
      required: [id, dayNumber, title, description]
    EducationCourseDaysInput:
      type: object
      properties:
        days:
          type: array
          minItems: 1
          maxItems: 31
          items:
            type: object
            properties:
              dayNumber: { type: number, multipleOf: 1, minimum: 1, maximum: 31 }
              title: { type: string, minLength: 2, maxLength: 160 }
              description: { type: string, maxLength: 2000 }
              durationMinutes: { type: ["number", "null"], multipleOf: 1, minimum: 0, maximum: 1440 }
            required: [dayNumber, title]
      required: [days]
    EducationMedia:
      type: object
      properties:
        id: { type: string }
        url: { type: string }
        altText: { type: string }
        sortOrder: { type: number, multipleOf: 1 }
      required: [id, url, altText, sortOrder]
    EducationGalleryUploadInput:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 240 }
        size: { type: number, minimum: 1, maximum: 8388608 }
        contentType: { type: string, minLength: 1, maxLength: 120 }
      required: [name, size, contentType]
    EducationGalleryUpload:
      type: object
      properties:
        uploadUrl: { type: string }
        mediaId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        imageUrl: { type: string }
      required: [uploadUrl, mediaId, imageUrl]
    EducationGalleryMediaInput:
      type: object
      properties:
        mediaId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        altText: { type: string, maxLength: 240 }
      required: [mediaId]
    EducationGalleryReorderInput:
      type: object
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 20
          items:
            type: object
            properties:
              mediaId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
              altText: { type: string, maxLength: 240 }
            required: [mediaId]
      required: [items]
    EducationCourseReview:
      type: object
      properties:
        id: { type: string }
        rating: { type: number, minimum: 1, maximum: 5 }
        comment: { type: string }
        createdAt: { type: string, format: date-time }
      required: [id, rating, comment, createdAt]
    EducationCategory:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        courseCount: { type: number, multipleOf: 1, minimum: 0 }
      required: [id, name, slug, courseCount]
    EducationCenterPublic:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        city: { type: string }
        description: { type: string }
        imageUrl: { type: string }
        websiteUrl: { type: ["string", "null"] }
        instagramUrl: { type: ["string", "null"] }
        verified: { type: boolean }
        rating: { type: number }
        reviewCount: { type: number, multipleOf: 1, minimum: 0 }
        courseCount: { type: number, multipleOf: 1, minimum: 0 }
        gallery:
          type: array
          items: { $ref: "#/components/schemas/EducationMedia" }
        courses:
          type: array
          items: { $ref: "#/components/schemas/Course" }
      required: [id, name, city, description, imageUrl, verified, rating, reviewCount, courseCount, gallery, courses]
    EducationEnrollmentInput:
      type: object
      properties:
        employeeId: { type: ["string", "null"] }
        sessionId: { type: ["string", "null"] }
        paymentMode: { type: string, enum: [online_full, live_deposit, live_off_platform] }
    EducationTaxonomyItem:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        sortOrder: { type: number, multipleOf: 1 }
        active: { type: boolean }
      required: [id, name, slug, sortOrder, active]
    EducationTaxonomyPatch:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string, minLength: 2, maxLength: 160 }
        sortOrder: { type: number, multipleOf: 1, minimum: 0 }
        active: { type: boolean }
    EducationCourseType:
      allOf:
        - $ref: "#/components/schemas/EducationTaxonomyItem"
        - type: object
          properties:
            subcategoryId: { type: string }
            status: { type: string, enum: [pending, approved, rejected] }
            proposedByCenterId: { type: ["string", "null"] }
            reviewNote: { type: ["string", "null"] }
          required: [subcategoryId, status]
    EducationTaxonomySubcategory:
      allOf:
        - $ref: "#/components/schemas/EducationTaxonomyItem"
        - type: object
          properties:
            courseCount: { type: number, multipleOf: 1, minimum: 0 }
            courseTypes:
              type: array
              items: { $ref: "#/components/schemas/EducationCourseTypeCount" }
          required: [courseCount, courseTypes]
    EducationCourseTypeCount:
      allOf:
        - $ref: "#/components/schemas/EducationTaxonomyItem"
        - type: object
          properties:
            courseCount: { type: number, multipleOf: 1, minimum: 0 }
          required: [courseCount]
    EducationTaxonomyCategory:
      allOf:
        - $ref: "#/components/schemas/EducationTaxonomyItem"
        - type: object
          properties:
            courseCount: { type: number, multipleOf: 1, minimum: 0 }
            subcategories:
              type: array
              items: { $ref: "#/components/schemas/EducationTaxonomySubcategory" }
          required: [courseCount, subcategories]
    EducationTaxonomySection:
      allOf:
        - $ref: "#/components/schemas/EducationTaxonomyItem"
        - type: object
          properties:
            courseCount: { type: number, multipleOf: 1, minimum: 0 }
            categories:
              type: array
              items: { $ref: "#/components/schemas/EducationTaxonomyCategory" }
          required: [courseCount, categories]
    EducationSearchSuggestion:
      type: object
      properties:
        kind: { type: string, enum: [section, category, subcategory, courseType, course] }
        id: { type: string }
        label: { type: string }
        path: { type: array, items: { type: string } }
      required: [kind, id, label, path]
    EducationInquiry:
      type: object
      properties:
        id: { type: string }
        courseId: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
      required: [id, courseId, status, createdAt]
    EducationRankedCategory:
      type: object
      properties:
        categoryId: { type: string }
        name: { type: string }
        views30d: { type: number }
        inquiries30d: { type: number }
        score: { type: number }
      required: [categoryId, name, views30d, inquiries30d, score]
    EducationRankedCenter:
      type: object
      properties:
        centerId: { type: string }
        name: { type: string }
        city: { type: string }
        metric: { type: number }
        evidenceCount: { type: integer, minimum: 0 }
        explanation: { type: string }
        createdAt: { type: string, format: date-time }
      required: [centerId, name, city, metric, createdAt]
    EducationRankings:
      type: object
      properties:
        popularCategories30d: { type: array, items: { $ref: "#/components/schemas/EducationRankedCategory" } }
        newCenters: { type: array, items: { $ref: "#/components/schemas/EducationRankedCenter" } }
        mostRequestedCenters90d: { type: array, items: { $ref: "#/components/schemas/EducationRankedCenter" } }
        topRatedCenters: { type: array, items: { $ref: "#/components/schemas/EducationRankedCenter" } }
      required: [popularCategories30d, newCenters, mostRequestedCenters90d, topRatedCenters]
    EducationInstructorSummary:
      type: object
      properties:
        id: { type: string }
        fullName: { type: string }
        photoUrl: { type: ["string", "null"] }
        biography: { type: string }
        industryYears: { type: integer, minimum: 0 }
        experienceYears: { type: integer, minimum: 0 }
        specializations: { type: array, items: { type: string } }
        qualifications: { type: array, items: { type: string } }
        portfolioMedia: { type: array, items: { type: string, format: uri } }
      required: [id, fullName, photoUrl, biography, industryYears, experienceYears, specializations, qualifications, portfolioMedia]
    EducationCenterReview:
      type: object
      properties:
        id: { type: string }
        centerId: { type: string }
        enrollmentId: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string }
        status: { type: string, enum: [pending, published, rejected] }
        createdAt: { type: string, format: date-time }
      required: [id, centerId, enrollmentId, rating, comment, status, createdAt]
    EducationCenterReviewInput:
      type: object
      additionalProperties: false
      properties:
        enrollmentId: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string, maxLength: 4000 }
      required: [enrollmentId, rating]
    EducationCenterReviewPage:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/EducationCenterReview" } }
        page: { type: integer }
        pageSize: { type: integer }
        total: { type: integer }
        viewerEligibility:
          type: object
          properties:
            canReview: { type: boolean }
            eligibleEnrollmentId: { type: string }
            reason: { type: string }
          required: [canReview]
      required: [items, page, pageSize, total, viewerEligibility]
    AdminEducationCenterReview:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        centerId: { type: string }
        enrollmentId: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string }
        status: { type: string, enum: [pending, published, rejected] }
        adminNote: { type: ["string", "null"] }
        moderatedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, centerId, enrollmentId, rating, comment, status, adminNote, moderatedAt, createdAt]
    AdminEducationCenterReviewPage:
      type: object
      additionalProperties: false
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/AdminEducationCenterReview" } }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
        total: { type: integer, minimum: 0 }
      required: [items, page, pageSize, total]
    EducationWishlistInput:
      type: object
      additionalProperties: false
      properties:
        courseId: { type: string }
      required: [courseId]
    EducationWishlistItem:
      type: object
      properties:
        id: { type: string }
        createdAt: { type: string, format: date-time }
        course: { $ref: "#/components/schemas/EducationCourseDetail" }
      required: [id, createdAt, course]
    EducationWishlistPage:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/EducationWishlistItem" } }
        page: { type: integer }
        pageSize: { type: integer }
        total: { type: integer }
      required: [items, page, pageSize, total]
    EducationGiftVoucher:
      type: object
      properties:
        id: { type: string }
        courseId: { type: string }
        centerId: { type: string }
        purchaserId: { type: string }
        recipientUserId: { type: ["string", "null"] }
        recipientEmail: { type: ["string", "null"] }
        recipientName: { type: ["string", "null"] }
        giftMessage: { type: ["string", "null"] }
        courseTitle: { type: string }
        courseImageUrl: { type: string }
        amount: { type: integer, minimum: 0 }
        currency: { type: string }
        codeLast4: { type: string }
        status: { type: string, enum: [pending_payment, active, redeemed, refunded, cancelled] }
        paymentReference: { type: string }
        redeemedEnrollmentId: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, courseId, centerId, purchaserId, recipientUserId, recipientEmail, recipientName, giftMessage, courseTitle, courseImageUrl, amount, currency, codeLast4, status, paymentReference, redeemedEnrollmentId, createdAt]
    EducationGiftVoucherPurchase:
      allOf:
        - $ref: "#/components/schemas/EducationGiftVoucher"
        - type: object
          properties:
            redemptionCode:
              type: string
              description: One-time purchase response disclosure. It is not persisted as plaintext or returned by list/admin APIs.
          required: [redemptionCode]
    AdminEducationGiftVoucher:
      allOf:
        - $ref: "#/components/schemas/EducationGiftVoucher"
        - type: object
          properties:
            maskedCode: { type: string }
            settledByUserId: { type: ["string", "null"] }
            settledAt: { type: ["string", "null"], format: date-time }
            redeemedByUserId: { type: ["string", "null"] }
            redeemedAt: { type: ["string", "null"], format: date-time }
            refundedByUserId: { type: ["string", "null"] }
            refundedAt: { type: ["string", "null"], format: date-time }
            refundNote: { type: ["string", "null"] }
            disputeId: { type: ["string", "null"] }
            updatedAt: { type: string, format: date-time }
          required: [maskedCode, settledByUserId, settledAt, redeemedByUserId, redeemedAt, refundedByUserId, refundedAt, refundNote, disputeId, updatedAt]
    AdminEducationGiftVoucherPage:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/AdminEducationGiftVoucher" } }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
        total: { type: integer, minimum: 0 }
      required: [items, page, pageSize, total]
    EducationGiftVoucherPage:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/EducationGiftVoucher" } }
        page: { type: integer }
        pageSize: { type: integer }
        total: { type: integer }
      required: [items, page, pageSize, total]
    EducationGiftVoucherPurchaseInput:
      type: object
      additionalProperties: false
      properties:
        courseId: { type: string }
        recipientUserId: { type: ["string", "null"] }
        recipientEmail: { type: ["string", "null"], maxLength: 320 }
        recipientName: { type: ["string", "null"], minLength: 1, maxLength: 160 }
        giftMessage: { type: ["string", "null"], minLength: 1, maxLength: 1000 }
      required: [courseId]
    EducationGiftVoucherRedeemInput:
      type: object
      additionalProperties: false
      properties:
        code: { type: string, minLength: 12, maxLength: 128 }
      required: [code]
    EducationGiftVoucherRefundInput:
      type: object
      additionalProperties: false
      properties:
        note: { type: string, minLength: 1, maxLength: 2000 }
        disputeId: { type: ["string", "null"] }
      required: [note]
    EducationPlacementSettingInput:
      type: object
      additionalProperties: false
      properties:
        kind: { type: string, enum: [featured_salon, featured_center, special_offer] }
        scope: { type: string, enum: [home, category, subcategory] }
        price: { type: number, minimum: 1, multipleOf: 1 }
        durationDays: { type: number, minimum: 1, maximum: 365, multipleOf: 1 }
        slotCount: { type: number, minimum: 1, maximum: 1000, multipleOf: 1 }
      required: [kind, scope, price, durationDays, slotCount]
    EducationPlacementSetting:
      allOf:
        - $ref: "#/components/schemas/EducationPlacementSettingInput"
        - type: object
          properties: { id: { type: string } }
          required: [id]
    EducationPlacementPurchaseInput:
      type: object
      additionalProperties: false
      properties:
        kind: { type: string, enum: [featured_center, special_offer] }
        scope: { type: string, enum: [home, category, subcategory] }
        scopeId: { type: ["string", "null"] }
        courseId: { type: ["string", "null"] }
      required: [kind, scope]
    EducationPlacement:
      type: object
      properties:
        id: { type: string }
        centerId: { type: ["string", "null"] }
        kind: { type: string, enum: [featured_center, special_offer] }
        label: { type: string }
        scope: { type: string, enum: [home, category, subcategory] }
        scopeId: { type: ["string", "null"] }
        courseId: { type: ["string", "null"] }
        status: { type: string, enum: [pending_payment, active, expired, cancelled, rejected] }
        price: { type: number }
        paymentReference: { type: string }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, centerId, kind, label, scope, courseId, status, price, paymentReference, createdAt]
    FeaturedPlacementInput:
      type: object
      additionalProperties: false
      properties:
        kind: { type: string, enum: [featured_salon, featured_center, special_offer] }
        scope: { type: string, enum: [home, category, subcategory], default: home }
        scopeId: { type: ["string", "null"] }
        targetId: { type: ["string", "null"] }
      required: [kind]
    FeaturedPlacement:
      type: object
      properties:
        id: { type: string }
        kind: { type: string, enum: [featured_salon, featured_center, special_offer] }
        label: { type: string }
        salonId: { type: ["string", "null"] }
        centerId: { type: ["string", "null"] }
        courseId: { type: ["string", "null"] }
        scope: { type: string, enum: [home, category, subcategory] }
        scopeId: { type: ["string", "null"] }
        status: { type: string, enum: [pending_payment, active, expired, cancelled, rejected] }
        priceSnapshot: { type: integer, minimum: 0 }
        durationDaysSnapshot: { type: integer, minimum: 1 }
        paymentReference: { type: string }
        ipsPayload: { type: ["string", "null"] }
        recipientName: { type: ["string", "null"] }
        recipientAccount: { type: ["string", "null"] }
        purpose: { type: ["string", "null"] }
        currency:
          type: ["string", "null"]
          enum: [RSD, null]
        paymentInstructionsAvailable: { type: boolean }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        settledAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, kind, label, salonId, centerId, courseId, scope, scopeId, status, priceSnapshot, durationDaysSnapshot, paymentReference, ipsPayload, recipientName, recipientAccount, purpose, currency, paymentInstructionsAvailable, startsAt, endsAt, settledAt, createdAt]
    FeaturedPlacementConfirmation:
      allOf:
        - $ref: "#/components/schemas/FeaturedPlacement"
        - type: object
          properties:
            activated:
              type: boolean
              description: True only when this request transitioned the placement from pending payment to active.
          required: [activated]
    FeaturedPlacementPage:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/FeaturedPlacement" }
        page: { type: integer }
        pageSize: { type: integer }
        total: { type: integer }
      required: [items, page, pageSize, total]
    EducationPublicPlacement:
      type: object
      properties:
        id: { type: string }
        centerId: { type: string }
        centerName: { type: string }
        centerImageUrl: { type: string }
        kind: { type: string, enum: [featured_center, special_offer] }
        label: { type: string }
        scope: { type: string, enum: [home, category, subcategory] }
        scopeId: { type: ["string", "null"] }
        courseId: { type: ["string", "null"] }
        courseTitle: { type: ["string", "null"] }
        courseImageUrl: { type: ["string", "null"] }
        coursePrice: { type: ["number", "null"] }
      required: [id, centerId, centerName, centerImageUrl, kind, label, scope, courseId, courseTitle, courseImageUrl, coursePrice]
    EducationEnrollment:
      type: object
      properties:
        id: { type: string }
        courseId: { type: string }
        courseTitle: { type: string }
        learnerName: { type: string }
        employeeId: { type: ["string", "null"] }
        status: { type: string, enum: [pending, active, completed, cancelled] }
        paymentStatus: { type: string, enum: [pending, paid, failed, refunded] }
        progress: { type: number }
        nextLesson: { type: ["string", "null"] }
        purchasedAt: { type: string, format: date-time }
        escrowStatus: { type: ["string", "null"], enum: [held, ready_for_payout, frozen, paid_out, refunded, partially_refunded, null] }
        escrowReleaseAt: { type: ["string", "null"], format: date-time }
      required: [id, courseId, courseTitle, learnerName, status, paymentStatus, progress, purchasedAt]
    EducationNotification:
      type: object
      properties:
        id: { type: string }
        type: { type: string }
        title: { type: string }
        body: { type: string }
        actionUrl: { type: ["string", "null"] }
        enrollmentId: { type: ["string", "null"] }
        waitlistId: { type: ["string", "null"] }
        readAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, type, title, body, actionUrl, enrollmentId, waitlistId, readAt, createdAt]
    EducationWaitlistOffer:
      type: object
      description: An active (offered, unexpired) waitlist offer that holds a reserved seat for the learner
      properties:
        id: { type: string }
        courseId: { type: string }
        courseTitle: { type: string }
        sessionId: { type: string }
        sessionStartsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [waiting, offered, enrolled, expired, cancelled] }
        position: { type: number, multipleOf: 1 }
        offeredAt: { type: ["string", "null"], format: date-time }
        expiresAt: { type: ["string", "null"], format: date-time }
      required: [id, courseId, courseTitle, sessionId, status, position, offeredAt, expiresAt]
    EducationNotificationList:
      type: object
      properties:
        notifications:
          type: array
          items: { $ref: "#/components/schemas/EducationNotification" }
        offers:
          type: array
          items: { $ref: "#/components/schemas/EducationWaitlistOffer" }
      required: [notifications, offers]
    MarkEducationNotificationReadResponse:
      type: object
      properties:
        ok: { type: boolean }
      required: [ok]
    EducationCenterStatus:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        verificationStatus: { type: string, enum: [pending, verified, rejected, suspended] }
        verificationNote: { type: ["string", "null"] }
        subscriptionStatus: { type: ["string", "null"], enum: [trial, active, past_due, cancelled, suspended, free_via_loyalty, null] }
        currentPeriodEnd: { type: ["string", "null"], format: date-time }
        eligible: { type: boolean }
        organicInquiriesAndCompletedEnrollments90d: { type: integer, minimum: 0 }
        completedLearnerCount: { type: integer, minimum: 0 }
        publishedReviewCount: { type: integer, minimum: 0 }
        publishedRating: { type: number, minimum: 0, maximum: 5 }
        qualifiesMostRequested: { type: boolean }
        qualifiesTopRated: { type: boolean }
        metricsExplanation: { type: string }
      required: [id, name, verificationStatus, eligible, organicInquiriesAndCompletedEnrollments90d, completedLearnerCount, publishedReviewCount, publishedRating, qualifiesMostRequested, qualifiesTopRated, metricsExplanation]
    EducationMessage:
      type: object
      properties:
        id: { type: string }
        body: { type: string }
        senderId: { type: string }
        senderName: { type: string }
        createdAt: { type: string, format: date-time }
        readAt: { type: ["string", "null"], format: date-time }
      required: [id, body, senderId, senderName, createdAt]
    EducationMessageThread:
      type: object
      properties:
        thread:
          oneOf:
            - type: "null"
            - type: object
              properties:
                id: { type: string }
                status: { type: string, enum: [open, closed] }
                enrollmentId: { type: string }
              required: [id, status, enrollmentId]
        messages:
          type: array
          items: { $ref: "#/components/schemas/EducationMessage" }
      required: [thread, messages]
    EducationDispute:
      type: object
      properties:
        id: { type: string }
        enrollmentId: { type: string }
        courseTitle: { type: string }
        reason: { type: string }
        details: { type: string }
        status: { type: string, enum: [open, under_review, resolved_refund, resolved_payout, rejected, cancelled] }
        resolutionNote: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        resolvedAt: { type: ["string", "null"], format: date-time }
      required: [id, enrollmentId, reason, details, status, createdAt]
    EducationMarketplaceSettingsInput:
      type: object
      additionalProperties: false
      properties:
        commissionPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        reservePercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        onlineRefundDays: { type: number, minimum: 0, maximum: 365, multipleOf: 1 }
        liveAppealDays: { type: number, minimum: 0, maximum: 365, multipleOf: 1 }
        featuredCoursePrice: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        ipsRecipientName: { type: ["string", "null"], maxLength: 140 }
        ipsRecipientAccount: { type: ["string", "null"], maxLength: 40 }
        ipsPurpose: { type: ["string", "null"], maxLength: 140 }
      required: [commissionPercent, reservePercent, onlineRefundDays, liveAppealDays, featuredCoursePrice]
    EducationMarketplaceSettings:
      allOf:
        - $ref: "#/components/schemas/EducationMarketplaceSettingsInput"
        - type: object
          properties:
            id: { type: string }
            updatedAt: { type: string, format: date-time }
          required: [id, updatedAt]
    EducationInstructorProfile:
      type: object
      properties:
        id: { type: string }
        centerId: { type: string }
        userId: { type: ["string", "null"] }
        fullName: { type: string }
        photoUrl: { type: ["string", "null"] }
        biography: { type: string }
        industryYears: { type: number }
        experienceYears: { type: number }
        specializations: { type: array, items: { type: string } }
        qualifications: { type: array, items: { type: string } }
        portfolioMedia: { type: array, items: { type: string, format: uri } }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, centerId, fullName, biography, industryYears, experienceYears, specializations, qualifications, portfolioMedia, createdAt, updatedAt]
    EducationInstructorInput:
      type: object
      properties:
        fullName: { type: string, minLength: 1, maxLength: 120 }
        photoUrl: { type: ["string", "null"] }
        biography: { type: string, maxLength: 4000 }
        industryYears: { type: number, minimum: 0 }
        experienceYears: { type: number, minimum: 0 }
        specializations: { type: array, items: { type: string } }
        qualifications: { type: array, items: { type: string } }
        portfolioMedia: { type: array, maxItems: 12, uniqueItems: true, items: { type: string, format: uri, pattern: '^https://' } }
        userId: { type: ["string", "null"] }
      required: [fullName]
    EducationInstructorPublicProfile:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        photoUrl: { type: ["string", "null"] }
        biography: { type: string }
        industryYears: { type: number }
        experienceYears: { type: number }
        specializations: { type: array, items: { type: string } }
        qualifications: { type: array, items: { type: string } }
        portfolioMedia: { type: array, items: { type: string, format: uri } }
        rating: { type: number }
        reviewCount: { type: number }
        ratingSource: { type: string, enum: [published_course_reviews] }
        participantCount: { type: number }
        courses:
          type: array
          items: { $ref: "#/components/schemas/Course" }
      required: [id, name, biography, industryYears, experienceYears, specializations, qualifications, portfolioMedia, rating, reviewCount, ratingSource, participantCount, courses]
    EducationCourseFeaturedInput:
      type: object
      properties:
        active: { type: boolean }
        paymentReference: { type: ["string", "null"], maxLength: 200 }
      required: [active]
    EducationCourseFeaturedCharge:
      type: object
      properties:
        id: { type: string }
        amount: { type: number }
        status: { type: string, enum: [pending, paid, cancelled, refunded] }
        paymentReference: { type: ["string", "null"] }
        activatedAt: { type: string, format: date-time }
        settledAt: { type: ["string", "null"], format: date-time }
      required: [id, amount, status, activatedAt]
    EducationFeaturedChargeSettlementInput:
      type: object
      additionalProperties: false
      properties:
        paymentReference: { type: ["string", "null"], minLength: 1, maxLength: 200 }
    EducationFeaturedChargeSettlement:
      allOf:
        - $ref: "#/components/schemas/EducationCourseFeaturedCharge"
        - type: object
          properties:
            courseId: { type: string }
          required: [courseId]
    EducationCourseFeaturedStatus:
      type: object
      properties:
        courseId: { type: string }
        isFeatured: { type: boolean }
        featuredUntil: { type: ["string", "null"], format: date-time }
        featuredFee: { type: number }
        featuredCoursePrice: { type: number }
        charge:
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/EducationCourseFeaturedCharge"
      required: [courseId, isFeatured, featuredFee, featuredCoursePrice]
    EducationAdminCenter:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        city: { type: string }
        pib: { type: ["string", "null"] }
        verificationStatus: { type: string, enum: [pending, verified, rejected, suspended] }
        verificationNote: { type: ["string", "null"] }
        subscriptionStatus: { type: ["string", "null"] }
        subscriptionPlanId: { type: ["string", "null"] }
        subscriptionPlan: { type: ["string", "null"] }
        heldAmount: { type: number }
      required: [id, name, city, verificationStatus, heldAmount]
    EducationAdminCenterUpdate:
      type: object
      additionalProperties: false
      properties:
        verificationStatus: { type: string, enum: [pending, verified, rejected, suspended] }
        verificationNote: { type: ["string", "null"] }
        subscriptionStatus: { type: string, enum: [trial, active, past_due, cancelled, suspended, free_via_loyalty] }
        planId: { type: string }
        pib: { type: ["string", "null"], maxLength: 50 }
        billingOverrides:
          $ref: "#/components/schemas/EducationBillingOverridesInput"
    EducationBillingOverridesInput:
      type: object
      additionalProperties: false
      properties:
        commissionPercent: { type: ["integer", "null"], minimum: 0, maximum: 100 }
        reservePercent: { type: ["integer", "null"], minimum: 0, maximum: 100 }
        onlineRefundDays: { type: ["integer", "null"], minimum: 0, maximum: 365 }
        liveAppealDays: { type: ["integer", "null"], minimum: 0, maximum: 365 }
        featuredCoursePrice: { type: ["integer", "null"], minimum: 0, maximum: 100000000 }
    EducationResolvedBillingValue:
      type: object
      properties:
        override: { type: ["integer", "null"] }
        globalDefault: { type: integer }
        effectiveValue: { type: integer }
        source: { type: string, enum: [global, custom] }
      required: [override, globalDefault, effectiveValue, source]
    EducationResolvedBillingSettings:
      type: object
      properties:
        commissionPercent: { $ref: "#/components/schemas/EducationResolvedBillingValue" }
        reservePercent: { $ref: "#/components/schemas/EducationResolvedBillingValue" }
        onlineRefundDays: { $ref: "#/components/schemas/EducationResolvedBillingValue" }
        liveAppealDays: { $ref: "#/components/schemas/EducationResolvedBillingValue" }
        featuredCoursePrice: { $ref: "#/components/schemas/EducationResolvedBillingValue" }
      required: [commissionPercent, reservePercent, onlineRefundDays, liveAppealDays, featuredCoursePrice]
    EducationAdminCenterDetail:
      allOf:
        - $ref: "#/components/schemas/EducationAdminCenter"
        - type: object
          properties:
            description: { type: string }
            contactEmail: { type: ["string", "null"] }
            contactPhone: { type: ["string", "null"] }
            billingSettings: { $ref: "#/components/schemas/EducationResolvedBillingSettings" }
          required: [description, billingSettings]
    EducationFinanceEscrow:
      type: object
      properties:
        id: { type: string }
        enrollmentId: { type: string }
        centerId: { type: string }
        centerName: { type: string }
        courseTitle: { type: string }
        grossAmount: { type: number }
        platformFee: { type: number }
        reserveAmount: { type: number }
        netAmount: { type: number }
        status: { type: string, enum: [held, ready_for_payout, frozen, paid_out, refunded, partially_refunded] }
        releaseAt: { type: string, format: date-time }
        disputeOpen: { type: boolean }
      required: [id, enrollmentId, centerId, grossAmount, platformFee, reserveAmount, netAmount, status, releaseAt, disputeOpen]
    EducationFinanceOverview:
      type: object
      properties:
        summary:
          type: object
          additionalProperties: { type: number }
        escrows:
          type: array
          items: { $ref: "#/components/schemas/EducationFinanceEscrow" }
        payouts:
          type: array
          items: { $ref: "#/components/schemas/EducationPayout" }
      required: [summary, escrows, payouts]
    EducationPayoutInput:
      type: object
      properties:
        centerId: { type: string }
        includeReserve: { type: boolean }
        reference: { type: string }
        note: { type: string }
      required: [centerId]
    EducationPayout:
      type: object
      properties:
        id: { type: string }
        centerId: { type: string }
        amount: { type: number }
        status: { type: string, enum: [pending, paid, cancelled] }
        paidAt: { type: ["string", "null"], format: date-time }
      required: [id, centerId, amount, status]
    EducationDisputeResolutionInput:
      type: object
      properties:
        action: { type: string, enum: [refund, release, reject] }
        resolutionNote: { type: string, minLength: 1, maxLength: 4000 }
      required: [action, resolutionNote]
    EducationLms:
      type: object
      properties:
        enrollment: { $ref: "#/components/schemas/EducationEnrollment" }
        course: { $ref: "#/components/schemas/EducationPrivateCourseDetail" }
        eligibility: { $ref: "#/components/schemas/EducationCertificateEligibility" }
      required: [enrollment, course, eligibility]
    EducationCertificateEligibility:
      type: object
      required: [total, completed, percent, certificateEligible, reasons]
      properties:
        total: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        percent: { type: integer, minimum: 0, maximum: 100 }
        certificateEligible: { type: boolean }
        reasons: { type: array, items: { type: string } }
    AdminSummary:
      type: object
      properties:
        totalUsers: { type: number }
        totalSalons: { type: number }
        activeSalons: { type: number }
        bookingsThisMonth: { type: number }
        bookingsLastMonth: { type: number }
        bookingsTrend: { type: number }
        grossMerchandiseValue: { type: number }
        newSalonsThisMonth: { type: number }
        totalReviews: { type: number }
        hiddenReviews: { type: number }
        activeSubscriptions: { type: number }
        galleryCleanupFailedTickets:
          type: number
          description: Number of eligible gallery upload tickets with at least one recorded cleanup failure.
        galleryCleanupFailureAttempts:
          type: number
          description: Total recorded cleanup failures across eligible gallery upload tickets.
        galleryCleanupOldestEligibleTicketAgeMinutes:
          type: ["number", "null"]
          description: Age in minutes of the oldest gallery upload ticket currently eligible for cleanup.
        galleryCleanupHasRepeatedFailures:
          type: boolean
          description: Whether any eligible gallery upload ticket has failed cleanup three or more times.
        deliveryReportStaleProviders:
          type: array
          description: Delivery-report providers (brevo = e-mail, infobip = SMS) whose webhooks look silent — automation messages were sent recently but no verified delivery report has arrived since.
          items: { type: string, enum: [brevo, infobip] }
        smsFallbackReachableAdminCount:
          type: number
          description: How many active administrators the total-email-outage SMS fallback could actually reach (active ADMIN/SUPER_ADMIN accounts with a usable phone number). Zero means an emergency SMS would reach nobody.
        schedulerJobs:
          type: array
          description: Last known local status of periodic database-backed scheduler jobs, including a pending bounded retry or a failed normal cycle.
          items: { $ref: "#/components/schemas/SchedulerJobHealth" }
        schedulerDatabaseCapacity:
          $ref: "#/components/schemas/SchedulerDatabaseCapacity"
        topCategories:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              count: { type: number }
            required: [name, count]
      required: [totalUsers, totalSalons, activeSalons, bookingsThisMonth, bookingsLastMonth, bookingsTrend, grossMerchandiseValue, newSalonsThisMonth, totalReviews, hiddenReviews, activeSubscriptions, galleryCleanupFailedTickets, galleryCleanupFailureAttempts, galleryCleanupOldestEligibleTicketAgeMinutes, galleryCleanupHasRepeatedFailures, deliveryReportStaleProviders, smsFallbackReachableAdminCount, schedulerJobs, schedulerDatabaseCapacity, topCategories]
    SchedulerDatabaseCapacity:
      type: object
      description: Process-local scheduler limiter state; this does not query or persist database state.
      properties:
        active: { type: number, minimum: 0, multipleOf: 1 }
        limit: { type: number, minimum: 1, multipleOf: 1 }
        queued: { type: number, minimum: 0, multipleOf: 1 }
      required: [active, limit, queued]
    AdminSalon:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        city: { type: string }
        active: { type: boolean }
        featured: { type: boolean }
        isVerified: { type: boolean }
        topSalon: { type: boolean }
        videoUrl: { type: ["string", "null"] }
        rating: { type: number }
        reviewCount: { type: number }
        subscriptionStatus: { type: ["string", "null"] }
        subscriptionPlan: { type: ["string", "null"] }
        loyaltyTier: { type: ["string", "null"] }
        loyaltySpend: { type: number }
        createdAt: { type: string, format: date-time }
      required: [id, name, slug, city, active, featured, isVerified, topSalon, videoUrl, rating, reviewCount, loyaltySpend, createdAt]
    AdminSalonUpdate:
      type: object
      properties:
        active: { type: boolean }
        featured: { type: boolean }
        isVerified: { type: boolean }
        topSalon: { type: boolean }
        videoUrl: { type: ["string", "null"] }
        pib: { type: string, minLength: 8, maxLength: 50 }
    AdminSalonOrderSummary:
      type: object
      properties:
        id: { type: string }
        createdAt: { type: string, format: date-time }
        status: { type: string }
        paymentStatus: { type: string }
        deliveryMethod: { type: string }
        total: { type: number }
        itemCount: { type: number }
      required: [id, createdAt, status, paymentStatus, deliveryMethod, total, itemCount]
    AdminSalonDetail:
      allOf:
        - $ref: "#/components/schemas/AdminSalon"
        - type: object
          properties:
            address: { type: string }
            postalCode: { type: ["string", "null"] }
            phone: { type: string }
            email: { type: string }
            subscriptionStatus: { type: ["string", "null"] }
            subscriptionPlan: { type: ["string", "null"] }
            loyaltyTier: { type: ["string", "null"] }
            orderCount: { type: number, multipleOf: 1 }
            orderTotal: { type: number }
            orders:
              type: array
              items: { $ref: "#/components/schemas/AdminSalonOrderSummary" }
          required: [address, postalCode, phone, email, subscriptionStatus, subscriptionPlan, loyaltyTier, orderCount, orderTotal, orders]
    AdminUser:
      type: object
      properties:
        id: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: string }
        phone: { type: ["string", "null"] }
        role:
          type: string
          enum: [SUPER_ADMIN, ADMIN, SALON_OWNER, SALON_EMPLOYEE, EDUKATIVNI_CENTAR, INSTRUCTOR, CUSTOMER, JOBSEEKER, STUDENT]
        active: { type: boolean }
        passwordSetAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, firstName, lastName, email, role, active, passwordSetAt, createdAt]
    AdminUserUpdate:
      type: object
      additionalProperties: false
      properties:
        role:
          type: string
          enum: [SUPER_ADMIN, ADMIN, SALON_OWNER, SALON_EMPLOYEE, EDUKATIVNI_CENTAR, INSTRUCTOR, CUSTOMER, JOBSEEKER, STUDENT]
        active: { type: boolean }
    AdminBusinessRelation:
      type: object
      additionalProperties: false
      required: [id, name, active, allowedActions]
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        name: { type: string }
        active: { type: boolean }
        allowedActions:
          type: array
          items: { type: string, enum: [transfer, deactivate, retain, unlink] }
    AdminBusinessRoleTransitionState:
      type: object
      additionalProperties: false
      required: [user, salonOwnerships, employments, educationCenterOwnerships, instructorRelations]
      properties:
        user: { $ref: "#/components/schemas/AdminUser" }
        salonOwnerships:
          type: array
          items: { $ref: "#/components/schemas/AdminBusinessRelation" }
        employments:
          type: array
          items: { $ref: "#/components/schemas/AdminBusinessRelation" }
        educationCenterOwnerships:
          type: array
          items: { $ref: "#/components/schemas/AdminBusinessRelation" }
        instructorRelations:
          type: array
          items: { $ref: "#/components/schemas/AdminBusinessRelation" }
    AdminOwnershipTransitionDecision:
      type: object
      additionalProperties: false
      required: [relationId, action]
      properties:
        relationId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        action: { type: string, enum: [transfer, deactivate, retain] }
        targetUserId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
    AdminLinkedRelationTransitionDecision:
      type: object
      additionalProperties: false
      required: [relationId, action]
      properties:
        relationId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        action: { type: string, enum: [deactivate, retain, unlink] }
    AdminInstructorTransitionDecision:
      type: object
      additionalProperties: false
      required: [relationId, action]
      properties:
        relationId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        action: { type: string, enum: [retain, unlink] }
    AdminBusinessRoleTransitionInput:
      type: object
      additionalProperties: false
      required: [role, active, activeSalonId, salonOwnerships, employments, educationCenterOwnerships, instructorRelations]
      properties:
        role:
          type: string
          enum: [ADMIN, SALON_OWNER, SALON_EMPLOYEE, EDUKATIVNI_CENTAR, INSTRUCTOR, CUSTOMER, JOBSEEKER, STUDENT]
        active: { type: boolean }
        activeSalonId: { type: [string, "null"], pattern: '^[0-9a-fA-F-]{36}$' }
        salonOwnerships:
          type: array
          items: { $ref: "#/components/schemas/AdminOwnershipTransitionDecision" }
        employments:
          type: array
          items: { $ref: "#/components/schemas/AdminLinkedRelationTransitionDecision" }
        educationCenterOwnerships:
          type: array
          items: { $ref: "#/components/schemas/AdminOwnershipTransitionDecision" }
        instructorRelations:
          type: array
          items: { $ref: "#/components/schemas/AdminInstructorTransitionDecision" }
    AdminBusinessRoleTransitionResult:
      allOf:
        - $ref: "#/components/schemas/AdminBusinessRoleTransitionState"
    AdminCreateCustomerSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email:
          type: string
          maxLength: 320
          pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$'
    AdminCreateAccountSetupInput:
      oneOf:
        - $ref: "#/components/schemas/AdminSalonOwnerAccountSetupInput"
        - $ref: "#/components/schemas/AdminSalonEmployeeAccountSetupInput"
        - $ref: "#/components/schemas/AdminEducationCenterAccountSetupInput"
        - $ref: "#/components/schemas/AdminInstructorAccountSetupInput"
        - $ref: "#/components/schemas/AdminStandaloneAdminAccountSetupInput"
        - $ref: "#/components/schemas/AdminStandaloneCustomerAccountSetupInput"
        - $ref: "#/components/schemas/AdminStandaloneStudentAccountSetupInput"
        - $ref: "#/components/schemas/AdminStandaloneJobseekerAccountSetupInput"
      discriminator:
        propertyName: role
        mapping:
          SALON_OWNER: "#/components/schemas/AdminSalonOwnerAccountSetupInput"
          SALON_EMPLOYEE: "#/components/schemas/AdminSalonEmployeeAccountSetupInput"
          EDUKATIVNI_CENTAR: "#/components/schemas/AdminEducationCenterAccountSetupInput"
          INSTRUCTOR: "#/components/schemas/AdminInstructorAccountSetupInput"
          ADMIN: "#/components/schemas/AdminStandaloneAdminAccountSetupInput"
          CUSTOMER: "#/components/schemas/AdminStandaloneCustomerAccountSetupInput"
          STUDENT: "#/components/schemas/AdminStandaloneStudentAccountSetupInput"
          JOBSEEKER: "#/components/schemas/AdminStandaloneJobseekerAccountSetupInput"
    AdminBusinessAccountConversionInput:
      oneOf:
        - $ref: "#/components/schemas/AdminSalonOwnerConversionInput"
        - $ref: "#/components/schemas/AdminSalonEmployeeConversionInput"
        - $ref: "#/components/schemas/AdminEducationCenterConversionInput"
        - $ref: "#/components/schemas/AdminInstructorConversionInput"
      discriminator:
        propertyName: role
        mapping:
          SALON_OWNER: "#/components/schemas/AdminSalonOwnerConversionInput"
          SALON_EMPLOYEE: "#/components/schemas/AdminSalonEmployeeConversionInput"
          EDUKATIVNI_CENTAR: "#/components/schemas/AdminEducationCenterConversionInput"
          INSTRUCTOR: "#/components/schemas/AdminInstructorConversionInput"
    AdminSalonOwnerConversionInput:
      type: object
      additionalProperties: false
      required: [role, salon]
      properties:
        role: { type: string, enum: [SALON_OWNER] }
        salon: { $ref: "#/components/schemas/AdminSalonOwnerSetup" }
    AdminSalonEmployeeConversionInput:
      type: object
      additionalProperties: false
      required: [role, employee]
      properties:
        role: { type: string, enum: [SALON_EMPLOYEE] }
        employee: { $ref: "#/components/schemas/AdminEmployeeSetup" }
    AdminEducationCenterConversionInput:
      type: object
      additionalProperties: false
      required: [role, educationCenter]
      properties:
        role: { type: string, enum: [EDUKATIVNI_CENTAR] }
        educationCenter: { $ref: "#/components/schemas/AdminEducationCenterSetup" }
    AdminInstructorConversionInput:
      type: object
      additionalProperties: false
      required: [role, instructor]
      properties:
        role: { type: string, enum: [INSTRUCTOR] }
        instructor: { $ref: "#/components/schemas/AdminInstructorSetup" }
    AdminSalonOwnerAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role, salon]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [SALON_OWNER] }
        salon: { $ref: "#/components/schemas/AdminSalonOwnerSetup" }
    AdminSalonEmployeeAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role, employee]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [SALON_EMPLOYEE] }
        employee: { $ref: "#/components/schemas/AdminEmployeeSetup" }
    AdminEducationCenterAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role, educationCenter]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [EDUKATIVNI_CENTAR] }
        educationCenter: { $ref: "#/components/schemas/AdminEducationCenterSetup" }
    AdminInstructorAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role, instructor]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [INSTRUCTOR] }
        instructor: { $ref: "#/components/schemas/AdminInstructorSetup" }
    AdminStandaloneAdminAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [ADMIN] }
    AdminStandaloneCustomerAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [CUSTOMER] }
    AdminStandaloneStudentAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [STUDENT] }
    AdminStandaloneJobseekerAccountSetupInput:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, role]
      properties:
        firstName: { type: string, minLength: 1, maxLength: 100 }
        lastName: { type: string, minLength: 1, maxLength: 100 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        role: { type: string, enum: [JOBSEEKER] }
    AdminSalonOwnerSetup:
      type: object
      additionalProperties: false
      required:
        [name, slug, city, municipality, address, phone, email, companyName, companyTaxId,
         companyRegistrationNumber, companyAddress, companyCity, shortDescription, description]
      properties:
        name: { type: string, minLength: 1, maxLength: 500 }
        slug: { type: string, minLength: 1, maxLength: 500 }
        city: { type: string, minLength: 1, maxLength: 500 }
        municipality: { type: string, minLength: 1, maxLength: 500 }
        address: { type: string, minLength: 1, maxLength: 500 }
        postalCode: { type: string, maxLength: 50 }
        phone: { type: string, minLength: 1, maxLength: 500 }
        email: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        companyName: { type: string, minLength: 1, maxLength: 500 }
        companyTaxId: { type: string, minLength: 8, maxLength: 50, pattern: '^(?:\D*\d){8,14}\D*$' }
        companyRegistrationNumber: { type: string, minLength: 1, maxLength: 500 }
        companyAddress: { type: string, minLength: 1, maxLength: 500 }
        companyCity: { type: string, minLength: 1, maxLength: 500 }
        companyPostalCode: { type: string, maxLength: 50 }
        shortDescription: { type: string, minLength: 1, maxLength: 500 }
        description: { type: string, minLength: 1, maxLength: 4000 }
    AdminEmployeeSetup:
      type: object
      additionalProperties: false
      required: [salonId, jobTitle]
      properties:
        salonId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        jobTitle: { type: string, minLength: 1, maxLength: 200 }
        bio: { type: string, maxLength: 4000 }
    AdminEducationCenterSetup:
      type: object
      additionalProperties: false
      required: [name, city, description, contactEmail, contactPhone, contactAddress, pib]
      properties:
        name: { type: string, minLength: 1, maxLength: 500 }
        city: { type: string, minLength: 1, maxLength: 500 }
        description: { type: string, minLength: 1, maxLength: 4000 }
        contactEmail: { type: string, maxLength: 320, pattern: '^[^@\s]+@[^@\s]+\.[^@\s]+$' }
        contactPhone: { type: string, minLength: 1, maxLength: 500 }
        contactAddress: { type: string, minLength: 1, maxLength: 500 }
        pib: { type: string, minLength: 8, maxLength: 50, pattern: '^(?:\D*\d){8,14}\D*$' }
    AdminInstructorSetup:
      type: object
      additionalProperties: false
      required: [centerId]
      properties:
        centerId: { type: string, pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$' }
        biography: { type: string, maxLength: 4000 }
        industryYears: { type: integer, minimum: 0 }
        experienceYears: { type: integer, minimum: 0 }
        specializations:
          type: array
          items: { type: string, minLength: 1, maxLength: 200 }
        qualifications:
          type: array
          items: { type: string, minLength: 1, maxLength: 200 }
    AdminCreateCustomerSetupResponse:
      type: object
      additionalProperties: false
      required: [user, setupUrl, expiresAt]
      properties:
        user: { $ref: "#/components/schemas/AdminUser" }
        setupUrl: { type: string, minLength: 1 }
        expiresAt: { type: string, format: date-time }
    CustomerPasswordSetupTokenInput:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token: { type: string, minLength: 32, maxLength: 256 }
    CustomerPasswordSetupValidation:
      type: object
      additionalProperties: false
      required: [valid, expiresAt]
      properties:
        valid: { type: boolean, const: true }
        expiresAt: { type: string, format: date-time }
    CustomerPasswordSetupCompletionInput:
      type: object
      additionalProperties: false
      required: [token, password, passwordConfirmation]
      properties:
        token: { type: string, minLength: 32, maxLength: 256 }
        password: { type: string, minLength: 8, maxLength: 200 }
        passwordConfirmation: { type: string, minLength: 8, maxLength: 200 }
    CustomerPasswordSetupCompletion:
      type: object
      additionalProperties: false
      required: [success, loginPath]
      properties:
        success: { type: boolean, const: true }
        loginPath: { type: string, const: /prijava }
    LoyaltyTier:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        sortOrder: { type: number }
        spendThreshold: { type: number }
        period: { type: string }
        subscriptionDiscountPercent: { type: number }
        productDiscountPercent: { type: number }
        freeSubscription: { type: boolean }
        premiumListing: { type: boolean }
        freeShipping: { type: boolean }
        benefits:
          type: array
          items: { type: string }
        active: { type: boolean }
      required: [id, name, sortOrder, spendThreshold, period, subscriptionDiscountPercent, productDiscountPercent, freeSubscription, premiumListing, freeShipping, benefits, active]
    LoyaltyTierInput:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        sortOrder: { type: number, minimum: 0, maximum: 100000, multipleOf: 1 }
        spendThreshold: { type: number, minimum: 0, maximum: 1000000000, multipleOf: 1 }
        period: { type: string, minLength: 1, maxLength: 40 }
        subscriptionDiscountPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        productDiscountPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        freeSubscription: { type: boolean }
        premiumListing: { type: boolean }
        freeShipping: { type: boolean }
        benefits:
          type: array
          items: { type: string }
        active: { type: boolean }
      required: [name, sortOrder, spendThreshold, period, subscriptionDiscountPercent, productDiscountPercent, freeSubscription, premiumListing, freeShipping, active]
    LoyaltyTierUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        sortOrder: { type: number, minimum: 0, maximum: 100000, multipleOf: 1 }
        spendThreshold: { type: number, minimum: 0, maximum: 1000000000, multipleOf: 1 }
        period: { type: string, minLength: 1, maxLength: 40 }
        subscriptionDiscountPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        productDiscountPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        freeSubscription: { type: boolean }
        premiumListing: { type: boolean }
        freeShipping: { type: boolean }
        benefits:
          type: array
          items: { type: string }
        active: { type: boolean }
    SubscriptionPlan:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        price: { type: number }
        trialDays: { type: number }
        features:
          type: array
          items: { type: string }
        limits:
          type: object
          additionalProperties: { type: number }
        active: { type: boolean }
      required: [id, name, price, trialDays, features, limits, active]
    SubscriptionPlanInput:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        price: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        trialDays: { type: number, minimum: 0, maximum: 3650, multipleOf: 1 }
        features:
          type: array
          items: { type: string }
        limits:
          type: object
          additionalProperties: { type: number, minimum: 0, multipleOf: 1 }
        active: { type: boolean }
      required: [name, price, trialDays, features, limits, active]
    SubscriptionPlanUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        price: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        trialDays: { type: number, minimum: 0, maximum: 3650, multipleOf: 1 }
        features:
          type: array
          items: { type: string }
        limits:
          type: object
          additionalProperties: { type: number, minimum: 0, multipleOf: 1 }
        active: { type: boolean }
    AdminReview:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        salonName: { type: string }
        customerId: { type: string }
        customerName: { type: string }
        serviceName: { type: string }
        rating: { type: number, minimum: 1, maximum: 5 }
        text: { type: string }
        visible: { type: boolean }
        date: { type: string, format: date-time }
      required: [id, salonId, salonName, customerId, customerName, serviceName, rating, text, visible, date]
    AdminReviewUpdate:
      type: object
      additionalProperties: false
      properties:
        visible: { type: boolean }
    ApiError:
      type: object
      properties:
        error: { type: string }
        code: { type: string }
        unavailableItems:
          type: array
          description: Products from the shopper's cart that are no longer available
          items:
            type: object
            properties:
              productId: { type: string }
              name: { type: string }
            required: [productId, name]
        issues:
          type: array
          items:
            type: object
            properties:
              path: { type: string }
              message: { type: string }
            required: [path, message]
      required: [error]
    ProductVariant:
      type: object
      properties:
        label: { type: string }
        value: { type: string }
        priceAdjust: { type: number, multipleOf: 1 }
        price: { type: number, minimum: 0, multipleOf: 1 }
        stock: { type: number, minimum: 0, multipleOf: 1 }
        sku: { type: string, minLength: 1 }
        swatch:
          type: ["object", "null"]
          properties:
            kind: { type: string, enum: [TEXT, COLOR, IMAGE] }
            text: { type: string, maxLength: 80 }
            hex: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
            imageUrl: { type: string }
          required: [kind]
        mainImageUrl: { type: ["string", "null"] }
        altText: { type: ["string", "null"], maxLength: 180 }
        sortOrder: { type: integer, minimum: 0, maximum: 10000 }
      required: [label, value]
    QuantityPricingTier:
      type: object
      additionalProperties: false
      properties:
        minQuantity: { type: integer, minimum: 1 }
        maxQuantity: { type: ["integer", "null"], minimum: 1 }
        unitPrice: { type: integer, minimum: 1 }
      required: [minQuantity, maxQuantity, unitPrice]
    ProductCharacteristic:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 100 }
        value: { type: string, minLength: 1, maxLength: 500 }
      required: [name, value]
    AdminProduct:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        categoryId: { type: ["string", "null"] }
        categoryName: { type: string }
        subcategoryName: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        description: { type: string }
        shortDescription: { type: ["string", "null"] }
        imageUrl: { type: string }
        images:
          type: array
          items: { type: string }
        price: { type: number, multipleOf: 1 }
        costPriceRsd: { type: ["integer", "null"], minimum: 0, maximum: 100000000 }
        averageDurationDays: { type: ["integer", "null"], minimum: 1, maximum: 3650 }
        treatmentTaxonomyIds: { type: array, uniqueItems: true, items: { type: string } }
        discountPrice: { type: ["number", "null"], multipleOf: 1 }
        discountPriceEndsAt: { type: ["string", "null"], format: date-time }
        retailEnabled: { type: boolean }
        priceOnRequest: { type: boolean, default: false }
        bulkMatrixEnabled: { type: boolean, default: false }
        professionalEnabled: { type: boolean }
        publicDescription: { type: ["string", "null"] }
        publicPrice: { type: ["number", "null"], minimum: 1, multipleOf: 1 }
        publicDiscountPrice: { type: ["number", "null"], minimum: 1, multipleOf: 1 }
        publicDiscountPriceEndsAt: { type: ["string", "null"], format: date-time }
        discountPercent: { type: ["number", "null"] }
        stock: { type: number, minimum: 0, multipleOf: 1 }
        catalogReference: { type: string, description: "Opaque immutable customer-facing catalog reference." }
        sku: { type: string }
        unit: { type: string }
        weightGrams: { type: ["number", "null"], multipleOf: 1 }
        isNew: { type: boolean }
        isBestseller: { type: boolean }
        variants:
          type: ["array", "null"]
          items: { $ref: "#/components/schemas/ProductVariant" }
        variantType: { type: ["string", "null"] }
        similarProductsMode: { type: string, enum: [AUTO_CATEGORY, MANUAL] }
        similarProductIds:
          type: array
          uniqueItems: true
          items: { type: string }
        crossSellProductIds:
          type: array
          uniqueItems: true
          maxItems: 5
          items: { type: string }
        quantityPricingTiers:
          type: array
          items: { $ref: "#/components/schemas/QuantityPricingTier" }
        minimumOrderQuantity: { type: integer, minimum: 1 }
        deliveryBusinessDaysOverride: { type: ["integer", "null"], minimum: 1, maximum: 365 }
        subscriptionAllowed: { type: boolean }
        subscriptionDiscountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        loyaltyPricingExcluded: { type: boolean }
        productTypeId: { type: ["string", "null"] }
        ingredients: { type: ["string", "null"] }
        usageInstructions: { type: ["string", "null"] }
        characteristics:
          type: array
          items: { $ref: "#/components/schemas/ProductCharacteristic" }
        searchSynonyms:
          type: array
          uniqueItems: true
          maxItems: 30
          items: { type: string, minLength: 1, maxLength: 100 }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
      required: [id, supplierId, name, categoryName, description, imageUrl, images, price, costPriceRsd, averageDurationDays, treatmentTaxonomyIds, discountPrice, discountPriceEndsAt, retailEnabled, professionalEnabled, publicDiscountPrice, publicDiscountPriceEndsAt, stock, catalogReference, sku, unit, isNew, isBestseller, similarProductsMode, similarProductIds, crossSellProductIds, quantityPricingTiers, minimumOrderQuantity, deliveryBusinessDaysOverride, subscriptionAllowed, subscriptionDiscountPercent, productTypeId, ingredients, usageInstructions, characteristics, searchSynonyms, active, createdAt]
    AdminProductList:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AdminProduct" }
        total: { type: number }
        page: { type: number }
        pageSize: { type: number }
        totalPages: { type: number }
      required: [items, total, page, pageSize, totalPages]
    AdminProductInput:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        categoryId: { type: ["string", "null"] }
        categoryName: { type: string, minLength: 1 }
        subcategoryName: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        description: { type: string, minLength: 1 }
        shortDescription: { type: ["string", "null"] }
        imageUrl: { type: string, minLength: 1 }
        images:
          type: array
          items: { type: string }
        price: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        costPriceRsd: { type: ["integer", "null"], minimum: 0, maximum: 100000000 }
        averageDurationDays: { type: ["integer", "null"], minimum: 1, maximum: 3650 }
        treatmentTaxonomyIds: { type: array, uniqueItems: true, maxItems: 100, items: { type: string } }
        discountPrice: { type: ["number", "null"], minimum: 0, maximum: 100000000, multipleOf: 1 }
        discountPriceEndsAt: { type: ["string", "null"], format: date-time }
        retailEnabled: { type: boolean }
        priceOnRequest: { type: boolean }
        bulkMatrixEnabled: { type: boolean }
        professionalEnabled: { type: boolean }
        publicDescription: { type: ["string", "null"], maxLength: 10000 }
        publicPrice: { type: ["number", "null"], minimum: 1, maximum: 100000000, multipleOf: 1 }
        publicDiscountPrice: { type: ["number", "null"], minimum: 1, maximum: 100000000, multipleOf: 1 }
        publicDiscountPriceEndsAt: { type: ["string", "null"], format: date-time }
        stock: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        sku: { type: string, minLength: 1 }
        unit: { type: string, minLength: 1 }
        weightGrams: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        isNew: { type: boolean }
        isBestseller: { type: boolean }
        variants:
          type: ["array", "null"]
          items: { $ref: "#/components/schemas/ProductVariant" }
        variantType: { type: ["string", "null"] }
        active: { type: boolean }
        similarProductsMode: { type: string, enum: [AUTO_CATEGORY, MANUAL], default: AUTO_CATEGORY }
        similarProductIds: { type: array, uniqueItems: true, items: { type: string }, default: [] }
        crossSellProductIds: { type: array, uniqueItems: true, maxItems: 5, items: { type: string }, default: [] }
        upsellProductIds: { type: array, uniqueItems: true, maxItems: 3, items: { type: string }, default: [] }
        quantityPricingTiers: { type: array, items: { $ref: "#/components/schemas/QuantityPricingTier" }, default: [] }
        minimumOrderQuantity: { type: integer, minimum: 1, default: 1 }
        deliveryBusinessDaysOverride: { type: ["integer", "null"], minimum: 1, maximum: 365 }
        subscriptionAllowed: { type: boolean, default: false }
        subscriptionDiscountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        loyaltyPricingExcluded: { type: boolean, default: false }
        productTypeId: { type: ["string", "null"] }
        needTagIds: { type: array, uniqueItems: true, items: { type: string } }
        ingredients: { type: ["string", "null"], maxLength: 20000 }
        usageInstructions: { type: ["string", "null"], maxLength: 20000 }
        characteristics:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/ProductCharacteristic" }
        searchSynonyms:
          type: array
          uniqueItems: true
          maxItems: 30
          items: { type: string, minLength: 1, maxLength: 100 }
      required: [categoryId, name, categoryName, description, imageUrl, price, stock, sku, unit, weightGrams]
    AdminProductUpdate:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        categoryId: { type: ["string", "null"] }
        categoryName: { type: string, minLength: 1 }
        subcategoryName: { type: ["string", "null"] }
        brand: { type: ["string", "null"] }
        description: { type: string, minLength: 1 }
        shortDescription: { type: ["string", "null"] }
        imageUrl: { type: string, minLength: 1 }
        images:
          type: array
          items: { type: string }
        price: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        costPriceRsd: { type: ["integer", "null"], minimum: 0, maximum: 100000000 }
        averageDurationDays: { type: ["integer", "null"], minimum: 1, maximum: 3650 }
        treatmentTaxonomyIds: { type: array, uniqueItems: true, maxItems: 100, items: { type: string } }
        discountPrice: { type: ["number", "null"], minimum: 0, maximum: 100000000, multipleOf: 1 }
        discountPriceEndsAt: { type: ["string", "null"], format: date-time }
        retailEnabled: { type: boolean }
        priceOnRequest: { type: boolean }
        bulkMatrixEnabled: { type: boolean }
        professionalEnabled: { type: boolean }
        publicDescription: { type: ["string", "null"], maxLength: 10000 }
        publicPrice: { type: ["number", "null"], minimum: 1, maximum: 100000000, multipleOf: 1 }
        publicDiscountPrice: { type: ["number", "null"], minimum: 1, maximum: 100000000, multipleOf: 1 }
        publicDiscountPriceEndsAt: { type: ["string", "null"], format: date-time }
        stock: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        sku: { type: string, minLength: 1 }
        unit: { type: string, minLength: 1 }
        weightGrams: { type: number, minimum: 0, maximum: 100000000, multipleOf: 1 }
        isNew: { type: boolean }
        isBestseller: { type: boolean }
        variants:
          type: ["array", "null"]
          items: { $ref: "#/components/schemas/ProductVariant" }
        variantType: { type: ["string", "null"] }
        active: { type: boolean }
        similarProductsMode: { type: string, enum: [AUTO_CATEGORY, MANUAL] }
        similarProductIds: { type: array, uniqueItems: true, items: { type: string } }
        crossSellProductIds: { type: array, uniqueItems: true, maxItems: 5, items: { type: string } }
        upsellProductIds: { type: array, uniqueItems: true, maxItems: 3, items: { type: string } }
        quantityPricingTiers: { type: array, items: { $ref: "#/components/schemas/QuantityPricingTier" } }
        minimumOrderQuantity: { type: integer, minimum: 1 }
        deliveryBusinessDaysOverride: { type: ["integer", "null"], minimum: 1, maximum: 365 }
        subscriptionAllowed: { type: boolean }
        subscriptionDiscountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        loyaltyPricingExcluded: { type: boolean }
        productTypeId: { type: ["string", "null"] }
        needTagIds: { type: array, uniqueItems: true, items: { type: string } }
        ingredients: { type: ["string", "null"], maxLength: 20000 }
        usageInstructions: { type: ["string", "null"], maxLength: 20000 }
        characteristics:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/ProductCharacteristic" }
        searchSynonyms:
          type: array
          uniqueItems: true
          maxItems: 30
          items: { type: string, minLength: 1, maxLength: 100 }
    AdminProductBulkUpdate:
      type: object
      additionalProperties: false
      properties:
        productIds:
          type: array
          minItems: 1
          items: { type: string }
        action:
          type: string
          enum: [activate, deactivate, set-category, adjust-price-percent, set-new, unset-new]
        categoryId: { type: ["string", "null"] }
        pricePercent: { type: number, minimum: -90, maximum: 500 }
      required: [productIds, action]
    AdminBulkResult:
      type: object
      properties:
        updated: { type: number }
      required: [updated]
    AdminProductCategory:
      type: object
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        slug: { type: string }
        parentId: { type: ["string", "null"] }
        sortOrder: { type: number }
        icon: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        active: { type: boolean }
        productCount: { type: number }
      required: [id, supplierId, name, slug, sortOrder, active, productCount]
    Supplier:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        scope: { type: string, enum: [B2B, B2C, BOTH] }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, name, slug, scope, logoUrl, active, createdAt, updatedAt]
    SupplierCategoryNode:
      type: object
      properties:
        id: { type: string }
        supplierId: { type: string }
        name: { type: string }
        slug: { type: string }
        parentId: { type: ["string", "null"] }
        path: { type: string }
        depth: { type: integer }
        sortOrder: { type: integer }
        active: { type: boolean }
        directProductCount: { type: integer }
        descendantProductCount: { type: integer }
      required: [id, supplierId, name, slug, parentId, path, depth, sortOrder, active, directProductCount, descendantProductCount]
    SupplierInput:
      type: object
      additionalProperties: false
      required: [name, slug, scope]
      properties:
        name: { type: string, minLength: 1 }
        slug: { type: string, minLength: 1, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$' }
        scope: { type: string, enum: [B2B, B2C, BOTH] }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
    SupplierUpdate:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1 }
        scope: { type: string, enum: [B2B, B2C, BOTH] }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
    AdminProductCategoryInput:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        parentId: { type: ["string", "null"] }
        sortOrder: { type: number, minimum: 0, maximum: 100000, multipleOf: 1 }
        icon: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        active: { type: boolean }
      required: [name]
    AdminProductCategoryUpdate:
      type: object
      additionalProperties: false
      properties:
        supplierId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, pattern: '.*\S.*' }
        parentId: { type: ["string", "null"] }
        sortOrder: { type: number, minimum: 0, maximum: 100000, multipleOf: 1 }
        icon: { type: ["string", "null"] }
        imageUrl: { type: ["string", "null"] }
        active: { type: boolean }
    AdminServiceCategory:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        description: { type: string }
        active: { type: boolean }
        fallbackImageUrl: { type: ["string", "null"] }
        serviceCount: { type: number, minimum: 0 }
      required: [id, name, slug, description, active, fallbackImageUrl, serviceCount]
    AdminServiceCategoryImageUpdate:
      type: object
      additionalProperties: false
      properties:
        fallbackImageUrl: { type: ["string", "null"], maxLength: 2000 }
      required: [fallbackImageUrl]
    ServiceCategoryImageUploadInput:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 240 }
        size: { type: number, minimum: 1, maximum: 8388608 }
        contentType: { type: string, minLength: 1, maxLength: 120 }
      required: [name, size, contentType]
    ServiceCategoryImageUpload:
      type: object
      properties:
        uploadUrl: { type: string }
        imageUrl: { type: string }
      required: [uploadUrl, imageUrl]
    AdminBrand:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        description: { type: string }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
        productCount: { type: number }
      required: [id, name, slug, description, active, productCount]
    ProductBrand:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        description: { type: string }
        logoUrl: { type: ["string", "null"] }
      required: [id, name, slug, description, logoUrl]
    AdminBrandInput:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1 }
        description: { type: string }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
      required: [name]
    AdminBrandUpdate:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1 }
        description: { type: string }
        logoUrl: { type: ["string", "null"] }
        active: { type: boolean }
    ShippingTier:
      type: object
      properties:
        maxWeightGrams: { type: number, minimum: 1, multipleOf: 1 }
        price: { type: number, minimum: 0, multipleOf: 1 }
        label: { type: string }
      required: [maxWeightGrams, price, label]
    ShippingConfig:
      type: object
      properties:
        freeShippingThreshold: { type: number }
        tiers:
          type: array
          items: { $ref: "#/components/schemas/ShippingTier" }
        personalDeliveryEnabled: { type: boolean }
        personalDeliveryName: { type: string }
        personalDeliveryPrice: { type: number, minimum: 0, multipleOf: 1 }
        personalDeliveryDescription: { type: string }
        updatedAt: { type: string, format: date-time }
      required: [freeShippingThreshold, tiers, personalDeliveryEnabled, personalDeliveryName, personalDeliveryPrice, personalDeliveryDescription, updatedAt]
    ShippingConfigInput:
      type: object
      additionalProperties: false
      properties:
        freeShippingThreshold: { type: number, minimum: 0, multipleOf: 1 }
        tiers:
          type: array
          items: { $ref: "#/components/schemas/ShippingTier" }
        personalDeliveryEnabled: { type: boolean }
        personalDeliveryName: { type: string, minLength: 1, maxLength: 120 }
        personalDeliveryPrice: { type: number, minimum: 0, multipleOf: 1 }
        personalDeliveryDescription: { type: string, maxLength: 500 }
      required: [freeShippingThreshold, tiers, personalDeliveryEnabled, personalDeliveryName, personalDeliveryPrice, personalDeliveryDescription]
    ShippingQuote:
      type: object
      properties:
        totalWeightGrams: { type: number }
        shippingCost: { type: number }
        freeShipping: { type: boolean }
        freeShippingThreshold: { type: number }
        amountToFreeShipping: { type: number }
        message: { type: ["string", "null"] }
        availableMethods:
          type: array
          items: { $ref: "#/components/schemas/DeliveryMethodOption" }
      required: [totalWeightGrams, shippingCost, freeShipping, freeShippingThreshold, amountToFreeShipping, message, availableMethods]
    DeliveryMethodOption:
      type: object
      properties:
        id: { type: string, enum: [courier, personal_belgrade] }
        name: { type: string }
        description: { type: string }
        price: { type: number, minimum: 0, multipleOf: 1 }
        available: { type: boolean }
      required: [id, name, description, price, available]

    # ---------------------------------------------------------------------------
    # Business Growth Phase 2 schemas
    # ---------------------------------------------------------------------------

    CustomerRetentionItem:
      type: object
      properties:
        salonCustomerId: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        status: { type: string, enum: [NEW, ACTIVE, VIP, AT_RISK, LOST] }
        completedCount: { type: number }
        lastVisitDaysAgo: { type: ["number", "null"] }
        typicalIntervalDays: { type: ["number", "null"] }
        totalSpend: { type: number }
        hasFutureAppointment: { type: boolean }
        explanation: { type: string }
        recommendedAction: { type: string }
        thresholdVersion: { type: number, description: "Active platform retention settings version used for this classification (0 = platform defaults)" }
      required: [salonCustomerId, firstName, lastName, email, phone, status, completedCount, lastVisitDaysAgo, typicalIntervalDays, totalSpend, hasFutureAppointment, explanation, recommendedAction, thresholdVersion]

    CustomerRetentionDetail:
      type: object
      properties:
        salonCustomerId: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        birthDate: { type: ["string", "null"], format: date, description: "Customer date of birth for birthday automation (YYYY-MM-DD)" }
        status: { type: string, enum: [NEW, ACTIVE, VIP, AT_RISK, LOST] }
        completedCount: { type: number }
        lastVisitDaysAgo: { type: ["number", "null"] }
        typicalIntervalDays: { type: ["number", "null"] }
        totalSpend: { type: number }
        hasFutureAppointment: { type: boolean }
        explanation: { type: string }
        recommendedAction: { type: string }
        thresholdVersion: { type: number, description: "Active platform retention settings version used for this classification (0 = platform defaults)" }
        recentAppointments:
          type: array
          items: { $ref: "#/components/schemas/AppointmentSummary" }
      required: [salonCustomerId, firstName, lastName, email, phone, status, completedCount, lastVisitDaysAgo, typicalIntervalDays, totalSpend, hasFutureAppointment, explanation, recommendedAction, thresholdVersion, recentAppointments]

    AppointmentSummary:
      type: object
      properties:
        id: { type: string }
        date: { type: string }
        status: { type: string }
        price: { type: number }
        serviceName: { type: string }
      required: [id, date, status, price, serviceName]

    AutomationRule:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        name: { type: string }
        trigger: { type: string, enum: [inactive_days, birthday, visit_count, first_visit_completed, package_completed, appointment_cancelled, expected_return_overdue] }
        triggerConfig:
          type: object
          additionalProperties: {}
        action: { type: string, enum: [send_email, send_sms, send_email_and_sms] }
        emailSubject: { type: ["string", "null"] }
        emailBody: { type: ["string", "null"] }
        smsBody: { type: ["string", "null"] }
        voucherCode: { type: ["string", "null"], description: "Optional discount/voucher code substituted into {{voucherCode}} template placeholders" }
        status: { type: string, enum: [active, paused, draft] }
        aiProposed: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, salonId, name, trigger, triggerConfig, action, status, aiProposed, createdAt, updatedAt]

    CreateAutomationRuleBody:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        trigger: { type: string, enum: [inactive_days, birthday, visit_count, first_visit_completed, package_completed, appointment_cancelled, expected_return_overdue] }
        triggerConfig:
          type: object
          additionalProperties: {}
        action: { type: string, enum: [send_email, send_sms, send_email_and_sms] }
        emailSubject: { type: ["string", "null"] }
        emailBody: { type: ["string", "null"], description: "Supports {{firstName}}, {{lastName}}, {{salonName}}, {{voucherCode}} placeholders" }
        smsBody: { type: ["string", "null"], description: "Supports {{firstName}}, {{lastName}}, {{salonName}}, {{voucherCode}} placeholders" }
        voucherCode: { type: ["string", "null"] }
      required: [name, trigger, action]

    UpdateAutomationRuleBody:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        trigger: { type: string, enum: [inactive_days, birthday, visit_count, first_visit_completed, package_completed, appointment_cancelled, expected_return_overdue] }
        triggerConfig:
          type: object
          additionalProperties: {}
        action: { type: string, enum: [send_email, send_sms, send_email_and_sms] }
        emailSubject: { type: ["string", "null"] }
        emailBody: { type: ["string", "null"] }
        smsBody: { type: ["string", "null"] }
        voucherCode: { type: ["string", "null"] }

    CreateAutomationFromAiProposalBody:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        trigger: { type: string, enum: [inactive_days, birthday, visit_count, first_visit_completed, package_completed, appointment_cancelled, expected_return_overdue] }
        triggerConfig:
          type: object
          additionalProperties: {}
        action: { type: string, enum: [send_email, send_sms, send_email_and_sms] }
        emailSubject: { type: ["string", "null"] }
        emailBody: { type: ["string", "null"] }
        smsBody: { type: ["string", "null"] }
        voucherCode: { type: ["string", "null"] }
        aiProposalContext: { type: string }
      required: [name, trigger, action, aiProposalContext]

    AutomationStats:
      type: object
      properties:
        ruleId: { type: string }
        totalRuns: { type: number }
        sentCount: { type: number }
        skippedCount: { type: number }
        failedCount: { type: number }
        attributedAppointments: { type: number }
        attributedRevenue:
          type: number
          description: Total price (RSD) of appointments attributed to this rule (cancelled and no-show excluded).
        completedAppointments:
          type: number
          description: Attributed appointments already completed (money earned).
        completedRevenue:
          type: number
          description: Price (RSD) of attributed appointments already completed. completedRevenue + upcomingRevenue = attributedRevenue.
        upcomingAppointments:
          type: number
          description: Attributed appointments still upcoming (pending/confirmed).
        upcomingRevenue:
          type: number
          description: Price (RSD) of attributed appointments still upcoming (pending/confirmed). completedRevenue + upcomingRevenue = attributedRevenue.
        cancelledAttributedAppointments:
          type: number
          description: Attributed appointments that were cancelled (excluded from realized numbers).
        cancelledAttributedRevenue:
          type: number
          description: Total price (RSD) of cancelled attributed appointments — revenue lost to cancellations.
        noShowAttributedAppointments:
          type: number
          description: Attributed appointments that became no-shows (excluded from realized numbers).
        noShowAttributedRevenue:
          type: number
          description: Total price (RSD) of no-show attributed appointments — not realized as campaign revenue.
        deliveredCount: { type: number }
        openedCount: { type: number }
        emailSentCount: { type: number }
        emailDeliveredCount: { type: number }
        emailOpenedCount: { type: number }
        emailFailedCount: { type: number }
        smsSentCount: { type: number }
        smsDeliveredCount: { type: number }
        smsFailedCount: { type: number }
        lastRunAt: { type: ["string", "null"], format: date-time }
        previous:
          type: object
          description: Counts for the preceding window of the same length. Present only when compare=previous is combined with a bounded preset or complete custom from/to range.
          properties:
            attributedAppointments: { type: number }
            attributedRevenue:
              type: number
              description: Total price (RSD) of appointments attributed in the preceding window (cancelled and no-show excluded), for the revenue trend.
            noShowAttributedAppointments:
              type: number
              description: Attributed appointments that became no-shows in the preceding window, excluded from realized numbers.
            noShowAttributedRevenue:
              type: number
              description: Total price (RSD) of no-show attributed appointments in the preceding window — not realized as campaign revenue.
            newClientCount:
              type: number
              description: Attributed appointments in the preceding window whose salon client had no completed appointment before the campaign message was sent.
            returningClientCount:
              type: number
              description: Attributed appointments in the preceding window whose salon client had at least one completed appointment before the campaign message was sent.
            knownClientCount:
              type: number
              description: Total attributed appointments from known clients in the preceding window (newClientCount + returningClientCount); clients without a linked salon customer are excluded.
            unknownClientCount:
              type: number
              description: Attributed appointments in the preceding window without a linked salon customer; these are excluded from the known-client denominator.
            newClientShare:
              type: ["number", "null"]
              minimum: 0
              maximum: 100
              description: Percentage of attributed appointments from new clients among appointments with known client history in the preceding window; null when no client history is known.
            emailDeliveredCount: { type: number }
            emailOpenedCount: { type: number }
            smsDeliveredCount: { type: number }
          required: [attributedAppointments, attributedRevenue, noShowAttributedAppointments, noShowAttributedRevenue, newClientCount, returningClientCount, knownClientCount, unknownClientCount, newClientShare, emailDeliveredCount, emailOpenedCount, smsDeliveredCount]
        newClientCount:
          type: number
          description: Attributed appointments from new clients in the requested period.
        knownClientCount:
          type: number
          description: Total attributed appointments from known clients in the requested period (newClientCount + returningClientCount); clients without a linked salon customer are excluded.
        unknownClientCount:
          type: number
          description: Attributed appointments in the requested period without a linked salon customer; these are excluded from the known-client denominator.
        newClientShare:
          type: ["number", "null"]
          minimum: 0
          maximum: 100
          description: Percentage of attributed appointments from new clients among appointments with known client history; unknown clients are excluded from the denominator, and null means no client history is known.
      required: [ruleId, totalRuns, sentCount, skippedCount, failedCount, attributedAppointments, attributedRevenue, completedAppointments, completedRevenue, upcomingAppointments, upcomingRevenue, cancelledAttributedAppointments, cancelledAttributedRevenue, noShowAttributedAppointments, noShowAttributedRevenue, newClientCount, knownClientCount, unknownClientCount, newClientShare, deliveredCount, openedCount, emailSentCount, emailDeliveredCount, emailOpenedCount, emailFailedCount, smsSentCount, smsDeliveredCount, smsFailedCount]

    AutomationAttributedAppointment:
      type: object
      properties:
        appointmentId: { type: string }
        date: { type: string, description: "Appointment date (YYYY-MM-DD)" }
        serviceName: { type: string }
        price:
          type: number
          description: Appointment price in RSD.
        clientFirstName:
          type: ["string", "null"]
          description: First name of the salon client who booked the appointment; null when the appointment has no linked salon customer.
        clientLastName:
          type: ["string", "null"]
          description: Last name of the salon client who booked the appointment; null when the appointment has no linked salon customer.
        isReturning:
          type: ["boolean", "null"]
          description: True when the salon client had at least one completed appointment before this campaign message was sent (returning client), false when this is their first appointment at the salon (new client), null when the appointment has no linked salon customer.
        salonCustomerId:
          type: ["string", "null"]
          description: Id of the linked salon customer, for jumping to the owner's client detail view; null when the appointment has no linked salon customer.
      required: [appointmentId, date, serviceName, price, clientFirstName, clientLastName, isReturning, salonCustomerId]

    AutomationAttributedAppointmentsPage:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AutomationAttributedAppointment" }
        total:
          type: number
          description: Total attributed (non-cancelled) appointments for the rule within the requested period, matching the stats count.
        newClientCount:
          type: number
          description: Attributed appointments (within the requested period) whose salon client had no completed appointment before the campaign message was sent — new clients the campaign brought in. The summary counts always cover the whole attributed set for the window, ignoring any clientType filter, so newClientCount + returningClientCount + unknownClientCount equals the unfiltered total.
        returningClientCount:
          type: number
          description: Attributed appointments (within the requested period) whose salon client already had at least one completed appointment before the campaign message was sent — returning clients the campaign won back. Ignores any clientType filter.
        unknownClientCount:
          type: number
          description: Attributed appointments (within the requested period) without a linked salon customer, so the new/returning split is unknown for them. Ignores any clientType filter.
        limit:
          type: number
          description: Page size actually applied by the server.
        offset:
          type: number
          description: Offset actually applied by the server.
      required: [items, total, newClientCount, returningClientCount, unknownClientCount, limit, offset]

    AutomationStatsOverviewItem:
      type: object
      properties:
        ruleId: { type: string }
        ruleName: { type: string }
        ruleStatus: { type: string }
        action: { type: string }
        totalRuns: { type: number }
        sentCount: { type: number }
        skippedCount: { type: number }
        failedCount: { type: number }
        attributedAppointments: { type: number }
        attributedRevenue:
          type: number
          description: Total price (RSD) of appointments attributed to this rule (cancelled and no-show excluded).
        completedAppointments:
          type: number
          description: Attributed appointments already completed (money earned).
        completedRevenue:
          type: number
          description: Price (RSD) of attributed appointments already completed. completedRevenue + upcomingRevenue = attributedRevenue.
        upcomingAppointments:
          type: number
          description: Attributed appointments still upcoming (pending/confirmed).
        upcomingRevenue:
          type: number
          description: Price (RSD) of attributed appointments still upcoming (pending/confirmed). completedRevenue + upcomingRevenue = attributedRevenue.
        cancelledAttributedAppointments:
          type: number
          description: Attributed appointments that were cancelled (excluded from realized numbers).
        cancelledAttributedRevenue:
          type: number
          description: Total price (RSD) of cancelled attributed appointments — revenue lost to cancellations.
        noShowAttributedAppointments:
          type: number
          description: Attributed appointments that became no-shows (excluded from realized numbers).
        noShowAttributedRevenue:
          type: number
          description: Total price (RSD) of no-show attributed appointments — not realized as campaign revenue.
        newClientCount:
          type: number
          description: Attributed appointments from new clients in the requested period, using the same derivation as the attributed-appointments response.
        knownClientCount:
          type: number
          description: Total attributed appointments from known clients in the requested period (newClientCount + returningClientCount); clients without a linked salon customer are excluded.
        returningClientCount:
          type: number
          description: Attributed appointments from returning clients in the requested period; a prior completed appointment must be strictly before coalesce(sentAt, executedAt, createdAt).
        unknownClientCount:
          type: number
          description: Attributed appointments in the requested period without a linked salon customer, so the new/returning split is unknown.
        emailSentCount: { type: number }
        emailDeliveredCount: { type: number }
        emailOpenedCount: { type: number }
        emailFailedCount: { type: number }
        smsSentCount: { type: number }
        smsDeliveredCount: { type: number }
        smsFailedCount: { type: number }
        lastRunAt: { type: ["string", "null"], format: date-time }
        previous:
          type: object
          description: Counts for the preceding window of the same length. Present only when compare=previous is combined with a bounded preset or complete custom from/to range.
          properties:
            attributedAppointments: { type: number }
            attributedRevenue:
              type: number
              description: Total price (RSD) of appointments attributed in the preceding window (cancelled and no-show excluded), for the revenue trend.
            noShowAttributedAppointments:
              type: number
              description: Attributed appointments that became no-shows in the preceding window, excluded from realized numbers.
            noShowAttributedRevenue:
              type: number
              description: Total price (RSD) of no-show attributed appointments in the preceding window — not realized as campaign revenue.
            newClientCount:
              type: number
              description: Attributed appointments from new clients in the preceding window.
            knownClientCount:
              type: number
              description: Total attributed appointments from known clients in the preceding window (newClientCount + returningClientCount); clients without a linked salon customer are excluded.
            newClientShare:
              type: ["number", "null"]
              minimum: 0
              maximum: 100
              description: Percentage of attributed appointments from new clients among appointments with known client history in the preceding window; null when no client history is known.
            emailDeliveredCount: { type: number }
            emailOpenedCount: { type: number }
            smsDeliveredCount: { type: number }
          required: [attributedAppointments, attributedRevenue, noShowAttributedAppointments, noShowAttributedRevenue, newClientCount, knownClientCount, newClientShare, emailDeliveredCount, emailOpenedCount, smsDeliveredCount]
        newClientShare:
          type: ["number", "null"]
          minimum: 0
          maximum: 100
          description: Percentage of attributed appointments from new clients among appointments with known client history; unknown clients are excluded from the denominator, and null means no client history is known.
      required: [ruleId, ruleName, ruleStatus, action, totalRuns, sentCount, skippedCount, failedCount, attributedAppointments, attributedRevenue, completedAppointments, completedRevenue, upcomingAppointments, upcomingRevenue, cancelledAttributedAppointments, cancelledAttributedRevenue, noShowAttributedAppointments, noShowAttributedRevenue, newClientCount, returningClientCount, unknownClientCount, knownClientCount, newClientShare, emailSentCount, emailDeliveredCount, emailOpenedCount, emailFailedCount, smsSentCount, smsDeliveredCount, smsFailedCount]

    AutomationTestRunResult:
      type: object
      properties:
        wouldTriggerCount: { type: number }
        eligibleCustomers: { type: number }
        skippedDueToOptOut: { type: number }
        skippedDueToRecentRun: { type: number }
        triggerBreakdown:
          type: object
          additionalProperties: { type: number }
      required: [wouldTriggerCount, eligibleCustomers, skippedDueToOptOut, skippedDueToRecentRun]

    TreatmentPackage:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        name: { type: string }
        description: { type: string }
        priceInDinars: { type: number }
        sessionCount: { type: number }
        validityDays: { type: number }
        active: { type: boolean }
        quotaPolicy: { type: string, enum: [shared_pool, per_service] }
        serviceIds:
          type: array
          items: { type: string }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PackageServiceQuota" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, salonId, name, description, priceInDinars, sessionCount, validityDays, active, quotaPolicy, serviceIds, serviceQuotas, createdAt, updatedAt]

    TreatmentPackagePublic:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        name: { type: string }
        description: { type: string }
        priceInDinars: { type: number }
        sessionCount: { type: number }
        validityDays: { type: number }
        quotaPolicy: { type: string, enum: [shared_pool, per_service] }
        serviceIds:
          type: array
          items: { type: string }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PackageServiceQuota" }
      required: [id, salonId, name, description, priceInDinars, sessionCount, validityDays, quotaPolicy, serviceIds, serviceQuotas]

    PackageServiceQuota:
      type: object
      additionalProperties: false
      properties:
        serviceId: { type: string }
        quota: { type: number, minimum: 1, maximum: 100, multipleOf: 1 }
      required: [serviceId, quota]

    CreateTreatmentPackageBody:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        description: { type: string }
        priceInDinars: { type: number, minimum: 0, multipleOf: 1 }
        # Deprecated compatibility fields. Runtime requires serviceQuotas for
        # every new definition; retained temporarily so older generated clients
        # can be upgraded without a compile-time break.
        sessionCount: { type: number, minimum: 1, maximum: 100, multipleOf: 1 }
        serviceIds:
          type: array
          items: { type: string }
        validityDays: { type: number, minimum: 1, maximum: 3650, multipleOf: 1 }
        active: { type: boolean }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PackageServiceQuota" }
          minItems: 1
      required: [name, priceInDinars]

    UpdateTreatmentPackageBody:
      type: object
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        description: { type: string }
        priceInDinars: { type: number, minimum: 0, multipleOf: 1 }
        sessionCount: { type: number, minimum: 1, maximum: 100, multipleOf: 1 }
        serviceIds:
          type: array
          items: { type: string }
        validityDays: { type: number, minimum: 1, maximum: 3650, multipleOf: 1 }
        active: { type: boolean }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PackageServiceQuota" }
          minItems: 1

    QuickPackagePurchaseInput:
      type: object
      additionalProperties: false
      properties:
        salonCustomerId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1, maxLength: 200 }
        description: { type: string }
        priceInDinars: { type: number, minimum: 0, multipleOf: 1 }
        validityDays: { type: number, minimum: 1, maximum: 3650, multipleOf: 1 }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PackageServiceQuota" }
          minItems: 1
        paymentStatus: { type: string, enum: [active, pending_payment] }
        paymentMethod: { type: string, enum: [pay_at_salon, bank_transfer] }
        notes: { type: ["string", "null"] }
      required: [salonCustomerId, name, priceInDinars, validityDays, serviceQuotas, paymentStatus, paymentMethod]

    QuickPackagePurchaseResult:
      type: object
      additionalProperties: false
      properties:
        package: { $ref: "#/components/schemas/TreatmentPackage" }
        purchase: { $ref: "#/components/schemas/PackagePurchase" }
      required: [package, purchase]

    PackagePurchase:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        packageId: { type: string }
        packageName: { type: string }
        salonCustomerId: { type: string }
        totalSessions: { type: number }
        remainingSessions: { type: number }
        quotaPolicy: { type: string, enum: [shared_pool, per_service] }
        serviceQuotas:
          type: array
          items: { $ref: "#/components/schemas/PurchaseServiceQuota" }
        priceInDinars: { type: number }
        paymentMethod: { type: string, enum: [pay_at_salon, bank_transfer] }
        status: { type: string, enum: [pending_payment, active, completed, expired, cancelled] }
        expiresAt: { type: string, format: date-time }
        paymentConfirmedAt: { type: ["string", "null"], format: date-time }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, salonId, packageId, packageName, salonCustomerId, totalSessions, remainingSessions, quotaPolicy, serviceQuotas, priceInDinars, paymentMethod, status, expiresAt, createdAt, updatedAt]

    PurchaseServiceQuota:
      type: object
      additionalProperties: false
      properties:
        serviceId: { type: string }
        totalQuota: { type: number }
        remainingQuota: { type: number }
      required: [serviceId, totalQuota, remainingQuota]

    PurchasePackageBody:
      type: object
      additionalProperties: false
      properties:
        paymentMethod: { type: string, enum: [pay_at_salon, bank_transfer] }
        notes: { type: ["string", "null"] }

    RedeemSessionBody:
      type: object
      additionalProperties: false
      properties:
        appointmentId: { type: string }
      required: [appointmentId]

    RedeemSessionResult:
      type: object
      properties:
        redemptionId: { type: string }
        remainingSessions: { type: number }
        purchaseId: { type: string }
      required: [redemptionId, remainingSessions, purchaseId]

    EmployeePerformanceMetrics:
      type: object
      properties:
        employeeId: { type: string }
        employeeName: { type: string }
        completedAppointments: { type: number }
        totalRevenue: { type: number }
        commissionType: { type: string, enum: [percent_of_revenue, fixed_per_treatment] }
        commissionPercent: { type: number }
        fixedAmountInDinars: { type: number }
        estimatedCommission: { type: number }
        noShowCount: { type: number }
        cancelledCount: { type: number }
        averageAppointmentValue: { type: number }
        averageRating: { type: number, description: "Average rating × 10 (10–50 scale), 0 if no reviews" }
        reviewCount: { type: number }
        rebookingRate: { type: number, minimum: 0, maximum: 1, description: "Fraction of customers who booked a second appointment" }
        locationBreakdown:
          type: array
          description: Exact-location metrics for this employee within the selected scope.
          items:
            type: object
            properties:
              salonId: { type: string }
              locationName: { type: string }
              completedAppointments: { type: number }
              totalRevenue: { type: number }
              estimatedCommission: { type: number }
              noShowCount: { type: number }
              cancelledCount: { type: number }
            required: [salonId, locationName, completedAppointments, totalRevenue, estimatedCommission, noShowCount, cancelledCount]
      required: [employeeId, employeeName, completedAppointments, totalRevenue, commissionType, commissionPercent, fixedAmountInDinars, estimatedCommission, noShowCount, cancelledCount, averageAppointmentValue, averageRating, reviewCount, rebookingRate, locationBreakdown]
    EmployeePerformanceResponse:
      type: object
      properties:
        scope: { type: string, enum: [location, all] }
        locations:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
            required: [id, name]
        employees:
          type: array
          items: { $ref: "#/components/schemas/EmployeePerformanceMetrics" }
      required: [scope, locations, employees]

    EmployeeCommissionSettings:
      type: object
      properties:
        id: { type: string }
        salonId: { type: string }
        employeeId: { type: string }
        commissionType: { type: string, enum: [percent_of_revenue, fixed_per_treatment] }
        commissionPercent: { type: number }
        fixedAmountInDinars: { type: number }
        perServiceOverrides:
          type: object
          additionalProperties: { type: number }
        updatedAt: { type: string, format: date-time }
      required: [id, salonId, employeeId, commissionType, commissionPercent, fixedAmountInDinars, perServiceOverrides, updatedAt]

    UpdateCommissionBody:
      type: object
      additionalProperties: false
      properties:
        commissionType: { type: string, enum: [percent_of_revenue, fixed_per_treatment] }
        commissionPercent: { type: number, minimum: 0, maximum: 100, multipleOf: 1 }
        fixedAmountInDinars: { type: number, minimum: 0, multipleOf: 1 }
        perServiceOverrides:
          type: object
          additionalProperties: { type: number }
      required: [commissionType]

    GrowthAiQuestionBody:
      type: object
      additionalProperties: false
      properties:
        question: { type: string, minLength: 1, maxLength: 2000 }
        snapshotPeriodDays: { type: number, minimum: 7, maximum: 365, multipleOf: 1 }
      required: [question]

    GrowthAiAnswer:
      type: object
      properties:
        answer: { type: string }
        snapshot: { $ref: "#/components/schemas/SalonMetricSnapshot" }
      required: [answer, snapshot]

    SalonMetricSnapshot:
      type: object
      properties:
        salonId: { type: string }
        salonName: { type: string }
        periodFrom: { type: string }
        periodTo: { type: string }
        generatedAt: { type: string, format: date-time }
        totalRevenue: { type: number }
        totalBookings: { type: number }
        completedBookings: { type: number }
        cancelledBookings: { type: number }
        noShowBookings: { type: number }
        retentionCounts:
          type: object
          properties:
            NEW: { type: number }
            ACTIVE: { type: number }
            VIP: { type: number }
            AT_RISK: { type: number }
            LOST: { type: number }
          required: [NEW, ACTIVE, VIP, AT_RISK, LOST]
        topServices:
          type: array
          items:
            type: object
            properties:
              serviceName: { type: string }
              bookingCount: { type: number }
              revenue: { type: number }
            required: [serviceName, bookingCount, revenue]
        topEmployees:
          type: array
          items:
            type: object
            properties:
              employeeName: { type: string }
              completedCount: { type: number }
              revenue: { type: number }
            required: [employeeName, completedCount, revenue]
        totalCustomers: { type: number }
      required: [salonId, salonName, periodFrom, periodTo, generatedAt, totalRevenue, totalBookings, completedBookings, cancelledBookings, noShowBookings, retentionCounts, topServices, topEmployees, totalCustomers]

    RetentionCustomer:
      type: object
      properties:
        salonCustomerId: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        birthDate: { type: ["string", "null"], format: date }
        status: { type: string, enum: [NEW, ACTIVE, VIP, AT_RISK, LOST] }
        completedCount: { type: number }
        lastVisitDaysAgo: { type: ["number", "null"] }
        typicalIntervalDays: { type: ["number", "null"] }
        totalSpend: { type: number }
        hasFutureAppointment: { type: boolean }
        explanation: { type: string }
        recommendedAction: { type: string }
      required: [salonCustomerId, firstName, lastName, status, completedCount, totalSpend, hasFutureAppointment, explanation, recommendedAction]

    RetentionCustomerDetail:
      allOf:
        - $ref: "#/components/schemas/RetentionCustomer"
        - type: object
          properties:
            recentAppointments:
              type: array
              items:
                type: object
                properties:
                  id: { type: string }
                  date: { type: string, format: date }
                  status: { type: string }
                  price: { type: number }
                  serviceName: { type: string }
                required: [id, date, status, price, serviceName]
          required: [recentAppointments]

    ReversalResult:
      type: object
      properties:
        remainingSessions: { type: number }
      required: [remainingSessions]

    RetentionThresholds:
      type: object
      properties:
        newCustomerWindowDays: { type: number, minimum: 1, maximum: 365, multipleOf: 1, description: "A single completed visit within this many days still counts as NEW" }
        defaultIntervalDays: { type: number, minimum: 1, maximum: 365, multipleOf: 1, description: "Assumed visit interval (days) when fewer than 2 completed visits exist" }
        atRiskIntervalPercent: { type: number, minimum: 100, maximum: 1000, multipleOf: 1, description: "AT_RISK when overdue beyond typical interval × this percent (150 = 1.5×)" }
        lostIntervalPercent: { type: number, minimum: 100, maximum: 2000, multipleOf: 1, description: "LOST when overdue beyond typical interval × this percent (250 = 2.5×); must exceed atRiskIntervalPercent" }
        lostMinimumDays: { type: number, minimum: 1, maximum: 1095, multipleOf: 1, description: "LOST never triggers before this many days since the last visit" }
        vipMinCompletedVisits: { type: number, minimum: 1, maximum: 100, multipleOf: 1, description: "VIP when the customer has at least this many completed visits" }
        vipSpendPercentOfMedian: { type: number, minimum: 100, maximum: 1000, multipleOf: 1, description: "VIP when total spend exceeds salon median × this percent (200 = 2×)" }
      required: [newCustomerWindowDays, defaultIntervalDays, atRiskIntervalPercent, lostIntervalPercent, lostMinimumDays, vipMinCompletedVisits, vipSpendPercentOfMedian]

    RetentionSettings:
      type: object
      properties:
        version: { type: number, description: "Active settings version; 0 means platform defaults (no admin change yet)" }
        thresholds: { $ref: "#/components/schemas/RetentionThresholds" }
        changedByUserId: { type: ["string", "null"] }
        changedByName: { type: ["string", "null"], description: "Resolved display name of the admin who made the change; null for platform defaults or when the user is unknown" }
        changedAt: { type: ["string", "null"], description: "ISO timestamp of the change; null for platform defaults" }
        isDefault: { type: boolean }
        defaults: { $ref: "#/components/schemas/RetentionThresholds" }
        changeSource: { type: string, enum: [manual, restore_version, restore_defaults], description: "How the active version came to be: hand-edited, restored from an earlier version, or restored platform defaults" }
        restoredFromVersion: { type: ["number", "null"], description: "Source version when changeSource is restore_version; null otherwise" }
      required: [version, thresholds, changedByUserId, changedByName, changedAt, isDefault, defaults, changeSource, restoredFromVersion]

    RetentionStatusCounts:
      type: object
      properties:
        NEW: { type: number }
        ACTIVE: { type: number }
        VIP: { type: number }
        AT_RISK: { type: number }
        LOST: { type: number }
      required: [NEW, ACTIVE, VIP, AT_RISK, LOST]

    RetentionReclassificationShift:
      type: object
      properties:
        fromStatus: { type: string, enum: [NEW, ACTIVE, VIP, AT_RISK, LOST] }
        toStatus: { type: string, enum: [NEW, ACTIVE, VIP, AT_RISK, LOST] }
        count: { type: number, description: "Customers that would move fromStatus → toStatus" }
      required: [fromStatus, toStatus, count]

    RetentionPreviewAffectedSalon:
      type: object
      properties:
        salonId: { type: string }
        salonName: { type: string }
        reclassifiedCount: { type: number, description: "Customers of this salon whose status would change under the candidate thresholds" }
        totalCustomers: { type: number, description: "Total customers this salon has — puts the reclassified count in proportion" }
        reclassifiedCountMarginOfError:
          type: ["number", "null"]
          description: "Approximate 95% Wilson margin of error for reclassifiedCount in a stratified estimate; null for exact counts and full-salon censuses"
        sampleSize:
          type: ["number", "null"]
          description: "Customers classified for this salon in a stratified estimate; null for exact counts"
      required: [salonId, salonName, reclassifiedCount, totalCustomers, reclassifiedCountMarginOfError, sampleSize]

    RetentionSettingsPreview:
      type: object
      properties:
        currentVersion: { type: number, description: "Settings version the current counts were computed against (0 = platform defaults)" }
        totalCustomers: { type: number, description: "Salon customers evaluated platform-wide" }
        reclassifiedCount: { type: number, description: "Customers whose status would change under the candidate thresholds" }
        reclassifiedCountMarginOfError:
          type: ["number", "null"]
          description: "Approximate 95% margin of error for reclassifiedCount when isEstimate is true; null in exact mode"
        currentCounts: { $ref: "#/components/schemas/RetentionStatusCounts" }
        currentCountMarginsOfError:
          oneOf:
            - $ref: "#/components/schemas/RetentionStatusCounts"
            - type: "null"
          description: "Approximate 95% Wilson margin of error for each current status count when isEstimate is true; null in exact mode"
        candidateCounts: { $ref: "#/components/schemas/RetentionStatusCounts" }
        candidateCountMarginsOfError:
          oneOf:
            - $ref: "#/components/schemas/RetentionStatusCounts"
            - type: "null"
          description: "Approximate 95% Wilson margin of error for each candidate status count when isEstimate is true; null in exact mode"
        shifts:
          type: array
          description: "Status moves under the candidate thresholds, largest first"
          items: { $ref: "#/components/schemas/RetentionReclassificationShift" }
        topAffectedSalons:
          type: array
          description: "Salons with the most reclassified customers, largest first (top 10); estimate mode returns rows only when salonRankingAvailable is true"
          items: { $ref: "#/components/schemas/RetentionPreviewAffectedSalon" }
        topShareAffectedSalons:
          type: array
          description: "Salons with the highest SHARE of reclassified customers, largest first (top 10); only salons with at least shareRankingMinCustomers customers qualify; estimate mode returns rows only when salonRankingAvailable is true"
          items: { $ref: "#/components/schemas/RetentionPreviewAffectedSalon" }
        shareRankingMinCustomers: { type: number, description: "Minimum customers a salon needs to qualify for the share-based ranking" }
        isEstimate:
          type: boolean
          description: "True when the platform exceeded the exact-preview cap and counts are approximate; render such values as approximate (~), never as exact. Salon rankings are returned only when salonRankingAvailable is true for the opt-in stratified design."
        sampleSize:
          type: ["number", "null"]
          description: "Customers actually classified when isEstimate is true; null in exact mode"
        salonRankingAvailable:
          type: boolean
          description: "True for exact previews and for estimate previews backed by the opt-in stratified per-salon sample; false means per-salon rankings are intentionally unavailable"
      required: [currentVersion, totalCustomers, reclassifiedCount, reclassifiedCountMarginOfError, currentCounts, currentCountMarginsOfError, candidateCounts, candidateCountMarginsOfError, shifts, topAffectedSalons, topShareAffectedSalons, shareRankingMinCustomers, isEstimate, sampleSize, salonRankingAvailable]

    RetentionSettingsUpdate:
      allOf:
        - $ref: "#/components/schemas/RetentionThresholds"
        - type: object
          properties:
            changeSource:
              type: string
              enum: [manual, restore_version, restore_defaults]
              description: "How this version came to be; defaults to manual. restore_version requires restoredFromVersion and thresholds identical to that version; restore_defaults requires thresholds identical to the platform defaults"
            restoredFromVersion:
              type: number
              minimum: 1
              multipleOf: 1
              description: "Version whose values are being restored; only allowed (and required) when changeSource is restore_version"
            expectedVersion:
              type: number
              minimum: 0
              multipleOf: 1
              description: "Active settings version this edit was based on (0 = platform defaults); the server rejects with 409 when a newer version exists"
          required: [expectedVersion]

    RetentionSettingsVersionConflict:
      type: object
      properties:
        error: { type: string }
        code: { type: string, enum: [VERSION_CONFLICT] }
        expectedVersion: { type: number, description: "Version the client based its edit on" }
        activeVersion: { type: number, description: "Version currently active on the server" }
        changedByName: { type: ["string", "null"], description: "Display name of the administrator who saved the winning version; null when platform defaults are active or the user is unknown" }
        changedAt: { type: ["string", "null"], description: "ISO timestamp of the winning version; null when platform defaults are active" }
      required: [error, code, expectedVersion, activeVersion, changedByName, changedAt]

    RetentionSettingsHistoryEntry:
      type: object
      properties:
        version: { type: number }
        thresholds: { $ref: "#/components/schemas/RetentionThresholds" }
        previousThresholds: { $ref: "#/components/schemas/RetentionThresholds" }
        changedByUserId: { type: ["string", "null"] }
        changedByName: { type: ["string", "null"] }
        changedAt: { type: string, description: "ISO timestamp of the change" }
        changeSource: { type: string, enum: [manual, restore_version, restore_defaults], description: "How the version came to be: hand-edited, restored from an earlier version, or restored platform defaults" }
        restoredFromVersion: { type: ["number", "null"], description: "Source version when changeSource is restore_version; null otherwise" }
      required: [version, thresholds, previousThresholds, changedByUserId, changedByName, changedAt, changeSource, restoredFromVersion]

    GrowthAdminSummary:
      type: object
      properties:
        automation:
          type: object
          properties:
            totalRules: { type: number }
            activeRules: { type: number }
            byStatus:
              type: object
              additionalProperties: { type: number }
          required: [totalRules, activeRules, byStatus]
        packages:
          type: object
          properties:
            total: { type: number }
            active: { type: number }
          required: [total, active]
        purchases:
          type: object
          properties:
            total: { type: number }
            active: { type: number }
            pendingPayment: { type: number }
          required: [total, active, pendingPayment]
      required: [automation, packages, purchases]

    # ── Phase 3 schemas ──────────────────────────────────────────────────────
    TreatmentPhoto:
      type: object
      properties:
        id: { type: string }
        appointmentId: { type: string }
        salonCustomerId: { type: string }
        employeeId: { type: ["string", "null"] }
        employeeName: { type: ["string", "null"] }
        kind: { type: string, enum: [before, after] }
        url: { type: string }
        consentConfirmed: { type: boolean }
        createdAt: { type: string }
        appointmentDate: { type: ["string", "null"] }
        serviceName: { type: ["string", "null"] }
      required: [id, appointmentId, salonCustomerId, kind, url, consentConfirmed, createdAt]
    TreatmentPhotoCreate:
      type: object
      properties:
        kind: { type: string, enum: [before, after] }
        url: { type: string, minLength: 1, maxLength: 500 }
        consentConfirmed: { type: boolean }
      required: [kind, url, consentConfirmed]
    ServiceConsumption:
      type: object
      properties:
        serviceId: { type: string }
        productId: { type: string }
        quantityPerUse: { type: number }
        productName: { type: string }
        productUnit: { type: string }
      required: [serviceId, productId, quantityPerUse, productName, productUnit]
    ServiceConsumptionsPut:
      type: object
      properties:
        items:
          type: array
          maxItems: 50
          items:
            type: object
            properties:
              productId: { type: string }
              quantityPerUse: { type: number, minimum: 0.01, maximum: 1000000 }
            required: [productId, quantityPerUse]
      required: [items]
    SalonInventoryItem:
      type: object
      properties:
        productId: { type: string }
        productName: { type: string }
        productSku: { type: string }
        productImageUrl: { type: string }
        quantity: { type: number }
        unit: { type: string }
        unitContentAmount: { type: number }
        usageUnit: { type: ["string", "null"] }
        lowStockThreshold: { type: ["number", "null"] }
        effectiveThreshold: { type: number }
        lowStock: { type: boolean }
        updatedAt: { type: string }
      required: [productId, productName, productSku, productImageUrl, quantity, unit, unitContentAmount, effectiveThreshold, lowStock, updatedAt]
    SalonInventoryItemUpdate:
      type: object
      properties:
        quantity: { type: number, minimum: 0, maximum: 100000000 }
        lowStockThreshold: { type: ["number", "null"], minimum: 0, maximum: 100000000 }
        unitContentAmount: { type: number, minimum: 0.01, maximum: 100000000 }
        usageUnit: { type: ["string", "null"], maxLength: 20 }
    EmployeeClockEntry:
      type: object
      properties:
        id: { type: string }
        employeeId: { type: string }
        clockInAt: { type: string }
        clockOutAt: { type: ["string", "null"] }
        editedByOwner: { type: boolean }
        note: { type: ["string", "null"] }
        durationMinutes: { type: ["number", "null"] }
      required: [id, employeeId, clockInAt, editedByOwner]
    EmployeeClockStatus:
      type: object
      properties:
        openEntry:
          oneOf:
            - $ref: "#/components/schemas/EmployeeClockEntry"
            - type: "null"
        entries:
          type: array
          items: { $ref: "#/components/schemas/EmployeeClockEntry" }
        weekMinutes: { type: number }
        monthMinutes: { type: number }
      required: [entries, weekMinutes, monthMinutes]
    SalonClockEmployeeSummary:
      type: object
      properties:
        employeeId: { type: string }
        employeeName: { type: string }
        totalMinutes: { type: number }
        appointmentCount: { type: number }
        openEntry: { type: boolean }
        staleOpenEntry: { type: boolean }
        entries:
          type: array
          items: { $ref: "#/components/schemas/EmployeeClockEntry" }
      required: [employeeId, employeeName, totalMinutes, appointmentCount, openEntry, staleOpenEntry, entries]
    SalonClockEntryUpdate:
      type: object
      properties:
        clockOutAt: { type: string, minLength: 1 }
        note: { type: ["string", "null"], maxLength: 300 }
      required: [clockOutAt]
    ShiftSwapRequest:
      type: object
      properties:
        id: { type: string }
        requesterEmployeeId: { type: string }
        requesterName: { type: string }
        targetEmployeeId: { type: string }
        targetName: { type: string }
        swapDate: { type: string }
        note: { type: ["string", "null"] }
        status: { type: string, enum: [pending_colleague, colleague_declined, pending_owner, owner_declined, approved, cancelled] }
        colleagueRespondedAt: { type: ["string", "null"] }
        ownerReviewedAt: { type: ["string", "null"] }
        createdAt: { type: string }
      required: [id, requesterEmployeeId, requesterName, targetEmployeeId, targetName, swapDate, status, createdAt]
    EmployeeShiftSwapOverview:
      type: object
      properties:
        outgoing:
          type: array
          items: { $ref: "#/components/schemas/ShiftSwapRequest" }
        incoming:
          type: array
          items: { $ref: "#/components/schemas/ShiftSwapRequest" }
        colleagues:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
            required: [id, name]
      required: [outgoing, incoming, colleagues]
    ShiftSwapCreate:
      type: object
      properties:
        targetEmployeeId: { type: string }
        swapDate: { type: string }
        note: { type: ["string", "null"], maxLength: 300 }
      required: [targetEmployeeId, swapDate]
    ShiftSwapRespondBody:
      type: object
      properties:
        accept: { type: boolean }
      required: [accept]
    ShiftSwapReviewBody:
      type: object
      properties:
        approve: { type: boolean }
      required: [approve]
    ShiftSwapAppointmentPreview:
      type: object
      properties:
        id: { type: string }
        employeeId: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        serviceName: { type: string }
        customerName: { type: ["string", "null"] }
        status: { type: string }
      required: [id, employeeId, startTime, endTime, serviceName, status]
    SalonShiftSwapRequest:
      type: object
      properties:
        request: { $ref: "#/components/schemas/ShiftSwapRequest" }
        requesterAppointments:
          type: array
          items: { $ref: "#/components/schemas/ShiftSwapAppointmentPreview" }
        targetAppointments:
          type: array
          items: { $ref: "#/components/schemas/ShiftSwapAppointmentPreview" }
      required: [request, requesterAppointments, targetAppointments]
    WidgetSalon:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        city: { type: string }
        address: { type: string }
        services:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              durationMinutes: { type: number }
              price: { type: number }
              promoPrice: { type: ["number", "null"] }
              categoryName: { type: string }
            required: [id, name, durationMinutes, price, categoryName]
        employees:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              role: { type: string }
              serviceIds:
                type: array
                items: { type: string }
            required: [id, name, role, serviceIds]
      required: [id, name, slug, city, address, services, employees]
    WidgetSlot:
      type: object
      properties:
        start: { type: string }
        end: { type: string }
        employeeId: { type: string }
        employeeName: { type: string }
      required: [start, end, employeeId, employeeName]
    EmployeeTimeBlock:
      type: object
      properties:
        id: { type: string }
        employeeId: { type: string }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        endTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        reason: { type: string }
      required: [id, employeeId, date, startTime, endTime, reason]
    EmployeeTimeBlockInput:
      type: object
      properties:
        employeeId: { type: string, minLength: 1 }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        endTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        reason: { type: string, minLength: 1, maxLength: 500 }
      required: [employeeId, date, startTime, endTime, reason]
    AvailabilitySearchSlot:
      type: object
      properties:
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        endTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        employeeId: { type: string }
        employeeName: { type: string }
      required: [date, startTime, endTime, employeeId, employeeName]
    AvailabilitySearchResponse:
      type: array
      description: Server-confirmed canonical slots for exactly seven calendar days beginning at the requested startDate. The fixed window lets calendar clients represent every date and disable dates absent from this list.
      items: { $ref: "#/components/schemas/AvailabilitySearchSlot" }
    SalonCalendarScheduleWindow:
      type: object
      properties:
        startTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        endTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        breakStart: { type: ["string", "null"], pattern: '^\d{2}:\d{2}$' }
        breakEnd: { type: ["string", "null"], pattern: '^\d{2}:\d{2}$' }
      required: [startTime, endTime, breakStart, breakEnd]
    SalonCalendarDayEmployee:
      type: object
      properties:
        employeeId: { type: string }
        name: { type: string }
        role: { type: string }
        hasExplicitSchedule: { type: boolean }
        scheduleWindows:
          type: array
          items: { $ref: "#/components/schemas/SalonCalendarScheduleWindow" }
        unavailable: { type: boolean }
        unavailableReason: { type: ["string", "null"] }
      required: [employeeId, name, role, hasExplicitSchedule, scheduleWindows, unavailable, unavailableReason]
    EmployeeLocationScheduleWindow:
      type: object
      properties:
        weekday: { type: integer, minimum: 1, maximum: 7 }
        startTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        endTime: { type: string, pattern: '^\d{2}:\d{2}$' }
        breakStart: { type: ["string", "null"], pattern: '^\d{2}:\d{2}$' }
        breakEnd: { type: ["string", "null"], pattern: '^\d{2}:\d{2}$' }
      required: [weekday, startTime, endTime, breakStart, breakEnd]
    EmployeeLocationScheduleUpdate:
      type: object
      properties:
        windows:
          type: array
          items: { $ref: "#/components/schemas/EmployeeLocationScheduleWindow" }
      required: [windows]
    EmployeeLocationAssignmentUpdate:
      type: object
      properties:
        active: { type: boolean }
        isDefault: { type: boolean }
      minProperties: 1
    EmployeeLocationAssignment:
      type: object
      properties:
        employeeId: { type: string }
        salonId: { type: string }
        locationId: { type: string, description: Compatibility alias for salonId. }
        locationName: { type: string }
        active: { type: boolean }
        isDefault: { type: boolean }
        scheduleWindows:
          type: array
          items: { $ref: "#/components/schemas/EmployeeLocationScheduleWindow" }
      required: [employeeId, salonId, locationId, locationName, active, isDefault, scheduleWindows]
    CreateSalonLocationInput:
      type: object
      properties:
        idempotencyKey: { type: string, minLength: 1, maxLength: 200 }
        sourceSalonId: { type: ["string", "null"] }
        copyServices: { type: boolean, default: false }
        copyPackages: { type: boolean, default: false }
        activateAfterCreate: { type: boolean, default: false }
        name: { type: string, minLength: 1, maxLength: 160 }
        city: { type: string, minLength: 1, maxLength: 100 }
        municipality: { type: string, minLength: 1, maxLength: 100 }
        address: { type: string, minLength: 1, maxLength: 250 }
        postalCode: { type: ["string", "null"], maxLength: 30 }
        phone: { type: string, minLength: 1, maxLength: 50 }
        # The endpoint validates mailbox syntax. Avoid OpenAPI `format: email`
        # because this workspace's Zod-3 generator emits a Zod-4-only helper.
        email: { type: string, minLength: 3, maxLength: 254 }
        shortDescription: { type: string, minLength: 1, maxLength: 500 }
        description: { type: string, minLength: 1, maxLength: 10000 }
        imageUrl: { type: string, minLength: 1, maxLength: 2000 }
      required: [idempotencyKey, name, city, municipality, address, phone, email, shortDescription, description, imageUrl]
    CreateSalonLocationResult:
      type: object
      properties:
        location: { $ref: "#/components/schemas/SalonProfile" }
        salon: { $ref: "#/components/schemas/SalonProfile", description: Compatibility alias for location. }
        copiedServices: { type: integer, minimum: 0 }
        copiedPackages: { type: integer, minimum: 0 }
        copiedPackageServiceLinks: { type: integer, minimum: 0 }
        warnings: { type: array, items: { type: string } }
      required: [location, salon, copiedServices, copiedPackages, copiedPackageServiceLinks, warnings]
    WidgetAppointmentCreate:
      type: object
      properties:
        serviceId: { type: string }
        employeeId: { type: ["string", "null"] }
        date: { type: string }
        startTime: { type: string }
        firstName: { type: string, minLength: 1, maxLength: 80 }
        lastName: { type: string, minLength: 1, maxLength: 80 }
        phone: { type: string, minLength: 5, maxLength: 30 }
        email: { type: ["string", "null"], maxLength: 160 }
        note: { type: ["string", "null"], maxLength: 500 }
      required: [serviceId, date, startTime, firstName, lastName, phone]
    WidgetAppointmentCreated:
      type: object
      properties:
        appointmentId: { type: string }
        status: { type: string }
        date: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        employeeName: { type: string }
        serviceName: { type: string }
        salonName: { type: string }
      required: [appointmentId, status, date, startTime, endTime, employeeName, serviceName, salonName]
    WidgetBookingGroupCreate:
      type: object
      properties:
        firstName: { type: string, minLength: 1, maxLength: 80 }
        lastName: { type: string, minLength: 1, maxLength: 80 }
        phone: { type: string, minLength: 5, maxLength: 30 }
        email: { type: ["string", "null"], maxLength: 160 }
        note: { type: ["string", "null"], maxLength: 500 }
        treatments:
          type: array
          minItems: 1
          maxItems: 5
          items:
            type: object
            properties:
              serviceId: { type: string }
              employeeId: { type: ["string", "null"] }
              date: { type: string, format: date }
              startTime: { type: string }
            required: [serviceId, date, startTime]
      required: [firstName, lastName, phone, treatments]
    BeautyJobListing:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        categoryId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        categorySlug: { type: string }
        categoryName: { type: string }
        type: { type: string, enum: [job, equipment_rental, space_rental, freelance] }
        intent: { type: string, enum: [offering, seeking] }
        title: { type: string }
        description: { type: string }
        city: { type: string }
        region: { type: string }
        latitude: { type: [number, "null"] }
        longitude: { type: [number, "null"] }
        priceAmount: { type: [integer, "null"] }
        pricePeriod: { type: [string, "null"] }
        negotiable: { type: boolean }
        isUrgent: { type: boolean }
        photos: { type: array, items: { type: string } }
        status: { type: string }
        moderationStatus: { type: string }
        moderationReason: { type: [string, "null"] }
        moderatedAt: { type: [string, "null"], format: date-time }
        contactCount: { type: integer }
        viewCount: { type: integer }
        expiresAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        availabilityPattern: { type: [string, "null"] }
        dayLabels: { type: array, items: { type: string } }
        authorDisplayName: { type: string }
        postedByType: { type: string, enum: [salon, user] }
        updatedAt: { type: string, format: date-time }
        isSaved: { type: boolean }
        isOwner: { type: boolean }
        availableSlots: { type: array, items: { $ref: "#/components/schemas/BeautyJobRentalSlot" } }
      required: [id, categoryId, categorySlug, categoryName, type, intent, title, description, city, region, latitude, longitude, priceAmount, pricePeriod, negotiable, isUrgent, photos, status, moderationStatus, moderationReason, moderatedAt, contactCount, viewCount, expiresAt, createdAt, updatedAt, availabilityPattern, dayLabels, authorDisplayName, postedByType, isSaved, isOwner, availableSlots]
    BeautyJobModerationAuditEvent:
      type: object
      description: Administrator-only immutable moderation event. Public reasons may be shown to the listing author elsewhere; internal notes must remain admin-only.
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        action: { type: string }
        actingAdminUserId: { type: [string, "null"], pattern: '^[0-9a-fA-F-]{36}$' }
        administratorDisplayName: { type: string }
        publicReason: { type: [string, "null"] }
        internalNote: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, action, actingAdminUserId, administratorDisplayName, publicReason, internalNote, createdAt]
    BeautyJobAdminPreview:
      type: object
      properties:
        listing: { $ref: "#/components/schemas/BeautyJobListing" }
        moderationHistory:
          type: array
          items: { $ref: "#/components/schemas/BeautyJobModerationAuditEvent" }
      required: [listing, moderationHistory]
    BeautyJobCreateInput:
      type: object
      properties:
        categoryId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        type: { type: string, enum: [job, equipment_rental, space_rental, freelance] }
        intent: { type: string, enum: [offering, seeking] }
        title: { type: string, minLength: 3, maxLength: 160 }
        description: { type: string, minLength: 10, maxLength: 10000 }
        city: { type: string, minLength: 1, maxLength: 120 }
        region: { type: string, minLength: 1, maxLength: 120 }
        latitude: { type: number, minimum: -90, maximum: 90 }
        longitude: { type: number, minimum: -180, maximum: 180 }
        priceAmount: { type: integer, minimum: 0 }
        pricePeriod: { type: string, enum: [hour, day, week, month, project, fixed] }
        negotiable: { type: boolean }
        isUrgent: { type: boolean }
        photos: { type: array, maxItems: 8, items: { type: string, pattern: '^/api/media/images/[0-9a-fA-F-]{36}$' } }
        availabilityPattern: { type: string, minLength: 1, maxLength: 1000 }
        dayLabels: { type: array, maxItems: 14, items: { type: string, maxLength: 80 } }
        availableSlots: { type: array, maxItems: 100, items: { $ref: "#/components/schemas/BeautyJobRentalSlotInput" } }
      required: [categoryId, type, intent, title, description, city, region]
    BeautyJobUpdateInput:
      type: object
      properties:
        categoryId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        type: { type: string, enum: [job, equipment_rental, space_rental, freelance] }
        intent: { type: string, enum: [offering, seeking] }
        title: { type: string, minLength: 3, maxLength: 160 }
        description: { type: string, minLength: 10, maxLength: 10000 }
        city: { type: string, minLength: 1, maxLength: 120 }
        region: { type: string, minLength: 1, maxLength: 120 }
        latitude: { type: [number, "null"], minimum: -90, maximum: 90 }
        longitude: { type: [number, "null"], minimum: -180, maximum: 180 }
        priceAmount: { type: [integer, "null"], minimum: 0 }
        pricePeriod: { type: [string, "null"], enum: [hour, day, week, month, project, fixed] }
        negotiable: { type: boolean }
        isUrgent: { type: boolean }
        photos: { type: array, maxItems: 8, items: { type: string, pattern: '^/api/media/images/[0-9a-fA-F-]{36}$' } }
        availabilityPattern: { type: [string, "null"], minLength: 1, maxLength: 1000 }
        dayLabels: { type: array, maxItems: 14, items: { type: string, maxLength: 80 } }
        availableSlots: { type: array, maxItems: 100, items: { $ref: "#/components/schemas/BeautyJobRentalSlotInput" } }
    BeautyJobRentalSlotInput:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
      required: [startsAt, endsAt]
    BeautyJobRentalSlot:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        listingId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        available: { type: boolean }
      required: [id, listingId, startsAt, endsAt, available]
    BeautyJobRentalRequestCreateInput:
      type: object
      properties:
        slotId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        message: { type: string, maxLength: 1000 }
      required: [slotId]
    BeautyJobRentalRequestUpdateInput:
      type: object
      properties:
        status: { type: string, enum: [accepted, declined] }
      required: [status]
    BeautyJobRentalRequest:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        listingId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        slotId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        applicantUserId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        message: { type: [string, "null"] }
        status: { type: string, enum: [pending, accepted, declined] }
        respondedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        listingTitle: { type: string }
        applicantDisplayName: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
      required: [id, listingId, slotId, applicantUserId, message, status, respondedAt, createdAt, updatedAt, listingTitle, applicantDisplayName, startsAt, endsAt]
    BeautyJobContactCreateInput:
      type: object
      properties: { message: { type: string, minLength: 1, maxLength: 4000 } }
      required: [message]
    BeautyJobContactUpdateInput:
      type: object
      properties:
        authorReply: { type: string, minLength: 1, maxLength: 4000 }
        authorStatus: { type: string, enum: [pending, viewed, accepted, declined, replied] }
    BeautyJobApplicantDecisionInput:
      type: object
      properties:
        contactIds:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        action: { type: string, enum: [approve, reject] }
        internalNote: { type: string, maxLength: 2000 }
      required: [contactIds, action]
    BeautyJobReportCreateInput:
      type: object
      properties: { reason: { type: string, minLength: 3, maxLength: 1000 } }
      required: [reason]
    BeautyJobSettingsUpdateInput:
      type: object
      properties:
        listingExpiryDays: { type: integer, minimum: 1, maximum: 365 }
        hourlyPostingLimit: { type: integer, minimum: 1, maximum: 100 }
      required: [listingExpiryDays, hourlyPostingLimit]
    BeautyJobModerationInput:
      type: object
      properties:
        action: { type: string, enum: [approve, reject, close, reactivate] }
        reason: { type: string, maxLength: 1000 }
        internalNote: { type: string, maxLength: 2000, description: Administrator-only moderation context. }
      required: [action]
    BeautyJobReportResolveInput:
      type: object
      properties:
        status: { type: string, enum: [resolved, dismissed] }
        resolutionNote: { type: string, maxLength: 1000 }
      required: [status]
    BeautyJobCategory:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        slug: { type: string }
        name: { type: string }
        subtypeLabels: { type: array, items: { type: string } }
        featureFlag: { type: [string, "null"] }
      required: [id, slug, name, subtypeLabels, featureFlag]
    BeautyJobContact:
      type: object
      properties:
        id: { type: string }
        listingId: { type: string }
        applicantUserId: { type: string }
        applicantMessage: { type: string }
        applicantStatus: { type: string }
        authorReply: { type: [string, "null"] }
        authorStatus: { type: string }
        repliedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        listingTitle: { type: string }
        applicantDisplayName: { type: string }
      required: [id, listingId, applicantUserId, applicantMessage, applicantStatus, authorReply, authorStatus, repliedAt, createdAt, updatedAt]
    BeautyJobApplicant:
      type: object
      properties:
        id: { type: string }
        listingId: { type: string }
        applicantUserId: { type: string }
        applicantMessage: { type: string }
        applicantStatus: { type: string }
        authorReply: { type: [string, "null"] }
        authorStatus: { type: string }
        rejectionNote: { type: [string, "null"] }
        decisionActorUserId: { type: [string, "null"] }
        decisionAt: { type: [string, "null"], format: date-time }
        repliedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        applicantDisplayName: { type: string }
        actions:
          type: array
          items: { $ref: "#/components/schemas/BeautyJobApplicationAction" }
      required: [id, listingId, applicantUserId, applicantMessage, applicantStatus, authorReply, authorStatus, rejectionNote, decisionActorUserId, decisionAt, repliedAt, createdAt, updatedAt, applicantDisplayName, actions]
    BeautyJobApplicationAction:
      type: object
      properties:
        id: { type: string }
        contactId: { type: string }
        listingId: { type: string }
        fromStatus: { type: string }
        toStatus: { type: string }
        privateNote: { type: [string, "null"] }
        actorUserId: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, contactId, listingId, fromStatus, toStatus, privateNote, actorUserId, createdAt]
    BeautyJobReport:
      type: object
      properties:
        id: { type: string }
        listingId: { type: string }
        reporterUserId: { type: [string, "null"] }
        reason: { type: string }
        status: { type: string }
        resolvedByUserId: { type: [string, "null"] }
        resolutionNote: { type: [string, "null"] }
        resolvedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        listingTitle: { type: string }
        authorSalonId: { type: [string, "null"] }
        authorUserId: { type: [string, "null"] }
      required: [id, listingId, reporterUserId, reason, status, resolvedByUserId, resolutionNote, resolvedAt, createdAt]
    BeautyJobNotification:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        recipientUserId: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        listingId: { type: [string, "null"] }
        contactId: { type: [string, "null"] }
        type: { type: string }
        title: { type: string }
        body: { type: string }
        readAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, recipientUserId, listingId, contactId, type, title, body, readAt, createdAt]
    BeautyJobSettings:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        listingExpiryDays: { type: integer }
        hourlyPostingLimit: { type: integer }
        updatedByUserId: { type: [string, "null"] }
        updatedAt: { type: string, format: date-time }
      required: [id, listingExpiryDays, hourlyPostingLimit, updatedByUserId, updatedAt]
    BeautyJobDeliveryIssue:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        emailType:
          type: string
          enum: [beauty_job_new_contact, beauty_job_author_reply, beauty_job_moderation, beauty_job_expiry_warning]
        status: { type: string, enum: [queued, failed, skipped] }
        issueKind: { type: string, enum: [delayed, temporary, permanent, configuration] }
        retryCount: { type: integer, minimum: 0 }
        retryAvailable: { type: boolean }
        nextRetryAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
      required: [id, emailType, status, issueKind, retryCount, retryAvailable, nextRetryAt, createdAt]
    BeautyJobDeliveryIssueSummary:
      type: object
      properties:
        delayedQueuedCount: { type: integer, minimum: 0 }
        failedCount: { type: integer, minimum: 0 }
        skippedCount: { type: integer, minimum: 0 }
        totalIssueCount: { type: integer, minimum: 0 }
        terminalIssueCount: { type: integer, minimum: 0 }
        staleAfterMinutes: { type: integer, minimum: 1 }
        alertThreshold: { type: integer, minimum: 1 }
      required: [delayedQueuedCount, failedCount, skippedCount, totalIssueCount, terminalIssueCount, staleAfterMinutes, alertThreshold]
    BeautyJobDeliveryIssues:
      type: object
      properties:
        summary: { $ref: "#/components/schemas/BeautyJobDeliveryIssueSummary" }
        deliveries:
          type: array
          items: { $ref: "#/components/schemas/BeautyJobDeliveryIssue" }
      required: [summary, deliveries]
    BeautyJobDeliveryRetryResult:
      type: object
      properties:
        id: { type: string, pattern: '^[0-9a-fA-F-]{36}$' }
        status: { type: string, enum: [queued, processing, sent, failed, skipped] }
        retried: { type: boolean }
      required: [id, status, retried]
    BeautyJobSaveResult:
      type: object
      properties: { saved: { type: boolean } }
      required: [saved]
    BeautyJobSweepResult:
      type: object
      properties: { expired: { type: integer } }
      required: [expired]
    BeautyJobCategoriesResponse:
      type: object
      properties: { categories: { type: array, items: { $ref: "#/components/schemas/BeautyJobCategory" } } }
      required: [categories]
    BeautyJobListResponse:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/BeautyJobListing" } }
        total: { type: integer }
        page: { type: integer }
        pageSize: { type: integer }
      required: [items, total, page, pageSize]
    BeautyJobRejectedListing:
      allOf:
        - $ref: "#/components/schemas/BeautyJobListing"
        - type: object
          properties:
            internalNote: { type: [string, "null"], description: Administrator-only moderation context. }
          required: [internalNote]
    BeautyJobRejectedListResponse:
      type: object
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/BeautyJobRejectedListing" } }
        total: { type: integer }
        page: { type: integer }
        pageSize: { type: integer }
      required: [items, total, page, pageSize]
    BeautyJobContactsResponse:
      type: object
      properties: { contacts: { type: array, items: { $ref: "#/components/schemas/BeautyJobContact" } } }
      required: [contacts]
    BeautyJobApplicantsResponse:
      type: object
      properties: { applicants: { type: array, items: { $ref: "#/components/schemas/BeautyJobApplicant" } } }
      required: [applicants]
    BeautyJobNotificationsResponse:
      type: object
      properties: { notifications: { type: array, items: { $ref: "#/components/schemas/BeautyJobNotification" } } }
      required: [notifications]
    BeautyJobRentalRequestsResponse:
      type: object
      properties: { requests: { type: array, items: { $ref: "#/components/schemas/BeautyJobRentalRequest" } } }
      required: [requests]
    BeautyJobModerationQueue:
      type: object
      properties:
        listings: { type: array, items: { $ref: "#/components/schemas/BeautyJobModerationListing" } }
        reports: { type: array, items: { $ref: "#/components/schemas/BeautyJobReport" } }
        total: { type: integer }
        page: { type: integer }
        pageSize: { type: integer }
      required: [listings, reports, total, page, pageSize]
    BeautyJobModerationListing:
      allOf:
        - $ref: "#/components/schemas/BeautyJobListing"
        - type: object
          properties:
            reportCount: { type: integer }
            contactCount: { type: integer }
          required: [reportCount, contactCount]
    BeautyJobBulkModerationInput:
      type: object
      required: [listingIds, action]
      properties:
        listingIds: { type: array, minItems: 1, maxItems: 100, uniqueItems: true, items: { type: string, pattern: '^[0-9a-fA-F-]{36}$' } }
        action: { type: string, enum: [approve, reject] }
        reason: { type: string, maxLength: 1000 }
        internalNote: { type: string, maxLength: 2000 }
    BeautyJobBulkModerationResult:
      type: object
      required: [processed, results]
      properties:
        processed: { type: integer }
        results:
          type: array
          items:
            type: object
            required: [id, status]
            properties:
              id: { type: string }
              status: { type: string, enum: [processed, not_found] }
    RetailProductReviewInput:
      type: object
      additionalProperties: false
      required: [rating, comment]
      properties:
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string, minLength: 1, maxLength: 2000 }
        photoUrls: { type: array, maxItems: 6, items: { type: string } }
    RetailReviewModerationStatus:
      type: string
      enum: [PUBLISHED, REPORTED, AUTO_FLAGGED, REMOVED]
    PublicRetailProductReview:
      type: object
      required: [id, rating, comment, verifiedPurchase, reviewerName, createdAt, updatedAt]
      properties:
        id: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string }
        verifiedPurchase: { type: boolean }
        reviewerName: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RetailProductReviewSummary:
      type: object
      required: [averageRating, reviewCount]
      properties:
        averageRating: { type: integer, minimum: 0, maximum: 5 }
        reviewCount: { type: integer, minimum: 0 }
    PublicRetailProductReviewList:
      type: object
      required: [summary, items]
      properties:
        summary: { $ref: "#/components/schemas/RetailProductReviewSummary" }
        items: { type: array, items: { $ref: "#/components/schemas/PublicRetailProductReview" } }
    OwnRetailProductReview:
      type: object
      required: [id, rating, comment, verifiedPurchase, moderationStatus]
      properties:
        id: { type: string }
        rating: { type: integer }
        comment: { type: string }
        verifiedPurchase: { type: boolean }
        moderationStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
    RetailProductReviewContext:
      type: object
      required: [eligible, verifiedPurchase, review]
      properties:
        eligible: { type: boolean }
        verifiedPurchase: { type: boolean }
        review: { anyOf: [{ $ref: "#/components/schemas/OwnRetailProductReview" }, { type: "null" }] }
    RetailProductReviewReportInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, enum: [SPAM, ABUSE, HATE, PERSONAL_INFORMATION, MISLEADING, OTHER] }
        explanation: { type: string, maxLength: 2000 }
    RetailProductReviewReportResult:
      type: object
      required: [id, reportCount, moderationStatus]
      properties:
        id: { type: string }
        reportCount: { type: integer }
        moderationStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
    RetailProductReviewModerationInput:
      type: object
      additionalProperties: false
      required: [action]
      properties:
        action: { type: string, enum: [KEEP, DISMISS_REPORTS, REMOVE, RESTORE] }
        reason: { type: string, maxLength: 2000 }
        internalNote: { type: string, maxLength: 4000 }
    RetailProductReviewModerationResult:
      type: object
      required: [id, moderationStatus, verifiedPurchase]
      properties:
        id: { type: string }
        moderationStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
        verifiedPurchase: { type: boolean }
    AdminRetailProductReviewItem:
      type: object
      additionalProperties: false
      required: [id, productId, productName, supplierId, rating, comment, moderationStatus, moderationReason, removedAt, createdAt, updatedAt, verifiedPurchase, reportCount]
      properties:
        id: { type: string }
        productId: { type: string }
        productName: { type: string }
        supplierId: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string }
        moderationStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
        moderationReason: { anyOf: [{ type: string }, { type: "null" }] }
        removedAt: { anyOf: [{ type: string, format: date-time }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        verifiedPurchase: { type: boolean }
        reportCount: { type: integer, minimum: 0 }
    AdminRetailProductReviewReport:
      type: object
      additionalProperties: false
      required: [reason, explanation, createdAt]
      properties:
        reason: { type: string, enum: [SPAM, ABUSE, HATE, PERSONAL_INFORMATION, MISLEADING, OTHER] }
        explanation: { anyOf: [{ type: string }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
    AdminRetailProductReviewAudit:
      type: object
      additionalProperties: false
      required: [id, reviewId, moderatorUserId, action, previousStatus, nextStatus, reason, internalNote, createdAt]
      properties:
        id: { type: string }
        reviewId: { type: string }
        moderatorUserId: { type: string }
        action: { type: string, enum: [KEEP, DISMISS_REPORTS, REMOVE, RESTORE] }
        previousStatus: { anyOf: [{ $ref: "#/components/schemas/RetailReviewModerationStatus" }, { type: "null" }] }
        nextStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
        reason: { anyOf: [{ type: string }, { type: "null" }] }
        internalNote: { anyOf: [{ type: string }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
    AdminRetailProductReviewList:
      type: object
      required: [items, page, pageSize, total]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/AdminRetailProductReviewItem" } }
        page: { type: integer }
        pageSize: { type: integer }
        total: { type: integer }
    AdminRetailProductReviewDetail:
      type: object
      additionalProperties: false
      required: [id, productId, productName, supplierId, rating, comment, moderationStatus, moderationReason, removedAt, createdAt, updatedAt, verifiedPurchase, reportCount, reports, audits]
      properties:
        id: { type: string }
        productId: { type: string }
        productName: { type: string }
        supplierId: { type: string }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string }
        moderationStatus: { $ref: "#/components/schemas/RetailReviewModerationStatus" }
        moderationReason: { anyOf: [{ type: string }, { type: "null" }] }
        removedAt: { anyOf: [{ type: string, format: date-time }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        verifiedPurchase: { type: boolean }
        reportCount: { type: integer, minimum: 0 }
        reports: { type: array, items: { $ref: "#/components/schemas/AdminRetailProductReviewReport" } }
        audits: { type: array, items: { $ref: "#/components/schemas/AdminRetailProductReviewAudit" } }
    SearchProductSuggestion:
      type: object
      required: [id, name, imageUrl, audience, price, discountPrice, priceOnRequest, cartEligible, variantType, variants, automaticBestseller]
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        audience: { type: string, enum: [B2B, B2C] }
        price: { type: ["number", "null"] }
        discountPrice: { type: ["number", "null"] }
        priceOnRequest: { type: boolean }
        cartEligible: { type: boolean }
        variantType: { type: ["string", "null"] }
        variants: { type: array, items: { $ref: "#/components/schemas/PublicProductVariant" } }
        automaticBestseller: { type: boolean }
    BestsellerRanking:
      type: object
      required: [rank, productId, name, imageUrl, categoryId, quantitySold, audience, periodDays, automaticBestseller]
      properties:
        rank: { type: integer, minimum: 1, maximum: 10 }
        productId: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        categoryId: { type: ["string", "null"] }
        quantitySold: { type: integer, minimum: 1 }
        audience: { type: string, enum: [B2B, B2C] }
        periodDays: { type: integer, enum: [30, 60] }
        automaticBestseller: { type: boolean }
    ProductDocument:
      type: object
      required: [id, displayName, sortOrder, url, contentType]
      properties:
        id: { type: string }
        displayName: { type: string }
        sortOrder: { type: integer }
        url: { type: string }
        contentType: { type: string, enum: [application/pdf, application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document] }
    ProductDocumentInput:
      type: object
      required: [mediaUrl, displayName, sortOrder]
      properties:
        mediaUrl: { type: string, pattern: "^/api/media/[0-9a-fA-F-]{36}" }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        sortOrder: { type: integer, minimum: 0, maximum: 10000 }
    ProductDocumentOrderInput:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            type: object
            required: [id, sortOrder]
            properties:
              id: { type: string }
              sortOrder: { type: integer, minimum: 0, maximum: 10000 }
    HeaderBarMessage:
      type: object
      required: [text, backgroundColor, textColor]
      properties:
        text: { type: string, minLength: 1, maxLength: 200 }
        backgroundColor: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
        textColor: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
    HeaderBarConfig:
      type: object
      required: [enabled, messages, intervalSeconds]
      properties:
        enabled: { type: boolean }
        messages: { type: array, maxItems: 10, items: { $ref: "#/components/schemas/HeaderBarMessage" } }
        intervalSeconds: { type: integer, minimum: 2, maximum: 60 }
    CommerceExperienceSettings:
      type: object
      required: [headerEnabled, headerMessages, headerIntervalSeconds, smartSearchMode, smartSearchProductIds, bestsellerPeriodDays, version]
      properties:
        headerEnabled: { type: boolean }
        headerMessages: { type: array, maxItems: 10, items: { $ref: "#/components/schemas/HeaderBarMessage" } }
        headerIntervalSeconds: { type: integer, minimum: 2, maximum: 60 }
        smartSearchMode: { type: string, enum: [AUTOMATIC, MANUAL] }
        smartSearchProductIds: { type: array, maxItems: 5, items: { type: string } }
        bestsellerPeriodDays: { type: integer, enum: [30, 60] }
        version: { type: integer, minimum: 1 }
    CommerceExperienceSettingsInput:
      type: object
      required: [headerEnabled, headerMessages, headerIntervalSeconds, smartSearchMode, smartSearchProductIds, bestsellerPeriodDays, version]
      properties:
        headerEnabled: { type: boolean }
        headerMessages: { type: array, maxItems: 10, items: { $ref: "#/components/schemas/HeaderBarMessage" } }
        headerIntervalSeconds: { type: integer, minimum: 2, maximum: 60 }
        smartSearchMode: { type: string, enum: [AUTOMATIC, MANUAL] }
        smartSearchProductIds: { type: array, maxItems: 5, items: { type: string } }
        bestsellerPeriodDays: { type: integer, enum: [30, 60] }
        version: { type: integer, minimum: 1 }
    Market:
      type: string
      enum: [B2B, B2C, BOTH]
    VersionedRule:
      type: object
      required: [id, version, createdAt, updatedAt]
      properties:
        id: { type: string }
        version: { type: integer, minimum: 1 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RuleTarget:
      oneOf:
        - type: object
          additionalProperties: false
          required: [productId]
          properties: { productId: { type: string } }
        - type: object
          additionalProperties: false
          required: [categoryId]
          properties: { categoryId: { type: string } }
    UpsellProduct:
      type: object
      required: [id, name, imageUrl, price, priceOnRequest, sortOrder]
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: ["string", "null"] }
        price: { type: ["integer", "null"], minimum: 0 }
        priceOnRequest: { type: boolean }
        sortOrder: { type: integer, minimum: 1 }
    UpsellProductList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/UpsellProduct" } }
    UpsellProductInput:
      type: object
      additionalProperties: false
      required: [alternativeProductIds]
      properties:
        productId: { type: string, description: Returned source product id. }
        alternativeProductIds: { type: array, maxItems: 3, uniqueItems: true, items: { type: string } }
    PricingLoyaltyTier:
      allOf:
        - $ref: "#/components/schemas/VersionedRule"
        - type: object
          required: [name, market, spendThresholdRsd, discountPercent, active]
          properties:
            name: { type: string, minLength: 1 }
            market: { $ref: "#/components/schemas/Market" }
            spendThresholdRsd: { type: integer, minimum: 0 }
            discountPercent: { type: integer, minimum: 1, maximum: 100 }
            active: { type: boolean }
    PricingLoyaltyTierInput:
      type: object
      additionalProperties: false
      required: [name, market, spendThresholdRsd, discountPercent]
      properties:
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        spendThresholdRsd: { type: integer, minimum: 0 }
        discountPercent: { type: integer, minimum: 1, maximum: 100 }
        active: { type: boolean, default: true }
    PricingLoyaltyTierUpdate:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: integer, minimum: 1 }
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        spendThresholdRsd: { type: integer, minimum: 0 }
        discountPercent: { type: integer, minimum: 1, maximum: 100 }
        active: { type: boolean }
    LoyaltyProgress:
      type: object
      required: [market, netSettledSpendRsd, effectiveTier, nextTier]
      properties:
        market: { type: string, enum: [B2B, B2C] }
        netSettledSpendRsd: { type: integer, minimum: 0 }
        effectiveTier:
          type: ["object", "null"]
          properties:
            id: { type: string }
            name: { type: string }
            discountPercent: { type: integer, minimum: 1, maximum: 100 }
            spendThresholdRsd: { type: integer, minimum: 0 }
          required: [id, name, discountPercent, spendThresholdRsd]
        nextTier:
          type: ["object", "null"]
          properties:
            id: { type: string }
            name: { type: string }
            spendThresholdRsd: { type: integer, minimum: 0 }
            progressPercent: { type: integer, minimum: 0, maximum: 100 }
          required: [id, name, spendThresholdRsd, progressPercent]
    BulkSaleCampaign:
      allOf:
        - $ref: "#/components/schemas/VersionedRule"
        - type: object
          required: [name, market, discountType, discountValue, startsAt, endsAt, status, targets]
          properties:
            name: { type: string, minLength: 1 }
            market: { $ref: "#/components/schemas/Market" }
            discountType: { type: string, enum: [PERCENT, FIXED_RSD] }
            discountValue: { type: integer, minimum: 1 }
            startsAt: { type: string, format: date-time }
            endsAt: { type: ["string", "null"], format: date-time }
            status: { type: string, enum: [DRAFT, ACTIVE] }
            targets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
    BulkSaleCampaignInput:
      type: object
      additionalProperties: false
      required: [name, market, discountType, discountValue, startsAt, endsAt, status, targets]
      properties:
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        discountType: { type: string, enum: [PERCENT, FIXED_RSD] }
        discountValue: { type: integer, minimum: 1, description: Percent values cannot exceed 100. }
        startsAt: { type: string, format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [DRAFT, ACTIVE] }
        targets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
    BulkSaleCampaignUpdate:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: integer, minimum: 1 }
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        discountType: { type: string, enum: [PERCENT, FIXED_RSD] }
        discountValue: { type: integer, minimum: 1 }
        startsAt: { type: string, format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [DRAFT, ACTIVE] }
        targets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
    CartThresholdReward:
      type: object
      required: [id, name, market, spendThresholdRsd, rewardKind, discountPercent, giftProductId, giftQuantity, active, version, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        market: { $ref: "#/components/schemas/Market" }
        spendThresholdRsd: { type: integer, minimum: 0 }
        rewardKind: { type: string, enum: [FREE_SHIPPING, GIFT_PRODUCT, PERCENT_DISCOUNT] }
        discountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        giftProductId: { type: ["string", "null"] }
        giftQuantity: { type: ["integer", "null"], minimum: 1 }
        active: { type: boolean }
        version: { type: integer, minimum: 1 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CartThresholdRewardInput:
      oneOf:
        - type: object
          additionalProperties: false
          required: [name, market, spendThresholdRsd, rewardKind, active]
          properties:
            name: { type: string, minLength: 1 }
            market: { $ref: "#/components/schemas/Market" }
            spendThresholdRsd: { type: integer, minimum: 0 }
            rewardKind: { type: string, const: FREE_SHIPPING }
            active: { type: boolean }
        - type: object
          additionalProperties: false
          required: [name, market, spendThresholdRsd, rewardKind, giftProductId, giftQuantity, active]
          properties:
            name: { type: string, minLength: 1 }
            market: { $ref: "#/components/schemas/Market" }
            spendThresholdRsd: { type: integer, minimum: 0 }
            rewardKind: { type: string, const: GIFT_PRODUCT }
            giftProductId: { type: string }
            giftQuantity: { type: integer, minimum: 1 }
            active: { type: boolean }
        - type: object
          additionalProperties: false
          required: [name, market, spendThresholdRsd, rewardKind, discountPercent, active]
          properties:
            name: { type: string, minLength: 1 }
            market: { $ref: "#/components/schemas/Market" }
            spendThresholdRsd: { type: integer, minimum: 0 }
            rewardKind: { type: string, const: PERCENT_DISCOUNT }
            discountPercent: { type: integer, minimum: 1, maximum: 100 }
            active: { type: boolean }
    CartThresholdRewardUpdate:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: integer, minimum: 1 }
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        spendThresholdRsd: { type: integer, minimum: 0 }
        rewardKind: { type: string, enum: [FREE_SHIPPING, GIFT_PRODUCT, PERCENT_DISCOUNT] }
        discountPercent: { type: integer, minimum: 1, maximum: 100 }
        giftProductId: { type: string }
        giftQuantity: { type: integer, minimum: 1 }
        active: { type: boolean }
    CartThresholdRewardProgress:
      type: object
      required: [reached, next]
      properties:
        reached: { type: array, items: { $ref: "#/components/schemas/CartThresholdRewardDescriptor" } }
        next: { oneOf: [{ $ref: "#/components/schemas/CartThresholdRewardDescriptor" }, { type: "null" }] }
    CartThresholdRewardDescriptor:
      type: object
      required: [id, name, spendThresholdRsd, rewardKind, discountPercent, giftQuantity]
      properties:
        id: { type: string }
        name: { type: string }
        spendThresholdRsd: { type: integer, minimum: 0 }
        rewardKind: { type: string, enum: [FREE_SHIPPING, GIFT_PRODUCT, PERCENT_DISCOUNT] }
        discountPercent: { type: ["integer", "null"], minimum: 1, maximum: 100 }
        giftQuantity: { type: ["integer", "null"], minimum: 1 }
    AutomaticXyPromotion:
      type: object
      required: [id, name, market, buyQuantity, rewardQuantity, rewardPercent, perOrderRewardUnitCap, startsAt, endsAt, status, buyTargets, rewardTargets, version, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        market: { $ref: "#/components/schemas/Market" }
        buyQuantity: { type: integer, minimum: 1 }
        rewardQuantity: { type: integer, minimum: 1 }
        rewardPercent: { type: integer, minimum: 1, maximum: 100 }
        perOrderRewardUnitCap: { type: ["integer", "null"], minimum: 1 }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [DRAFT, ACTIVE] }
        buyTargets: { type: array, items: { $ref: "#/components/schemas/RuleTarget" } }
        rewardTargets: { type: array, items: { $ref: "#/components/schemas/RuleTarget" } }
        version: { type: integer, minimum: 1 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AutomaticXyPromotionInput:
      type: object
      additionalProperties: false
      required: [name, market, buyQuantity, rewardQuantity, rewardPercent, startsAt, endsAt, status, buyTargets, rewardTargets]
      properties:
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        buyQuantity: { type: integer, minimum: 1 }
        rewardQuantity: { type: integer, minimum: 1 }
        rewardPercent: { type: integer, minimum: 1, maximum: 100 }
        perOrderRewardUnitCap: { type: ["integer", "null"], minimum: 1 }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [DRAFT, ACTIVE] }
        buyTargets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
        rewardTargets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
    AutomaticXyPromotionUpdate:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: integer, minimum: 1 }
        name: { type: string, minLength: 1 }
        market: { $ref: "#/components/schemas/Market" }
        buyQuantity: { type: integer, minimum: 1 }
        rewardQuantity: { type: integer, minimum: 1 }
        rewardPercent: { type: integer, minimum: 1, maximum: 100 }
        perOrderRewardUnitCap: { type: ["integer", "null"], minimum: 1 }
        startsAt: { type: ["string", "null"], format: date-time }
        endsAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [DRAFT, ACTIVE] }
        buyTargets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
        rewardTargets: { type: array, minItems: 1, items: { $ref: "#/components/schemas/RuleTarget" } }
    AutomaticXyPromotionList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            type: object
            required: [id, name, buyQuantity, rewardQuantity, rewardPercent, perOrderRewardUnitCap]
            properties:
              id: { type: string }
              name: { type: string }
              buyQuantity: { type: integer, minimum: 1 }
              rewardQuantity: { type: integer, minimum: 1 }
              rewardPercent: { type: integer, minimum: 1, maximum: 100 }
              perOrderRewardUnitCap: { type: ["integer", "null"], minimum: 1 }
    CheckoutThresholdRewardDescriptor:
      type: object
      required: [id, thresholdRsd, kind]
      properties:
        id: { type: string }
        thresholdRsd: { type: integer, minimum: 0 }
        kind: { type: string, enum: [FREE_SHIPPING, GIFT_PRODUCT, PERCENT_DISCOUNT] }
    CheckoutThresholdNextDescriptor:
      allOf:
        - $ref: "#/components/schemas/CheckoutThresholdRewardDescriptor"
        - type: object
          required: [remainingRsd]
          properties:
            remainingRsd: { type: integer, minimum: 0 }
    CheckoutRewardGiftDescriptor:
      type: object
      required: [rewardId, productId, productName, productImageUrl, quantity, unitPrice, price, thresholdLabel]
      properties:
        rewardId: { type: string }
        productId: { type: string }
        productName: { type: string }
        productImageUrl: { type: ["string", "null"] }
        quantity: { type: integer, minimum: 1 }
        unitPrice: { type: integer, const: 0 }
        price: { type: integer, const: 0 }
        thresholdLabel: { type: string }
    EducationOperationalParticipantInput:
      type: object
      additionalProperties: false
      required: [fullName]
      properties:
        fullName: { type: string, minLength: 1, maxLength: 240 }
        email: { type: ["string", "null"], maxLength: 320 }
        phone: { type: ["string", "null"], maxLength: 64 }
        userId: { type: ["string", "null"] }
    EducationOperationalBookingInput:
      type: object
      additionalProperties: false
      required: [courseId, sessionId, participants]
      properties:
        courseId: { type: string }
        sessionId: { type: string }
        installmentCount: { type: integer, enum: [1, 2, 3], default: 1 }
        participants:
          type: array
          minItems: 1
          maxItems: 20
          items: { $ref: "#/components/schemas/EducationOperationalParticipantInput" }
    EducationOperationalBooking:
      type: object
      required: [id, courseId, sessionId, status, participantCount, paymentStatus, grossAmount, earlyBirdApplied, discountReason, installments]
      properties:
        id: { type: string }
        courseId: { type: string }
        sessionId: { type: string }
        status: { type: string, enum: [pending, active, waitlisted, cancelled] }
        participantCount: { type: integer, minimum: 1 }
        paymentStatus: { type: string, enum: [pending] }
        grossAmount: { type: integer, minimum: 0 }
        earlyBirdApplied: { type: boolean }
        discountReason: { type: string, enum: [none, early_bird, group, early_bird_and_group] }
        installments:
          type: array
          items: { $ref: "#/components/schemas/EducationInstallmentPayment" }
    EducationInstallmentPayment:
      type: object
      required: [installmentNumber, amount, paymentReference, status]
      properties:
        installmentNumber: { type: integer, minimum: 1 }
        amount: { type: integer, minimum: 1 }
        paymentReference: { type: string }
        status: { type: string, enum: [pending, settled, refunded] }
    EducationIpsQrPayment:
      type: object
      required: [payload, recipientName, recipientAccount, purpose, amount, currency, reference]
      properties:
        payload: { type: string }
        recipientName: { type: string }
        recipientAccount: { type: string }
        purpose: { type: string }
        amount: { type: integer, minimum: 1 }
        currency: { type: string, const: RSD }
        reference: { type: string }
    EducationPaymentPlan:
      type: object
      required: [bookingGroupId, grossAmount, capturedAmount, refundedAmount, netPaidAmount, outstandingAmount, paymentStatus, installments]
      properties:
        bookingGroupId: { type: string }
        grossAmount: { type: integer, minimum: 0 }
        capturedAmount: { type: integer, minimum: 0 }
        refundedAmount: { type: integer, minimum: 0 }
        netPaidAmount: { type: integer, minimum: 0 }
        outstandingAmount: { type: integer, minimum: 0 }
        paymentStatus: { type: string, enum: [pending, partial, paid, refunded] }
        installments: { type: array, items: { $ref: "#/components/schemas/EducationInstallmentPayment" } }
    EducationInstallmentSettlement:
      type: object
      required: [installmentId, paymentReference, amount, capturedAmount, paymentStatus, replayed]
      properties:
        installmentId: { type: string }
        paymentReference: { type: string }
        amount: { type: integer }
        capturedAmount: { type: integer }
        paymentStatus: { type: string, enum: [partial, paid] }
        replayed: { type: boolean }
    EducationAdminInstallment:
      type: object
      required: [id, bookingGroupId, centerId, courseTitle, customerName, installmentNumber, amount, status, paymentReference, dueAt, settledAt]
      properties:
        id: { type: string }
        bookingGroupId: { type: string }
        centerId: { type: string }
        courseTitle: { type: string }
        customerName: { type: string }
        installmentNumber: { type: integer }
        amount: { type: integer }
        status: { type: string, enum: [pending, settled, refunded, cancelled] }
        paymentReference: { type: string }
        dueAt: { type: ["string", "null"], format: date-time }
        settledAt: { type: ["string", "null"], format: date-time }
    EducationOperationalBookingParticipant:
      type: object
      required: [id, fullName, email, phone, status, userId, enrollmentId]
      properties:
        id: { type: string }
        userId: { type: ["string", "null"] }
        enrollmentId: { type: ["string", "null"] }
        fullName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        status: { type: string, enum: [reserved, waitlisted, cancelled] }
    EducationOperationalBookingDetail:
      type: object
      required: [id, centerId, courseId, courseTitle, sessionId, status, purchaserId, createdAt, updatedAt, session, participants]
      properties:
        id: { type: string }
        centerId: { type: string }
        courseId: { type: string }
        courseTitle: { type: string }
        sessionId: { type: ["string", "null"] }
        purchaserId: { type: ["string", "null"] }
        status: { type: string, enum: [pending, active, waitlisted, cancelled] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        session:
          type: ["object", "null"]
          required: [id, startsAt, endsAt, location, cancelledAt]
          properties:
            id: { type: string }
            startsAt: { type: string, format: date-time }
            endsAt: { type: string, format: date-time }
            location: { type: ["string", "null"] }
            cancelledAt: { type: ["string", "null"], format: date-time }
        participants:
          type: array
          items: { $ref: "#/components/schemas/EducationOperationalBookingParticipant" }
    EducationOperationalBookingCancelInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        participantIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string }
        reason: { type: string, minLength: 3, maxLength: 1000 }
    EducationOperationalBookingRescheduleInput:
      type: object
      additionalProperties: false
      required: [targetSessionId]
      properties:
        targetSessionId: { type: string }
        participantIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string }
    EducationOperationalBookingOperation:
      type: object
      required: [bookingGroupId, sessionId, status, affectedParticipantIds, cancelledSeats, movedSeats, refundAmount, depositDisposition, replayed]
      properties:
        bookingGroupId: { type: string }
        sessionId: { type: ["string", "null"] }
        status: { type: string, enum: [pending, active, waitlisted, cancelled] }
        affectedParticipantIds: { type: array, items: { type: string } }
        cancelledSeats: { type: integer, minimum: 0 }
        movedSeats: { type: integer, minimum: 0 }
        refundAmount: { type: integer, minimum: 0 }
        depositDisposition: { type: string, enum: [refund, forfeit, transfer] }
        replayed: { type: boolean }
    EducationOperationalAttendanceInput:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: { type: string, enum: [present, absent, excused] }
        occurredAt: { type: string, format: date-time }
    EducationOperationalAttendance:
      type: object
      required: [participantId, sessionId, status, recordedAt, recordedByUserId]
      properties:
        participantId: { type: string }
        sessionId: { type: string }
        status: { type: string, enum: [present, absent, excused] }
        recordedAt: { type: string, format: date-time }
        recordedByUserId: { type: ["string", "null"] }
    EducationSessionEducatorSubstitutionInput:
      type: object
      additionalProperties: false
      required: [educatorStaffId]
      properties:
        educatorStaffId: { type: string }
    EducationSessionEducatorAssignment:
      type: object
      required: [sessionId, educatorStaffId]
      properties:
        sessionId: { type: string }
        educatorStaffId: { type: string }
    EducationSessionCancellationInput:
      type: object
      additionalProperties: false
      properties:
        reason: { type: string, maxLength: 1000 }
    EducationSessionCancellation:
      type: object
      required: [sessionId, cancelledParticipants, cancelledEnrollments, refundAmount]
      properties:
        sessionId: { type: string }
        cancelledParticipants: { type: integer, minimum: 0 }
        cancelledEnrollments: { type: integer, minimum: 0 }
        refundAmount: { type: integer, minimum: 0 }
    EducationOperationalAvailabilitySlot:
      type: object
      required: [sessionId, date, startTime, endTime, educatorStaffId]
      properties:
        sessionId: { type: string }
        date: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        educatorStaffId: { type: ["string", "null"] }
    EducationOperationalAvailability:
      type: object
      required: [timeZone, occupancy, slots]
      properties:
        timeZone: { type: string, const: Europe/Belgrade }
        occupancy: { type: integer, minimum: 0 }
        capacity: { type: ["integer", "null"], minimum: 0 }
        freeSeats: { type: ["integer", "null"], minimum: 0 }
        waitlistOpen: { type: boolean }
        lastSpots: { type: boolean }
        nextAvailable:
          type: ["object", "null"]
          properties:
            sessionId: { type: string }
            date: { type: string }
            startTime: { type: string }
            endTime: { type: string }
            educatorStaffId: { type: ["string", "null"] }
        slots:
          type: array
          items: { $ref: "#/components/schemas/EducationOperationalAvailabilitySlot" }
    EducationCourseRecurrenceInput:
      type: object
      additionalProperties: false
      required: [educatorStaffId, weekdays, startTime, endTime, durationMinutes, startDate, endDate]
      properties:
        educatorStaffId: { type: string }
        weekdays: { type: array, minItems: 1, maxItems: 7, uniqueItems: true, items: { type: integer, minimum: 1, maximum: 7 } }
        startTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        durationMinutes: { type: integer, minimum: 5, maximum: 720 }
        granularityMinutes: { type: integer, minimum: 5, maximum: 180 }
        startDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        endDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        capacity: { type: integer, minimum: 1, maximum: 1000 }
        minimumEnrollments: { type: integer, minimum: 0 }
        minimumEnrollmentRiskDeadline: { type: ["string", "null"], format: date-time }
    EducationCourseRecurrenceCandidate:
      type: object
      required: [date, startTime, endTime]
      properties:
        date: { type: string }
        startTime: { type: string }
        endTime: { type: string }
    EducationCourseRecurrencePreview:
      type: object
      required: [timeZone, candidates, skippedAbsenceCount, skippedConflictCount]
      properties:
        timeZone: { type: string, const: Europe/Belgrade }
        candidates: { type: array, items: { $ref: "#/components/schemas/EducationCourseRecurrenceCandidate" } }
        skippedAbsenceCount: { type: integer, minimum: 0 }
        skippedConflictCount: { type: integer, minimum: 0 }
    EducationCourseRecurrenceCommit:
      type: object
      required: [sessionIds, replayed]
      properties:
        sessionIds: { type: array, items: { type: string } }
        replayed: { type: boolean }
    EducationOperationsCalendarParticipant:
      type: object
      required: [id, fullName, email, phone, status]
      properties:
        id: { type: string }
        fullName: { type: string }
        email: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        status: { type: string }
    EducationOperationsCalendarSession:
      type: object
      required: [id, courseId, educatorStaffId, startsAt, endsAt, capacity, reservedSeats, participants]
      properties:
        id: { type: string }
        courseId: { type: string }
        educatorStaffId: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        capacity: { type: integer }
        reservedSeats: { type: integer }
        participants: { type: array, items: { $ref: "#/components/schemas/EducationOperationsCalendarParticipant" } }
    EducationCenterStaffMember:
      type: object
      required: [id, centerId, userId, role, active, instructorProfileId]
      properties:
        id: { type: string }
        centerId: { type: string }
        userId: { type: string }
        instructorProfileId: { type: ["string", "null"] }
        role: { type: string, enum: [owner_admin, manager_reception, educator] }
        active: { type: boolean }
    EducationCenterStaffInput:
      type: object
      additionalProperties: false
      required: [userId, role]
      properties:
        userId: { type: string }
        instructorProfileId: { type: ["string", "null"] }
        role: { type: string, enum: [owner_admin, manager_reception, educator] }
    EducationCenterStaffUpdate:
      type: object
      additionalProperties: false
      properties:
        role: { type: string, enum: [owner_admin, manager_reception, educator] }
        active: { type: boolean }
        instructorProfileId: { type: ["string", "null"] }
    EducationCenterOperationalPermissions:
      type: object
      required: [centerId, role, canManageStaff, canManageCalendar, canTakeAttendance, educatorStaffId]
      properties:
        centerId: { type: string }
        role: { type: string, enum: [owner_admin, manager_reception, educator] }
        educatorStaffId: { type: ["string", "null"] }
        canManageStaff: { type: boolean }
        canManageCalendar: { type: boolean }
        canTakeAttendance: { type: boolean }
    EducationEducatorWeeklyAvailability:
      type: object
      required: [id, staffId, weekday, startTime, endTime]
      properties:
        id: { type: string }
        staffId: { type: string }
        weekday: { type: integer, minimum: 1, maximum: 7 }
        startTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
    EducationEducatorWeeklyAvailabilityInput:
      type: object
      additionalProperties: false
      required: [weekday, startTime, endTime]
      properties:
        weekday: { type: integer, minimum: 1, maximum: 7 }
        startTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: string, pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
    EducationEducatorAbsence:
      type: object
      required: [id, staffId, startDate, endDate, startTime, endTime, reason]
      properties:
        id: { type: string }
        staffId: { type: string }
        startDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        endDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: ["string", "null"], pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: ["string", "null"], pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        reason: { type: ["string", "null"], maxLength: 1000 }
    EducationEducatorAbsenceInput:
      type: object
      additionalProperties: false
      required: [startDate, endDate]
      properties:
        startDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        endDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        startTime: { type: ["string", "null"], pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: ["string", "null"], pattern: '^(?:[01]\d|2[0-3]):[0-5]\d$' }
        reason: { type: ["string", "null"], maxLength: 1000 }
    EducationEducatorAbsenceConflict:
      type: object
      additionalProperties: false
      required: [sessionId, courseId, courseTitle, startsAt, endsAt, reservedSeats]
      properties:
        sessionId: { type: string }
        courseId: { type: string }
        courseTitle: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        reservedSeats: { type: integer, minimum: 0 }
    EducationEducatorAbsencePreview:
      type: object
      additionalProperties: false
      required: [canCreate, conflicts]
      properties:
        canCreate: { type: boolean }
        conflicts:
          type: array
          items: { $ref: "#/components/schemas/EducationEducatorAbsenceConflict" }
